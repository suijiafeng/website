{"pageProps":{"postData":{"title":"Cookie、localStorage、sessionStorage：登录态该放哪，边界差在哪","date":"2019-11-18","excerpt":"登录态丢失、白屏和会话过期看起来像同一个问题，根因却可能落在不同存储里。把 Cookie、localStorage 和 sessionStorage 的生命周期、容量和安全边界一次理清。","tags":["前端基础","JavaScript"],"slug":"browser-storage-cookie-localstorage-2019","category":"2019","likeCount":9,"postId":"ecc94145-8b41-8229-5aa7-3f5c744172e1","viewCount":3486,"readTime":"9分钟","coverImage":null,"content":"\n登录态问题最烦的地方，是表面症状常常像同一个 bug，真正的根因却散在不同存储里。\"登录一会儿就掉\"、\"明明显示登录了，一操作就过期\"、\"iPad 上直接白屏\" 这三种反馈同时出现时，第一反应很容易是后端会话不稳；这次往下查，问题却几乎都在浏览器这一侧。\n\n更麻烦的是，这三种症状还真不是一个源头。有人踩的是会话级 Cookie，有人踩的是 localStorage 的使用边界，还有人踩中了移动端存储环境的不稳定行为。顺着工单一条条拆下去，我才把 Cookie、localStorage、sessionStorage 这三兄弟的生命周期、容量和安全边界重新捋清楚。\n\n## 登录态到底存在哪\n\n先交代背景。我们中后台的登录态是 token 放 localStorage、axios 请求拦截器统一往 header 里塞的那一套。但客服同时还要用一个更老的订单查询系统，那套系统走的是 Cookie 登录态。两套系统、两种存储，症状混在一起报上来，第一步就是把它们分开。\n\n浏览器能存东西的地方不止一个，登录 token、主题设置、搜索历史、临时表单、引导状态，好像都能存本地。但**能存不代表应该存**——三兄弟的生命周期、容量、安全边界都不一样，选错了地方，症状就会像这次一样诡异。\n\n## 没设过期时间的 Cookie\n\n先查\"每天早上必须重新登录\"的那位客服。她用的主要是那套老系统，我借她工位打开 DevTools 的 Application 面板一看，登录后的 Cookie 里那条 token，Expires 一栏写着 `Session`。\n\n问题就在这。Cookie 不设 `Expires` 或 `Max-Age`，就是会话级 Cookie，浏览器全部关掉就没了。她每天下班关浏览器，第二天自然要重新登。这不算 bug，是老系统当年就这么设计的，但没人跟客服说过。我提了个单给维护那套系统的同事，把登录态改成带 `Max-Age` 的持久 Cookie，按需求给了七天。\n\n借这个问题把 Cookie 的关键属性理一遍。Cookie 会随请求发送到服务端：\n\n```http\nCookie: token=abc\n```\n\n服务端可以设置：\n\n```http\nSet-Cookie: token=abc; HttpOnly; Secure; SameSite=Lax; Max-Age=604800\n```\n\n常见属性：\n\n- `HttpOnly`：JavaScript 不能通过 `document.cookie` 读取\n- `Secure`：只在 HTTPS 下发送\n- `SameSite`：限制跨站请求携带\n- `Expires/Max-Age`：过期时间，不设就是会话级\n- `Domain/Path`：限定哪些域名、路径会带上这个 Cookie\n\n`HttpOnly` 这条我特别想强调。登录 token 放 Cookie 且加 `HttpOnly`，就算页面被 XSS 注入了脚本，攻击者也读不到 token——`document.cookie` 拿不到它。这是把 token 放 Cookie 而不是 localStorage 的核心理由之一。反过来说，我们新系统把 token 存 localStorage 图取用方便，一旦中招 XSS，token 是可以被脚本直接捞走的。这是当初封装请求层时权衡过的决定，但**要清楚这是权衡，不是绝对安全**。\n\n`SameSite` 是防 CSRF 的关键，今年它的存在感明显越来越强，Chrome 那边已经在吹风要把默认值收紧到 `Lax`。几个值的实际区别：\n\n```text\nSameSite=Strict  跨站请求一律不带 Cookie，最安全但体验差（从别的站点点链接过来都要重新登录）\nSameSite=Lax     普通跨站请求不带，但顶级导航的 GET 会带，安全和体验的平衡点\nSameSite=None    任何跨站都带，但浏览器要求必须同时加 Secure\n```\n\n我现在给登录态的默认配方是 `HttpOnly; Secure; SameSite=Lax`，需要嵌 iframe 或跨站的场景才退到 `None`。\n\n排查过程中还翻出一个 `Domain` 的陈年坑：老系统曾经把 Cookie 的 Domain 写成了 `order.example.com`，后来加了个 `report.example.com` 的子系统想共享登录态，死活拿不到——Cookie 只发给设它的那个域。想让子域共享，Domain 要设成 `.example.com`。这类问题在 Application 面板里对着 Domain 一栏看一眼就明白，比瞪代码快得多。\n\n顺带说 `document.cookie` 的一个坑：它返回的不是对象，而是把当前域下所有非 `HttpOnly` 的 Cookie 用 `; ` 拼成的一整个字符串。比如设了两条：\n\n```js\ndocument.cookie = 'a=1'\ndocument.cookie = 'b=2'\ndocument.cookie // 'a=1; b=2'，是一整个字符串，不是对象\n```\n\n想取某一项得自己 split 解析，没有现成的按 key 取的 API。而且加了 `HttpOnly` 的那条（比如登录 token）根本不会出现在这个字符串里——这正是 `HttpOnly` 的意义。\n\nCookie 适合登录态、服务端需要读取的小数据，不适合存大对象。它有两个硬伤：单个域名的 Cookie 总量通常限制在 4KB 左右，而且**每次请求都会自动带上**。我在老系统里就见过有人往 Cookie 里塞了一大坨用户配置，结果每个请求头都背着几 K 的 Cookie，连请求图片、CSS 都带着，纯属浪费带宽。能不放 Cookie 的数据就别放。\n\n## iPad 白屏与 localStorage 的脾气\n\n第二个症状——iPad 上白屏——查起来最快，因为我们前端错误监控里躺着一串来自 iOS Safari 的报错。那位同事习惯用无痕模式浏览，而 Safari 的无痕模式下，`localStorage.setItem` 会直接抛异常。我们有一处埋点代码裸写 localStorage 没接住异常，页面一进来就崩在那儿，白屏。\n\n修法很简单：所有 localStorage 读写都收进一个带 try/catch 的工具。但先把 localStorage 的几个特性摆清楚，这次排查每一条都用上了：\n\n`localStorage` 持久保存，除非手动清除：\n\n```js\nlocalStorage.setItem('theme', 'dark')\nvar theme = localStorage.getItem('theme')\n```\n\n用之前最好知道这几点：\n\n- **只能存字符串。** 存对象得自己 `JSON.stringify`，读出来 `JSON.parse`。直接 `setItem('user', obj)` 会被转成字符串 `\"[object Object]\"`，组里上个月刚有人踩过。控制台跑一遍就现原形：\n\n  ```js\n  localStorage.setItem('k', { a: 1 })\n  localStorage.getItem('k')        // '[object Object]'，对象被 toString 了，数据没了\n  typeof localStorage.getItem('k') // 'string'\n\n  // 正确：自己序列化\n  localStorage.setItem('k', JSON.stringify({ a: 1 }))\n  JSON.parse(localStorage.getItem('k')) // { a: 1 }\n  ```\n\n  顺带一提，存进去的东西在 Chrome DevTools 的 Application 面板 → Local Storage 里能直接看到键值，这次排查我基本住在这个面板里，比反复 `getItem` 方便。\n- **容量大约 5MB**（各浏览器略有差异），比 Cookie 的 4KB 宽裕得多，但也不是无限。塞太多大字符串照样会满，满了 `setItem` 会抛 `QuotaExceededError`。\n- **同步阻塞主线程。** 读写都是同步 API，存几 MB 的大字符串时 `JSON.stringify` 加写入会卡一下，高频写入要节流。\n- **按域名隔离。** `http://` 和 `https://`、不同端口算不同源，存的东西互相看不到。本地开发切协议时数据\"丢了\"，多半是这个原因——我们测试环境上 HTTPS 那周，好几个人以为自己的偏好设置被清了。\n\n修白屏的时候我把项目里所有裸调 `setItem` 的地方统一换成了这个工具，至少别让写满或解析失败把页面搞崩：\n\n```js\nvar storage = {\n  set: function (key, value) {\n    try {\n      localStorage.setItem(key, JSON.stringify(value))\n      return true\n    } catch (e) {\n      // 可能是隐私模式不让写，也可能是超配额\n      return false\n    }\n  },\n  get: function (key, fallback) {\n    try {\n      var raw = localStorage.getItem(key)\n      return raw === null ? fallback : JSON.parse(raw)\n    } catch (e) {\n      return fallback\n    }\n  }\n}\n```\n\n那个 try/catch 不是摆设，这次的白屏就是它没在场的代价。\n\nlocalStorage 适合存主题、语言、非敏感偏好、简单草稿；不适合存密码、敏感个人信息、大量列表数据、重要权限数据。一旦发生 XSS，localStorage 里的内容可以被脚本读取，**不要因为方便就把敏感信息随便存进去**。\n\n## 顺带讲清楚的 sessionStorage\n\n这次排查里 sessionStorage 其实没有问题，但排查时它出过场——我们下单审核有个分步表单，进度存在 sessionStorage 里，客服一度怀疑是它把登录态吃了。趁机把它也讲清楚。\n\n`sessionStorage` 在当前标签页会话内有效，关闭标签页后清除：\n\n```js\nsessionStorage.setItem('step', '2')\n```\n\n适合存临时流程状态，比如分步表单当前步骤、某个页面的临时筛选。\n\n它不会在不同标签页之间共享，这点和 localStorage 完全相反，是选型的关键依据。我们那个审核流程分好几步，用 sessionStorage 存当前进度和填过的数据，用户中途刷新页面不丢、能接着填；但另开一个标签页处理另一单，那是独立的会话，两边数据不串——这正是想要的隔离。要是用 localStorage，两个标签页就会互相覆盖，乱套。\n\n还有个容易混的点：**\"关闭标签页清除\"里的\"关闭\"指的是这个标签页的生命周期结束**。刷新（F5）不算关闭，数据还在；通过链接在同一标签页内跳转也还在；但关掉标签再重开就没了。另外，从一个页面用 `target=\"_blank\"` 打开的新标签，在某些浏览器里会复制一份 sessionStorage 过去，之后两者各自独立——这个行为我是查了文档才确认的，别想当然。\n\n## 对照表\n\n三种存储的特性都过了一遍，整理成一张表，以后选型扫一眼就够：\n\n```text\n                 Cookie          localStorage      sessionStorage\n容量             ~4KB            ~5MB              ~5MB\n随请求发送       是              否                否\n生命周期         可设过期        永久（手动清）    标签页会话\nJS 可访问        除非 HttpOnly   可                可\n跨标签页共享     同源共享        同源共享          不共享\n典型用途         登录态          偏好/草稿         分步流程临时态\n```\n\n如果服务端需要识别登录态，Cookie 更自然；如果只是前端偏好设置，localStorage 更合适；如果只是当前标签页的临时状态，sessionStorage 更合适。\n\n一句话决策：**服务端要读 → Cookie；前端长期留 → localStorage；当前页用完就扔 → sessionStorage。**\n\n## 多标签页的\"假登录\"\n\n还剩一个症状没查清楚：明明显示登着，一点就弹\"登录已过期\"。\n\n我去客服工位蹲了一上午才看明白她们的用法：习惯性开五六个标签页，各干各的活。其中一个标签退出登录，token 从 localStorage 里被清掉了——localStorage 是同源标签页共享的，清是全清。但其他标签页的页面还停在退出前的样子，内存里的用户信息、界面上的头像昵称都还在，看起来\"登着\"。下一个请求发出去，header 里没 token，后端返回 401，前端弹\"登录已过期\"。用户视角就是：我什么都没干，它突然把我踢了。\n\n修这个用到了 localStorage 的一个隐藏能力——同源的另一个标签页改了 localStorage，当前页能收到 `storage` 事件。用它做\"一处退出，所有标签页同步登出\"：\n\n```js\nwindow.addEventListener('storage', function (e) {\n  if (e.key === 'token' && !e.newValue) {\n    // 别的标签页清掉了 token，说明在那边登出了，这边也跳登录页\n    location.href = '/login'\n  }\n})\n```\n\n注意两个坑：一是 `storage` 事件**只在\"其他\"标签页触发，当前操作的这个页不会收到自己的事件**，所以本页的登出逻辑得自己处理；二是它只对 localStorage/sessionStorage 的变化生效，且只在同源标签页间传播。据说规范里还有个更正经的 `BroadcastChannel` 专门做跨标签页通信，但现在浏览器支持还不齐，这个 `storage` 事件就是眼下最实用的手段。\n\n上线之后我特地又去客服那边坐了会儿：一个标签退出，其他标签齐刷刷跳到登录页。她们说这下\"讲道理了\"。\n\n## 别假设本地数据永远合法\n\n再补一条这次排查的意外收获。翻 localStorage 的时候，我在好几个人的浏览器里看到了三个月前旧版本留下的脏数据——结构跟现在的代码对不上，读出来就是 undefined 一片。本地存储的结构是会变的，写的时候最好带上版本：\n\n```js\nlocalStorage.setItem('settings', JSON.stringify({\n  version: 1,\n  theme: 'dark'\n}))\n```\n\n读取时检查：\n\n```js\nfunction getSettings() {\n  try {\n    var value = JSON.parse(localStorage.getItem('settings'))\n\n    if (!value || value.version !== 1) {\n      return defaultSettings\n    }\n\n    return value\n  } catch (error) {\n    return defaultSettings\n  }\n}\n```\n\n**不要假设本地数据永远合法。**用户可能手动改，旧版本也可能留下脏数据，隐私模式可能根本不让你写。\n\n三个症状，三个根因：会话级 Cookie、无痕模式抛异常、多标签不同步。回头看，每一个都不是什么高深的 bug，全是\"没搞清楚这个数据到底存在哪、活多久、谁能看见\"。浏览器存储三兄弟长得像，脾气完全不同——登录、安全、状态管理全押在它们身上，把边界摸清楚，比多背十遍对比表有用。\n"},"recommendedPosts":[{"title":"看 ES2026 和 ESNext 新特性时，我更关心它什么时候能进业务代码","date":"2026-04-24","excerpt":"不可变数据、资源管理、新语法这些 ES2026/ESNext 能力，怎么判断它什么时候能真正进业务代码：分清标准阶段、核对工具链转译和 polyfill 成本。","tags":["前端基础","JavaScript"],"slug":"frontend-es2026-features","category":"2026","likeCount":10,"postId":"0ac5cfee-fe01-706f-b371-6fe7cc8af036","viewCount":1111,"readTime":null,"coverImage":null},{"title":"用 Biome 替掉 ESLint + Prettier：一次工具链瘦身的得与失","date":"2026-04-18","excerpt":"把项目的 lint 和格式化从 ESLint+Prettier 换成 Biome 的过程：速度提升有多明显、规则迁移缺了什么、以及哪些场景我还是留着 ESLint。","tags":["前端基础","构建与模块化","JavaScript"],"slug":"biome-toolchain-2026","category":"2026","likeCount":6,"postId":"37e39142-8bf2-d042-6270-2228c894603c","viewCount":1314,"readTime":null,"coverImage":null},{"title":"TC39 Signals 提案与框架里的信号：响应式终于要进语言层了吗","date":"2026-03-07","excerpt":"从 Vue ref、Solid signal 到 TC39 的 Signals 提案，聊聊信号这套响应式模型解决了什么、polyfill 怎么用、以及它和现有框架的关系。","tags":["JavaScript","前端基础","组件与状态"],"slug":"js-signals-proposal-2026","category":"2026","likeCount":62,"postId":"10625fc7-1fb0-e156-eaf9-5cace589f74e","viewCount":768,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 生命周期：图表宽度为什么会偶发性量成 0","date":"2019-11-23","excerpt":"图表只画一半、容器宽度偶发性拿到 0，问题通常不在图表库，而在初始化时机。这里把 `created`、`mounted`、`activated` 和父子组件执行顺序放回同一个现场里理顺。","tags":["Vue","组件与状态"],"slug":"vue-lifecycle-2019","category":"2019","likeCount":6,"postId":"1aed563e-f493-9fc3-c590-482b0540e49b","viewCount":2316,"readTime":null,"coverImage":null},"nextPost":{"title":"消息队列基础：一个重接口为什么必须学会拆开做","date":"2019-11-16","excerpt":"下单接口为什么会被短信、日志和同步任务拖慢，异步拆分后又会多出哪些消息丢失、重复消费和幂等问题。这里把消息队列放回一条真实业务链路里讲。","tags":["其他"],"slug":"message-queue-basics-2019","category":"2019","likeCount":5,"postId":"f8515abf-4372-5151-b11c-5bbb00f90689","viewCount":3249,"readTime":null,"coverImage":null}},"__N_SSG":true}