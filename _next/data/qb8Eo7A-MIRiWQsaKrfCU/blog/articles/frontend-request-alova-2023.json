{"pageProps":{"postData":{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"从后台项目里反复写 loading/data/error 三件套的经历出发，聊聊请求库为什么会从 HTTP 客户端走向状态管理、缓存和统一错误处理。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2362,"readTime":"8分钟","coverImage":null,"content":"\n很长一段时间里，前端发请求最常见的选择是 Axios。\n\n它解决了 `XMLHttpRequest` 写起来麻烦的问题，也提供了拦截器、请求配置、响应处理、取消请求等能力。对很多项目来说，Axios 仍然是一个稳定、清晰、足够好用的 HTTP 客户端。\n\n但今天的前端请求，已经不只是“把接口调通”。页面越来越复杂以后，请求会和状态、缓存、分页、重试、权限、乐观更新、错误展示绑在一起。继续只封装一个 `request` 函数，很多问题会被推回到业务组件里。\n\n我在后台项目里最常见的重复代码，就是 `loading/data/error` 三件套。每个列表页写一遍，每个详情页写一遍，最后错误提示、重试按钮、空状态还都不一样。后来才意识到，请求库选型不只是“Axios 还是 fetch”，而是要不要把服务端状态当成一个独立问题来管理。\n\n## 请求背后还有很多状态\n\n一个普通列表请求，通常不只包含接口调用。\n\n页面还要处理：\n\n- 加载中\n- 请求失败\n- 空数据\n- 分页\n- 搜索条件变化\n- 缓存和失效\n- 重试\n- 取消请求\n- 乐观更新\n- 重复请求合并\n\n如果这些状态都由业务页面自己写，代码很快会变重复。\n\n比如在 Vue 组件里，你可能经常看到类似代码：\n\n```javascript\nconst loading = ref(false);\nconst data = ref([]);\nconst error = ref(null);\n\nasync function loadList() {\n  loading.value = true;\n  error.value = null;\n\n  try {\n    const result = await request.get(\"/api/posts\");\n    data.value = result.data;\n  } catch (err) {\n    error.value = err;\n  } finally {\n    loading.value = false;\n  }\n}\n```\n\n这段代码本身没问题。问题是项目里有几十个列表、详情、搜索和提交表单时，每个页面都在重复类似流程。更麻烦的是，每个人对错误处理、空状态、取消请求和缓存的处理方式可能还不一样。\n\n特别是取消请求。用户快速切换 tab 或搜索关键词时，旧请求可能后返回，把新结果覆盖掉。手写页面很容易漏这个边界，最后表现就是“我明明搜的是 A，列表却闪成了上一次的 B”。\n\n## Axios 更像基础 HTTP 客户端\n\nAxios 的定位比较清楚：发请求、收响应、处理拦截。\n\n它非常适合做统一 HTTP 层，比如：\n\n- 统一 baseURL\n- 注入 token\n- 处理响应格式\n- 处理 401 登录过期\n- 处理网络错误\n- 设置超时时间\n\n一个常见封装可能是这样：\n\n```javascript\nimport axios from \"axios\";\n\nexport const request = axios.create({\n  baseURL: \"/api\",\n  timeout: 10000,\n});\n\nrequest.interceptors.response.use(\n  (response) => response.data,\n  (error) => {\n    const message = error.response?.data?.message || \"请求失败\";\n    return Promise.reject(new Error(message));\n  }\n);\n```\n\n这层封装负责的是“请求怎么发、错误怎么抛”。但它不会自动帮你维护组件里的 `loading`、`error`、`data`，也不会主动管理缓存和分页状态。\n\n所以 Axios 更像底层能力。它并不差，只是它解决的问题边界和 Alova、TanStack Query、SWR 这类工具不完全一样。\n\n我仍然会把 Axios 这层保留下来做基础协议封装：baseURL、token、超时、统一错误、登录失效。上层再决定用 Alova、Query 还是页面自己的 composable。底层 HTTP 客户端和服务端状态管理不是互斥关系，很多项目是组合使用。\n\n## Alova 这类库关注请求策略\n\nAlova 这类现代请求库更关注“请求策略”。\n\n也就是说，它不只是发出 HTTP 请求，还希望帮你处理请求相关状态。例如：\n\n- 自动维护 loading\n- 自动维护 error\n- 提供分页请求策略\n- 支持缓存和失效\n- 支持请求共享\n- 支持乐观更新\n- 和 Vue、React 等框架集成\n\n这类思路和 TanStack Query、SWR 等工具有相似之处：把请求当成应用状态的一部分来管理。\n\n比如一个列表页面，真正关心的不只是 `GET /posts`，而是“当前搜索条件下的文章列表状态”。当关键词变化、页码变化或用户重新进入页面时，工具可以根据策略决定是重新请求、读缓存，还是保留旧数据等待新数据回来。\n\n这比每个页面手写状态更稳定，也更容易形成团队统一风格。\n\n不过请求策略库也不是银弹。缓存时间、失效规则、重试次数、乐观更新回滚，这些都需要团队约定。工具能提供能力，但不会替你判断“订单详情应该缓存多久”“权限接口失败是否要全局退出登录”。\n\n## 请求状态和服务端状态要分清\n\n前端里有两类状态容易混在一起：本地 UI 状态和服务端状态。\n\n本地 UI 状态包括弹窗是否打开、当前选中的 tab、输入框临时内容。服务端状态包括用户信息、文章列表、订单详情、权限配置。\n\n服务端状态有几个特点：\n\n- 来源不在前端\n- 可能被其他用户或其他页面修改\n- 需要重新获取或失效\n- 可能存在缓存\n- 请求失败时要有降级展示\n\n如果把服务端状态当成本地状态随便存，很容易出现数据不同步。比如列表页删除一条数据后，详情页缓存还在；或者用户提交成功后，统计数字没有更新。\n\n请求状态管理工具真正解决的就是这类问题。\n\n我现在会把服务端状态的 key 设计当成重点。比如列表请求不能只用 `/api/orders` 当缓存 key，还要包含页码、筛选条件和排序字段。key 设计不清楚，缓存命中就会变成事故。\n\n```javascript\nconst key = [\"orders\", { page, keyword, status }];\n```\n\n这个思想在不同库里写法不一样，但本质相同：请求状态必须能准确描述“这份数据是谁”。\n\n缓存 key 之外，还要想清楚什么时候失效。比如订单列表和订单详情不是两份完全独立的数据：详情页改了状态，列表页对应那一行也应该刷新；新增订单后，第一页列表和统计数字也要失效。工具一般都会提供 invalidate 或 refetch 能力，但调用时机要靠业务自己定。\n\n我习惯把请求 key 做成函数，避免页面里手写字符串：\n\n```typescript\nconst orderKeys = {\n  all: ['orders'] as const,\n  list: (params: { page: number; keyword?: string; status?: string }) =>\n    ['orders', 'list', params] as const,\n  detail: (id: string) => ['orders', 'detail', id] as const,\n}\n```\n\n这样改订单状态后，代码能清楚表达要刷新哪一类数据：\n\n```typescript\nasync function updateOrderStatus(id: string, status: string) {\n  await request.post(`/orders/${id}/status`, { status })\n\n  invalidate(orderKeys.detail(id))\n  invalidate(orderKeys.all)\n}\n```\n\n这里的 `invalidate(orderKeys.all)` 不是随手一刷，而是明确告诉请求层：所有订单相关列表、统计、详情都有可能过期。这个动作如果散落在每个页面里，很快就会漏；收进一套 key 约定后，团队看代码就知道数据关系。\n\n## 统一错误处理很重要\n\n无论用 Axios 还是 Alova，请求错误都不应该散落在每个页面里随便处理。\n\n更稳的方式是先统一错误结构：\n\n```typescript\ntype ApiError = {\n  code: string;\n  message: string;\n  status?: number;\n};\n\nfunction normalizeError(error: unknown): ApiError {\n  if (error instanceof Error) {\n    return {\n      code: \"REQUEST_ERROR\",\n      message: error.message,\n    };\n  }\n\n  return {\n    code: \"UNKNOWN_ERROR\",\n    message: \"未知错误\",\n  };\n}\n```\n\n页面层可以决定如何展示，但错误格式应该尽量统一。否则同一个接口失败，有的页面弹 toast，有的页面静默失败，有的页面展示英文报错，用户体验会非常割裂。\n\n统一错误处理还方便做登录失效、权限不足、限流、网络断开等全局逻辑。\n\n错误也要区分层级。401、403、网络断开这类适合全局处理；表单字段错误、业务校验失败更适合页面局部展示。所有错误都弹全局 toast，会让用户很烦；所有错误都交给页面，又会导致体验不一致。\n\n搜索和筛选类页面还要额外处理竞态。用户连续输入 `a`、`ab`、`abc`，三个请求都发出去，最后返回的不一定是最后一次请求。如果旧请求后返回并覆盖新数据，页面就会闪回旧结果。\n\n不依赖库时，我会用“取消旧请求 + 只接受最新 taskId”双保险：\n\n```typescript\nlet currentId = 0\nlet controller: AbortController | null = null\n\nasync function search(keyword: string) {\n  currentId += 1\n  const id = currentId\n\n  controller?.abort()\n  controller = new AbortController()\n\n  const result = await fetch(`/api/search?q=${encodeURIComponent(keyword)}`, {\n    signal: controller.signal,\n  }).then((res) => res.json())\n\n  if (id !== currentId) {\n    return\n  }\n\n  renderResult(result.data)\n}\n```\n\n请求状态管理库能把这层封装得更自然，但原理还是一样：旧请求不能覆盖新意图。这个边界不处理，搜索页、级联选择器、快速切换 tab 的页面都会偶发脏数据。\n\n## 什么时候需要换请求方案\n\n如果项目只是少量接口，Axios 或原生 `fetch` 完全够用。不要为了“技术更现代”强行引入复杂工具。\n\n但如果项目里有大量列表、搜索、分页、缓存、重复请求和状态同步，继续手写请求状态会越来越累。这时可以考虑引入请求状态管理工具，而不是只封装一个 `request` 函数。\n\n可以用下面几个问题判断是否值得升级：\n\n- 页面里是否大量重复 `loading/data/error`\n- 列表页是否经常处理分页、搜索和刷新\n- 详情页和列表页是否经常数据不同步\n- 是否需要缓存接口结果\n- 是否存在重复请求浪费\n- 是否需要统一处理乐观更新和回滚\n\n如果答案大多是“是”，就说明问题已经不只是 HTTP 客户端层面了。\n\n引入新请求方案时，我不建议一次性全量替换。可以先选列表页、详情页、搜索页这种重复问题明显的模块试点。等团队把缓存、错误、分页、失效这些约定跑顺，再逐步扩展。请求层改动影响面很大，迁移节奏要比 UI 组件更谨慎。\n\n\n我现在选请求方案，会先看页面里重复的是不是已经超过了“封装一个 request 函数”能解决的范围。\n\n如果只是少量接口，Axios 或 `fetch` 加一层统一错误处理就够了；如果列表、搜索、分页、缓存、刷新、乐观更新到处都是，再考虑 Alova 这类请求状态管理工具才有意义。\n\n请求层最重要的不是库名，而是边界：网络错误、HTTP 错误、业务错误要统一转换；401、403 这类全局状态要集中处理；表单字段错误要能落回页面。服务端状态一旦散到各个组件里，后面换什么库都救不回来。\n"},"recommendedPosts":[{"title":"前端实现 AI 对话流式输出：SSE 是最容易落地的方案","date":"2025-04-23","excerpt":"AI 对话的逐字输出不是前端定时器假装出来的；SSE 和 fetch 流式读取各有适用场景和失败处理。","tags":["前端基础","JavaScript","请求与异步"],"slug":"frontend-sse-streaming-output","category":"2025","likeCount":6,"postId":"902675a9-29c6-977a-537e-32c5ace13c14","viewCount":1711,"readTime":null,"coverImage":null},{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2023-09-17","excerpt":"无限级菜单看着是递归组件，实际牵着路由、高亮、权限、图标和接口数据清洗，边界要先想清楚。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2023","category":"2023","likeCount":5,"postId":"73d4dc9e-8fc7-3ce9-8dcb-31a0122f0b98","viewCount":3749,"readTime":null,"coverImage":null},{"title":"刚开始用 Vue3 时，最容易踩的几个坑","date":"2023-06-07","excerpt":"从我把 Vue2 项目迁到 Vue3 时踩过的坑出发，聊聊响应式、props、watch、组合式 API、生命周期清理和 Pinia 使用误区。","tags":["Vue","前端基础"],"slug":"vue3-common-mistakes-2023","category":"2023","likeCount":6,"postId":"06bb42c9-56d9-0865-c906-88f606e068a9","viewCount":1085,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vercel 推出存储能力后，前端部署不只是静态页面了","date":"2023-05-05","excerpt":"从一次给小工具补数据库和文件上传的经历出发，聊聊 Vercel Postgres、Blob、KV 对前端应用部署和数据边界的影响。","tags":["前端基础","数据可视化与存储"],"slug":"frontend-vercel-storage-2023","category":"2023","likeCount":35,"postId":"9de962c3-48c9-94c0-fe0b-42c6a0ff4b77","viewCount":777,"readTime":null,"coverImage":null},"nextPost":{"title":"CSS Custom Highlight：不改 DOM 也能做文本高亮","date":"2023-03-30","excerpt":"从一次站内搜索高亮改造说起，记录 CSS Custom Highlight API 在不改 DOM、高亮多节点文本和兼容降级里的实践边界。","tags":["前端基础","CSS"],"slug":"frontend-css-custom-highlight-2023","category":"2023","likeCount":8,"postId":"4c14a010-dfea-92b0-8192-129629cccd5c","viewCount":2095,"readTime":null,"coverImage":null}},"__N_SSG":true}