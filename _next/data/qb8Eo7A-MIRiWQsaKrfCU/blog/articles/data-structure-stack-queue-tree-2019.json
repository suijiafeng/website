{"pageProps":{"postData":{"title":"栈、队列和树：前端面试里的数据结构不是白问的","date":"2019-07-16","excerpt":"从括号匹配、任务队列、菜单树和递归渲染出发，整理栈、队列、树这些基础数据结构的前端使用场景。","tags":["JavaScript"],"slug":"data-structure-stack-queue-tree-2019","category":"2019","likeCount":7,"postId":"947f33a6-2530-939f-d7a3-c2dc8fbbbb9b","viewCount":1550,"readTime":"3分钟","coverImage":null,"content":"\n刚工作那两年我一直觉得数据结构是算法课的事，跟我天天写表单、调样式没关系。直到有一次接了个后台权限系统，接口返回一坨扁平的菜单数据，要我渲染成左侧多级折叠菜单，还要根据角色把没权限的节点连同它的子树一起砍掉。我一开始用了三层嵌套的 `for` 循环硬怼，写到第二天自己都看不懂了，改个需求要动五个地方。后来组里一个老哥过来瞄了一眼，说你这就是个树啊，递归一下不就完了。那一刻我才反应过来：原来面试官天天问的栈、队列、树，不是为了为难我，是这些东西真的天天在用，只是我以前没把它们认出来。\n\n这里把我这几年踩过的坑串一串，讲讲这三个结构在前端项目里到底长什么样、什么时候该用、用的时候容易栽在哪。\n\n## 栈：后进先出，撤销和回溯的本质\n\n栈的规则就一句话：后进先出（LIFO）。最后放进去的，最先拿出来。JS 里压根不需要专门实现一个栈类，一个数组配 `push` 和 `pop` 就是个现成的栈。\n\n可以在控制台直接验证它的「后进先出」：\n\n```js\nvar stack = []\nstack.push('a')\nstack.push('b')\nconsole.log(stack)       // ['a', 'b']\nconsole.log(stack.pop()) // 'b' ← 最后进的最先出\nconsole.log(stack)       // ['a']\n```\n\n最容易理解的例子是函数调用栈。你写个递归忘了写终止条件，浏览器报 `Maximum call stack size exceeded`，那个 stack 就是调用栈——每调一层函数压一个帧进去，函数返回再弹出来，爆栈就是压进去的没机会弹出来，把空间撑满了。浏览器的前进后退、编辑器的撤销重做，本质也都是栈。\n\n我自己用得最多的实战场景是括号 / 标签匹配。早期做一个简易的公式编辑器，要校验用户输入的括号是不是配对的，这就是栈的经典用法：\n\n```js\nfunction isValid(text) {\n  var stack = []\n  var pairs = {\n    ')': '(',\n    ']': '[',\n    '}': '{'\n  }\n\n  for (var i = 0; i < text.length; i++) {\n    var char = text[i]\n\n    if (char === '(' || char === '[' || char === '{') {\n      // 左括号一律入栈\n      stack.push(char)\n    } else if (pairs[char]) {\n      // 遇到右括号，看栈顶是不是它对应的左括号\n      if (stack.pop() !== pairs[char]) {\n        return false\n      }\n    }\n  }\n\n  // 全部匹配完，栈应该是空的\n  return stack.length === 0\n}\n\nisValid('([]{})') // true\nisValid('([)]')   // false，顺序错了\nisValid('(()')    // false，最后栈里还剩一个 (\n```\n\n这里有个我当时栽过的坑：只判断左右括号数量相等是不够的。`([)]` 左右括号各两个，数量对，但顺序是错的。必须用栈才能保证「最近打开的最先关闭」这个嵌套关系。我最初版本就是数数量，结果用户输入 `[(])` 也判过了，被测试同学逮个正着。\n\n栈的思路还能往外延伸。比如做一个面包屑导航的「返回上一级」，或者实现一个简单的状态回退，都可以维护一个栈，每进一层 `push`，每退一步 `pop`。再比如把递归改成迭代——递归本质就是在用系统的调用栈，当数据层级太深可能爆栈时，可以自己拿一个数组当栈来手动模拟，这点后面讲树的遍历会用到。\n\n## 队列：先进先出，控制并发的利器\n\n队列跟栈正好相反，先进先出（FIFO），像排队买奶茶，先来的先被服务。数组用 `push` 进队、`shift` 出队就行：\n\n```js\nvar queue = []\n\nqueue.push('task1')\nqueue.push('task2')\n\nconsole.log(queue.shift()) // task1\nconsole.log(queue.shift()) // task2\n```\n\n队列最有价值的实战场景是并发控制。我当年做过一个批量上传图片的功能，用户一次选几十张，如果一股脑全发出去，浏览器对同一域名的并发连接本来就有上限（一般 6 个），剩下的请求全在那干等，而且后端也容易被打挂。正确做法是维护一个等待队列，同时只跑固定数量的请求，跑完一个就从队列里取下一个补上：\n\n```js\nfunction runWithLimit(tasks, limit) {\n  return new Promise(function (resolve) {\n    var results = []\n    var index = 0      // 下一个要取的任务下标\n    var running = 0    // 当前正在跑的数量\n    var finished = 0   // 已完成的数量\n\n    function next() {\n      // 队列空了且都跑完了，整体结束\n      if (finished === tasks.length) {\n        resolve(results)\n        return\n      }\n      // 只要还有空位、还有没派发的任务，就继续派\n      while (running < limit && index < tasks.length) {\n        var current = index++\n        running++\n        tasks[current]().then(function (res) {\n          results[current] = res\n        }).catch(function (err) {\n          results[current] = err\n        }).then(function () {\n          running--\n          finished++\n          next() // 腾出一个空位，立刻补一个\n        })\n      }\n    }\n\n    next()\n  })\n}\n\n// 用法：tasks 是一组返回 Promise 的函数\nvar tasks = urls.map(function (url) {\n  return function () { return fetch(url) }\n})\n\nrunWithLimit(tasks, 3).then(function (results) {\n  console.log('全部完成', results)\n})\n```\n\n这里有两个细节是我踩过坑才记住的。一是 `tasks` 里存的得是「返回 Promise 的函数」，不能直接存 Promise，因为 Promise 一创建就立刻执行了，那样所谓的并发限制根本拦不住，请求早就全发出去了。二是 `results[current] = res` 用下标存，是为了保证结果顺序跟输入一致——队列是先进先出，但每个请求的耗时不一样，回来的顺序是乱的，靠下标对齐才不会错位。\n\n再说个更底层的：浏览器的事件循环里那些宏任务（`setTimeout`、事件回调）排的就是一个任务队列，一个一个先进先出地执行。理解了队列，再去看事件循环为什么是那个执行顺序，会顺很多。\n\n## 树：层级关系的天然表达\n\n前端遇到的层级数据基本都是树：左侧菜单、组织部门、多级评论、文件目录、路由配置、省市区三级联动。树的特点是每个节点可以有若干子节点，从根一路往下分叉。\n\nJS 里表达树通常就是对象嵌 `children` 数组：\n\n```js\nvar tree = [\n  {\n    id: 1,\n    name: '系统管理',\n    children: [\n      {\n        id: 2,\n        name: '用户管理',\n        children: [\n          { id: 4, name: '新增用户' },\n          { id: 5, name: '删除用户' }\n        ]\n      },\n      { id: 3, name: '角色管理' }\n    ]\n  }\n]\n```\n\n### 递归遍历\n\n遍历树最自然的方式是递归，因为树本身就是递归定义的（一棵树的子节点又是一棵子树）：\n\n```js\nfunction walk(nodes, callback) {\n  nodes.forEach(function (node) {\n    callback(node)\n    if (node.children && node.children.length) {\n      walk(node.children, callback)\n    }\n  })\n}\n\nwalk(tree, function (node) {\n  console.log(node.name)\n})\n```\n\n这种是深度优先（DFS）——一条路走到黑再回头。如果想按层级一层层扫（广度优先 BFS），就该把队列请出来了，正好把前面两个结构串起来：\n\n```js\nfunction walkByLevel(nodes, callback) {\n  var queue = nodes.slice() // 拷一份，别污染原数组\n  while (queue.length) {\n    var node = queue.shift()\n    callback(node)\n    if (node.children) {\n      // 子节点排到队尾，保证先把同一层处理完\n      queue.push.apply(queue, node.children)\n    }\n  }\n}\n```\n\n我做菜单查找「某个 id 对应的节点」时一般用 DFS 递归；而要做「展开到第二层」这种按深度的操作，BFS 更顺手。顺带提一句，如果你的树深到几千层（比如某些异常的脏数据），递归可能爆栈，这时候就得用上面那种「拿数组当栈/队列」的迭代写法来代替递归。\n\n### 按条件过滤子树\n\n回到开头那个权限菜单的需求：根据角色把没权限的节点连同子树一起砍掉。这正是树的递归威力所在，几行就搞定：\n\n```js\nfunction filterTree(nodes, predicate) {\n  var result = []\n  nodes.forEach(function (node) {\n    if (predicate(node)) {\n      var copy = Object.assign({}, node)\n      if (node.children) {\n        // 递归过滤子节点\n        copy.children = filterTree(node.children, predicate)\n      }\n      result.push(copy)\n    }\n  })\n  return result\n}\n\nvar allowedIds = { 1: true, 2: true, 4: true }\nvar menu = filterTree(tree, function (node) {\n  return allowedIds[node.id]\n})\n```\n\n当年我用嵌套循环写这功能写了一百多行，换成这个递归就十几行，改需求只改 `predicate` 一处。这就是认出「它是棵树」之后的差别。\n\n### 二叉树的前 / 中 / 后序\n\n前端的树大多是「任意多个 `children`」，但面试常考的是二叉树的三种深度优先遍历，差别只在「什么时候访问根节点」。拿这棵小树验证一下最直观：\n\n```text\n      1\n     / \\\n    2   3\n   / \\\n  4   5\n```\n\n```js\nvar tree = { v: 1, l: { v: 2, l: { v: 4 }, r: { v: 5 } }, r: { v: 3 } }\n\nfunction preOrder(n, out) {   // 根 → 左 → 右\n  if (!n) return out\n  out.push(n.v)\n  preOrder(n.l, out)\n  preOrder(n.r, out)\n  return out\n}\nfunction inOrder(n, out) {     // 左 → 根 → 右\n  if (!n) return out\n  inOrder(n.l, out)\n  out.push(n.v)\n  inOrder(n.r, out)\n  return out\n}\nfunction postOrder(n, out) {   // 左 → 右 → 根\n  if (!n) return out\n  postOrder(n.l, out)\n  postOrder(n.r, out)\n  out.push(n.v)\n  return out\n}\n\nconsole.log(preOrder(tree, []))  // [1, 2, 4, 5, 3]\nconsole.log(inOrder(tree, []))   // [4, 2, 5, 1, 3]\nconsole.log(postOrder(tree, [])) // [4, 5, 2, 3, 1]\n```\n\n三种顺序里只有「访问根」那一句的位置在变，其余完全一样——记住这个就不用死背遍历结果了。\n\n## 扁平数组转树：后台接口的常客\n\n实际开发里，接口为了方便存数据库，返回的往往不是嵌套树，而是带 `parentId` 的扁平数组：\n\n```js\nvar list = [\n  { id: 1, parentId: 0, name: '系统管理' },\n  { id: 2, parentId: 1, name: '用户管理' },\n  { id: 3, parentId: 1, name: '角色管理' },\n  { id: 4, parentId: 2, name: '新增用户' }\n]\n```\n\n转成树最容易想到的是双重循环：对每个节点都遍历一遍找它的孩子，时间复杂度 O(n²)。数据量小无所谓，但我碰过一次几千条的省市区数据，页面卡了一下肉眼可见。更好的做法是先用一个 map 把 id 映射到节点，再一遍循环把每个节点挂到它父亲下面，整体 O(n)：\n\n```js\nfunction listToTree(list) {\n  var map = {}\n  var roots = []\n\n  // 第一遍：建立 id -> 节点 的索引，顺便给每个节点加上 children\n  list.forEach(function (item) {\n    map[item.id] = Object.assign({}, item, { children: [] })\n  })\n\n  // 第二遍：根据 parentId 挂到父节点下\n  list.forEach(function (item) {\n    var node = map[item.id]\n    if (item.parentId === 0) {\n      roots.push(node)\n    } else if (map[item.parentId]) {\n      map[item.parentId].children.push(node)\n    } else {\n      // 父节点不存在，说明是脏数据，当成根处理，别让它凭空消失\n      roots.push(node)\n    }\n  })\n\n  return roots\n}\n```\n\n这里最后那个 `else` 分支是我加的「保命」逻辑。早期我没写它，结果有一条数据的 `parentId` 指向了一个已经被删掉的父节点，那个节点和它整棵子树就直接从界面上消失了，排查了半天才发现是数据的锅。宁可把它当根节点露出来让人看见异常，也别让它静默丢失。\n\n反过来，树转扁平数组也偶尔要用，比如要把树形数据传给一个只认平铺列表的组件。直接复用前面的 `walk` 就行：\n\n```js\nfunction treeToList(nodes) {\n  var list = []\n  walk(nodes, function (node) {\n    var copy = Object.assign({}, node)\n    delete copy.children\n    list.push(copy)\n  })\n  return list\n}\n```\n\n\n栈、队列、树这三样，刚学时觉得离业务很远，真正写过几个项目就会发现它们无处不在：括号校验、撤销回退靠栈，批量请求的并发控制靠队列，菜单、权限、评论、目录这些层级数据全是树。面试官问这些不是在炫技，是想看你能不能在一坨需求里认出背后的结构——一旦认出来，原本要堆几百行 `if-else` 的活，常常十几行递归就解决了。\n\n把这几个结构的「脾气」记牢：栈是后进先出、队列是先进先出、树是递归的。下次再碰到嵌套数据或者要排队执行的场景，先别急着撸循环，想想它是不是其中之一。\n"},"recommendedPosts":[{"title":"看 ES2026 和 ESNext 新特性时，我更关心它什么时候能进业务代码","date":"2026-04-24","excerpt":"从不可变数据、资源管理和新语法落地说起，记录我如何判断 JavaScript 新特性是否适合进入真实项目。","tags":["前端基础","JavaScript"],"slug":"frontend-es2026-features","category":"2026","likeCount":85,"postId":"0ac5cfee-fe01-706f-b371-6fe7cc8af036","viewCount":910,"readTime":null,"coverImage":null},{"title":"用 Biome 替掉 ESLint + Prettier：一次工具链瘦身的得与失","date":"2026-04-18","excerpt":"把项目的 lint 和格式化从 ESLint+Prettier 换成 Biome 的过程：速度提升有多明显、规则迁移缺了什么、以及哪些场景我还是留着 ESLint。","tags":["前端基础","构建与模块化","JavaScript"],"slug":"biome-toolchain-2026","category":"2026","likeCount":6,"postId":"37e39142-8bf2-d042-6270-2228c894603c","viewCount":1089,"readTime":null,"coverImage":null},{"title":"qiankun JS 沙箱隔离机制：Proxy 篇","date":"2026-03-15","excerpt":"从一次微前端全局变量污染排查说起，拆解 qiankun ProxySandbox 和 LegacySandbox 为什么要设计 fakeWindow、with、函数绑定和生命周期补丁。","tags":["JavaScript","组件与状态"],"slug":"qiankun-proxy-sandbox-2026","category":"2026","likeCount":5,"postId":"7c4e7a87-f85b-f3d0-a5fb-ae8f04c726ff","viewCount":1201,"readTime":null,"coverImage":null}],"prevPost":{"title":"防抖和节流：搜索框、滚动监听和按钮点击里的老问题","date":"2019-07-24","excerpt":"结合输入搜索、滚动加载和按钮重复点击场景，整理 debounce、throttle 的区别、手写实现和使用边界。","tags":["JavaScript","性能与缓存"],"slug":"javascript-debounce-throttle-2019","category":"2019","likeCount":7,"postId":"8d7cde98-8899-c9e3-0aa5-63f58a2c0f93","viewCount":1191,"readTime":null,"coverImage":null},"nextPost":{"title":"DOM 事件模型：捕获、冒泡和事件委托为什么总被问","date":"2019-07-05","excerpt":"从点击事件、事件委托和动态列表出发，整理 DOM 事件捕获、冒泡、target、currentTarget、阻止默认行为和解绑问题。","tags":["JavaScript","前端基础"],"slug":"dom-event-capture-bubble-2019","category":"2019","likeCount":8,"postId":"4cce3fb9-ca62-23dc-a750-dea4f54c7879","viewCount":3285,"readTime":null,"coverImage":null}},"__N_SSG":true}