{"pageProps":{"postData":{"title":"深拷贝和浅拷贝：对象复制这道题，项目里真会踩坑","date":"2019-03-08","excerpt":"从编辑弹窗污染原列表的 bug 出发，聊聊对象引用、浅拷贝、深拷贝、JSON 方法、递归和循环引用边界。","tags":["JavaScript","前端基础"],"slug":"javascript-deep-copy-interview-2019","category":"2019","likeCount":5,"postId":"9f8ee93e-3f5d-1993-981e-90bd97530b71","viewCount":3262,"readTime":"9分钟","coverImage":null,"content":"\n深拷贝和浅拷贝是很经典的面试题。\n\n它看起来像基础概念，但项目里确实经常踩坑。表单编辑时改了临时对象，结果列表也变了；Vuex 里直接改了引用数据，页面状态串了；弹窗取消时，本来应该回滚的数据已经被改掉。\n\n这些问题背后都是对象引用。\n\n我印象最深的一次线上 bug 就跟这个有关。一个商品编辑弹窗，点「编辑」把列表里那条数据塞进表单，用户改了几个字段又点「取消」——结果列表里那行也跟着变了。测试提单说「取消按钮没用」，我查了半天 `cancel` 方法逻辑明明没错，最后才反应过来：表单拿的就是列表对象本身的引用，用户在表单里改，等于直接改了列表里的数据，根本没有「副本」可言，取消时也就无从回滚。这道题别看面试问得多基础，真正能在项目里第一时间反应过来「这是引用问题」的人，其实没那么多。\n\n## 基本类型和引用类型\n\n基本类型复制的是值：\n\n```js\nvar a = 1\nvar b = a\n\nb = 2\n\nconsole.log(a) // 1\n```\n\n对象复制的是引用：\n\n```js\nvar a = { name: 'Tom' }\nvar b = a\n\nb.name = 'Jerry'\n\nconsole.log(a.name) // Jerry\n```\n\n`a` 和 `b` 指向同一个对象。栈里存的是同一个堆地址，改谁都是改那块堆内存。理解这点就能解释一堆「灵异现象」：函数参数传对象进去，函数里改了属性外面也变了（传的是引用的拷贝，指向同一对象）；两个看着一模一样的对象 `{a:1} === {a:1}` 却是 `false`（地址不同）。这些都不是 bug，是引用类型的本性。\n\n数组同理，数组也是对象。`var b = a` 之后 `b.push(1)`，`a` 也多了一项。我早期把这种「共享」当过 feature 用，省了来回赋值，结果埋下一堆隐患，后来宁可多写一行拷贝也不敢图省事了。\n\n## 浅拷贝\n\n浅拷贝只复制第一层：\n\n```js\nvar user = {\n  name: 'Tom',\n  address: {\n    city: 'Shanghai'\n  }\n}\n\nvar copy = Object.assign({}, user)\n\ncopy.name = 'Jerry'\ncopy.address.city = 'Beijing'\n\nconsole.log(user.name) // Tom\nconsole.log(user.address.city) // Beijing\n```\n\n第一层 `name` 没互相影响，但嵌套对象 `address` 还是同一个引用。\n\n展开运算符也是浅拷贝：\n\n```js\nvar copy = {\n  ...user\n}\n```\n\n`Object.assign` 和展开运算符本质一样，都只复制第一层、嵌套对象仍是共享引用。数组的 `slice()`、`concat()`、`[...arr]` 同理，都是浅拷贝。\n\n那是不是浅拷贝就没用？恰恰相反，我项目里九成的「复制」需求用浅拷贝就够了。关键看你的数据结构有多深。如果对象就一层、全是基本类型（很多表单数据就是这样），`{ ...form }` 完全够用，没必要上深拷贝那套重家伙。开头那个编辑弹窗的 bug，因为表单字段都是平铺的字符串和数字，我最后就是用 `this.form = { ...row }` 一行修好的。\n\n真正需要深拷贝的，是那种带嵌套对象/数组、而你又要独立修改内层的场景。判断标准很简单：**你会不会去改嵌套那一层？** 会，就得深；不会（只读或只换整个引用），浅拷贝足矣。别一看到「复制」就条件反射上深拷贝。\n\n## JSON 深拷贝\n\n最常见的简单深拷贝：\n\n```js\nvar copy = JSON.parse(JSON.stringify(user))\n```\n\n它适合普通 JSON 数据，但有很多限制：\n\n- 函数会丢失\n- `undefined` 会丢失\n- `Date` 会变成字符串\n- `RegExp` 会变成空对象\n- 循环引用会报错\n\n例如：\n\n```js\nvar obj = {\n  time: new Date(),\n  fn: function() {}\n}\n\nvar copy = JSON.parse(JSON.stringify(obj))\n\nconsole.log(copy.time) // 字符串\nconsole.log(copy.fn) // undefined\n```\n\n还有几个更隐蔽、坑过我的点：\n\n- **`NaN`、`Infinity` 会变成 `null`**。做数值统计时这个最阴，一个 `NaN` 拷完变 `null`，下游计算结果全错。\n- **`Map`、`Set` 会变成空对象 `{}`**，里面的数据全没了。\n- **`Symbol` 类型的 key 直接丢失**。\n- **`BigInt` 会直接抛错**（`stringify` 不支持）。\n- **属性顺序里如果有 `toJSON` 方法会被悄悄调用**，结果可能跟你想的不一样（`Date` 变字符串就是因为它有 `toJSON`）。\n\n下面这段贴进 Node 或控制台就能复现，每一项都标了确切结果：\n\n```js\nvar obj = {\n  a: undefined,          // 丢失\n  fn: function () {},    // 丢失\n  d: new Date('2019-03-08T00:00:00.000Z'), // 变字符串\n  n: NaN,                // 变 null\n  inf: Infinity,         // 变 null\n  arr: [NaN, undefined]  // 变 [null, null]\n}\n\nJSON.parse(JSON.stringify(obj))\n// => {\n//   d: '2019-03-08T00:00:00.000Z',\n//   n: null,\n//   inf: null,\n//   arr: [ null, null ]\n// }\n// 注意：a 和 fn 这两个 key 整个消失了\n```\n\n`undefined`、函数、`Symbol` 作为对象属性值时会被直接抹掉（key 都不剩）；但出现在数组里时为了占位会变成 `null`（上面 `arr` 里那个 `undefined` 就变 `null` 了），这点很容易踩。循环引用更干脆，直接抛错：\n\n```js\nvar c = {}\nc.self = c\nJSON.stringify(c)\n// => 抛 TypeError: Converting circular structure to JSON\n```\n\n接口返回的普通数据（纯 JSON：对象、数组、字符串、数字、布尔、null）可以放心这样处理，又快又简单，我自己也常用。但只要数据里可能掺了上面这些类型，就别用它，否则 bug 静默发生、还特别难查——它不报错，只是悄悄把你的数据改了。我的经验是：能确定数据「干净」就用 JSON 法图省事，拿不准就老实用结构化的深拷贝。\n\n## 手写递归深拷贝\n\n基础版本：\n\n```js\nfunction deepClone(value) {\n  if (value === null || typeof value !== 'object') {\n    return value\n  }\n\n  if (Array.isArray(value)) {\n    return value.map(item => deepClone(item))\n  }\n\n  var result = {}\n\n  Object.keys(value).forEach(key => {\n    result[key] = deepClone(value[key])\n  })\n\n  return result\n}\n```\n\n使用：\n\n```js\nvar copy = deepClone(user)\n```\n\n这个版本能处理普通对象和数组，但还没处理 Date、RegExp、循环引用。\n\n面试到这一步，面试官十有八九会追问「Date 怎么办」「RegExp 怎么办」，把特殊类型补上能加不少分：\n\n```js\nfunction deepClone(value) {\n  if (value === null || typeof value !== 'object') {\n    return value\n  }\n\n  // Date 和 RegExp 要用构造函数重新造一个，否则属性拷不全\n  if (value instanceof Date) {\n    return new Date(value.getTime())\n  }\n  if (value instanceof RegExp) {\n    return new RegExp(value.source, value.flags)\n  }\n\n  if (Array.isArray(value)) {\n    return value.map(item => deepClone(item))\n  }\n\n  var result = {}\n  Object.keys(value).forEach(key => {\n    result[key] = deepClone(value[key])\n  })\n  return result\n}\n```\n\n为什么 `Date` 不能直接 `Object.keys` 遍历拷贝？因为它的时间值存在内部槽里，不是普通可枚举属性，遍历出来是空的，拷出来就是个 `Invalid Date`。`RegExp` 同理，`source` 和 `flags` 得显式拿出来重建。这俩是最常被忽略的，我第一次手写就栽在 `Date` 上，拷完一打印 `Invalid Date` 一脸懵。\n\n另外这个递归版有个理论隐患：对象嵌套特别深时会爆栈（`Maximum call stack size exceeded`）。正常业务数据没那么深，不用管；真遇到（比如某些树形结构无限嵌套），得改成用栈/队列的迭代写法，但那属于面试加试题了，项目里我从没真碰上过。\n\n## 循环引用\n\n```js\nvar obj = {}\nobj.self = obj\n\ndeepClone(obj)\n```\n\n普通递归会死循环。\n\n可以用缓存记录已拷贝对象：\n\n```js\nfunction deepClone(value, cache) {\n  cache = cache || []\n\n  if (value === null || typeof value !== 'object') {\n    return value\n  }\n\n  var hit = cache.find(item => item.source === value)\n\n  if (hit) {\n    return hit.copy\n  }\n\n  var copy = Array.isArray(value) ? [] : {}\n\n  cache.push({\n    source: value,\n    copy: copy\n  })\n\n  Object.keys(value).forEach(key => {\n    copy[key] = deepClone(value[key], cache)\n  })\n\n  return copy\n}\n```\n\n面试里能写到这一步，基本说明理解了引用和递归。\n\n这里用数组当缓存其实有点慢——`find` 每次都线性扫一遍，对象一多就拖。更地道的写法是用 `WeakMap`，key 直接存原对象，查找 O(1)，而且 `WeakMap` 不阻止垃圾回收、不会内存泄漏：\n\n```js\nfunction deepClone(value, cache = new WeakMap()) {\n  if (value === null || typeof value !== 'object') return value\n  if (cache.has(value)) return cache.get(value)\n\n  var copy = Array.isArray(value) ? [] : {}\n  cache.set(value, copy)\n\n  Object.keys(value).forEach(key => {\n    copy[key] = deepClone(value[key], cache)\n  })\n  return copy\n}\n```\n\n注意顺序：**先把 `copy` 存进缓存，再去递归子属性**。如果反过来，等递归到 `obj.self` 时缓存里还没有这个对象，又会陷进死循环——这一步的顺序就是循环引用能不能解掉的关键。我面试时被追问过「为什么要先 set 再递归」，答得上来这一点，基本就过了。\n\n补一个写这篇时还没有、但现在（回头补记）值得知道的标准方案：`structuredClone`。它是浏览器和 Node 17+ 内置的结构化克隆，能正确处理 `Date`、`Map`、`Set`、`ArrayBuffer`，连循环引用都能拷贝，不用自己维护缓存：\n\n```js\nvar m = structuredClone(new Map([['a', 1]]))\nm instanceof Map // => true\nm.get('a')       // => 1\n\nvar c = {}\nc.self = c\nvar sc = structuredClone(c)\nsc.self === sc   // => true   循环引用被正确克隆，没死循环\n```\n\n但它有个硬限制：**不能克隆函数**，碰到函数（或 DOM 节点等不可克隆对象）会抛 `DataCloneError`：\n\n```js\nstructuredClone({ fn: function () {} })\n// => 抛 DataCloneError（一个 DOMException）\n```\n\n所以「带函数的对象要深拷贝」这种需求，`structuredClone` 和 `JSON` 法都做不了，还是得手写或上库。\n\n说句实在话，2019 年项目里真要深拷贝，我大多直接 `import cloneDeep from 'lodash/cloneDeep'` 了事。它把 Date、RegExp、Map、Set、循环引用、Symbol 这些边界全处理好了，自己手写很难考虑这么周全。手写实现主要是面试用、和理解原理用，生产环境没必要重复造这个轮子。\n\n## 项目里什么时候需要深拷贝\n\n常见场景：\n\n- 编辑表单临时副本\n- 弹窗取消回滚\n- Vuex 状态快照\n- 拖拽排序前备份\n- 复杂配置复制\n\n但不要到处深拷贝。深拷贝有性能成本，也可能让引用关系丢失。\n\n比如列表渲染时每次都深拷贝大数组，会影响性能。更好的方式是明确哪些地方需要隔离修改，哪些地方只读。\n\n举两个我实际拿捏过的尺度。Vuex 里我反对「为了不报 mutation 警告就到处 `cloneDeep` 整个 state」，那性能账很亏；正确做法是只在 mutation 里改、组件拿到的当只读用。表单编辑场景，我会按数据深浅来选：平铺表单 `{ ...row }` 浅拷贝就够，带嵌套的（比如带 SKU 列表的商品）才上深拷贝，而且只拷要编辑的那部分，不整个对象一锅端。\n\n还有个反直觉的坑：深拷贝会**切断所有引用关系**。有时候这正是你要的（隔离），但有时候你其实依赖某些共享引用（比如内部缓存、DOM 节点引用、事件回调），深拷贝一刀切下去，这些全断了，功能反而坏掉。所以深拷贝不是「更安全的拷贝」，它是「语义不同的拷贝」，用之前想清楚你到底要不要断引用。\n\n\n浅拷贝复制第一层，深拷贝递归复制嵌套结构。JSON 方法简单但限制多，手写深拷贝要考虑数组、对象、特殊类型和循环引用。\n\n面试问这题，是因为它能看出你是否理解对象引用。项目里真正要做的是判断什么时候需要隔离数据，什么时候不该为了省心到处深拷贝。\n"},"recommendedPosts":[{"title":"看 ES2026 和 ESNext 新特性时，我更关心它什么时候能进业务代码","date":"2026-04-24","excerpt":"从不可变数据、资源管理和新语法落地说起，记录我如何判断 JavaScript 新特性是否适合进入真实项目。","tags":["前端基础","JavaScript"],"slug":"frontend-es2026-features","category":"2026","likeCount":85,"postId":"0ac5cfee-fe01-706f-b371-6fe7cc8af036","viewCount":910,"readTime":null,"coverImage":null},{"title":"用 Biome 替掉 ESLint + Prettier：一次工具链瘦身的得与失","date":"2026-04-18","excerpt":"把项目的 lint 和格式化从 ESLint+Prettier 换成 Biome 的过程：速度提升有多明显、规则迁移缺了什么、以及哪些场景我还是留着 ESLint。","tags":["前端基础","构建与模块化","JavaScript"],"slug":"biome-toolchain-2026","category":"2026","likeCount":6,"postId":"37e39142-8bf2-d042-6270-2228c894603c","viewCount":1089,"readTime":null,"coverImage":null},{"title":"TC39 Signals 提案与框架里的信号：响应式终于要进语言层了吗","date":"2026-03-07","excerpt":"从 Vue ref、Solid signal 到 TC39 的 Signals 提案，聊聊信号这套响应式模型解决了什么、polyfill 怎么用、以及它和现有框架的关系。","tags":["JavaScript","前端基础","组件与状态"],"slug":"js-signals-proposal-2026","category":"2026","likeCount":59,"postId":"10625fc7-1fb0-e156-eaf9-5cace589f74e","viewCount":691,"readTime":null,"coverImage":null}],"prevPost":{"title":"Linux 常用命令：部署前端项目时，不能只会 npm run build","date":"2019-03-19","excerpt":"从前端部署和服务器排查出发，整理 cd、ls、grep、tail、ps、curl、chmod、scp 等常用 Linux 命令。","tags":["构建与模块化"],"slug":"linux-command-line-basics-2019","category":"2019","likeCount":8,"postId":"aeaf3e93-16f2-0e61-6c80-29379113ad5f","viewCount":3844,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue 过渡动画怎么用才不显得多余：先服务交互，再谈效果","date":"2019-02-15","excerpt":"Vue 过渡动画先服务交互，再谈炫不炫；弹窗、列表和 transition-group 的坑我放在一起记录。","tags":["Vue"],"slug":"vue-transitions-2019","category":"2019","likeCount":9,"postId":"96f94d73-28d6-fb4f-acb1-5871fa38f537","viewCount":5349,"readTime":null,"coverImage":null}},"__N_SSG":true}