{"pageProps":{"postData":{"title":"JavaScript 里的 this 为什么总让人别扭：它不是指向写代码的人","date":"2021-04-21","excerpt":"围绕函数调用方式梳理 JavaScript 中 this 的常见取值规则，解释为什么它经常让人误判。","tags":["前端基础"],"slug":"frontend-javascript-this-2021","category":"2021","likeCount":5,"postId":"324cb1df-05aa-f7e1-87c3-004d82bc46f3","viewCount":1634,"readTime":"10分钟","coverImage":null,"content":"\nJavaScript 里 `this` 一直是很有代表性的一个坑。\n\n它不难遇到，但很容易理解得不稳定。很多人刚学时会把 `this` 想成“当前函数所在对象”，结果一换调用方式就乱了。我自己刚入行那两年，定位过不少诡异的 bug，最后查到根上都是 `this` 跑偏了——明明上一行还好好的，换个地方调一下就变成 `undefined`，或者悄悄指到了 `window` 上。\n\n真正要抓住的是：`this` 不是在函数定义时固定死的，它和函数是怎么被调用的关系更大。我后来把一句口诀放在脑子里：**看谁在点号左边**。绝大多数日常场景，`this` 就是调用时点号左边那个东西；没有点号、直接调用的，才需要去想严格模式和全局对象那一套。这句话不严谨，但能帮我在 review 代码时第一时间嗅出哪里会出问题。\n\n## 为什么 `this` 容易让人误会\n\n平时看代码时，我们习惯根据书写位置理解语义。\n\n例如一个函数写在对象里，直觉上就会觉得它里的 `this` 应该指向这个对象。但 JavaScript 并不是总按“写在哪里”来决定 `this`，而是更看“怎么调用”。\n\n这也是为什么同一个函数，换一种调用方式，`this` 就可能变。\n\n## 普通函数调用时，`this` 往往不按你想的来\n\n如果一个普通函数被直接调用：\n\n```javascript\nfunction show() {\n  console.log(this);\n}\n\nshow();\n```\n\n这时 `this` 的值取决于运行环境和严格模式。非严格模式下，浏览器里它指向 `window`，Node 里指向 `global`（模块作用域下其实是 `module.exports` 那套，又是另一层坑）；一旦加了 `'use strict'`，同样的 `show()` 里 `this` 就变成 `undefined` 了。\n\n```javascript\n'use strict';\nfunction show() {\n  console.log(this); // undefined\n}\nshow();\n```\n\n这个差异坑过我一次：一段在普通脚本里跑得好好的工具函数，被搬进一个用 Babel 编译、默认开了严格模式的模块后，里面 `this.xxx` 直接抛 `Cannot read properties of undefined`。当时还以为是打包配置出了问题，折腾半天才反应过来是严格模式把隐式的 `window` 兜底给掐掉了。说实话掐得对——那种依赖全局兜底的写法本来就该报错。\n\n也正因为行为不够直观，很多项目开发里会尽量避免依赖这种不明确的 `this`。\n\n## 作为对象方法调用时，`this` 才更像“当前对象”\n\n例如：\n\n```javascript\nconst user = {\n  name: 'Tom',\n  say() {\n    console.log(this.name);\n  }\n};\n\nuser.say();\n```\n\n这里 `say` 是通过 `user.say()` 这种方式调用的，所以 `this` 才会指向 `user`。\n\n但要注意，决定结果的是调用方式，不是函数写在对象字面量里这件事本身。\n\n如果把它拆出来：\n\n```javascript\nconst fn = user.say;\nfn(); // this 不再是 user，严格模式下是 undefined，打印就报错\n```\n\n那 `this` 就不再稳定指向 `user` 了。因为这一步 `const fn = user.say` 只是把函数本身拿出来赋给了一个变量，绑定关系在赋值那一刻就断了，等到 `fn()` 执行时点号左边已经空了。\n\n这个“拆出来就丢”的现象在实战里特别常见，而且往往是无意中发生的。比如把方法当回调传出去：\n\n```javascript\n// 看起来人畜无害，其实 this 已经丢了\nbutton.addEventListener('click', user.say);\n\n// 数组遍历里也是同理\n[1, 2, 3].forEach(user.say);\n\n// 解构更隐蔽，很多人完全没意识到这会断绑定\nconst { say } = user;\nsay();\n```\n\n我见过最典型的一次，是同事把一个对象方法直接 `setTimeout(obj.handle, 1000)` 传进去，结果一秒后 `this` 指向了 `window`（定时器回调就是这么调的），里面读的实例字段全是 `undefined`。正确写法要么 `setTimeout(() => obj.handle(), 1000)` 包一层箭头函数，要么 `setTimeout(obj.handle.bind(obj), 1000)` 提前锁死上下文。两种我都用，短回调用箭头函数更顺手，需要复用同一个绑定后的引用时才用 `bind`。\n\n## 事件处理里也很容易踩 `this`\n\n前端开发里常见的一类误区，发生在 DOM 事件回调中。\n\n例如有人以为回调里的 `this` 一定是外层组件实例，结果实际上它可能是触发事件的元素，或者在某些封装里根本不是你期待的值。\n\n用 `addEventListener` 加普通函数当回调时，`this` 是被绑定到了那个 DOM 元素上的——这其实是浏览器特意做的设定，方便你直接操作触发元素：\n\n```javascript\nbtn.addEventListener('click', function () {\n  console.log(this); // 这个 this 是 btn 本身\n  this.disabled = true; // 用起来很方便\n});\n```\n\n但麻烦在于，如果你想在回调里访问外层对象的状态，普通函数的这个 `this` 就帮倒忙了。我早年写过类似这样的代码，调了半天找不到 `this.list`：\n\n```javascript\nclass Panel {\n  constructor() {\n    this.list = [];\n    document.querySelector('#add').addEventListener('click', function () {\n      this.list.push(1); // ❌ this 是按钮，不是 Panel 实例，报错\n    });\n  }\n}\n```\n\n换成箭头函数就对了，因为它不抢 `this`，沿用了 `constructor` 执行时的实例：\n\n```javascript\naddEventListener('click', () => {\n  this.list.push(1); // ✅ this 是 Panel 实例\n});\n```\n\n代价是箭头函数里拿不到那个 DOM 元素了，只能从 `event.currentTarget` 取。所以到底用哪种，取决于回调里更想要谁：要操作触发元素就用普通函数，要操作外层状态就用箭头函数。\n\n也正因为这样，很多现代前端代码会倾向于少依赖隐式 `this`，而是显式使用参数、闭包或实例引用。\n\n## 箭头函数为什么能缓解一部分困扰\n\n箭头函数的一个重要特点，是它没有自己的 `this`，而是继承外层作用域的 `this`。\n\n这使得它在很多回调场景里更稳定：\n\n```javascript\nconst obj = {\n  count: 0,\n  addLater() {\n    setTimeout(() => {\n      this.count += 1;\n    }, 1000);\n  }\n};\n```\n\n如果这里用普通函数，`setTimeout` 里的 `this` 就未必还是外层对象。箭头函数则会沿用 `addLater` 执行时的 `this`。\n\n不过箭头函数也不是万能的。它因为没有自己的 `this`，所以并不适合所有需要动态绑定上下文的场景。有几个地方我是明确不用箭头函数的：\n\n- **对象方法**：写成箭头函数后，`this` 会沿用定义时的外层作用域（通常是模块顶层或 `window`），而不是这个对象。\n\n```javascript\nconst obj = {\n  name: 'Tom',\n  say: () => console.log(this.name) // ❌ this 不是 obj\n};\nobj.say(); // undefined\n```\n\n- **需要 `this` 指向触发元素的事件回调**，前面已经说过。\n- **原型方法、构造函数**：箭头函数没有 `prototype`，也不能 `new`，强行用直接报错。\n- **任何依赖 `arguments` 的老式函数**：箭头函数同样没有自己的 `arguments`。\n\n我的习惯是：默认写箭头函数，只有在“这个函数需要自己的动态 `this`”时才退回普通函数。这条规则覆盖了我日常九成以上的场景。\n\n## `call`、`apply`、`bind` 本质上是在控制调用上下文\n\n这三个方法总被一起讲，本质上都和手动指定 `this` 有关。\n\n```javascript\nfunction greet() {\n  console.log(this.name);\n}\n\nconst person = { name: 'Alice' };\n\ngreet.call(person);\n```\n\n这意味着你可以不改变函数定义位置，只改变它执行时绑定的上下文。三者的区别说穿了很简单：`call` 和 `apply` 都是立即执行，差别只在传参——`call` 一个个传，`apply` 用数组传；`bind` 不执行，而是返回一个永久绑定好 `this` 的新函数。\n\n```javascript\ngreet.call(person, 'a', 'b');     // 立即执行，逐个传参\ngreet.apply(person, ['a', 'b']);  // 立即执行，数组传参\nconst bound = greet.bind(person); // 不执行，返回锁定 this 的新函数\nbound();\n```\n\n`apply` 在 ES6 展开运算符出来之前用得很多，比如求数组最大值要写 `Math.max.apply(null, arr)`，现在直接 `Math.max(...arr)` 就行了，`apply` 的这类用法基本退场。\n\n`bind` 倒是一直没被淘汰。React 还在用 class 组件的那几年，构造函数里清一色 `this.handleClick = this.handleClick.bind(this)`，就是为了把方法传给 JSX 当回调时不丢 `this`。这里有个细节坑过我：**`bind` 一旦绑定就锁死了，再 `call` 也改不动**。\n\n```javascript\nconst bound = greet.bind(person);\nbound.call({ name: 'Bob' }); // 仍然打印 Alice，bind 的优先级更高\n```\n\n在一些工具函数、数组借用方法（比如 `Array.prototype.slice.call(arguments)` 把类数组转真数组）、老代码兼容场景里，这类写法还挺常见。新代码里我更愿意用箭头函数和扩展运算符替代，但读老项目时这套还是绕不开。\n\n## 真实项目里最稳的做法，往往是少制造模糊上下文\n\n我后来对 `this` 的态度越来越务实：理解它很重要，但写代码时要尽量少依赖不清楚的 `this`。\n\n例如：\n\n- 回调里优先用箭头函数或显式参数\n- 组件方法里明确调用入口\n- 工具函数尽量做成纯函数\n\n这样一来，就算你懂 `this` 的规则，也不用天天和它较劲。\n\nJavaScript 的灵活性有时就是这样，它给了你很多可能，但项目维护更看重稳定。`this` 不是不能用，而是要知道什么时候它会帮你，什么时候它只会制造歧义。\n\n## `this` 真正让人难受的，是它经常在“看起来差不多”的代码里变化\n\n例如同样是调用一个函数，写成对象方法调用、事件回调、定时器回调，或者把方法先取出来再执行，`this` 结果就可能完全不同。也正因为这种差异往往藏在调用方式里，而不是藏在函数体里，所以它特别容易造成误判。\n\n很多人不是不知道 `this` 有规则，而是规则一旦进入真实项目语境，就不再像单独小例子那么直观。\n\n## 在团队代码里，少制造模糊上下文通常更重要\n\n我后来越来越少把 `this` 当成“必须灵活使用的能力”，而更倾向于把它当成一个需要谨慎对待的上下文机制。因为项目里更重要的通常不是写法能不能成立，而是后来的人能不能一眼看明白。\n\n这也是为什么很多代码会偏向：\n\n- 用显式参数代替隐式上下文\n- 用箭头函数稳定回调里的外层引用\n- 让工具函数尽量保持纯粹\n\n理解 `this` 当然必要，但真正成熟一点的写法，往往是在理解之后主动减少歧义。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":20,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":246,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":44,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":293,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":43,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":224,"readTime":null,"coverImage":null}],"prevPost":{"title":"Flex 布局为什么这么常用：从主轴思维到常见踩坑","date":"2021-05-01","excerpt":"Flex 好用，但真正出问题时多半卡在主轴、交叉轴、伸缩规则和子项收缩这些基础细节。","tags":["前端基础"],"slug":"frontend-css-flex-layout-2021","category":"2021","likeCount":5,"postId":"b38dcde9-e50d-98e1-b541-3d8be8382aca","viewCount":2518,"readTime":"10分钟","coverImage":null},"nextPost":{"title":"CSS 优先级为什么总打架：选择器、继承和覆盖规则怎么理解","date":"2021-04-06","excerpt":"样式覆盖失效通常不是一个选择器的问题，而是优先级、作用域和团队写法长期叠在一起。","tags":["前端基础"],"slug":"frontend-css-selector-specificity-2021","category":"2021","likeCount":8,"postId":"110b85e0-fc8f-6b14-d033-e3ceb7ced6b1","viewCount":2232,"readTime":"10分钟","coverImage":null}},"__N_SSG":true}