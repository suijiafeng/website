{"pageProps":{"postData":{"title":"flv.js 直播优化：追帧、断流重连和延迟控制","date":"2022-07-21","excerpt":"直播播放器的问题很少只看代码就能定位，延迟、卡顿、断流和实例生命周期都要配合数据看。","tags":["前端基础","音视频","性能与缓存"],"slug":"frontend-flvjs-live-optimization-2022","category":"2022","likeCount":8,"postId":"83eefc7b-1b30-dfde-f416-2561a478dc10","viewCount":1997,"readTime":"11分钟","coverImage":null,"content":"\n在浏览器里做直播播放，flv.js 是一个常见选择。\n\n它可以把 FLV 流转换后交给 HTML5 Video 播放，让浏览器在不依赖 Flash 的情况下播放直播内容。\n\n但直播场景比普通视频复杂，最常见的问题就是延迟、卡顿和断流。\n\n普通点播视频更关心能不能完整播放、拖动是否顺畅；直播更关心实时性和恢复能力。用户进入直播间后，不一定能接受画面落后几十秒，也不能接受网络抖动后只能刷新页面。\n\n我第一次接手直播播放的需求时，天真地以为接个播放器、传个 url 就完事了。结果上线第二天就被投诉：有人说画面比解说慢半拍，有人说网络一抖就黑屏再也回不来，还有人在地铁里打开直接转圈到天荒地老。后来才明白，flv.js 给的只是\"能播\"，剩下\"播得好\"全要自己兜。\n\n所以做 flv.js 直播播放时，我通常会把目标拆成三件事：先稳定播放，再控制延迟，最后处理异常恢复。这三件事是有优先级的，顺序不能乱——延迟控制做得再好，断流恢复不了，用户照样跑光。\n\n现在如果从零做直播播放，我不会默认只看 flv.js。要先问清楚链路：延迟要求是秒级还是毫秒级，浏览器覆盖范围是什么，服务端已经提供 FLV、HLS、WebRTC 还是多协议都有。flv.js 很适合已有 HTTP-FLV 链路的低成本接入；如果追求极低延迟，可能要评估 WebRTC；如果更重视兼容和稳定，HLS 也经常是更现实的方案。播放器优化之前，协议选型就已经决定了上限。\n\n## 基础初始化\n\n使用 flv.js 前，要先判断当前环境是否支持：\n\n```javascript\nfunction createLivePlayer(videoElement, url) {\n  if (!flvjs.isSupported()) {\n    return {\n      ok: false,\n      message: '当前浏览器不支持直播播放',\n    };\n  }\n\n  const player = flvjs.createPlayer(\n    {\n      type: 'flv',\n      url,\n      isLive: true,\n    },\n    {\n      enableStashBuffer: false,\n      stashInitialSize: 128,\n    }\n  );\n\n  player.attachMediaElement(videoElement);\n  player.load();\n  player.play();\n\n  return {\n    ok: true,\n    player,\n  };\n}\n```\n\n`isLive: true` 表示这是直播流。`enableStashBuffer` 会影响缓冲策略，关闭后延迟可能更低，但抗网络抖动能力也会下降。这个参数不能盲目照抄，要结合实际网络环境测试。\n\n我踩过的一个坑是 `stashInitialSize`：它的单位是字节，不是秒。早期我以为这是缓冲秒数，随手填了个 384 想着\"缓三秒应该稳\"，结果延迟和卡顿一点没改善，因为 384 字节连一个关键帧都装不下。后来才搞清楚，这个 stash 是 IO 层的预缓冲，主要影响首帧速度和小抖动的平滑度，真正决定播放缓冲的是 video 元素自己的 buffered。所以低延迟场景我一般是 `enableStashBuffer: false` + 较小的 `stashInitialSize`，让数据尽快往 MSE 喂。\n\n还有几个我后来固定会加上的配置：\n\n```javascript\nconst player = flvjs.createPlayer(\n  {\n    type: 'flv',\n    url,\n    isLive: true,\n    hasAudio: true,\n    hasVideo: true,\n  },\n  {\n    enableStashBuffer: false,\n    stashInitialSize: 128,\n    // 直播必开，否则播放器会按点播逻辑无限往后缓冲，延迟越积越大\n    autoCleanupSourceBuffer: true,\n    autoCleanupMaxBackwardDuration: 30,\n    autoCleanupMinBackwardDuration: 10,\n  }\n);\n```\n\n`autoCleanupSourceBuffer` 这个我吃过亏：不开的话，SourceBuffer 里的数据只增不减，播了一两个小时之后内存涨到几百兆，低端安卓机直接把标签页崩掉。开了之后它会定期清理播放点后面的旧数据，把内存压在一个稳定区间。这是长时间挂着直播间才会暴露的问题，短时间测试根本发现不了。\n\n## 为什么直播会有延迟\n\n直播不是一次性下载完整文件，而是持续接收流数据。\n\n只要播放端积累的缓冲越来越多，用户看到的画面就会慢慢落后于真实直播时间。\n\n延迟来源可能包括：\n\n- 网络波动\n- 播放缓冲过长\n- 解码压力\n- 服务端推流不稳定\n- 浏览器处理不及时\n\n所以直播优化通常不是一个参数能解决的。\n\n延迟最好能量化，而不是只靠体感判断。浏览器的 `video.buffered` 可以看到当前缓冲区：\n\n```javascript\nfunction getBufferedEnd(video) {\n  const { buffered } = video;\n\n  if (!buffered.length) {\n    return 0;\n  }\n\n  return buffered.end(buffered.length - 1);\n}\n\nfunction getLiveDelay(video) {\n  const bufferedEnd = getBufferedEnd(video);\n\n  return bufferedEnd - video.currentTime;\n}\n```\n\n这不是绝对直播延迟，只是播放位置距离当前缓冲末尾的差值。对浏览器端追帧来说，它已经是很有用的指标。\n\n要分清楚两种延迟：一种是\"端到端延迟\"，从主播说话到你听见，这里面有推流、转码、CDN 分发，前端基本管不着；另一种是\"播放缓冲延迟\"，就是数据已经到了你浏览器、却堆在缓冲区里没播出来。前端能优化的只有后者。我一开始把两者混为一谈，盯着总延迟死磕参数，怎么调都降不下去——因为大头在 CDN 那边，前端再努力也只能抠那几秒缓冲。想清楚这点之后心态就平和多了，前端的目标是把 `bufferedEnd - currentTime` 控制在一个合理范围，别让它无限膨胀。\n\n## 追帧是什么\n\n追帧就是当播放器落后太多时，主动加快播放进度或跳到更靠近直播点的位置。\n\n简单理解：不要让播放器一直慢慢播旧内容。\n\n常见策略包括：\n\n- 检查当前缓冲区\n- 判断延迟是否超过阈值\n- 超过阈值时调整 `currentTime`\n- 必要时重新加载流\n\n追帧要谨慎，太激进会导致画面跳动，太保守又无法降低延迟。\n\n一个简单策略是设置两个阈值：轻微落后时加速播放，严重落后时直接跳到缓冲末尾附近。\n\n```javascript\nfunction syncLiveDelay(video) {\n  const delay = getLiveDelay(video);\n\n  if (delay > 8) {\n    video.currentTime = getBufferedEnd(video) - 1.5;\n    video.playbackRate = 1;\n    return;\n  }\n\n  if (delay > 4) {\n    video.playbackRate = 1.25;\n    return;\n  }\n\n  video.playbackRate = 1;\n}\n```\n\n这里的数字只是示例，不能当成通用答案。体育赛事、会议直播、监控画面，对延迟和跳帧的容忍度不一样。监控可能更重视实时，课程直播可能更重视连贯。\n\n讲两个调这套逻辑时的教训。一是 `playbackRate` 别拉太高。我有次为了快点追上，直接给到 1.5 倍速，结果音频明显变调，用户感觉像是\"主播突然吸了口气加速说话\"，体感很怪。后来收敛到 1.25 以内，加速过程几乎察觉不到，落后个几秒钟用十几秒慢慢追平，比硬跳要平滑得多。二是硬跳 `currentTime` 之后一定要留一点余量，我代码里写的是跳到 `bufferedEnd - 1.5` 而不是直接贴到 `bufferedEnd`。贴太近会立刻触发 `waiting`，因为缓冲末尾的数据还没解码好，跳过去就卡住，反而陷入\"跳→卡→再跳\"的循环。留 1 到 1.5 秒缓冲是用脚试出来的安全垫。\n\n追帧逻辑也不要过于频繁执行，可以用定时器每隔几秒检查一次：\n\n```javascript\nconst delayTimer = window.setInterval(() => {\n  syncLiveDelay(videoElement);\n}, 3000);\n```\n\n组件销毁时要清理这个定时器，否则页面切换后仍然会继续访问旧 video。\n\n## 断流重连\n\n直播流可能因为网络、服务端、推流端异常而中断。\n\n播放器需要监听错误状态，并在合适时机重连。\n\n重连时要注意：\n\n- 避免无限快速重试\n- 设置最大重试次数\n- 每次重试前销毁旧实例\n- 给用户明确的加载或失败提示\n\n如果旧实例没有清理干净，可能会造成内存泄漏或重复请求。\n\n重连不能简单地 `createPlayer` 一遍。旧实例要完整销毁：\n\n```javascript\nfunction destroyPlayer(player) {\n  if (!player) {\n    return;\n  }\n\n  try {\n    player.pause();\n    player.unload();\n    player.detachMediaElement();\n    player.destroy();\n  } catch (error) {\n    console.warn('destroy flv player failed', error);\n  }\n}\n```\n\n重连还要做退避，避免服务端已经异常时，前端每秒疯狂重试：\n\n```javascript\nfunction getRetryDelay(retryCount) {\n  return Math.min(1000 * 2 ** retryCount, 15000);\n}\n```\n\n前三次可以快一点，后面逐步拉长间隔。达到最大次数后，要进入明确失败状态，而不是无限转圈。\n\n完整的重连流程我一般写成这样，关键是状态锁和销毁顺序：\n\n```javascript\nlet retryCount = 0;\nlet reconnecting = false;\nconst MAX_RETRY = 6;\n\nfunction reconnect() {\n  // 状态锁：同一时间只允许一个重连流程，\n  // 否则错误事件和卡顿超时可能同时触发，建出两个播放器\n  if (reconnecting) {\n    return;\n  }\n  reconnecting = true;\n\n  if (retryCount >= MAX_RETRY) {\n    setPlayerStatus(PlayerStatus.ERROR, '直播连接失败，请稍后重试');\n    reconnecting = false;\n    return;\n  }\n\n  setPlayerStatus(PlayerStatus.RETRYING);\n  destroyPlayer(currentPlayer);\n  currentPlayer = null;\n\n  const delay = getRetryDelay(retryCount);\n  retryCount += 1;\n\n  window.setTimeout(() => {\n    const result = createLivePlayer(videoElement, getFreshUrl());\n    reconnecting = false;\n    if (result.ok) {\n      currentPlayer = result.player;\n      // 注意：成功播放后再清零，不要一进 reconnect 就清，\n      // 否则永远到不了 MAX_RETRY\n    }\n  }, delay);\n}\n```\n\n`reconnecting` 这个锁是我后来补上的。早期没加，遇到过 flv.js 的 ERROR 事件和 `waiting` 超时几乎同时触发，两条路径各自调了一次 `reconnect`，结果同一个 video 上挂了两个 player 实例，画面鬼畜，控制台报一堆 MSE 错误。加了锁之后干净很多。还有 `retryCount` 的清零时机也很微妙——一定要等真正播放成功（比如监听到 `playing` 事件）再清零，如果一进重连就清，计数永远到不了上限，达不到\"放弃\"那一步。\n\n页面生命周期也要管。用户切换路由、关闭弹窗、浏览器标签页进入后台时，播放器不能还在后台拉流。我一般会在组件卸载时统一做三件事：清定时器、解绑事件、销毁 player。直播页如果只是隐藏，不是真的离开，也要明确是继续播放、暂停，还是降级到低频检查，不能让资源消耗变成隐形问题。\n\n```javascript\nfunction cleanupLivePlayer() {\n  window.clearInterval(delayTimer);\n  window.clearTimeout(waitingTimer);\n  videoElement.removeEventListener('waiting', handleWaiting);\n  videoElement.removeEventListener('playing', handlePlaying);\n  destroyPlayer(currentPlayer);\n  currentPlayer = null;\n}\n```\n\n## 统一错误状态\n\n直播错误来源很多，flv.js 错误、video 元素错误、网络错误、播放权限错误都有可能出现。页面层最好不要直接处理一堆底层事件，而是收敛成统一状态。\n\n```javascript\nconst PlayerStatus = {\n  IDLE: 'idle',\n  LOADING: 'loading',\n  PLAYING: 'playing',\n  RETRYING: 'retrying',\n  ERROR: 'error',\n};\n\nfunction normalizePlayerError(error) {\n  return {\n    ok: false,\n    code: error?.code || 'LIVE_PLAYER_ERROR',\n    message: '直播播放异常，正在尝试恢复',\n  };\n}\n```\n\n这样 UI 可以稳定地根据状态展示加载、重连、失败提示，而不是每个错误分支里各自操作 DOM。\n\nflv.js 可以监听错误事件：\n\n```javascript\nplayer.on(flvjs.Events.ERROR, (errorType, errorDetail) => {\n  handlePlayerError({\n    code: errorType,\n    detail: errorDetail,\n  });\n});\n```\n\n错误处理里要避免重复重连。可以用状态锁或重试计数控制，确保同一时间只有一个重连流程。\n\n## 实时更新播放地址\n\n有些直播地址会带鉴权参数，或者服务端会切换流地址。\n\n这时播放器要支持更新播放源。\n\n实际处理时，通常要先停止旧流，再创建新实例，最后重新挂载到 video 元素上。\n\n不要直接在复杂状态下硬改 url，否则可能出现不可预期的播放问题。\n\n可以把“切流”当成一次完整重建：\n\n```javascript\nfunction switchLiveUrl(nextUrl) {\n  destroyPlayer(currentPlayer);\n\n  const result = createLivePlayer(videoElement, nextUrl);\n\n  if (!result.ok) {\n    setPlayerStatus(PlayerStatus.ERROR, result.message);\n    return;\n  }\n\n  currentPlayer = result.player;\n  setPlayerStatus(PlayerStatus.PLAYING);\n}\n```\n\n如果直播地址带过期签名，还要在重连前重新向服务端获取地址。不要拿已经过期的 URL 一直重试，否则前端看起来在努力恢复，实际永远不会成功。\n\n## 卡顿和缓冲处理\n\n卡顿不一定等于断流。有时候只是网络抖动，缓冲短暂不足。\n\n可以监听 video 元素事件：\n\n```javascript\nvideoElement.addEventListener('waiting', () => {\n  setPlayerStatus(PlayerStatus.LOADING);\n});\n\nvideoElement.addEventListener('playing', () => {\n  setPlayerStatus(PlayerStatus.PLAYING);\n});\n```\n\n如果长时间处于 `waiting`，再触发重连：\n\n```javascript\nlet waitingTimer = null;\n\nvideoElement.addEventListener('waiting', () => {\n  waitingTimer = window.setTimeout(() => {\n    reconnect();\n  }, 8000);\n});\n\nvideoElement.addEventListener('playing', () => {\n  window.clearTimeout(waitingTimer);\n});\n```\n\n这样可以避免一遇到轻微卡顿就重建播放器。直播优化里，过度恢复本身也会制造不稳定。\n\n## 用户体验边界\n\n直播播放器不应该只有一个黑屏。\n\n至少要有这些状态：\n\n- 加载中\n- 正在重连\n- 播放失败\n- 浏览器不支持\n- 用户手动暂停\n\n移动端还要注意自动播放限制。很多浏览器要求视频静音后才能自动播放，或者必须由用户手势触发播放。所以 `player.play()` 可能失败，应该捕获：\n\n```javascript\nvideoElement.play().catch(() => {\n  showPlayButton();\n});\n```\n\n这不是播放器异常，而是浏览器策略。给用户一个明确播放按钮，比静默失败更好。\n\n## 监控指标\n\n上线后只靠用户反馈不够。可以记录一些基础指标：\n\n- 首帧时间\n- 卡顿次数\n- 重连次数\n- 当前延迟\n- 播放失败错误码\n\n这些数据能帮助你判断问题来自网络、服务端、播放器配置还是浏览器兼容。没有监控时，直播问题很容易变成\"有些用户说卡\"，但团队不知道从哪里查。\n\n我还会把播放器版本、浏览器、网络类型、当前协议一起带上。直播问题很依赖环境，同一套代码在桌面 Chrome、安卓 WebView、iOS Safari 里的表现可能完全不同。没有这些维度，指标只能告诉你“卡了”，很难告诉你“哪一类用户卡”。\n\n埋点的时候有几个细节值得记下来。首帧时间我是从用户点开页面（或调 `load()`）那一刻开始计时，到 video 的 `loadeddata` 事件为止，而不是用 `play()` 调用时间，因为很多浏览器要等用户手势才真正解码，用 play 的时间会算出一堆假的超长首帧。卡顿次数我统计的是 `waiting` 事件触发的频次，同时记下每次卡顿的持续时长——偶尔一次半秒的抖动和持续好几秒的卡死，对体验的影响完全不是一个量级，只数次数会掩盖严重问题。延迟则是按固定间隔上报 `getLiveDelay` 的采样值，这样能在后台画出延迟随时间的曲线，一眼看出是稳定偏高还是某个时间点突然飙升。\n\n```javascript\nconst metrics = {\n  startAt: performance.now(),\n  firstFrameTime: 0,\n  stallCount: 0,\n  retryCount: 0,\n};\n\nvideoElement.addEventListener('loadeddata', () => {\n  if (!metrics.firstFrameTime) {\n    metrics.firstFrameTime = performance.now() - metrics.startAt;\n    reportMetric('first_frame', metrics.firstFrameTime);\n  }\n}, { once: true });\n\nvideoElement.addEventListener('waiting', () => {\n  metrics.stallCount += 1;\n});\n```\n\n有了这些数据之后，再遇到\"有人说卡\"，我能直接去后台按用户的网络类型、机型分布过滤，往往一眼就能看出是某类低端机内存吃紧，还是某个区域的 CDN 节点抖动，定位效率高了不止一个档次。\n\n\nflv.js 能解决浏览器播放 FLV 直播流的问题，但真正上线还要处理延迟、断流、重连、实例销毁和用户提示。\n\n直播优化要把稳定放在前面：能播放、能恢复、延迟可控，比单纯追求某个参数更重要。\n\n我现在做直播播放时，会把播放器封装成一个明确状态机：创建、播放、追帧、卡顿、重连、销毁都要有边界。flv.js 提供的是底层播放能力，真正的用户体验还要靠外层状态管理和错误恢复来兜住。\n"},"recommendedPosts":[{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":44,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":293,"readTime":null,"coverImage":null},{"title":"Vite 用上 Rolldown 之后：一次构建提速的真实体感和注意点","date":"2026-02-07","excerpt":"Rolldown 把 Vite 的打包从 esbuild/Rollup 双引擎统一成一套 Rust 实现，记录我在中型项目里切过去的体感、兼容性问题和回退策略。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"vite-rolldown-2026","category":"2026","likeCount":8,"postId":"27afacf5-76c4-4c26-58fd-15ea35bac2d8","viewCount":1571,"readTime":null,"coverImage":null},{"title":"WebAssembly 3.0 我会怎么判断该不该用：别把它当成更快的 JS","date":"2025-09-20","excerpt":"从图像处理、文件解析和跨语言复用说起，记录我如何判断 WebAssembly 是否真的适合前端项目，以及它会带来哪些成本。","tags":["前端基础","JavaScript","性能与缓存"],"slug":"frontend-webassembly-3-2025","category":"2025","likeCount":51,"postId":"62dae08d-dc55-8a4c-89a2-8f2d8c981441","viewCount":460,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端拖拽排序怎么选库：别一上来就自己写","date":"2022-07-27","excerpt":"从拖拽排序落地细节出发，聊聊选库、数据同步、移动端手势、并发保存和可访问性这些生产问题。","tags":["前端基础","交互与动效"],"slug":"frontend-drag-sort-libraries-2022","category":"2022","likeCount":55,"postId":"1cebaf33-2eed-3c86-20e4-44af5730f2db","viewCount":792,"readTime":null,"coverImage":null},"nextPost":{"title":"前端测试实践：Vitest 和 Cypress 分别该测什么","date":"2022-06-09","excerpt":"从几次线上问题反复出现的经历出发，聊聊 Vitest 和 Cypress 分别该保护什么，以及前端测试怎么从低成本场景开始补。","tags":["前端基础","构建与模块化"],"slug":"frontend-testing-vitest-cypress-2022","category":"2022","likeCount":6,"postId":"d4eeba88-80ba-2f65-8763-ac5da1a534df","viewCount":2027,"readTime":null,"coverImage":null}},"__N_SSG":true}