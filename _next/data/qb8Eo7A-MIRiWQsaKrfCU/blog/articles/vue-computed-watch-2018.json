{"pageProps":{"postData":{"title":"Vue 里 computed 和 watch 到底怎么选：别把两种能力写成一回事","date":"2018-11-16","excerpt":"回到 Vue 日常开发最容易混淆的一组能力，通过派生状态、异步副作用和错误处理示例讲清 computed 和 watch 的职责边界。","tags":["Vue","前端基础"],"slug":"vue-computed-watch-2018","category":"2018","likeCount":6,"postId":"afe7173c-7cea-829f-39ab-d16d3ae725af","viewCount":1249,"readTime":"9分钟","coverImage":null,"content":"\n刚开始用 Vue 时，很多人都会把 `computed` 和 `watch` 混着用。\n\n表面上看，它们都能“跟着数据变化做点事”，写起来也都不难。可项目一大，差别就会越来越明显。该放在 `computed` 里的逻辑写进了 `watch`，后面会越来越绕；反过来，把副作用塞进 `computed`，代码也会慢慢变得不好维护。\n\n这两个能力看着相近，职责其实完全不同。\n\n我自己也是踩过坑才把这件事想明白的。早些时候做一个表单页，里面有一堆联动字段：选了省份要联动市、选了套餐要联动价格、勾了优惠券要重算实付金额。我当时全用 `watch` 写，一个字段变了就在 handler 里手动去改另外几个字段。一开始还行，等需求加到第五版，那个组件的 `watch` 块已经互相触发、彼此依赖，改一个地方另一个地方就莫名其妙地不更新或者更新两次。最后调一个“金额偶尔差一分钱”的 bug 调了一下午，根因就是几个 `watch` 的触发顺序在某种操作下变了。那次重构我把所有“派生值”全挪进 `computed`，组件代码直接少了三分之一，bug 也没了。从那以后我对这条边界特别敏感。\n\n## 先说最短的结论\n\n如果你只是想基于已有状态推导出一个新值，用 `computed`。\n\n如果你是想在某个值变化后执行副作用，比如发请求、写本地存储、调第三方接口，用 `watch`。\n\n这个边界一旦立住，很多代码会立刻顺起来。\n\n更直接一点：`computed` 应该像一个值，`watch` 应该像一个动作。值要稳定、可预测；动作可以处理副作用，但要控制边界。\n\n## computed 更像“结果”\n\n`computed` 最适合表达派生状态。\n\n例如购物车里你已经有：\n\n- 商品列表\n- 数量\n- 单价\n\n那总价就很适合放进 `computed`。\n\n```javascript\ncomputed: {\n  totalPrice() {\n    return this.items.reduce((sum, item) => {\n      return sum + item.price * item.count;\n    }, 0);\n  },\n},\n```\n\n这个值不需要手动维护，也不需要额外存一份。只要原始数据变了，结果自然就变。\n\n这就是 `computed` 最舒服的地方：它让状态表达更完整，而不是让你再多维护一份变量。\n\n如果页面上还要展示商品数量，也可以继续派生：\n\n```javascript\ncomputed: {\n  selectedItems() {\n    return this.items.filter((item) => item.checked);\n  },\n  selectedCount() {\n    return this.selectedItems.length;\n  },\n  selectedTotalPrice() {\n    return this.selectedItems.reduce((sum, item) => {\n      return sum + item.price * item.count;\n    }, 0);\n  },\n},\n```\n\n这些值都不应该手动存在 `data` 里。它们只是由 `items` 推导出来的结果。\n\n这里还藏着一个 `computed` 容易被忽略的好处：缓存。`computed` 是带依赖缓存的，只要 `items` 没变，无论模板里读多少次 `selectedTotalPrice`，那个 `reduce` 都只算一遍。我之前在一个列表页里把一段稍微复杂的过滤逻辑写成了 `methods` 里的函数，模板里 `{{ filterRows() }}` 调了好几处，结果每次重渲染那个过滤都跑很多遍，列表一长就明显卡。换成 `computed` 之后立刻顺了。所以判断该用 `computed` 还是 `methods`，除了语义，性能也是个实打实的理由：模板里要反复读、且依赖不常变的派生值，优先 `computed`。\n\n不过缓存也有个反直觉的坑：`computed` 只追踪它“响应式依赖”的东西。如果你在 `computed` 里读了 `Date.now()` 或者某个非响应式的外部变量，它不会因为时间流逝而重算，因为 Vue 根本不知道那个东西变了。这种“为什么我的 computed 不更新”的问题，十有八九是依赖里混进了 Vue 追踪不到的值。\n\n## watch 更像“反应”\n\n`watch` 则不是拿来推导结果的，它更像对变化的响应。\n\n比如用户切换搜索条件后，你要重新拉一遍列表：\n\n```javascript\nwatch: {\n  keyword(newValue) {\n    this.fetchList(newValue);\n  },\n},\n```\n\n这里真正有意义的不是“生成了一个新值”，而是“变化发生后，要去做一件额外的事”。\n\n请求、日志、埋点、缓存同步，这些都属于 `watch` 更擅长的范围。\n\n实际项目里，请求要处理加载、失败和过期响应：\n\n```javascript\ndata() {\n  return {\n    keyword: '',\n    list: [],\n    loading: false,\n    errorMessage: '',\n    requestId: 0,\n  };\n},\nwatch: {\n  keyword(newValue) {\n    this.search(newValue);\n  },\n},\nmethods: {\n  async search(keyword) {\n    const currentRequestId = this.requestId + 1;\n\n    this.requestId = currentRequestId;\n    this.loading = true;\n    this.errorMessage = '';\n\n    const result = await this.requestList(keyword);\n\n    if (currentRequestId !== this.requestId) {\n      return;\n    }\n\n    this.loading = false;\n\n    if (!result.ok) {\n      this.errorMessage = result.message;\n      return;\n    }\n\n    this.list = result.data;\n  },\n  async requestList(keyword) {\n    try {\n      const response = await fetch(`/api/search?keyword=${encodeURIComponent(keyword)}`);\n\n      if (!response.ok) {\n        return { ok: false, message: '搜索失败，请稍后重试' };\n      }\n\n      return { ok: true, data: await response.json() };\n    } catch (error) {\n      return { ok: false, message: '网络异常，请检查连接' };\n    }\n  },\n},\n```\n\n这里 `watch` 只是触发动作，真正的请求和统一错误处理放在方法里。这样后面换触发方式时，逻辑也能复用。\n\n上面那段 `requestId` 的写法别看不起眼，它解决的是异步副作用里最经典的“竞态”问题。用户在搜索框里快速输入，先发出去的请求可能比后发的还晚回来，如果不加判断，屏幕上最终显示的就是早先那次搜索的结果，和当前输入框里的关键词对不上。我第一次遇到这个 bug 是在一个城市选择器里：快速切几个城市，列表最后停在了某个中间城市的数据上，怎么都复现不稳定。后来才明白是响应顺序的锅。`watch` 触发异步副作用时，几乎都要考虑这个问题——这也正是为什么副作用不该塞进 `computed`，`computed` 的心智模型里根本没有“哪次结果该作废”这回事。\n\n实际项目里我还会给这种搜索 `watch` 配上防抖，避免每敲一个字就打一次接口：\n\n```javascript\nimport debounce from 'lodash/debounce';\n\nwatch: {\n  keyword(newValue) {\n    this.debouncedSearch(newValue);\n  },\n},\ncreated() {\n  this.debouncedSearch = debounce(this.search, 300);\n},\n```\n\n注意 `debounce` 包出来的函数要在 `created` 里挂到实例上，而不是直接写在 `methods` 里。如果写在 `methods` 里，组件多个实例会共享同一个防抖函数，互相干扰。这个细节也是被坑过才记住的。\n\n## 为什么很多代码会把它们写反\n\n因为 `watch` 看起来什么都能做。\n\n你完全可以监听某个字段变化，再手动去改另一个字段，于是代码慢慢变成这样：\n\n```javascript\ndata() {\n  return {\n    firstName: 'Su',\n    lastName: 'Jf',\n    fullName: '',\n  };\n},\nwatch: {\n  firstName() {\n    this.fullName = this.firstName + ' ' + this.lastName;\n  },\n  lastName() {\n    this.fullName = this.firstName + ' ' + this.lastName;\n  },\n},\n```\n\n功能没问题，但结构已经开始别扭了。\n\n因为 `fullName` 并不是独立状态，它只是另外两个值的组合。既然只是组合，就不该手动维护。\n\n这种写法改成 `computed` 会更自然，也更不容易漏掉边界情况。\n\n```javascript\ncomputed: {\n  fullName() {\n    return `${this.firstName} ${this.lastName}`;\n  },\n},\n```\n\n判断状态是否应该存在 `data` 里，可以问一句：如果刷新或重新计算，这个值能不能从其他状态恢复出来？能恢复出来的，多半就是派生状态。\n\n把这种值放进 `data` 再用 `watch` 同步，除了啰嗦，还有个隐患：你必须监听所有相关字段，漏一个就出 bug。上面那段如果只 `watch` 了 `firstName` 忘了 `lastName`，改姓不改名时 `fullName` 就是错的，而且这种 bug 不报错、不抛异常，只是数据悄悄不对，特别难发现。`computed` 从机制上就杜绝了这种“漏监听”，因为它的依赖是 Vue 自动收集的，你用到了哪个值它就追踪哪个，根本不存在“忘了写”的可能。这是我更倾向 `computed` 的一个底层原因——它把“别漏掉依赖”这件事从人脑转移给了框架。\n\n## 一个简单判断办法\n\n可以问自己一句话：\n\n“这个值是要算出来，还是要顺手做点别的？”\n\n如果答案是“算出来”，优先 `computed`。\n\n如果答案是“要顺手去做请求、写缓存、触发外部逻辑”，那就是 `watch`。\n\n再补几个例子：\n\n- 表单里 `firstName + lastName` 得到 `fullName`：用 `computed`\n- 购物车商品列表得到总价：用 `computed`\n- 搜索条件变化后请求接口：用 `watch`\n- 主题切换后写入本地存储：用 `watch`\n- 路由参数变化后重新加载详情：用 `watch`\n\n## computed 也能写，但要克制\n\n很多人以为 `computed` 只能读不能写，其实它支持 `get` 和 `set` 两种形态。最典型的用处是给 Vuex 的状态做 `v-model` 双向绑定：模板里要双向绑，但 Vuex 不允许直接改 state，于是用带 setter 的 `computed` 当中间层：\n\n```javascript\ncomputed: {\n  keyword: {\n    get() {\n      return this.$store.state.search.keyword;\n    },\n    set(value) {\n      this.$store.commit('search/setKeyword', value);\n    },\n  },\n},\n```\n\n这样模板里 `<input v-model=\"keyword\">` 就能正常用，读走 getter、写触发 commit。但要提醒一句：setter 里只该做“把值写回源头”这一件事，别在里面顺手发请求、做校验。一旦 setter 里塞了副作用，它就又变成了一个伪装成 `computed` 的 `watch`，前面说的那些边界问题会原样回来。读归读、写归写、副作用归 `watch`，这条线在 `computed` 内部也得守。\n\n## 别让 watch 变成补丁桶\n\n项目里有个很常见的现象：一开始只是监听一个字段，后面慢慢加条件、加分支、加节流、加异步请求，最后一个 `watch` 变成了功能中心。\n\n这并不是 `watch` 的错，而是它太容易被拿来打补丁。\n\n所以在写 `watch` 时最好克制一点，只让它做“变化后的动作”，不要拿它承担状态建模。\n\n复杂 watch 至少要拆出方法：\n\n```javascript\nwatch: {\n  filters: {\n    deep: true,\n    handler() {\n      this.reloadList();\n    },\n  },\n},\nmethods: {\n  reloadList() {\n    this.resetPagination();\n    this.fetchList();\n  },\n},\n```\n\n如果 `watch` 的 handler 已经长到几十行，通常说明它承担了太多职责。\n\n## 立即执行和深度监听要慎用\n\n`watch` 支持 `immediate` 和 `deep`：\n\n```javascript\nwatch: {\n  filters: {\n    immediate: true,\n    deep: true,\n    handler(newFilters) {\n      this.fetchList(newFilters);\n    },\n  },\n},\n```\n\n`immediate` 适合组件初始化时就执行一次副作用。`deep` 适合监听对象内部变化，但它成本更高，也更容易造成“改一个字段触发一堆逻辑”。\n\n很多时候，把监听对象拆成更明确的字段会更清楚：\n\n```javascript\nwatch: {\n  categoryId() {\n    this.fetchList();\n  },\n  keyword() {\n    this.fetchList();\n  },\n},\n```\n\n不要为了省几行代码，把所有变化都塞进一个深度监听。\n\n`deep` 的代价具体在哪？它会递归遍历整个对象建立依赖，对象层级深、字段多的时候，每次比对都有开销，而且 handler 拿到的 `newVal` 和 `oldVal` 往往是同一个引用——因为深度监听的是同一个对象内部变化，对象本身没换，新旧值指向同一处。我被这个坑过：想在 handler 里对比改动前后的差异，结果 `newVal === oldVal` 永远成立，拿不到旧值。要保留旧值得自己提前深拷贝一份，成本就更高了。所以能用 `string`/`number` 这种基本类型的字段精确监听，就别图省事上 `deep`。\n\n`immediate` 也有个我常用的小判断：如果你写完一个 `watch`，发现 `created` 或 `mounted` 里还得手动再调一次同样的逻辑来做初始化，那这个 `watch` 多半就该加 `immediate: true`，省掉那次重复调用，也避免初始化逻辑和监听逻辑哪天改得不一致。\n\n\n`computed` 和 `watch` 都很重要，但它们处理的问题不是一类。\n\n一个是“结果”，一个是“反应”。当你不再把它们混着用，Vue 代码会轻很多，状态关系也会更清楚。\n\n优先用 `computed` 表达派生关系，用 `watch` 处理副作用。只要这个边界守住，组件里的状态流就不会轻易变成一团线。\n"},"recommendedPosts":[{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2023-09-17","excerpt":"无限级菜单看着是递归组件，实际牵着路由、高亮、权限、图标和接口数据清洗，边界要先想清楚。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2023","category":"2023","likeCount":5,"postId":"73d4dc9e-8fc7-3ce9-8dcb-31a0122f0b98","viewCount":3749,"readTime":null,"coverImage":null},{"title":"刚开始用 Vue3 时，最容易踩的几个坑","date":"2023-06-07","excerpt":"从我把 Vue2 项目迁到 Vue3 时踩过的坑出发，聊聊响应式、props、watch、组合式 API、生命周期清理和 Pinia 使用误区。","tags":["Vue","前端基础"],"slug":"vue3-common-mistakes-2023","category":"2023","likeCount":6,"postId":"06bb42c9-56d9-0865-c906-88f606e068a9","viewCount":1085,"readTime":null,"coverImage":null},{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"从后台项目里反复写 loading/data/error 三件套的经历出发，聊聊请求库为什么会从 HTTP 客户端走向状态管理、缓存和统一错误处理。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2362,"readTime":null,"coverImage":null}],"prevPost":{"title":"CSS transition 入门：实现简单的滑入滑出效果","date":"2018-12-20","excerpt":"从滑入滑出和展开收起两个小需求出发，记录 transition 真正卡人的点：状态起点、重排、性能和可访问性。","tags":["前端基础","CSS"],"slug":"frontend-css-transition-slide-2018","category":"2018","likeCount":5,"postId":"2ff304f8-e478-f10a-7dba-6fbb62801faa","viewCount":1205,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue 动态组件和 keep-alive：后台 tab 页别每次都重新加载","date":"2018-10-28","excerpt":"记录 Vue 后台系统中动态组件、keep-alive、tab 缓存、刷新页面和状态清理的实践经验。","tags":["Vue","前端基础"],"slug":"vue-dynamic-components-tabs-2018","category":"2018","likeCount":6,"postId":"6360f47d-c9da-cca9-475f-aff8d6cea6c9","viewCount":2909,"readTime":null,"coverImage":null}},"__N_SSG":true}