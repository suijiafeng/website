{"pageProps":{"postData":{"title":"Vue 生命周期别背顺序了：先弄清楚每个阶段该做什么","date":"2019-11-23","excerpt":"把 Vue 生命周期从死记硬背拉回到工程实践，讨论数据请求、DOM 操作、更新时机、keep-alive 和资源清理应该放在哪里。","tags":["Vue"],"slug":"vue-lifecycle-2019","category":"2019","likeCount":5,"postId":"11946d43-16f0-646e-ea58-e6095bbba5db","viewCount":3362,"readTime":"5分钟","coverImage":null,"content":"\n学 Vue 时，生命周期几乎是绕不过去的一章。\n\n很多人一开始会背：\n\n- `created`\n- `mounted`\n- `updated`\n- `destroyed`\n\n顺序当然要知道，但真正写项目时，更重要的问题其实是：哪些逻辑该放在哪个阶段。\n\n## 生命周期不是为了背名词\n\n生命周期存在的意义，是让你知道组件此刻处于什么状态。\n\n数据是不是已经初始化了，DOM 是不是已经挂载了，页面是不是刚更新完，这些信息会直接决定你在这个阶段能做什么。\n\n可以把生命周期理解成组件运行过程中的几个检查点。每个检查点能做的事不同，能拿到的资源也不同。\n\n## 先建立阶段感\n\n一个常见的 Vue 2 组件，大致会经历：\n\n- `beforeCreate`：实例刚开始初始化，数据和方法还没准备好\n- `created`：数据、方法、计算属性已经准备好，DOM 还没挂载\n- `mounted`：DOM 已经进入页面，可以访问真实节点\n- `updated`：响应式数据导致 DOM 更新完成\n- `beforeDestroy` / `destroyed`：组件准备销毁或已经销毁\n\n如果使用 `keep-alive`，还会遇到：\n\n- `activated`：缓存组件重新激活\n- `deactivated`：缓存组件被切走\n\n记顺序只是开始，关键是知道每个阶段的能力边界。\n\n## created 更适合处理数据准备\n\n到了 `created`，组件的数据和方法已经准备好了，但真实 DOM 还没挂到页面上。\n\n所以这个阶段更适合：\n\n- 初始化数据\n- 发起请求\n- 做纯逻辑处理\n\n如果你只是要拿数据、准备状态，很多时候放在这里就够了。\n\n例如列表页初始化请求：\n\n```javascript\nexport default {\n  data() {\n    return {\n      loading: false,\n      list: [],\n      errorMessage: '',\n      destroyed: false,\n    };\n  },\n  created() {\n    this.fetchList();\n  },\n  beforeDestroy() {\n    this.destroyed = true;\n  },\n  methods: {\n    async fetchList() {\n      this.loading = true;\n      this.errorMessage = '';\n\n      const result = await this.requestList();\n\n      if (this.destroyed) {\n        return;\n      }\n\n      this.loading = false;\n\n      if (!result.ok) {\n        this.errorMessage = result.message;\n        return;\n      }\n\n      this.list = result.data;\n    },\n    async requestList() {\n      try {\n        const response = await fetch('/api/list');\n\n        if (!response.ok) {\n          return { ok: false, message: '列表加载失败，请稍后重试' };\n        }\n\n        return { ok: true, data: await response.json() };\n      } catch (error) {\n        return { ok: false, message: '网络异常，请检查连接' };\n      }\n    },\n  },\n};\n```\n\n这里把接口结果统一成 `{ ok, data, message }`，组件就不需要在多个地方写不同的异常文案。`destroyed` 标记用于避免组件销毁后继续写状态。\n\n## mounted 才适合碰 DOM\n\n如果你要做的是：\n\n- 获取元素尺寸\n- 操作第三方 DOM 库\n- 依赖页面真实节点\n\n那就更适合放在 `mounted`。\n\n因为只有到了这个阶段，组件的 DOM 才真正进了页面。\n\n例如初始化第三方图表：\n\n```javascript\nexport default {\n  mounted() {\n    this.chart = createChart(this.$refs.chart);\n    this.chart.setOption(this.chartOption);\n  },\n  beforeDestroy() {\n    if (this.chart) {\n      this.chart.dispose();\n      this.chart = null;\n    }\n  },\n};\n```\n\n如果 DOM 依赖某个状态变化后的最新渲染结果，可以用 `$nextTick`：\n\n```javascript\nmethods: {\n  async openPanel() {\n    this.visible = true;\n\n    await this.$nextTick();\n    this.$refs.input.focus();\n  },\n},\n```\n\n`mounted` 代表初次挂载完成，`nextTick` 代表这次响应式更新对应的 DOM 已经刷新。两者解决的问题不同。\n\n## updated 不是补丁桶\n\n很多人一发现界面更新后要再做点事，就会想到 `updated`。它当然能用，但也最容易被滥用。\n\n因为一旦组件更新频繁，这个钩子也会跟着频繁触发。\n\n所以如果你只是想在某个字段变化后做动作，很多时候用 `watch` 会更明确，而不是把逻辑一股脑塞进 `updated`。\n\n例如不要在 `updated` 里判断一堆字段：\n\n```javascript\nupdated() {\n  if (this.keyword !== this.previousKeyword) {\n    this.fetchList();\n  }\n}\n```\n\n更清楚的方式是直接监听目标：\n\n```javascript\nwatch: {\n  keyword() {\n    this.fetchList();\n  },\n},\n```\n\n`updated` 更适合处理“任意更新后都要同步的 DOM 任务”，而不是业务状态变化的分发中心。即便要用，也要非常克制，避免在里面继续改响应式数据造成更新循环。\n\n## destroyed 别只想着“组件被删了”\n\n这个阶段真正要关注的是清理。\n\n例如：\n\n- 解绑事件\n- 清理定时器\n- 销毁第三方实例\n\n这些事情如果不做，页面看起来可能没马上出问题，但后面很容易留下隐患。\n\n典型清理包括：\n\n```javascript\nexport default {\n  mounted() {\n    window.addEventListener('resize', this.handleResize);\n    this.timer = window.setInterval(this.poll, 5000);\n  },\n  beforeDestroy() {\n    window.removeEventListener('resize', this.handleResize);\n    window.clearInterval(this.timer);\n  },\n};\n```\n\n如果组件里有订阅、WebSocket、第三方实例、定时器、全局事件，基本都应该有对应清理。\n\n## keep-alive 下要注意激活和停用\n\n被 `keep-alive` 包住的组件不会每次切换都销毁，所以 `created` 和 `mounted` 不会反复执行。\n\n如果你希望页面每次回到前台都刷新，可以用 `activated`：\n\n```javascript\nexport default {\n  activated() {\n    this.refreshIfNeeded();\n  },\n  deactivated() {\n    this.pauseVideo();\n  },\n};\n```\n\n这类页面最容易出问题的地方是：你以为组件重新创建了，实际上它只是从缓存里恢复。列表滚动位置、筛选条件、定时器状态，都要根据业务决定是保留还是重置。\n\n## 生命周期不是业务架构\n\n生命周期只是时机，不应该成为业务逻辑的唯一组织方式。\n\n如果 `created` 里有几十行请求、权限、格式化和状态拼装，问题不在生命周期，而在逻辑没有拆分。可以把请求放到 service，把格式化放到工具函数，把复杂状态拆到子组件或组合函数。\n\n生命周期里最好只做调度：\n\n```javascript\ncreated() {\n  this.initPage();\n},\nmethods: {\n  async initPage() {\n    await this.loadUser();\n    await this.loadList();\n  },\n},\n```\n\n它告诉读代码的人“什么时候开始”，而不是把所有细节都塞在钩子里。\n\n## 写在最后\n\nVue 生命周期真正难的地方，不是顺序，而是时机感。\n\n什么时候该拿数据，什么时候该碰 DOM，什么时候该做清理，只要这些边界分清楚，生命周期就不再是一串需要背诵的名字，而是你组织组件逻辑的坐标点。\n\n专业一点的写法是：`created` 准备数据，`mounted` 处理真实 DOM，`watch` 响应具体状态，`updated` 谨慎使用，销毁阶段彻底清理资源，`keep-alive` 单独考虑激活和停用。\n"},"randomPosts":[{"title":"前端 API 错误处理：不要让每个页面各写一套兜底","date":"2026-05-02","excerpt":"说明为什么前端项目需要统一 API 错误处理，以及如何把网络错误、业务错误和页面提示拆开。","tags":["前端开发","工程化"],"slug":"frontend-api-error-handling-2026","category":"2026","likeCount":13,"postId":"9319de77-fb0b-1e95-b6b6-b08bb46e1453","viewCount":51,"readTime":null,"coverImage":null},{"title":"CSS transition 入门：实现简单的滑入滑出效果","date":"2018-12-20","excerpt":"介绍 CSS transition 的基本用法，并用滑入滑出、遮罩和展开收起示例说明状态变化、性能和可访问性处理。","tags":["前端开发"],"slug":"frontend-css-transition-slide-2018","category":"2018","likeCount":5,"postId":"2ff304f8-e478-f10a-7dba-6fbb62801faa","viewCount":1201,"readTime":null,"coverImage":null},{"title":"Vue 3 响应式变化初看：Proxy 带来的不只是写法变化","date":"2020-06-27","excerpt":"从我迁移和阅读 Vue 3 代码时的感受出发，聊聊 Proxy 响应式带来的不只是底层实现变化，还有状态建模方式的变化。","tags":["Vue"],"slug":"vue3-reactivity-2020","category":"2020","likeCount":5,"postId":"4e0e4e1a-88a5-ccba-163b-ffb9a2b130a3","viewCount":2296,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 单文件组件写久了以后，真正拉开差距的是组织方式","date":"2020-03-21","excerpt":"讨论 Vue 单文件组件在真实项目里怎么组织更稳，从职责拆分、目录结构、服务层、样式边界和大组件治理角度避免 .vue 文件失控。","tags":["Vue"],"slug":"vue-sfc-organization-2020","category":"2020","likeCount":5,"postId":"9d9e32d2-1561-6dc5-590d-1081279404e6","viewCount":1793,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue 异步组件值得早点用：页面越大，按需加载越像基本功","date":"2019-10-08","excerpt":"从加载优先级、chunk 拆分、加载失败处理和用户路径几个角度，说明 Vue 异步组件为什么是页面结构设计的一部分。","tags":["Vue","性能优化"],"slug":"vue-async-components-2019","category":"2019","likeCount":7,"postId":"3ddfaba3-8409-e84f-4001-fd4d6b1073d8","viewCount":3999,"readTime":null,"coverImage":null}},"__N_SSG":true}