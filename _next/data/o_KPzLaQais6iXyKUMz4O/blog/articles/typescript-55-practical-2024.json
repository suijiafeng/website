{"pageProps":{"postData":{"title":"TypeScript 5.5 实用变化：类型推断越来越懂业务代码","date":"2024-06-28","excerpt":"围绕 TypeScript 5.5 的推断改进和项目实践，讨论类型收窄、数组过滤、配置边界和团队落地方式。","tags":["TypeScript","前端基础"],"slug":"typescript-55-practical-2024","category":"2024","likeCount":5,"postId":"fa5d2ef8-0fba-56af-0db0-a46175a0cb51","viewCount":2355,"readTime":"5分钟","coverImage":null,"content":"\nTypeScript 的版本更新很容易被低估。很多人只关心有没有新语法，但真正影响日常开发体验的，往往是类型推断变聪明了一点、错误提示更准确了一点、某些以前需要手写类型守卫的地方现在能自动理解。\n\nTypeScript 5.5 给我的一个明显感受是：它越来越能理解业务代码里常见的判断逻辑。尤其是数组过滤、类型收窄这类场景，写起来比以前顺了不少。\n\n这篇文章不追求罗列所有新特性，而是从项目里最常见的几个痛点讲起。\n\n## 数组过滤后的类型问题\n\n以前写 TypeScript，经常会遇到这样的代码：\n\n```ts\ntype User = {\n  id: string\n  name: string\n}\n\nconst users: Array<User | null> = [\n  { id: '1', name: 'Alice' },\n  null,\n  { id: '2', name: 'Bob' },\n]\n\nconst validUsers = users.filter((user) => user !== null)\n```\n\n直觉上，`validUsers` 应该是 `User[]`。但在一些旧版本或复杂写法里，TypeScript 不一定能准确推断出来，后续访问可能还要处理 `null`。\n\n于是项目里会出现工具函数：\n\n```ts\nfunction isNonNullable<T>(value: T): value is NonNullable<T> {\n  return value !== null && value !== undefined\n}\n\nconst validUsers = users.filter(isNonNullable)\n```\n\n这种写法很稳，但对新手不友好。很多业务开发只是想过滤空值，却被迫学习类型谓词。\n\nTypeScript 5.5 对这类推断做了改进，让常见过滤逻辑更容易得到正确结果。它的意义不是让类型体操更强，而是减少业务代码里的“为了哄类型系统而写的代码”。\n\n## 类型守卫仍然重要\n\n推断变聪明，不代表类型守卫不需要了。\n\n当判断逻辑比较复杂时，显式类型守卫仍然更清楚：\n\n```ts\ntype ApiSuccess<T> = {\n  ok: true\n  data: T\n}\n\ntype ApiFailure = {\n  ok: false\n  error: {\n    message: string\n    code?: string\n  }\n}\n\ntype ApiResult<T> = ApiSuccess<T> | ApiFailure\n\nfunction isSuccess<T>(result: ApiResult<T>): result is ApiSuccess<T> {\n  return result.ok\n}\n```\n\n使用时：\n\n```ts\nconst results = await Promise.allSettled([\n  fetchUser(),\n  fetchPosts(),\n])\n\nconst fulfilled = results.filter(\n  (result): result is PromiseFulfilledResult<unknown> =>\n    result.status === 'fulfilled'\n)\n```\n\n这里显式写类型谓词更合适，因为它表达的是业务结构，不只是简单的空值过滤。\n\n我的习惯是：简单判断交给推断，跨模块复用的业务判断写成类型守卫。\n\n## 类型收窄要贴近运行时\n\nTypeScript 类型不能替代运行时校验。尤其是接口返回、URL 参数、本地存储、消息事件，这些数据都来自外部，不能只靠 `as`。\n\n危险写法：\n\n```ts\nconst user = await response.json() as User\nconsole.log(user.name.toUpperCase())\n```\n\n这只是告诉 TypeScript “相信我”，但运行时并没有任何保证。\n\n更稳的方式是做基础校验：\n\n```ts\ntype User = {\n  id: string\n  name: string\n}\n\nfunction parseUser(value: unknown): User {\n  if (\n    typeof value === 'object' &&\n    value !== null &&\n    'id' in value &&\n    'name' in value &&\n    typeof value.id === 'string' &&\n    typeof value.name === 'string'\n  ) {\n    return value\n  }\n\n  throw {\n    code: 'INVALID_USER',\n    message: 'Invalid user response',\n  }\n}\n```\n\n然后：\n\n```ts\nconst body = await response.json()\nconst user = parseUser(body)\n```\n\n类型系统负责开发期约束，运行时校验负责边界安全。两者不能互相替代。\n\n## satisfies 比 as 更适合配置\n\n项目里经常有配置对象：\n\n```ts\ntype RouteConfig = {\n  path: string\n  title: string\n  auth?: boolean\n}\n\nconst routes = [\n  { path: '/', title: '首页' },\n  { path: '/admin', title: '管理后台', auth: true },\n] satisfies RouteConfig[]\n```\n\n`satisfies` 的好处是：它检查对象满足指定类型，同时保留更具体的字面量信息。\n\n如果写成：\n\n```ts\nconst routes = [\n  { path: '/', title: '首页' },\n] as RouteConfig[]\n```\n\n`as` 更像是断言，容易掩盖错误。\n\n我在配置、路由表、菜单、权限点、埋点事件名里很喜欢用 `satisfies`。这些地方既需要结构约束，又希望保留具体值，方便后续推导联合类型。\n\n```ts\nconst events = {\n  login: 'user_login',\n  logout: 'user_logout',\n  submitPost: 'post_submit',\n} as const satisfies Record<string, string>\n\ntype EventName = keyof typeof events\n```\n\n## 严格模式不要一次性硬上\n\n很多老项目想升级 TypeScript，第一步就打开所有严格配置：\n\n```json\n{\n  \"compilerOptions\": {\n    \"strict\": true\n  }\n}\n```\n\n然后报出几千个错误，团队直接放弃。\n\n更现实的方式是分阶段推进：\n\n```json\n{\n  \"compilerOptions\": {\n    \"noImplicitAny\": true,\n    \"strictNullChecks\": true\n  }\n}\n```\n\n先处理最有价值的两类问题：隐式 `any` 和空值错误。\n\n`strictNullChecks` 对业务质量提升非常明显。很多线上错误本质上就是没有处理 `null`、`undefined`：\n\n```ts\nfunction getUserName(user?: User) {\n  return user.name\n}\n```\n\n开启后，TypeScript 会要求你面对这个边界：\n\n```ts\nfunction getUserName(user?: User) {\n  return user?.name || '匿名用户'\n}\n```\n\n这不是类型系统挑刺，而是在逼你把运行时可能发生的事情写清楚。\n\n## 类型不要过度抽象\n\nTypeScript 用久了，很容易走向另一个极端：什么都想抽象成高级类型。\n\n例如：\n\n```ts\ntype DeepReadonly<T> = {\n  readonly [K in keyof T]: T[K] extends object\n    ? DeepReadonly<T[K]>\n    : T[K]\n}\n```\n\n这类工具类型有价值，但不应该成为业务代码的常态。业务团队最需要的是可读、可维护、错误清楚，而不是展示类型技巧。\n\n我更倾向于把类型分三层：\n\n- 领域类型：User、Post、Order\n- 边界类型：ApiResponse、FormState、RouteParams\n- 工具类型：少量通用辅助\n\n领域类型要清楚，边界类型要稳定，工具类型要克制。\n\n## API 错误类型要统一\n\n既然用了 TypeScript，就应该把 API 错误结构也统一起来。\n\n```ts\ntype ApiError = {\n  status: number\n  code: string\n  message: string\n}\n\ntype ApiResponse<T> =\n  | { ok: true; data: T }\n  | { ok: false; error: ApiError }\n```\n\n请求函数：\n\n```ts\nasync function request<T>(url: string): Promise<ApiResponse<T>> {\n  try {\n    const response = await fetch(url)\n    const body = await response.json().catch(() => null)\n\n    if (!response.ok) {\n      return {\n        ok: false,\n        error: {\n          status: response.status,\n          code: body?.error?.code || 'HTTP_ERROR',\n          message: body?.error?.message || '请求失败',\n        },\n      }\n    }\n\n    return { ok: true, data: body as T }\n  } catch (error) {\n    return {\n      ok: false,\n      error: {\n        status: 0,\n        code: 'NETWORK_ERROR',\n        message: '网络异常',\n      },\n    }\n  }\n}\n```\n\n页面使用时：\n\n```ts\nconst result = await request<User>('/api/user')\n\nif (!result.ok) {\n  showToast(result.error.message)\n  return\n}\n\nrenderUser(result.data)\n```\n\n这种联合类型能让调用方必须处理失败分支，比到处 `throw` 再随缘 `catch` 更适合一些业务页面。\n\n## 小结\n\nTypeScript 5.5 的意义不只是新增几个特性，而是让类型推断更贴近日常业务写法。数组过滤、类型收窄、配置校验这些场景越顺，开发者越愿意让类型系统参与真实项目。\n\n但类型系统不是万能的。外部数据要运行时校验，错误结构要统一，严格模式要分阶段推进，高级类型要克制使用。写 TypeScript 的目标不是让代码看起来更复杂，而是让边界更清楚、重构更放心、线上错误更少。\n"},"randomPosts":[{"title":"React Hooks 入门指南","date":"2023-08-15","excerpt":"探索 React Hooks 的基础知识，了解如何简化你的函数组件","tags":["其他"],"slug":"introduction-to-react-hooks","category":"2023","likeCount":7,"postId":"8cd9c4fa-d5ef-897b-1e8b-ef602f10e233","viewCount":1883,"readTime":null,"coverImage":null},{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"介绍现代请求库从简单 HTTP 客户端走向请求状态管理、缓存、分页和框架集成的趋势。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2342,"readTime":null,"coverImage":null},{"title":"前端缓存基础：强缓存、协商缓存和资源更新到底该怎么理解","date":"2022-10-03","excerpt":"介绍前端开发中最常见的浏览器缓存机制，帮助你理解资源为什么有时更新了却没生效。","tags":["前端基础","性能与缓存"],"slug":"frontend-http-cache-2022","category":"2022","likeCount":5,"postId":"893c3552-ee46-d6a3-fb1d-42e18cf3373c","viewCount":3140,"readTime":null,"coverImage":null}],"prevPost":{"title":"Next.js App Router 缓存理解：页面为什么没有按你想的更新","date":"2024-07-18","excerpt":"从实际问题出发梳理 Next.js App Router 中的静态渲染、fetch 缓存、revalidate、no-store 和手动刷新策略。","tags":["React","性能与缓存"],"slug":"frontend-nextjs-app-router-cache-2024","category":"2024","likeCount":7,"postId":"240dd306-8bd6-dcdf-2abd-1601499a6871","viewCount":1576,"readTime":null,"coverImage":null},"nextPost":{"title":"React 19 表单与 Actions：别再把提交状态全塞进 useState","date":"2024-06-12","excerpt":"结合 React 19 的 Actions、useActionState、useFormStatus 和 useOptimistic，讨论表单提交流程、乐观更新和错误处理的更清晰写法。","tags":["React","前端基础"],"slug":"react-19-actions-form-2024","category":"2024","likeCount":26,"postId":"32b76503-f486-38fb-e293-699c0cdea8a5","viewCount":826,"readTime":null,"coverImage":null}},"__N_SSG":true}