{"pageProps":{"postData":{"title":"Next.js App Router 缓存理解：页面为什么没有按你想的更新","date":"2024-07-18","excerpt":"从实际问题出发梳理 Next.js App Router 中的静态渲染、fetch 缓存、revalidate、no-store 和手动刷新策略。","tags":["React","性能与缓存"],"slug":"frontend-nextjs-app-router-cache-2024","category":"2024","likeCount":7,"postId":"240dd306-8bd6-dcdf-2abd-1601499a6871","viewCount":1576,"readTime":"6分钟","coverImage":null,"content":"\nNext.js App Router 刚开始用时，最容易让人困惑的不是路由，而是缓存。\n\n很多问题看起来像 bug：接口明明返回了新数据，页面却还是旧的；本地开发正常，部署后不更新；加了 `revalidate` 以后，有时更新有时不更新；后台刚发布文章，前台列表过一会儿才变化。\n\n这些现象背后通常不是框架失控，而是缓存层次没有想清楚。App Router 把渲染、数据获取和缓存绑定得更紧，性能上有好处，但也要求开发者明确表达数据新鲜度。\n\n## 先不要急着写 no-store\n\n遇到页面不更新，很多人的第一反应是：\n\n```js\nfetch(url, { cache: 'no-store' })\n```\n\n这确实能解决“旧数据”问题，但它也会放弃缓存收益。所有请求都实时获取，页面响应变慢，服务端压力变大，CDN 也帮不上太多忙。\n\n更合理的做法是先判断数据类型：\n\n- 博客文章：分钟级或小时级更新可以接受\n- 商品库存：需要更短缓存，甚至实时\n- 用户订单：通常不能共享缓存\n- 后台列表：优先保证数据正确\n- 首页推荐：可以缓存，但要允许后台刷新\n\n不同数据应该有不同策略。缓存不是开关，而是业务新鲜度的表达。\n\n## 静态渲染和动态渲染\n\nApp Router 默认会尽量把页面静态化。只要页面没有使用动态函数，也没有显式要求实时数据，Next.js 就可能在构建时或缓存中复用结果。\n\n一个博客列表可以这样写：\n\n```tsx\nexport default async function BlogPage() {\n  const posts = await fetch('https://api.example.com/posts', {\n    next: { revalidate: 3600 },\n  }).then((res) => res.json())\n\n  return (\n    <main>\n      {posts.map((post) => (\n        <article key={post.id}>{post.title}</article>\n      ))}\n    </main>\n  )\n}\n```\n\n这表示数据最多缓存一小时。用户访问速度会比较快，内容也会周期性更新。\n\n如果页面必须每次请求都读取最新数据，可以使用：\n\n```tsx\nexport const dynamic = 'force-dynamic'\n\nexport default async function OrdersPage() {\n  const orders = await getOrders()\n  return <OrderTable orders={orders} />\n}\n```\n\n或者针对请求使用：\n\n```tsx\nawait fetch('https://api.example.com/orders', {\n  cache: 'no-store',\n})\n```\n\n两者不是完全一样的心智。`force-dynamic` 更像是告诉页面“这个路由整体动态”，`no-store` 则是告诉某个请求“这份数据不要缓存”。\n\n## fetch 缓存不是浏览器缓存\n\nApp Router 中的 `fetch` 缓存发生在 Next.js 的数据层，不要把它简单理解成浏览器 HTTP 缓存。\n\n例如：\n\n```tsx\nconst data = await fetch('https://api.example.com/posts', {\n  next: { revalidate: 60 },\n}).then((res) => res.json())\n```\n\n这里的含义是：Next.js 可以复用这次数据结果，60 秒后允许重新验证。它不是让用户浏览器缓存 60 秒，而是影响服务端渲染和数据获取行为。\n\n这也是为什么很多“我接口已经变了，页面怎么没变”的问题，不能只看 Network 面板。你需要同时理解 Next.js 是否复用了上一次渲染结果。\n\n## revalidate 的适用场景\n\n`revalidate` 适合“可以短时间接受旧数据”的页面。\n\n```tsx\nawait fetch('https://api.example.com/articles', {\n  next: { revalidate: 300 },\n})\n```\n\n这表示 5 分钟内复用缓存，过期后允许重新生成。\n\n适合它的场景包括：\n\n- 博客列表\n- 文档目录\n- 商品分类\n- 营销页配置\n- 首页非强实时模块\n\n不适合它的场景包括：\n\n- 用户余额\n- 支付状态\n- 权限菜单\n- 订单详情\n- 私密消息\n\n我做项目时会把数据分成“内容数据”和“账户数据”。内容数据尽量缓存，账户数据默认动态。这个分类比死记 API 更有用。\n\n## 手动刷新：revalidatePath 和 revalidateTag\n\n如果后台发布文章后希望前台立刻更新，仅靠定时 `revalidate` 不够。这时需要手动刷新缓存。\n\n可以给请求打 tag：\n\n```tsx\nawait fetch('https://api.example.com/posts', {\n  next: {\n    tags: ['posts'],\n    revalidate: 3600,\n  },\n})\n```\n\n后台发布文章后调用：\n\n```ts\nimport { revalidateTag } from 'next/cache'\n\nexport async function POST(request: Request) {\n  await publishPost(request)\n  revalidateTag('posts')\n\n  return Response.json({ ok: true })\n}\n```\n\n也可以按路径刷新：\n\n```ts\nimport { revalidatePath } from 'next/cache'\n\nrevalidatePath('/blog')\nrevalidatePath('/blog/my-new-post')\n```\n\n我的经验是：列表适合 tag，详情页适合 path。比如文章发布影响文章列表、标签页、归档页，这些可以用 `posts` tag 管起来；单篇文章更新，则可以刷新具体详情路径。\n\n## 客户端刷新不等于服务端缓存失效\n\nApp Router 里客户端可以调用 `router.refresh()`：\n\n```tsx\n'use client'\n\nimport { useRouter } from 'next/navigation'\n\nexport default function RefreshButton() {\n  const router = useRouter()\n\n  return <button onClick={() => router.refresh()}>刷新</button>\n}\n```\n\n它会重新请求当前路由的服务端组件 payload。但如果服务端数据本身还命中缓存，用户看到的仍然可能是旧数据。\n\n所以 `router.refresh()` 不是万能刷新按钮。它适合“重新获取当前路由结果”，但不等于“清空所有缓存”。如果数据需要失效，还是要配合 `revalidatePath`、`revalidateTag` 或 `no-store`。\n\n## 统一错误处理也要考虑缓存\n\n缓存策略里还有一个容易忽略的点：错误响应要不要缓存？\n\n比如文章详情页：\n\n```tsx\nexport default async function PostPage({ params }) {\n  const post = await fetchPost(params.slug)\n\n  if (!post) {\n    notFound()\n  }\n\n  return <Article post={post} />\n}\n```\n\n如果接口短暂异常，你不能把异常结果当成正常空数据缓存起来。请求层应该区分“业务不存在”和“服务异常”：\n\n```ts\nasync function fetchJson(url: string) {\n  const response = await fetch(url, {\n    next: { revalidate: 300 },\n  })\n\n  const body = await response.json().catch(() => null)\n\n  if (response.status === 404) {\n    return null\n  }\n\n  if (!response.ok) {\n    throw {\n      status: response.status,\n      message: body?.error?.message || 'Request failed',\n    }\n  }\n\n  return body\n}\n```\n\n404 可以走 `notFound()`，500 应该抛给错误边界。不要把所有失败都当空数据处理，否则会把系统问题伪装成内容不存在。\n\n## 调试缓存的几个方法\n\n排查缓存问题时，我一般按这个顺序：\n\n第一，确认页面是否用了动态函数，例如 cookies、headers、searchParams。它们会影响渲染模式。\n\n第二，检查每个 `fetch` 的缓存配置，是默认、`no-store`，还是 `revalidate`。\n\n第三，确认数据源是否本身还有缓存，比如接口网关、CDN、数据库查询缓存。\n\n第四，确认是否触发了 `revalidatePath` 或 `revalidateTag`，以及 tag 名称是否一致。\n\n第五，区分本地开发和生产部署。开发环境为了体验会有一些行为差异，不要只凭本地判断。\n\n一个很实用的小技巧是在服务端打印时间戳：\n\n```tsx\nconsole.log('render posts page', new Date().toISOString())\n```\n\n如果刷新页面时没有新的日志，说明页面结果可能被复用了；如果有日志但数据旧，问题可能在数据源或 fetch 缓存。\n\n## 业务上怎么定策略\n\n我会让每个页面在评审时回答三个问题：\n\n第一，用户能接受看到多久以前的数据？\n\n第二，数据是否和当前登录用户相关？\n\n第三，后台操作后是否需要立刻对前台可见？\n\n答案决定策略：\n\n```text\n内容页：revalidate 3600 + 发布后 revalidatePath\n文章列表：revalidate 300 + revalidateTag('posts')\n用户订单：cache: 'no-store'\n后台管理：force-dynamic 或 no-store\n营销页配置：revalidate 600 + 后台手动刷新\n```\n\n这样写出来的缓存策略是可解释的，而不是遇到问题就到处加 `no-store`。\n\n## 小结\n\nNext.js App Router 的缓存不是一个单点功能，而是渲染模式、`fetch` 策略、路径刷新、tag 刷新和业务新鲜度共同作用的结果。\n\n遇到页面不更新，不要第一时间关闭所有缓存。先判断数据属于内容、账户、交易还是后台管理，再选择 `revalidate`、`no-store`、`force-dynamic`、`revalidatePath` 或 `revalidateTag`。\n\n缓存写清楚了，Next.js 会带来很好的性能收益；缓存写含糊了，页面就会变成“有时更新，有时不更新”的黑盒。\n"},"randomPosts":[{"title":"React Context 性能优化笔记：2025 年还该不该滥用全局状态","date":"2025-11-18","excerpt":"讨论 React Context 在中大型项目中的边界、常见误用，以及更稳妥的状态拆分思路。","tags":["React","组件与状态","性能与缓存"],"slug":"react-context-performance-2025","category":"2025","likeCount":27,"postId":"db7109f3-9a87-58bf-7673-6d4cfc58f288","viewCount":669,"readTime":null,"coverImage":null},{"title":"Ajax、JSONP 和跨域：接口联调时最常被问的老问题","date":"2018-05-09","excerpt":"结合接口联调经验，整理 XMLHttpRequest、Ajax、JSONP、CORS、预检请求和统一错误处理这些常见面试点。","tags":["JavaScript","前端基础"],"slug":"ajax-jsonp-cors-interview-2018","category":"2018","likeCount":5,"postId":"e2cded49-c9c7-f85d-8697-13b49a2d98dc","viewCount":4620,"readTime":null,"coverImage":null},{"title":"Vue Router 导航守卫：登录拦截和权限控制别写成一团","date":"2019-09-13","excerpt":"从后台系统常见登录拦截出发，整理 Vue Router beforeEach、白名单、动态权限、next 调用和接口错误处理。","tags":["Vue","组件与状态"],"slug":"vue-router-guard-permission-interview-2019","category":"2019","likeCount":8,"postId":"26b8c2e8-9b7b-456a-d711-c9eca5adc8e8","viewCount":2325,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端监控从 Web Vitals 开始：别只在用户投诉后才看性能","date":"2024-08-09","excerpt":"围绕 Web Vitals、错误采集、接口耗时和用户行为上下文，梳理前端监控体系的落地方式和边界。","tags":["前端基础","性能与缓存","构建与模块化"],"slug":"frontend-observability-web-vitals-2024","category":"2024","likeCount":28,"postId":"a4472209-40c8-7d31-0d01-f7a0a89cea1f","viewCount":838,"readTime":null,"coverImage":null},"nextPost":{"title":"TypeScript 5.5 实用变化：类型推断越来越懂业务代码","date":"2024-06-28","excerpt":"围绕 TypeScript 5.5 的推断改进和项目实践，讨论类型收窄、数组过滤、配置边界和团队落地方式。","tags":["TypeScript","前端基础"],"slug":"typescript-55-practical-2024","category":"2024","likeCount":5,"postId":"fa5d2ef8-0fba-56af-0db0-a46175a0cb51","viewCount":2355,"readTime":null,"coverImage":null}},"__N_SSG":true}