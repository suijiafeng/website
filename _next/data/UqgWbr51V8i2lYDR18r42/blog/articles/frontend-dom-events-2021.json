{"pageProps":{"postData":{"title":"DOM 事件机制入门：捕获、冒泡和事件委托到底该怎么理解","date":"2021-11-21","excerpt":"介绍 DOM 事件机制中的几个核心概念，帮助你理解事件传播、事件委托以及日常开发中的常见问题。","tags":["前端基础"],"slug":"frontend-dom-events-2021","category":"2021","likeCount":7,"postId":"0c1209e1-551f-35a4-a489-a3c18ab70bb4","viewCount":1827,"readTime":"12分钟","coverImage":null,"content":"\n前端开发里，事件是最基础也最常见的能力之一。\n\n点击按钮、输入内容、提交表单、滚动页面，这些交互的背后其实都离不开事件机制。\n\n实际开发中大家会直接使用事件，但一旦遇到下面这些情况，就会发现自己其实并没有真正理解它：\n\n- 为什么点击子元素时父元素的事件也触发了\n- 为什么有些事件监听加在父级上也能生效\n- 为什么 `stopPropagation` 有时能解决问题，有时又不够\n\n这些问题都和 DOM 事件机制有关。\n\n## 什么是事件传播\n\n当你点击一个页面元素时，浏览器并不是只把这次点击交给当前元素处理。\n\n它会沿着 DOM 树进行一轮传播，这个传播过程通常分成三个阶段：\n\n1. 捕获阶段\n2. 目标阶段\n3. 冒泡阶段\n\n简单理解就是，事件会先从外往里走，再从里往外走。\n\n假设页面结构是这样：\n\n```html\n<div id=\"app\">\n  <button id=\"save\">保存</button>\n</div>\n```\n\n当点击按钮时，事件路径大致会经过 `window`、`document`、`html`、`body`、`#app`、`#save`，然后再沿着路径往回走。捕获阶段是从外到内，冒泡阶段是从内到外。\n\n大多数时候我们写的监听器都在冒泡阶段执行，因为 `addEventListener` 默认的 `capture` 是 `false`。\n\n## 冒泡为什么这么重要\n\n在日常开发中，最常遇到的通常是冒泡。\n\n例如：\n\n```javascript\nparent.addEventListener('click', () => {\n  console.log('parent clicked');\n});\n\nchild.addEventListener('click', () => {\n  console.log('child clicked');\n});\n```\n\n当点击子元素时，通常会先触发子元素，再触发父元素。这就是冒泡。\n\n理解这一点很重要，因为很多交互问题其实不是“事件绑错了”，而是没有意识到事件还会继续向上传播。\n\n一个很典型的场景是卡片点击：\n\n```html\n<article class=\"card\">\n  <h3>文章标题</h3>\n  <button type=\"button\" class=\"delete\">删除</button>\n</article>\n```\n\n```javascript\ncard.addEventListener('click', () => {\n  openDetail();\n});\n\ndeleteButton.addEventListener('click', (event) => {\n  event.stopPropagation();\n  deleteArticle();\n});\n```\n\n点击删除按钮时，如果不阻止传播，卡片的 `openDetail` 也会执行。这不是浏览器异常，而是事件冒泡的正常结果。\n\n## 捕获阶段什么时候会用到\n\n捕获阶段在业务开发里不像冒泡那样常见，但它确实存在。\n\n如果你通过第三个参数或配置项启用捕获监听，事件会先在外层元素触发，再到目标元素。\n\n```javascript\nparent.addEventListener(\n  'click',\n  () => {\n    console.log('capture');\n  },\n  true\n);\n```\n\n大多数场景下你不一定主动用它，但知道它的存在，能帮助你更好理解事件执行顺序。\n\n捕获阶段适合少数需要更早拦截事件的场景，比如全局点击统计、弹窗外点击判断、某些基础组件的统一拦截。不过它不应该被滥用，因为捕获监听比普通冒泡监听更不容易被业务代码察觉。\n\n现在更推荐用配置对象写法：\n\n```javascript\nparent.addEventListener(\n  'click',\n  () => {\n    console.log('capture');\n  },\n  {\n    capture: true,\n  }\n);\n```\n\n配置对象可读性更好，后续也方便加 `once`、`passive` 等选项。\n\n## 什么是事件委托\n\n事件委托是前端里非常实用的一种技巧。\n\n它的思路是：不把事件直接绑定在每个子元素上，而是绑定在它们的父元素上，再通过事件对象判断具体是哪个子元素触发的。\n\n例如列表：\n\n```javascript\nlist.addEventListener('click', (event) => {\n  if (event.target.matches('li')) {\n    console.log('clicked item');\n  }\n});\n```\n\n这样做的好处很明显：\n\n- 减少重复绑定\n- 动态新增元素也能响应\n- 结构更集中\n\n不过真实业务里，点击目标不一定正好是 `li`。用户可能点到列表项里的文字、图标、按钮，这时 `event.target.matches('li')` 就不一定成立。\n\n更稳的方式通常是使用 `closest`：\n\n```javascript\nlist.addEventListener('click', (event) => {\n  const item = event.target.closest('[data-id]');\n\n  if (!item || !list.contains(item)) {\n    return;\n  }\n\n  openDetail(item.dataset.id);\n});\n```\n\n这里有两个边界：\n\n- 用 `closest` 向上找真正的列表项\n- 用 `contains` 确认找到的元素仍然属于当前列表\n\n第二个判断看起来多余，但在嵌套列表、弹层、复杂组件里很有用，可以避免误处理外部元素。\n\n事件委托也不是所有场景都适合。如果每个子元素的交互差异很大，或者需要独立清理资源，直接绑定在子组件上反而更清楚。\n\n## stopPropagation 是做什么的\n\n如果你不希望事件继续向上传播，可以调用：\n\n```javascript\nevent.stopPropagation();\n```\n\n这通常用于阻止父级事件误触发。\n\n例如一个卡片整体可点击，卡片内部又有删除按钮，这时删除按钮点击后如果不阻止传播，卡片点击逻辑也可能被触发。\n\n但 `stopPropagation` 不是万能药。它只阻止事件继续向父级传播，不会阻止当前元素上其他监听器执行。如果你需要阻止同一元素上后续监听器执行，才会用到 `stopImmediatePropagation`。\n\n日常业务里我会谨慎使用这类方法。很多时候更好的方式是让父级判断点击来源，而不是让子级强行打断所有传播。\n\n```javascript\ncard.addEventListener('click', (event) => {\n  if (event.target.closest('button')) {\n    return;\n  }\n\n  openDetail();\n});\n```\n\n这种写法把规则放在父级：如果点到按钮，就不打开详情。相比在每个内部按钮都写 `stopPropagation`，更容易维护。\n\n## 事件对象里最常用的几个属性\n\n日常开发中比较常见的有：\n\n- `target`\n- `currentTarget`\n- `preventDefault`\n\n其中最容易混的是：\n\n- `event.target` 表示实际触发事件的元素\n- `event.currentTarget` 表示当前正在处理事件的元素\n\n理解这两个区别，对写事件委托特别重要。\n\n举个例子：\n\n```javascript\nlist.addEventListener('click', (event) => {\n  console.log(event.target);\n  console.log(event.currentTarget);\n});\n```\n\n如果点击的是 `li` 里面的 `span`，`target` 可能是那个 `span`，而 `currentTarget` 始终是绑定监听器的 `list`。写委托时要找真实业务节点，通常看 `target`；要访问监听器所在容器，通常看 `currentTarget`。\n\n还有一个常用属性是 `event.defaultPrevented`。如果前面的逻辑已经调用过 `preventDefault`，后面的逻辑可以根据它决定是否继续处理。\n\n## 默认行为和传播不是一回事\n\n很多人刚开始会把这两个概念混在一起。\n\n例如点击链接时：\n\n- 阻止跳转，要用 `preventDefault`\n- 阻止事件继续向父级传播，要用 `stopPropagation`\n\n它们解决的是两类不同问题。\n\n表单提交也是类似的例子：\n\n```javascript\nform.addEventListener('submit', (event) => {\n  event.preventDefault();\n  submitForm();\n});\n```\n\n这里阻止的是浏览器默认提交行为，不是阻止事件冒泡。如果这个表单外层还有监听器，事件仍然可能继续传播。\n\n反过来，只调用 `stopPropagation` 也不会阻止链接跳转或表单提交。把这两个 API 的职责分清，能减少很多“明明写了阻止，为什么还跳转”的问题。\n\n## once、passive 和清理监听\n\n`addEventListener` 的第三个参数除了 `capture`，还可以设置其他选项。\n\n`once` 表示监听器只执行一次：\n\n```javascript\nbutton.addEventListener(\n  'click',\n  () => {\n    submitOnce();\n  },\n  {\n    once: true,\n  }\n);\n```\n\n这适合一次性确认、引导提示、动画结束等场景。\n\n`passive` 常用于滚动相关事件：\n\n```javascript\nwindow.addEventListener('scroll', handleScroll, {\n  passive: true,\n});\n```\n\n它告诉浏览器这个监听器不会调用 `preventDefault`，浏览器可以更放心地优化滚动性能。注意，如果设置了 `passive: true`，再调用 `preventDefault` 就不会按预期生效。\n\n还有一个经常被忽略的点是清理监听。组件销毁、弹窗关闭、页面切换时，如果之前绑定过全局事件，应该移除：\n\n```javascript\nfunction handleResize() {\n  updateLayout();\n}\n\nwindow.addEventListener('resize', handleResize);\n\nfunction destroy() {\n  window.removeEventListener('resize', handleResize);\n}\n```\n\n匿名函数不方便移除，所以需要清理的监听器最好使用具名函数或保存函数引用。\n\n## 输入、组合输入和边界\n\n输入框事件看起来简单，但中文输入法会带来一个常见边界：组合输入。\n\n如果你在 `input` 事件里实时搜索，用户输入拼音的过程中可能会不断触发请求。可以结合 `compositionstart` 和 `compositionend` 判断用户是否还在组合输入：\n\n```javascript\nlet composing = false;\n\ninput.addEventListener('compositionstart', () => {\n  composing = true;\n});\n\ninput.addEventListener('compositionend', () => {\n  composing = false;\n  search(input.value);\n});\n\ninput.addEventListener('input', () => {\n  if (composing) {\n    return;\n  }\n\n  search(input.value);\n});\n```\n\n这类细节不一定每个页面都需要，但搜索框、自动补全、在线编辑器会经常遇到。\n\n## 写在最后\n\nDOM 事件机制最重要的，不是死记捕获和冒泡的定义，而是理解事件在页面中的传播路径。\n\n只要把事件传播、事件委托、默认行为和当前目标这几个概念理顺，很多日常交互问题都会更容易分析。事件机制看起来基础，但它几乎贯穿了整个前端交互开发。\n\n我现在写事件逻辑时会习惯性问几个问题：这个监听器应该绑在哪一层？是否需要委托？是否会误触发父级逻辑？是否需要阻止默认行为？组件销毁时是否要清理？这些问题想清楚，事件代码通常就不会太乱。\n"},"randomPosts":[{"title":"虚拟 DOM 和 diff：框架更新页面时到底在比较什么","date":"2019-09-29","excerpt":"从面试常问的虚拟 DOM 出发，解释 VNode、diff、key、列表更新和为什么不要用 index 做复杂列表 key。","tags":["JavaScript","组件与状态"],"slug":"virtual-dom-diff-basics-2019","category":"2019","likeCount":6,"postId":"26fe1ed1-9465-3637-a248-976886694972","viewCount":3743,"readTime":null,"coverImage":null},{"title":"Ajax、JSONP 和跨域：接口联调时最常被问的老问题","date":"2018-05-09","excerpt":"结合接口联调经验，整理 XMLHttpRequest、Ajax、JSONP、CORS、预检请求和统一错误处理这些常见面试点。","tags":["JavaScript","前端基础"],"slug":"ajax-jsonp-cors-interview-2018","category":"2018","likeCount":5,"postId":"e2cded49-c9c7-f85d-8697-13b49a2d98dc","viewCount":4624,"readTime":null,"coverImage":null},{"title":"UED 和 UCD：前端为什么也要理解用户体验","date":"2018-12-17","excerpt":"用前端视角理解 UED 和 UCD，结合加载、空状态、错误提示、表单反馈和响应式细节说明体验不是设计师一个人的事情。","tags":["前端基础","交互与动效"],"slug":"frontend-ued-ucd-2018","category":"2018","likeCount":5,"postId":"3427e5f7-158d-89de-39bd-c446de8aa491","viewCount":3700,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端代码评审在看什么：不是挑格式，而是提前拦住后续成本","date":"2021-11-22","excerpt":"从协作和维护角度讨论前端代码评审的重点，说明 review 为什么不只是找语法问题。","tags":["前端基础"],"slug":"frontend-code-review-2021","category":"2021","likeCount":7,"postId":"ee5aa082-328e-2198-376e-f5dd296eba81","viewCount":3863,"readTime":"10分钟","coverImage":null},"nextPost":{"title":"为什么前端要关心可访问性：它不是额外需求，而是页面基本质量","date":"2021-11-03","excerpt":"从键盘操作、语义结构和辅助技术支持出发，讨论前端可访问性为什么值得被当成基础能力。","tags":["前端基础"],"slug":"frontend-accessibility-basics-2021","category":"2021","likeCount":7,"postId":"dd5dcb9b-2bc2-c786-c7ec-6a4a56dff023","viewCount":1341,"readTime":"10分钟","coverImage":null}},"__N_SSG":true}