{"pageProps":{"postData":{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2022-09-17","excerpt":"介绍 Vue3 和 Element Plus 中封装无限级菜单组件的核心数据结构和递归渲染思路。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2022","category":"2022","likeCount":7,"postId":"3462b2e8-f24c-3869-fb44-dc017a7c3f2f","viewCount":4208,"readTime":"5分钟","coverImage":null,"content":"\n后台系统里经常会有侧边菜单。\n\n普通菜单只有一两级还好，一旦菜单来自接口，并且层级不固定，就需要封装一个支持无限级的菜单组件。\n\n在 Vue3 和 Element Plus 中，实现这类菜单的关键是两点：统一数据结构，递归渲染菜单项。\n\n这类组件看起来只是 UI 递归，但实际项目里通常还会牵涉路由、高亮、权限、图标、折叠状态和接口数据清洗。如果这些逻辑边界不清楚，菜单组件很容易越写越重。\n\n## 先定义菜单数据结构\n\n后端返回的菜单数据最好整理成树形结构。\n\n一个菜单项通常需要这些字段：\n\n```typescript\nexport interface MenuItem {\n  title: string;\n  index: string;\n  icon?: string;\n  path?: string;\n  children?: MenuItem[];\n}\n```\n\n其中：\n\n- `title` 用来显示菜单名称\n- `index` 对应 Element Plus 菜单项的唯一标识\n- `path` 可以用于路由跳转\n- `children` 表示子菜单\n\n只要数据结构稳定，组件渲染就会清楚很多。\n\n实际后端返回的字段不一定刚好适合前端组件。最好先做一层转换，把接口数据整理成菜单组件需要的结构：\n\n```typescript\ninterface ApiMenu {\n  name: string;\n  routePath?: string;\n  code: string;\n  children?: ApiMenu[];\n}\n\nfunction normalizeMenus(list: ApiMenu[]): MenuItem[] {\n  return list.map((item) => ({\n    title: item.name,\n    index: item.routePath || item.code,\n    path: item.routePath,\n    children: item.children ? normalizeMenus(item.children) : undefined,\n  }));\n}\n```\n\n组件不要直接依赖后端原始字段。这样后端字段调整时，影响范围会小很多。\n\n## 判断菜单项是否有子级\n\nElement Plus 中，有子菜单时应该渲染 `el-sub-menu`，没有子菜单时渲染 `el-menu-item`。\n\n判断逻辑很简单：\n\n```typescript\nfunction hasChildren(item: MenuItem) {\n  return Array.isArray(item.children) && item.children.length > 0;\n}\n```\n\n这个判断会在递归组件里反复使用。\n\n还可以顺手过滤空子级，避免渲染出没有内容的 `el-sub-menu`：\n\n```typescript\nfunction getChildren(item: MenuItem) {\n  return item.children?.filter(Boolean) || [];\n}\n\nfunction hasChildren(item: MenuItem) {\n  return getChildren(item).length > 0;\n}\n```\n\n## 用递归组件渲染无限级菜单\n\n可以把单个菜单项封装成一个组件，例如 `MenuItem.vue`。\n\n伪代码大概是这样：\n\n```vue\n<template>\n  <el-sub-menu v-if=\"hasChildren(item)\" :index=\"item.index\">\n    <template #title>\n      <span>{{ item.title }}</span>\n    </template>\n\n    <MenuItem\n      v-for=\"child in item.children\"\n      :key=\"child.index\"\n      :item=\"child\"\n    />\n  </el-sub-menu>\n\n  <el-menu-item v-else :index=\"item.index\">\n    <span>{{ item.title }}</span>\n  </el-menu-item>\n</template>\n```\n\n它的核心就是：组件内部再次调用自己。\n\n只要 `children` 还有数据，就继续往下渲染。\n\n在 Vue3 的 `<script setup>` 中，递归组件通常需要组件文件有明确名字，或者通过文件名被编译器识别。一个更完整的写法可以是：\n\n```vue\n<script setup lang=\"ts\">\nimport type { MenuItem } from './types';\n\ndefineOptions({\n  name: 'MenuItem',\n});\n\ndefineProps<{\n  item: MenuItem;\n}>();\n\nfunction hasChildren(item: MenuItem) {\n  return Array.isArray(item.children) && item.children.length > 0;\n}\n</script>\n```\n\n递归组件的关键是 `key` 要稳定。不要用数组下标作为 key，否则菜单顺序变化或权限过滤后，组件状态可能错乱。\n\n## 图标动态渲染\n\n菜单通常会带图标。如果后端只返回图标名称，不建议在模板里到处写判断。\n\n可以维护一个图标映射：\n\n```typescript\nimport { House, Setting } from '@element-plus/icons-vue';\n\nconst iconMap = {\n  home: House,\n  setting: Setting,\n};\n\nfunction resolveIcon(name?: string) {\n  return name ? iconMap[name] : null;\n}\n```\n\n模板里再统一渲染：\n\n```vue\n<el-icon v-if=\"resolveIcon(item.icon)\">\n  <component :is=\"resolveIcon(item.icon)\" />\n</el-icon>\n```\n\n这样图标白名单是明确的，也避免后端随便返回一个字符串就影响前端渲染。\n\n## 外层菜单负责整体配置\n\n外层组件只需要负责 `el-menu` 的整体属性。\n\n```vue\n<template>\n  <el-menu :default-active=\"activeMenu\" router>\n    <MenuItem\n      v-for=\"item in menus\"\n      :key=\"item.index\"\n      :item=\"item\"\n    />\n  </el-menu>\n</template>\n```\n\n这样职责更清楚：\n\n- 外层菜单管理当前激活项、路由模式、折叠状态\n- 递归菜单项只负责渲染每一项\n\n当前激活项通常可以从路由里推导：\n\n```typescript\nimport { computed } from 'vue';\nimport { useRoute } from 'vue-router';\n\nconst route = useRoute();\n\nconst activeMenu = computed(() => {\n  return route.meta.activeMenu || route.path;\n});\n```\n\n有些详情页不在菜单里，但仍然希望高亮列表页菜单，可以在路由 meta 里配置 `activeMenu`。这比在菜单组件里写一堆路径判断更清楚。\n\n## 权限过滤放在哪里\n\n菜单权限过滤最好在数据进入组件前完成，而不是在递归模板里到处判断。\n\n```typescript\nfunction filterMenusByPermission(\n  menus: MenuItem[],\n  permissions: Set<string>\n): MenuItem[] {\n  return menus\n    .filter((item) => !item.permission || permissions.has(item.permission))\n    .map((item) => ({\n      ...item,\n      children: item.children\n        ? filterMenusByPermission(item.children, permissions)\n        : undefined,\n    }))\n    .filter((item) => item.path || hasChildren(item));\n}\n```\n\n最后一行用于去掉没有路径、也没有可见子菜单的空节点。权限过滤如果不处理空父级，侧边栏里很容易出现点不开的菜单。\n\n## 路由跳转和 index\n\nElement Plus 的 `el-menu` 支持 `router` 模式，这时 `index` 通常会作为路由路径使用。但实际项目里，`index` 不一定总等于 `path`。\n\n我更倾向于明确约定：\n\n- 如果开启 `router`，`index` 使用可跳转路径\n- 如果不开启 `router`，点击事件里根据 `path` 手动跳转\n\n不要一会儿用 code，一会儿用 path。菜单高亮和跳转逻辑一旦混乱，后面会很难排查。\n\n```vue\n<el-menu-item :index=\"item.path || item.index\">\n  <span>{{ item.title }}</span>\n</el-menu-item>\n```\n\n如果 `path` 为空，说明它可能不是可点击菜单项，要在数据层提前处理清楚。\n\n## 实际项目要注意什么\n\n无限级菜单看起来通用，但实际项目里还要考虑：\n\n- 菜单权限过滤\n- 路由地址和菜单 `index` 的对应关系\n- 图标动态渲染\n- 折叠时标题显示\n- 刷新页面后高亮当前路由\n- 菜单数据为空时的兜底\n\n这些逻辑不要全部塞进递归项里，否则组件会越来越重。\n\n还有两个边界经常被忽略：\n\n- 菜单层级过深时，交互体验会变差\n- 菜单数据可能存在循环引用或重复 index\n\n前端可以在转换数据时做基本校验：\n\n```typescript\nfunction assertUniqueMenuIndex(menus: MenuItem[], seen = new Set<string>()) {\n  menus.forEach((item) => {\n    if (seen.has(item.index)) {\n      throw new Error(`Duplicate menu index: ${item.index}`);\n    }\n\n    seen.add(item.index);\n\n    if (item.children) {\n      assertUniqueMenuIndex(item.children, seen);\n    }\n  });\n}\n```\n\n后台系统菜单一旦重复 index，高亮、展开和跳转都可能出问题。早点暴露错误，比线上出现奇怪状态更好。\n\n## 小结\n\nVue3 封装 Element Plus 无限级菜单，本质是树形数据加递归组件。\n\n先把菜单数据整理清楚，再把外层配置和单项渲染拆开，后续接权限、路由和图标都会更容易维护。\n\n我现在做这类菜单，会把它拆成三层：接口数据转换、权限和路由规则、递归 UI 渲染。三层分开以后，菜单组件本身就不会承担太多业务细节。\n"},"randomPosts":[{"title":"HTML 表单基础回顾：前端交互实际开发中就是从这里开始的","date":"2020-06-06","excerpt":"从语义、可访问性、原生校验、提交行为和错误处理角度回顾 HTML 表单基础，说明真实交互质量从表单开始。","tags":["前端基础"],"slug":"frontend-form-basics-2020","category":"2020","likeCount":6,"postId":"4a440c00-6629-3095-c28e-3e3ffd71f508","viewCount":1373,"readTime":null,"coverImage":null},{"title":"盒模型和 BFC：CSS 面试题背后，其实是布局边界","date":"2018-07-26","excerpt":"从页面布局和面试题出发，整理标准盒模型、IE 盒模型、box-sizing、margin 合并、BFC 和清除浮动。","tags":["CSS","前端基础"],"slug":"css-box-model-bfc-interview-2018","category":"2018","likeCount":8,"postId":"b0641fd0-7c70-fa2e-493b-2ae31e6ac940","viewCount":2291,"readTime":null,"coverImage":null},{"title":"Constructable Stylesheets：Web Components 样式复用的新方式","date":"2025-04-28","excerpt":"介绍 Constructable Stylesheets 的作用，说明它如何让多个 Shadow DOM 共享同一份样式对象。","tags":["前端基础","组件与状态","CSS"],"slug":"frontend-constructable-stylesheets-2026","category":"2025","likeCount":8,"postId":"2bdd2eab-e346-8064-35e9-713d211a8e0c","viewCount":2082,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端缓存基础：强缓存、协商缓存和资源更新到底该怎么理解","date":"2022-10-03","excerpt":"介绍前端开发中最常见的浏览器缓存机制，帮助你理解资源为什么有时更新了却没生效。","tags":["前端基础","性能与缓存"],"slug":"frontend-http-cache-2022","category":"2022","likeCount":5,"postId":"893c3552-ee46-d6a3-fb1d-42e18cf3373c","viewCount":3147,"readTime":null,"coverImage":null},"nextPost":{"title":"移动端适配入门：做前端页面时，别把响应式理解成只改宽度","date":"2022-08-17","excerpt":"介绍移动端适配中常见的问题和基本思路，帮助你理解响应式设计不只是调整布局宽度。","tags":["前端基础"],"slug":"frontend-mobile-adaptation-2022","category":"2022","likeCount":5,"postId":"118ede4b-790d-2888-f60d-1fb066215068","viewCount":2623,"readTime":null,"coverImage":null}},"__N_SSG":true}