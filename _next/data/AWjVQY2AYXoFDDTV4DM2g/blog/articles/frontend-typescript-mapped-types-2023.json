{"pageProps":{"postData":{"title":"TypeScript 映射类型入门：让类型跟着数据结构一起变化","date":"2023-01-08","excerpt":"从表单草稿、权限开关和接口 payload 的重复类型维护说起，聊聊 TypeScript 映射类型怎么减少字段不同步。","tags":["前端基础","TypeScript"],"slug":"frontend-typescript-mapped-types-2023","category":"2023","likeCount":7,"postId":"ff0194c8-f4d0-b832-75b2-0a440c49b2ab","viewCount":3523,"readTime":"8分钟","coverImage":null,"content":"\nTypeScript 的类型写多了以后，很容易出现重复。\n\n一个数据类型改了，另一个相关类型也要跟着改。如果忘了同步，类型就会和真实业务慢慢脱节。\n\n映射类型就是用来减少这类重复的。\n\n它的核心思想是：不要手动复制一份类型，而是基于已有类型推导出新类型。这样数据结构变化时，相关类型能自动跟着变化。\n\n我最早觉得映射类型只是 TypeScript 的“高级语法”。后来做表单和配置平台多了，才发现它真正解决的是维护成本：后端模型、表单草稿、更新接口、展示字段、权限开关，这些结构经常长得很像，但又不是完全一样。如果每一份都手写，字段一多就很容易漏。\n\n所以这篇不是为了写复杂类型，而是记录一个更实用的思路：同一个业务模型的不同视角，尽量让类型关系可推导。\n\n## 从 keyof 开始\n\n`keyof` 可以拿到一个对象类型的所有 key。\n\n```typescript\ntype User = {\n  name: string;\n  age: number;\n};\n\ntype UserKey = keyof User;\n```\n\n这里的 `UserKey` 等于 `\"name\" | \"age\"`。\n\n有了 key 的联合类型，就可以继续基于这些 key 生成新类型。\n\n`keyof` 常用于约束函数参数：\n\n```typescript\nfunction getValue<T, K extends keyof T>(target: T, key: K): T[K] {\n  return target[key];\n}\n\nconst user = {\n  name: 'Tom',\n  age: 18,\n};\n\nconst name = getValue(user, 'name');\n```\n\n这里 `K extends keyof T` 保证 key 必须来自对象本身，`T[K]` 则表示对应 key 的值类型。理解这两个语法后，映射类型会更自然。\n\n这个写法在配置平台里很常见。比如表格列配置里要声明 `dataIndex`，如果直接写成 `string`，很容易写错字段名还不报错。用 `keyof` 约束后，字段改名时类型系统能第一时间提醒。\n\n```typescript\ntype Column<T> = {\n  title: string;\n  dataIndex: keyof T;\n};\n\nconst userColumns: Column<User>[] = [\n  { title: '姓名', dataIndex: 'name' },\n  { title: '年龄', dataIndex: 'age' },\n];\n```\n\n## 什么是映射类型\n\n映射类型可以遍历一个类型的 key，并生成新的属性。\n\n```typescript\ntype ReadonlyUser = {\n  readonly [Key in keyof User]: User[Key];\n};\n```\n\n这段代码会生成：\n\n```typescript\ntype ReadonlyUser = {\n  readonly name: string;\n  readonly age: number;\n};\n```\n\n如果以后 `User` 新增 `email`，`ReadonlyUser` 也会自动跟着变化。\n\n映射类型的基本模板可以记成：\n\n```typescript\ntype NewType<T> = {\n  [Key in keyof T]: T[Key];\n};\n```\n\n`Key in keyof T` 负责遍历属性名，`T[Key]` 负责取出属性值类型。后面加 `readonly`、`?`、重命名 key，都是在这个模板上变化。\n\n映射类型最有价值的地方不是少写几行代码，而是把“这两个类型有关联”表达出来。以后改 `User` 时，不需要靠人记得同步 `ReadonlyUser`、`UserDraft`、`UserValidators`。\n\n## Partial 的原理\n\n我们常用的 `Partial` 就是映射类型。\n\n```typescript\ntype MyPartial<T> = {\n  [Key in keyof T]?: T[Key];\n};\n```\n\n它的作用是把一个类型的所有属性变成可选。\n\n```typescript\ntype UserDraft = MyPartial<User>;\n```\n\n适合编辑表单、更新接口参数这类场景。\n\n不过 `Partial` 也不能乱用。比如创建用户接口如果明确要求 `name` 必填，就不要直接把 payload 写成 `Partial<User>`。它太宽了，会把不该可选的字段也变成可选。\n\n更稳的做法是先区分业务动作：\n\n```typescript\ntype CreateUserPayload = Pick<User, 'name' | 'age'>;\ntype UpdateUserPayload = Partial<Pick<User, 'name' | 'age'>>;\n```\n\n这样类型能表达真实约束：创建必须给核心字段，更新可以只传变化字段。\n\n和它相对的是 `Required`：\n\n```typescript\ntype MyRequired<T> = {\n  [Key in keyof T]-?: T[Key];\n};\n```\n\n这里的 `-?` 表示移除可选修饰符。类似地，`-readonly` 可以移除只读修饰符：\n\n```typescript\ntype Mutable<T> = {\n  -readonly [Key in keyof T]: T[Key];\n};\n```\n\n这些修饰符让我们可以在类型层面批量调整属性规则。\n\n## Pick、Record 和 Omit\n\n`Pick` 可以从一个类型里挑出部分字段：\n\n```typescript\ntype MyPick<T, K extends keyof T> = {\n  [Key in K]: T[Key];\n};\n\ntype UserBase = MyPick<User, 'name'>;\n```\n\n`Record` 则是根据一组 key 生成同一种值类型：\n\n```typescript\ntype Role = 'admin' | 'editor' | 'guest';\n\ntype RolePermission = Record<Role, boolean>;\n```\n\n它等价于：\n\n```typescript\ntype RolePermission = {\n  admin: boolean;\n  editor: boolean;\n  guest: boolean;\n};\n```\n\n`Omit` 可以理解为先排除一部分 key，再 `Pick` 剩余字段。业务里经常用它从完整模型里去掉不该提交的字段：\n\n```typescript\ntype CreateUserPayload = Omit<User, 'id' | 'createdAt'>;\n```\n\n这些工具类型背后都离不开 `keyof`、映射类型和条件类型。\n\n`Record` 在权限、字典和状态映射里尤其好用。比如订单状态和展示文案必须一一对应，用 `Record` 可以避免漏配：\n\n```typescript\ntype OrderStatus = 'pending' | 'paid' | 'cancelled';\n\nconst statusText: Record<OrderStatus, string> = {\n  pending: '待支付',\n  paid: '已支付',\n  cancelled: '已取消',\n};\n```\n\n如果后面新增 `refunded`，这里会直接报错，提醒你补文案。这个提醒比线上看到一个空状态要早得多。\n\n## 重新映射 key\n\nTypeScript 还支持在映射时改变 key 名称。\n\n```typescript\ntype Permissions<T> = {\n  [Key in keyof T as `canChange${Capitalize<string & Key>}`]: boolean;\n};\n```\n\n如果传入：\n\n```typescript\ntype Config = {\n  username: string;\n  layout: string;\n};\n```\n\n就能得到：\n\n```typescript\ntype ConfigPermissions = {\n  canChangeUsername: boolean;\n  canChangeLayout: boolean;\n};\n```\n\n这种能力很适合把业务模型转换成权限、表单、校验器等衍生类型。\n\n在配置平台里，我更常见的用法是把字段模型转换成权限开关。比如某些字段只有管理员能编辑，就可以让权限对象跟着字段走，而不是手写一份容易漏的配置。\n\n除了新增前缀，也可以过滤 key。例如只保留字符串字段：\n\n```typescript\ntype StringFieldKeys<T> = {\n  [Key in keyof T]: T[Key] extends string ? Key : never;\n}[keyof T];\n\ntype UserStringKeys = StringFieldKeys<User>;\n```\n\n这里最后的 `[keyof T]` 是把映射后的对象类型再取值，得到一个联合类型。这个写法刚看会绕，但在工具类型里很常见。\n\n如果项目里用到了 TypeScript 4.9 之后的 `satisfies`，也可以把它和映射类型配合起来做配置校验。它不会把对象强行收窄成目标类型，但能检查结构是否满足约束：\n\n```typescript\nconst validators = {\n  name: (value: string) => (value ? null : '请输入用户名'),\n} satisfies Validators<User>;\n```\n\n这类写法很适合“配置对象要被检查，但又希望保留字面量推断”的场景。\n\n`satisfies` 和映射类型配在一起，还能强制配置不漏字段，同时保留每一项自己的字面量信息：\n\n```typescript\ntype FieldMeta<T> = {\n  [Key in keyof T]: {\n    label: string;\n    required?: boolean;\n  };\n};\n\nconst userFields = {\n  name: { label: '姓名', required: true },\n  age: { label: '年龄' },\n} satisfies FieldMeta<User>;\n```\n\n如果 `User` 新增了 `email`，这里会报错，提醒你补展示配置。这个比 `const userFields: FieldMeta<User> = ...` 更舒服的地方在于：对象本身的字面量推断还在，后续如果要读取 `required: true` 这类更窄的信息，不会被过早抹成普通 `boolean`。\n\n## 映射类型适合哪些业务\n\n映射类型很适合处理“同一个数据模型的不同视角”：\n\n- 表单草稿：所有字段可选\n- 详情展示：所有字段只读\n- 更新接口：只允许提交部分字段\n- 权限配置：每个字段对应一个权限开关\n- 校验规则：每个字段对应一个校验函数\n\n例如校验规则：\n\n```typescript\ntype Validators<T> = {\n  [Key in keyof T]?: (value: T[Key]) => string | null;\n};\n\nconst userValidators: Validators<User> = {\n  name(value) {\n    return value ? null : '请输入用户名';\n  },\n};\n```\n\n这样 `name` 校验函数拿到的就是 `string`，`age` 校验函数拿到的就是 `number`。类型能跟着字段走，减少手动维护。\n\n类似的还有表单状态：\n\n```typescript\ntype FieldState<T> = {\n  [Key in keyof T]: {\n    value: T[Key];\n    touched: boolean;\n    error?: string;\n  };\n};\n```\n\n如果 `User.age` 是 `number`，那么 `FieldState<User>['age'].value` 也会是 `number`。这种类型关系能让表单封装更可靠，尤其是字段很多的中后台页面。\n\n更新接口也很适合用映射类型约束“只能提交可修改字段”。不要直接 `Partial<User>`，因为它可能把 `id`、`createdAt`、`role` 这类不该由前端修改的字段也放进去：\n\n```typescript\ntype User = {\n  id: string;\n  name: string;\n  age: number;\n  role: 'admin' | 'user';\n  createdAt: string;\n};\n\ntype EditableUserField = 'name' | 'age';\ntype UpdateUserPayload = Partial<Pick<User, EditableUserField>>;\n\nfunction updateUser(id: string, payload: UpdateUserPayload) {\n  return request.patch(`/users/${id}`, payload);\n}\n\nupdateUser('1', { name: 'Tom' });\nupdateUser('1', { role: 'admin' }); // 报错，role 不允许从这个接口改\n```\n\n这个约束看起来小，但对后台系统很实用。前端页面上可能拿着完整 `User`，如果提交时随手把整对象丢给接口，轻则传一堆无用字段，重则把权限、创建时间这类敏感字段也带上。类型提前收窄，能把这种风险压在开发阶段。\n\n## 不要为了复杂而复杂\n\n映射类型很强，但也容易写过头。\n\n如果一个类型工具需要嵌套多层条件、递归和模板字符串，团队里大多数人都看不懂，那它可能会成为维护负担。类型系统应该帮助表达业务约束，而不是把简单问题变成类型体操。\n\n我会优先把常用工具封装成清楚的名字：\n\n```typescript\ntype FormDraft<T> = Partial<T>;\ntype ReadonlyModel<T> = Readonly<T>;\n```\n\n名字清楚，比每次把复杂映射类型直接写在业务代码里更好。\n\n如果确实需要写复杂工具类型，我会先问两个问题：\n\n- 这个工具类型是否解决了真实重复维护问题\n- 团队里其他人能不能在几分钟内理解它的输入和输出\n\n如果答案是否定的，宁愿写得朴素一点。类型系统是为了降低维护成本，不是为了证明语法掌握得多。\n\n\n我现在写映射类型，会先确认它是不是真的能减少维护成本。\n\n表单草稿、权限开关、接口更新 payload 这类结构，确实经常和原始模型强相关，用 `Partial`、`Pick`、`Omit`、`Record` 或自己包一层清楚的工具类型，会比手写多份字段稳得多。字段一改，相关类型能跟着走，这就是它最实在的价值。\n\n但如果一个类型工具写到团队里没人能快速看懂，那就已经偏离目的了。类型系统是帮业务表达约束的，不是用来炫技的。先把 `keyof`、`T[Key]`、`[Key in keyof T]` 这几块吃透，再按真实重复场景去抽工具类型，通常就够用了。\n"},"recommendedPosts":[{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":47,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":246,"readTime":null,"coverImage":null},{"title":"TypeScript 7 Beta 用 Go 重写之后：我更关心反馈链路有没有变短","date":"2026-04-28","excerpt":"从大型前端项目的类型检查、编辑器响应和 CI 等待说起，聊聊我会怎么评估 TypeScript 7 Beta 的性能收益和升级风险。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"frontend-typescript-7-go-2026","category":"2026","likeCount":24,"postId":"c582b9f3-9184-63db-3ced-7b4c1e26ca12","viewCount":275,"readTime":null,"coverImage":null},{"title":"TanStack Router 的类型安全路由：把跳转和参数错误挪到编译期","date":"2026-02-21","excerpt":"用过一段时间 TanStack Router 后的复盘：文件式与代码式路由、search params 的类型校验、loader 数据预取，以及它和 React Router 的取舍。","tags":["React","TypeScript","前端基础"],"slug":"tanstack-router-type-safe-2026","category":"2026","likeCount":25,"postId":"ba186679-ff87-187d-4d34-cff3f06231d3","viewCount":646,"readTime":null,"coverImage":null}],"prevPost":{"title":"React Query 管服务端状态：别再手写一堆 loading 和 error","date":"2023-01-13","excerpt":"从列表、详情、缓存、重新请求和错误处理出发，梳理 React Query 管理服务端状态的实践方式。","tags":["React","前端基础","构建与模块化"],"slug":"frontend-react-query-server-state-2023","category":"2023","likeCount":5,"postId":"6bf0b6ac-f5e3-13a0-402f-32debedc52e4","viewCount":4061,"readTime":null,"coverImage":null},"nextPost":{"title":"前端 Monorepo 与 pnpm workspace：多包项目先把边界想清楚","date":"2022-12-06","excerpt":"从组件库和多个后台应用联调的经历出发，聊聊 pnpm workspace 管理多包项目时，哪些边界必须先想清楚。","tags":["构建与模块化","前端基础"],"slug":"frontend-monorepo-pnpm-workspace-2022","category":"2022","likeCount":5,"postId":"b16db011-44a7-a782-6230-94523e9962e0","viewCount":2542,"readTime":null,"coverImage":null}},"__N_SSG":true}