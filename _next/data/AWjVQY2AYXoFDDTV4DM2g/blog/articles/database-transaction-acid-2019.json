{"pageProps":{"postData":{"title":"数据库事务和 ACID：转账问题为什么总被拿来面试","date":"2019-05-07","excerpt":"从转账、订单和库存场景出发，整理事务、ACID、隔离级别、脏读、不可重复读、幻读和前端理解接口一致性的方式。","tags":["数据可视化与存储"],"slug":"database-transaction-acid-2019","category":"2019","likeCount":5,"postId":"65ac459b-9fd7-1492-7f1e-225dfda367d3","viewCount":4109,"readTime":"9分钟","coverImage":null,"content":"\n数据库事务是后端面试里的经典题，但前端也应该懂一点。我以前一直觉得这是后端的事，跟我没关系，直到有一次线上出了个事故才被狠狠教育了一回：那是个发优惠券的活动，用户领券接口偶发性地「券领了，但账户余额没扣」，财务对账时发现送出去的钱对不上，追查了两天，最后定位是后端一段没包在事务里的代码——两条 SQL 中间抛了异常，前一条已经提交，后一条没跑，数据就裂成两半了。我作为前端虽然不背这个锅，但那次复盘会上听他们讲事务、讲回滚，我才第一次认真去补了这块知识。后来发现，懂一点事务对前端排查问题、设计接口契约、做防重提交，帮助比想象中大。\n\n下单、支付、扣库存、发优惠券，这些接口背后都依赖事务保证一致性。前端看到的只是一个“提交订单”按钮，服务端里面可能做了很多步：校验库存、创建订单、扣减库存、生成支付单、记流水、可能还要发消息通知。这一长串操作里任何一步失败，前面已经做的都得撤销，否则就是数据不一致。\n\n如果这些步骤只成功一半，业务就会出问题。而「只成功一半」这件事在没有事务保护时，是相当常见的——网络抖动、进程被 kill、数据库连接超时、代码里一个没接住的异常，都能让一组操作半途而废。\n\n## 转账例子\n\n从 A 给 B 转 100 元：\n\n```sql\nupdate account set balance = balance - 100 where id = 'A';\nupdate account set balance = balance + 100 where id = 'B';\n```\n\n如果第一句成功，第二句失败，钱就丢了。\n\n事务要保证这两步要么都成功，要么都失败。\n\n```sql\nbegin;\nupdate account set balance = balance - 100 where id = 'A';\nupdate account set balance = balance + 100 where id = 'B';\ncommit;\n```\n\n失败时：\n\n```sql\nrollback;\n```\n\n`begin` 到 `commit` 之间这一段，数据库会把它当成一个不可分割的整体。中途任意一句报错，应用层捕获到异常就执行 `rollback`，已经改过的行会被还原成事务开始前的样子，就像什么都没发生过。这就是开头那个事故的正解——只要那两条 SQL 被 `begin/commit` 包起来，第二条失败时第一条会自动回滚，绝不会出现「扣了余额没发券」或者「发了券没扣余额」的半截状态。\n\n这里有个我后来才搞明白的细节：自动提交（autocommit）。MySQL 默认 `autocommit=1`，意思是你不显式开事务时，每条单独的 SQL 都被当成一个独立事务自动提交了。所以单条 `update` 天然是原子的，问题只出在「多条语句要绑在一起」的时候。前端调试时如果直接用客户端连库改数据，改完没看到效果，有时候就是因为开了手动事务模式却忘了 `commit`，数据其实还卡在你这个会话的事务里没落地。\n\n## ACID\n\n事务常说 ACID：\n\n- Atomicity：原子性\n- Consistency：一致性\n- Isolation：隔离性\n- Durability：持久性\n\n原子性（Atomicity）：事务里的操作要么全成功，要么全失败，不存在做一半的中间态。开头那个发券事故，缺的就是原子性。\n\n一致性（Consistency）：事务前后数据要符合业务规则。转账例子里，A 扣 100、B 加 100，转账前后两个账户的总额不变，这个「总额守恒」就是一种一致性约束。一致性其实是目的，原子性、隔离性、持久性是手段——它们一起保证数据库从一个合法状态迁移到另一个合法状态。\n\n隔离性（Isolation）：多个事务并发执行时互不干扰到不该干扰的程度。注意是「不该干扰的程度」，不是完全隔离——完全隔离意味着所有事务串行排队，并发就没了。这也是后面隔离级别要解决的核心矛盾。\n\n持久性（Durability）：提交后的数据要持久保存，哪怕下一秒数据库进程崩了、机器断电了，重启后这笔数据也还在。数据库靠的是 redo log 这类机制——先把改动顺序写进日志落盘，再慢慢刷数据页，崩溃恢复时拿日志重放。我面试时被追问过「commit 返回成功后断电，数据会丢吗」，答案是不会，这就是持久性的承诺，而它的代价是每次提交都要等日志真正落盘（`innodb_flush_log_at_trx_commit=1`），这也是高并发写入时的一个性能点。\n\n这四个字母里，原子性和持久性相对好懂，真正在业务里反复咬人的是隔离性。\n\n## 并发带来的问题\n\n多个事务同时执行，可能出现：\n\n- 脏读\n- 不可重复读\n- 幻读\n\n脏读：读到了别人还没提交的数据。事务 B 读到了事务 A 改了但还没 commit 的值，结果 A 又回滚了，B 拿到的就是一个根本没存在过的「脏」数据。落到业务上：风控系统读到一笔还在处理、随时可能撤销的订单金额去算额度，A 一回滚，风控的判断就建立在幻觉之上。\n\n不可重复读：同一个事务里，两次读同一行，结果不同。中间被别的已提交事务改了。\n\n幻读：同一个事务里，两次范围查询，结果行数不同。区别在于不可重复读针对的是「同一行的值变了」，幻读针对的是「行数变了」——别的事务插入或删除了符合条件的行，你第二次 `count(*)` 就多出来或少掉一些「幻影」。\n\n我一开始老分不清不可重复读和幻读，后来记了个口诀：值变了是不可重复读，行多了少了是幻读。一个是 update 引起的，一个是 insert/delete 引起的。\n\n这些问题不是背概念用的，它们会实打实影响订单、库存、账户这类业务。举个我们真实碰过的超卖：两个用户同时下单买最后一件库存，两个事务都先 `select` 到库存是 1，都判断「够」，都去扣减，结果库存变成了 -1，超卖了。这背后就是隔离没做好——下面讲怎么解决。\n\n## 隔离级别\n\n常见隔离级别：\n\n- Read Uncommitted\n- Read Committed\n- Repeatable Read\n- Serializable\n\n这四级从上到下越来越严，能解决的问题也越多：\n\n| 隔离级别 | 脏读 | 不可重复读 | 幻读 |\n| --- | --- | --- | --- |\n| Read Uncommitted | 可能 | 可能 | 可能 |\n| Read Committed | 不会 | 可能 | 可能 |\n| Repeatable Read | 不会 | 不会 | 可能（MySQL 下因 MVCC 基本规避） |\n| Serializable | 不会 | 不会 | 不会 |\n\n隔离越强，并发性能可能越低，因为更强的隔离要靠更多、更长的锁来保证，事务之间互相等待的概率就高。\n\n数据库不会无脑选择最强隔离，因为性能和一致性要平衡。这里有个常被搞混的点：MySQL（InnoDB）默认是 Repeatable Read，而 PostgreSQL、Oracle 默认是 Read Committed。我刚跨库做项目时就栽过——在 MySQL 上习惯了「一个事务里反复读同一行结果不变」，换到默认 Read Committed 的库上，同一个事务里两次查询拿到不同结果，业务逻辑算错了，排查半天才想起来是隔离级别默认值不一样。所以代码不能想当然地依赖默认隔离级别，关键业务最好在事务开头显式设置。\n\nInnoDB 能在 Repeatable Read 下还保持不错的并发，靠的是 MVCC（多版本并发控制）——读操作读的是某个时间点的快照版本，不加锁，写操作改的是新版本，读写不互相阻塞。这也是为什么 MySQL 里普通 `select` 即使在严格隔离级别下也很少卡住。但要注意，MVCC 只管「快照读」；如果你需要读到最新值并锁住它（比如下单扣库存那种），得用 `select ... for update` 走「当前读」，这才是解决超卖的常用手段：\n\n```sql\nbegin;\n-- for update 会锁住这一行，别的事务想改它得排队\nselect stock from product where id = 100 for update;\n-- 这里在应用层判断 stock 是否充足\nupdate product set stock = stock - 1 where id = 100;\ncommit;\n```\n\n更轻量的做法是把判断直接写进 update 的条件里，靠数据库的行锁保证原子性，连显式 `for update` 都省了：\n\n```sql\nupdate product set stock = stock - 1 where id = 100 and stock > 0;\n-- 然后看影响行数：affected rows = 0 就是没扣成功（库存不够），= 1 才是扣成功\n```\n\n我们后来线上的扣库存基本都用这种「乐观条件 update + 看影响行数」的写法，简单、不容易死锁，比先查再扣那一套省事多了。\n\n## 前端为什么要懂\n\n前端不直接写事务，但要理解接口不是简单保存。\n\n比如用户点击“提交订单”，前端应该做重复提交保护：\n\n```js\nif (submitting) return\nsubmitting = true\n```\n\n但真正保证订单不会重复创建，还要靠后端幂等和事务。前端这道禁用只能挡住「同一个页面手抖连点」，挡不住的情况多了去了：用户网络慢以为没点上又点一次、请求超时前端自动重试、用户开了两个标签页各下一单、甚至有人抓包重放。这些场景里前端的 `submitting` 标记根本不在场。\n\n我们后来跟后端约定的标准做法是「幂等键」：前端在进入下单页时生成一个唯一的 `requestId`（比如 UUID），下单请求带上它，后端用这个 key 加唯一约束或 Redis 占位，同一个 key 只会真正创建一次订单，重复请求直接返回第一次的结果。\n\n```js\n// 进入结算页时生成一次，整个下单流程复用同一个，重试也用它\nconst requestId = crypto.getRandomValues\n  ? genUUID()\n  : Date.now() + '-' + Math.random().toString(36).slice(2)\n\nfunction submitOrder() {\n  if (submitting) return\n  submitting = true\n  return request('/api/order/create', {\n    method: 'POST',\n    body: { ...orderData, requestId }\n  }).finally(() => {\n    submitting = false\n  })\n}\n```\n\n关键点：重试时一定要复用同一个 `requestId`，如果每次请求都新生成一个，幂等就失效了——这是我见过最常见的实现错误，写了幂等键却每次都换，等于没写。\n\n前端不能把“按钮禁用”当成最终一致性保障，它只是体验层的第一道防线，真正的防重在后端的幂等键 + 事务里。\n\n## 接口错误要表达清楚\n\n事务失败后，接口要返回明确错误：\n\n```json\n{\n  \"code\": \"STOCK_NOT_ENOUGH\",\n  \"message\": \"库存不足\"\n}\n```\n\n前端才能给用户正确提示。库存不足应该提示「手慢了，已抢光」并把按钮还原让用户能换商品，而不是弹个红框写「系统异常」让用户一脸懵地狂点重试——后者反而加重了后端压力。\n\n不要所有失败都返回“系统异常”。业务失败和系统失败处理方式不同：业务失败（库存不足、余额不够、活动已结束）是预期内的，前端要给具体可操作的提示，且**不应该重试**，重试也没用；系统失败（超时、500、数据库连接断了）才是真异常，可以提示「网络开小差，请重试」并允许重试。\n\n我们后来在前端封装了一层统一处理，把这两类彻底分开：\n\n```js\nrequest('/api/order/create', { ... })\n  .then(handleSuccess)\n  .catch((err) => {\n    if (err.bizCode) {\n      // 业务失败：后端明确返回了 code，照着提示，不重试\n      toast(err.message)\n      if (err.bizCode === 'STOCK_NOT_ENOUGH') refreshStock()\n    } else {\n      // 系统失败：网络/超时/5xx，提示可重试\n      toast('网络异常，请稍后重试')\n    }\n  })\n```\n\n这套约定推下去之后，线上「用户说报错但客服看不懂啥错」的工单少了一大半。错误契约清晰，前端才能做出对的交互，事务在后端默默回滚，用户在前端看到一句人话——这才是一条完整的链路。\n\n\n事务保证一组数据库操作作为整体执行。ACID 描述事务的基本特性，隔离级别处理并发下的数据问题。\n\n前端理解事务，不是为了写 SQL，而是为了知道提交、支付、库存这类接口为什么要防重复、为什么错误要明确、为什么一致性不能只靠页面状态。\n"},"recommendedPosts":[{"title":"Vercel 推出存储能力后，前端部署不只是静态页面了","date":"2023-05-05","excerpt":"从一次给小工具补数据库和文件上传的经历出发，聊聊 Vercel Postgres、Blob、KV 对前端应用部署和数据边界的影响。","tags":["前端基础","数据可视化与存储"],"slug":"frontend-vercel-storage-2023","category":"2023","likeCount":35,"postId":"9de962c3-48c9-94c0-fe0b-42c6a0ff4b77","viewCount":780,"readTime":null,"coverImage":null},{"title":"前端数据可视化库怎么选：我后来不再只看图表类型","date":"2022-05-10","excerpt":"从中后台看板和数据规模出发，聊聊我如何在 ECharts、D3、Chart.js、Recharts 等方案之间做选择。","tags":["前端基础","数据可视化与存储"],"slug":"frontend-data-visualization-libraries-2022","category":"2022","likeCount":34,"postId":"0dcf3827-fbbf-c7f2-905a-3c704398ea84","viewCount":801,"readTime":null,"coverImage":null},{"title":"Redis 缓存基础：接口快了，也要小心缓存穿透和雪崩","date":"2019-11-23","excerpt":"从接口性能和面试常题出发，整理 Redis 缓存、过期时间、缓存穿透、击穿、雪崩和前端缓存体验。","tags":["数据可视化与存储"],"slug":"redis-cache-basics-2019","category":"2019","likeCount":6,"postId":"a27bccd1-eb63-44dc-9ee2-cecfe331d99d","viewCount":2738,"readTime":null,"coverImage":null}],"prevPost":{"title":"webpack loader 和 plugin：常被问到，也决定构建能不能扩展","date":"2019-05-21","excerpt":"从实际构建配置出发，聊聊 webpack loader、plugin 的职责差异、执行顺序、配置边界和简化实现思路。","tags":["构建与模块化"],"slug":"webpack-loader-plugin-interview-2019","category":"2019","likeCount":8,"postId":"a6d0ab3f-757f-571a-3ae6-1079d59fe636","viewCount":3760,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue 表单组件拆分经验：可复用输入框不是把所有 props 都塞进去","date":"2019-04-20","excerpt":"结合 Vue 表单场景，讨论输入组件、字段容器、校验提示和提交错误处理怎么拆，避免复用组件变成万能配置层。","tags":["Vue","组件与状态"],"slug":"vue-form-components-2019","category":"2019","likeCount":5,"postId":"d0db4543-db2c-450f-def4-3bbe57b7eec9","viewCount":4087,"readTime":null,"coverImage":null}},"__N_SSG":true}