{"pageProps":{"postData":{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2023-09-17","excerpt":"无限级菜单看着是递归组件，实际牵着路由、高亮、权限、图标和接口数据清洗，边界要先想清楚。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2023","category":"2023","likeCount":5,"postId":"73d4dc9e-8fc7-3ce9-8dcb-31a0122f0b98","viewCount":3765,"readTime":"10分钟","coverImage":null,"content":"\n后台系统里经常会有侧边菜单。\n\n普通菜单只有一两级还好，一旦菜单来自接口，并且层级不固定，就需要封装一个支持无限级的菜单组件。\n\n在 Vue3 和 Element Plus 中，实现这类菜单的关键是两点：统一数据结构，递归渲染菜单项。\n\n这类组件看起来只是 UI 递归，但实际项目里通常还会牵涉路由、高亮、权限、图标、折叠状态和接口数据清洗。如果这些逻辑边界不清楚，菜单组件很容易越写越重。\n\n我之前接手过一个项目，菜单组件有快 400 行，模板里塞满了 `v-if`，权限判断、图标判断、路由判断全揉在一起。改一个小需求都得提心吊胆，因为你不知道哪个分支又会触发别的逻辑。后来重写时我把它彻底拆开，组件本身才不到 60 行，剩下的逻辑全挪到了数据处理那一层。这里记一下当时的思路。\n\n## 先定义菜单数据结构\n\n后端返回的菜单数据最好整理成树形结构。\n\n一个菜单项通常需要这些字段：\n\n```typescript\nexport interface MenuItem {\n  title: string;\n  index: string;\n  icon?: string;\n  path?: string;\n  permission?: string;\n  external?: boolean;\n  children?: MenuItem[];\n}\n```\n\n其中：\n\n- `title` 用来显示菜单名称\n- `index` 对应 Element Plus 菜单项的唯一标识\n- `path` 可以用于路由跳转\n- `children` 表示子菜单\n\n只要数据结构稳定，组件渲染就会清楚很多。\n\n实际后端返回的字段不一定刚好适合前端组件。最好先做一层转换，把接口数据整理成菜单组件需要的结构：\n\n```typescript\ninterface ApiMenu {\n  name: string;\n  routePath?: string;\n  code: string;\n  children?: ApiMenu[];\n}\n\nfunction normalizeMenus(list: ApiMenu[]): MenuItem[] {\n  return list.map((item) => ({\n    title: item.name,\n    index: item.routePath || item.code,\n    path: item.routePath,\n    children: item.children ? normalizeMenus(item.children) : undefined,\n  }));\n}\n```\n\n组件不要直接依赖后端原始字段。这样后端字段调整时，影响范围会小很多。\n\n这一点是我吃过亏才认真做的。早期图省事，组件里直接用了后端的 `name`、`routePath`，结果有次后端把 `routePath` 改成了 `route_path`，加了个下划线，整个菜单全空了。如果当时有这层转换函数，改一行就能搞定，影响也能控制在一个文件里。后端字段是你控制不了的，给它一个缓冲层，心里会踏实很多。\n\n转换的时候顺便处理一些脏数据也很合适。比如某些菜单的 `name` 可能带前后空格，或者 `children` 是空数组而不是 `undefined`，都可以在这里统一抹平：\n\n```typescript\nfunction normalizeMenus(list: ApiMenu[]): MenuItem[] {\n  return (list || []).map((item) => {\n    const children =\n      item.children && item.children.length\n        ? normalizeMenus(item.children)\n        : undefined;\n\n    return {\n      title: item.name?.trim() ?? '',\n      index: item.routePath || item.code,\n      path: item.routePath,\n      children,\n    };\n  });\n}\n```\n\n外层加个 `list || []` 是因为接口偶尔会返回 `null`，尤其是某些角色没有任何菜单的时候。不加这一行，页面直接白屏报错，挺难看的。\n\n如果菜单还会驱动动态路由，更不能直接信任后端传来的组件路径。后端可以返回页面 code，但前端应该维护组件白名单：\n\n```typescript\nconst viewModules = import.meta.glob('@/views/**/*.vue');\n\nfunction resolveView(componentPath: string) {\n  const loader = viewModules[`/src/views/${componentPath}.vue`];\n\n  if (!loader) {\n    throw new Error(`Unknown menu component: ${componentPath}`);\n  }\n\n  return loader;\n}\n```\n\n这样后端只能选择前端已经打包进来的页面，不能随便传一个字符串让前端去 import。动态菜单和动态路由越灵活，边界越要收紧。\n\n## 判断菜单项是否有子级\n\nElement Plus 中，有子菜单时应该渲染 `el-sub-menu`，没有子菜单时渲染 `el-menu-item`。\n\n判断逻辑很简单：\n\n```typescript\nfunction hasChildren(item: MenuItem) {\n  return Array.isArray(item.children) && item.children.length > 0;\n}\n```\n\n这个判断会在递归组件里反复使用。\n\n还可以顺手过滤空子级，避免渲染出没有内容的 `el-sub-menu`：\n\n```typescript\nfunction getChildren(item: MenuItem) {\n  return item.children?.filter(Boolean) || [];\n}\n\nfunction hasChildren(item: MenuItem) {\n  return getChildren(item).length > 0;\n}\n```\n\n## 用递归组件渲染无限级菜单\n\n可以把单个菜单项封装成一个组件，例如 `MenuItem.vue`。\n\n伪代码大概是这样：\n\n```vue\n<template>\n  <el-sub-menu v-if=\"hasChildren(item)\" :index=\"item.index\">\n    <template #title>\n      <span>{{ item.title }}</span>\n    </template>\n\n    <MenuItem\n      v-for=\"child in item.children\"\n      :key=\"child.index\"\n      :item=\"child\"\n    />\n  </el-sub-menu>\n\n  <el-menu-item v-else :index=\"item.index\">\n    <span>{{ item.title }}</span>\n  </el-menu-item>\n</template>\n```\n\n它的核心就是：组件内部再次调用自己。\n\n只要 `children` 还有数据，就继续往下渲染。\n\n在 Vue 3.3 之后，`<script setup>` 可以直接用 `defineOptions` 声明组件名。递归组件通常需要组件文件有明确名字，或者通过文件名被编译器识别。一个更完整的写法可以是：\n\n```vue\n<script setup lang=\"ts\">\nimport type { MenuItem } from './types';\n\ndefineOptions({\n  name: 'MenuItem',\n});\n\ndefineProps<{\n  item: MenuItem;\n}>();\n\nfunction hasChildren(item: MenuItem) {\n  return Array.isArray(item.children) && item.children.length > 0;\n}\n</script>\n```\n\n这里 `defineOptions({ name: 'MenuItem' })` 不建议省。Vue3 的 `<script setup>` 里组件名如果没有被明确推断出来，递归时模板引用自身那个标签可能会找不到组件，控制台报一个 `Failed to resolve component` 的警告，菜单的子级直接渲染不出来。我第一次写递归组件就栽在这上面，盯着代码看半天没发现问题，最后才想起来名字这回事。Vue 也支持靠文件名推断组件名，但显式写一个名字最稳，不依赖构建配置。\n\n如果项目还停在 Vue 3.2 或更早版本，`defineOptions` 不能直接用，可以额外保留一个普通 `<script>`：\n\n```vue\n<script lang=\"ts\">\nexport default {\n  name: 'MenuItem',\n};\n</script>\n```\n\n递归组件的关键是 `key` 要稳定。不要用数组下标作为 key，否则菜单顺序变化或权限过滤后，组件状态可能错乱。\n\n具体会错乱成什么样？我遇到过的情况是：用 `el-sub-menu` 的展开/折叠是组件内部状态，如果用下标做 key，权限过滤砍掉前面一个菜单后，后面菜单的下标整体前移，Vue 复用了 DOM 但状态对不上，结果就是你点开了 A 菜单，刷新权限后展开的却是 B。用稳定的 `index` 做 key 能彻底避免这个问题，反正我们前面已经保证 `index` 唯一了。\n\n## 图标动态渲染\n\n菜单通常会带图标。如果后端只返回图标名称，不建议在模板里到处写判断。\n\n可以维护一个图标映射：\n\n```typescript\nimport { House, Setting } from '@element-plus/icons-vue';\n\nconst iconMap = {\n  home: House,\n  setting: Setting,\n};\n\nfunction resolveIcon(name?: string) {\n  return name ? iconMap[name] : null;\n}\n```\n\n模板里再统一渲染：\n\n```vue\n<el-icon v-if=\"resolveIcon(item.icon)\">\n  <component :is=\"resolveIcon(item.icon)\" />\n</el-icon>\n```\n\n这样图标白名单是明确的，也避免后端随便返回一个字符串就影响前端渲染。\n\n有个细节得提一下：Element Plus 的图标是一个个组件，全量按需引入还好，但如果你想把所有图标都注册进 `iconMap`，包体积会很可观。我们项目后台菜单图标其实就十来个，所以手动维护这个映射完全够用，也更可控——设计稿上没有的图标，根本不会被打进包里。如果图标种类特别多，再考虑用 `import.meta.glob` 之类的方式动态加载。\n\n另外别忘了 `resolveIcon` 返回 `null` 时模板那个 `v-if` 要兜住，否则 `<component :is=\"null\" />` 在某些版本里会报警告。折叠菜单（`collapse`）时图标尤其重要，因为这时候标题被收起来了，用户只能靠图标认菜单，没图标的一级菜单折叠后就是一片空白，很难用。\n\n## 外层菜单负责整体配置\n\n外层组件只需要负责 `el-menu` 的整体属性。\n\n```vue\n<template>\n  <el-menu :default-active=\"activeMenu\" router>\n    <MenuItem\n      v-for=\"item in menus\"\n      :key=\"item.index\"\n      :item=\"item\"\n    />\n  </el-menu>\n</template>\n```\n\n这样职责更清楚：\n\n- 外层菜单管理当前激活项、路由模式、折叠状态\n- 递归菜单项只负责渲染每一项\n\n当前激活项通常可以从路由里推导：\n\n```typescript\nimport { computed } from 'vue';\nimport { useRoute } from 'vue-router';\n\nconst route = useRoute();\n\nconst activeMenu = computed(() => {\n  return route.meta.activeMenu || route.path;\n});\n```\n\n有些详情页不在菜单里，但仍然希望高亮列表页菜单，可以在路由 meta 里配置 `activeMenu`。这比在菜单组件里写一堆路径判断更清楚。\n\n举个真实场景：订单列表 `/order/list` 在菜单里，点进去的订单详情 `/order/detail/123` 不在菜单里。用户在详情页时，左侧菜单本该还高亮\"订单列表\"，但因为 `route.path` 变成了详情页地址，`default-active` 匹配不上，高亮就掉了。这时候在详情页路由的 meta 里写上 `activeMenu: '/order/list'`，问题就解决了：\n\n```typescript\n{\n  path: '/order/detail/:id',\n  component: () => import('@/views/order/Detail.vue'),\n  meta: { activeMenu: '/order/list' },\n}\n```\n\n我刚开始是想在菜单组件里用正则匹配前缀来兜这种情况，写了几条之后发现完全没法维护——业务路由各式各样，规则越堆越多。后来全部改成 meta 声明式配置，谁加的路由谁负责声明，菜单组件一个字都不用动，清爽太多了。\n\n## 权限过滤放在哪里\n\n菜单权限过滤最好在数据进入组件前完成，而不是在递归模板里到处判断。\n\n```typescript\nfunction filterMenusByPermission(\n  menus: MenuItem[],\n  permissions: Set<string>\n): MenuItem[] {\n  return menus\n    .filter((item) => !item.permission || permissions.has(item.permission))\n    .map((item) => ({\n      ...item,\n      children: item.children\n        ? filterMenusByPermission(item.children, permissions)\n        : undefined,\n    }))\n    .filter((item) => item.path || hasChildren(item));\n}\n```\n\n最后一行用于去掉没有路径、也没有可见子菜单的空节点。权限过滤如果不处理空父级，侧边栏里很容易出现点不开的菜单。\n\n## 路由跳转和 index\n\nElement Plus 的 `el-menu` 支持 `router` 模式，这时 `index` 通常会作为路由路径使用。但实际项目里，`index` 不一定总等于 `path`。\n\n我更倾向于明确约定：\n\n- 如果开启 `router`，`index` 使用可跳转路径\n- 如果不开启 `router`，点击事件里根据 `path` 手动跳转\n\n不要一会儿用 code，一会儿用 path。菜单高亮和跳转逻辑一旦混乱，后面会很难排查。\n\n```vue\n<el-menu-item :index=\"item.path || item.index\">\n  <span>{{ item.title }}</span>\n</el-menu-item>\n```\n\n如果 `path` 为空，说明它可能不是可点击菜单项，要在数据层提前处理清楚。\n\n还有一个坑是 `router` 模式下的外链。后台菜单偶尔会配一个外部地址，比如跳到某个监控平台。`el-menu` 的 `router` 模式会拿 `index` 当路由 path 喂给 `router.push`，外链 `https://...` 直接就跳挂了。这种我一般在数据转换时打个标记，渲染时单独处理，用 `window.open` 跳出去，而不是让它走 vue-router：\n\n```vue\n<el-menu-item\n  :index=\"item.index\"\n  @click=\"item.external && openExternal(item.path)\"\n>\n  <span>{{ item.title }}</span>\n</el-menu-item>\n```\n\n## 折叠和空数据的兜底\n\n后台布局基本都有侧边栏收起功能，`el-menu` 加个 `:collapse=\"isCollapse\"` 就行。但有个体验细节：折叠状态切换时如果你正展开着某个子菜单，动画有时会有点跳。Element Plus 内部用 `unique-opened` 配合 `collapse-transition` 能缓解一部分，我一般会把 `unique-opened` 打开，同一时刻只展开一个子菜单，既好看也省得用户在深层级里迷路：\n\n```vue\n<el-menu\n  :default-active=\"activeMenu\"\n  :collapse=\"isCollapse\"\n  unique-opened\n  router\n>\n```\n\n空数据兜底也别漏。菜单接口慢、或者某个角色压根没配菜单时，侧边栏不能就这么空着。我习惯加个简单的判断，没有菜单时给一句提示，而不是让用户对着一片空白的侧边栏发愣：\n\n```vue\n<template>\n  <el-menu v-if=\"menus.length\" ...>\n    <MenuItem v-for=\"item in menus\" :key=\"item.index\" :item=\"item\" />\n  </el-menu>\n  <el-empty v-else description=\"暂无菜单权限\" :image-size=\"80\" />\n</template>\n```\n\n## 实际项目要注意什么\n\n无限级菜单看起来通用，但实际项目里还要考虑：\n\n- 菜单权限过滤\n- 路由地址和菜单 `index` 的对应关系\n- 图标动态渲染\n- 折叠时标题显示\n- 刷新页面后高亮当前路由\n- 菜单数据为空时的兜底\n\n这些逻辑不要全部塞进递归项里，否则组件会越来越重。\n\n还有两个边界经常被忽略：\n\n- 菜单层级过深时，交互体验会变差\n- 菜单数据可能存在循环引用或重复 index\n\n前端可以在转换数据时做基本校验：\n\n```typescript\nfunction assertUniqueMenuIndex(menus: MenuItem[], seen = new Set<string>()) {\n  menus.forEach((item) => {\n    if (seen.has(item.index)) {\n      throw new Error(`Duplicate menu index: ${item.index}`);\n    }\n\n    seen.add(item.index);\n\n    if (item.children) {\n      assertUniqueMenuIndex(item.children, seen);\n    }\n  });\n}\n```\n\n后台系统菜单一旦重复 index，高亮、展开和跳转都可能出问题。早点暴露错误，比线上出现奇怪状态更好。\n\n接口异常也要有统一兜底。菜单接口失败时，不要让整个布局白屏。可以把错误收敛成统一结构，再决定跳登录、展示无权限，还是展示重试按钮：\n\n```typescript\nasync function loadMenus() {\n  try {\n    menus.value = await fetchMenus();\n  } catch (error) {\n    menuError.value = normalizeApiError(error);\n    menus.value = [];\n  }\n}\n```\n\n菜单是后台系统的入口，它的错误处理要比普通列表页更谨慎。用户没有菜单时，要能区分“没有权限”和“接口加载失败”，这两种状态给人的提示完全不同。\n\n\nVue3 封装 Element Plus 无限级菜单，说到底就是树形数据加递归组件。\n\n先把菜单数据整理清楚，再把外层配置和单项渲染拆开，后续接权限、路由和图标都会更容易维护。\n\n我现在做这类菜单，会把它拆成三层：接口数据转换、权限和路由规则、递归 UI 渲染。三层分开以后，菜单组件本身就不会承担太多业务细节。\n"},"recommendedPosts":[{"title":"刚开始用 Vue3 时，最容易踩的几个坑","date":"2023-06-07","excerpt":"从我把 Vue2 项目迁到 Vue3 时踩过的坑出发，聊聊响应式、props、watch、组合式 API、生命周期清理和 Pinia 使用误区。","tags":["Vue","前端基础"],"slug":"vue3-common-mistakes-2023","category":"2023","likeCount":6,"postId":"06bb42c9-56d9-0865-c906-88f606e068a9","viewCount":1090,"readTime":null,"coverImage":null},{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"从后台项目里反复写 loading/data/error 三件套的经历出发，聊聊请求库为什么会从 HTTP 客户端走向状态管理、缓存和统一错误处理。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2371,"readTime":null,"coverImage":null},{"title":"前端权限路由和菜单：前端控制显示，后端控制安全","date":"2022-05-13","excerpt":"从动态路由、菜单、按钮权限、刷新恢复和接口兜底出发，梳理后台系统权限控制实践。","tags":["前端基础","Vue"],"slug":"frontend-permission-routing-2022","category":"2022","likeCount":6,"postId":"3bddf70f-a638-7349-67b3-9a05893604db","viewCount":1465,"readTime":null,"coverImage":null}],"prevPost":{"title":"Web Components 入门：跨框架复用组件的一种思路","date":"2023-10-09","excerpt":"Web Components 真正适合的是跨框架复用和边界清晰的组件；表单集成、样式隔离和事件通信都要提前试。","tags":["前端基础","组件与状态"],"slug":"frontend-web-components-2023","category":"2023","likeCount":6,"postId":"39308ef5-b713-b161-7f2e-1ffb5edc3a27","viewCount":1334,"readTime":null,"coverImage":null},"nextPost":{"title":"前端安全基线：日常项目先守住这些底线","date":"2023-09-15","excerpt":"从 XSS、CSRF、依赖安全、上传、跳转和错误信息出发，整理前端项目应该具备的安全基线。","tags":["前端基础","构建与模块化"],"slug":"frontend-security-baseline-2023","category":"2023","likeCount":5,"postId":"c8760c21-1051-29f4-ecf2-405cfb30987e","viewCount":3108,"readTime":null,"coverImage":null}},"__N_SSG":true}