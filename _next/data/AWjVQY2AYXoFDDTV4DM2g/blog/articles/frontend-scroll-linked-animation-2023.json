{"pageProps":{"postData":{"title":"滚动驱动动画入门：让页面动画跟着滚动走","date":"2023-08-24","excerpt":"滚动驱动动画能少写监听，但我会先看兼容、调试手段和性能预算，再决定用 CSS 时间线还是 JS。","tags":["前端基础","CSS"],"slug":"frontend-scroll-linked-animation-2023","category":"2023","likeCount":5,"postId":"922a678a-2fd4-856a-3b20-7dcd1c0141e6","viewCount":1599,"readTime":"10分钟","coverImage":null,"content":"\n很多网页都会做滚动相关动画。\n\n比如页面滚动时进度条变长、标题吸顶、图片视差移动、内容逐步出现。\n\n过去这类效果通常依赖 JavaScript 监听滚动位置，再手动修改元素样式。\n\n滚动动画本身不新，但它很容易从“增强体验”变成“制造负担”。所以讨论这类效果时，我更关心两个问题：动画是否真的服务内容，以及实现方式是否会给性能和维护带来过多成本。\n\n## 传统做法的问题\n\n常见写法是监听 `scroll`：\n\n```javascript\nwindow.addEventListener(\"scroll\", () => {\n  const progress =\n    window.scrollY /\n    (document.documentElement.scrollHeight - window.innerHeight);\n\n  document.querySelector(\".progress\").style.transform =\n    `scaleX(${progress})`;\n});\n```\n\n这能工作，但要注意很多细节：\n\n- 滚动事件触发频繁\n- 需要节流或 `requestAnimationFrame`\n- 需要计算滚动距离和页面高度\n- 多个动画会让逻辑变复杂\n\n我自己最早踩的坑是 `scrollHeight - innerHeight` 这个分母。它不是常量。页面里只要有图片懒加载、字体回流、折叠面板展开，这个值就会变，进度条就会突然往回跳一下。后来我学乖了，要么在内容稳定后重新测一次，要么干脆监听 `ResizeObserver`，在文档高度变化时刷新缓存的 `maxScroll`，而不是每帧重新读 DOM。\n\n还有一个容易忽略的点：移动端 Safari 的地址栏会随滚动伸缩，`innerHeight` 跟着变。如果进度条计算依赖它，滚到底部时进度可能卡在 0.97 而不是 1，看着很别扭。我最后给结果加了个 `Math.min(progress, 1)` 兜底，虽然不优雅，但比用户看到“永远差一点”的进度条强。\n\n如果只是让动画进度跟随滚动，CSS 原生能力会更适合。\n\n传统 JavaScript 写法不是不能用，而是需要写得很谨慎。至少要避免在 `scroll` 回调里频繁读写布局属性，尤其是同时读取 `offsetHeight`、`getBoundingClientRect`，再写 `style`。读写交错会增加浏览器重新计算布局的压力。\n\n如果必须用 JavaScript，通常会用 `requestAnimationFrame` 合并更新：\n\n```javascript\nlet ticking = false;\n\nwindow.addEventListener('scroll', () => {\n  if (ticking) {\n    return;\n  }\n\n  ticking = true;\n\n  window.requestAnimationFrame(() => {\n    updateProgress();\n    ticking = false;\n  });\n});\n```\n\n这不能让逻辑自动变快，但至少能避免滚动事件触发多少次就更新多少次。`scroll` 在快速滑动时一秒能触发上百次，而屏幕一秒最多刷新 60 次（高刷屏 120 次），`rAF` 的作用就是把多余的回调合并掉，让计算节奏对齐屏幕刷新。\n\n另外提一句监听器本身。我习惯给 `scroll` 加上 `{ passive: true }`：\n\n```javascript\nwindow.addEventListener('scroll', onScroll, { passive: true });\n```\n\n这是承诺回调里不会调用 `preventDefault`，浏览器就不用等 JS 执行完再决定要不要滚动，滚动手感会明显跟手。很多人不知道这个标记，结果在低端安卓机上滚动一卡一卡的，查半天以为是动画太重，其实是监听器把滚动主线程拖住了。\n\n还有一种场景不需要每一帧都跟随滚动，只关心“滚动结束后做点事”，比如同步当前章节、保存阅读位置、懒更新目录高亮。这类逻辑不要硬塞进 `scroll` 高频回调里。浏览器逐步支持 `scrollend` 事件后，这类需求可以写得更直接；不支持时再用定时器 debounce 降级。\n\n```javascript\nwindow.addEventListener('scrollend', () => {\n  updateCurrentHeading();\n});\n```\n\n我以前把目录高亮写在 `scroll` 里，每滚一下就遍历所有标题算位置。文章长一点 CPU 就飙。后来改成滚动中只做轻量状态，滚动结束后再修正高亮，体验反而更稳定。\n\n## 什么是滚动驱动动画\n\n滚动驱动动画的核心思想是：动画进度不再由时间决定，而是由滚动位置决定。\n\n普通动画是“3 秒内从 0 到 100%”。\n\n滚动动画是“页面从顶部滚到底部时，从 0 到 100%”。\n\n这样用户滚得快，动画就快；用户停下，动画也停下。\n\n这里要区分两类效果：\n\n- 跟随整个页面滚动进度，比如阅读进度条\n- 跟随某个元素进入视口的进度，比如图片进入时淡入、卡片移动\n\n前者更像页面级时间线，后者更像视口级时间线。实现时如果把两者混在一起，动画就容易出现触发位置不准确、移动距离难调的问题。\n\n在 CSS 滚动时间线里，这两类分别对应 `scroll()` 和 `view()`。`scroll()` 描述的是某个滚动容器的整体进度，`view()` 描述的是某个元素相对视口进出的进度。我第一次做视差时图省事，全用 `scroll()` 去算单个图片的位移，结果发现图片在长页面里几乎不动——因为它的进度被整个文档的滚动距离稀释了。换成 `view()` 之后，进度才回到“这张图自己进出视口”的尺度上，调参一下子就直观了。一句话记忆：要看整页用 `scroll()`，要看某个元素自己用 `view()`。\n\n## 一个进度条例子\n\n现代 CSS 中可以使用滚动时间线来描述这类效果。\n\n示意代码如下：\n\n```css\n.progress {\n  position: fixed;\n  top: 0;\n  left: 0;\n  height: 4px;\n  width: 100%;\n  transform-origin: left;\n  background: #1677ff;\n  animation: grow linear;\n  animation-timeline: scroll();\n}\n\n@keyframes grow {\n  from {\n    transform: scaleX(0);\n  }\n\n  to {\n    transform: scaleX(1);\n  }\n}\n```\n\n这段代码表达的是：页面滚动时，让进度条从 0 逐渐变到完整宽度。\n\n这里有个细节值得说一下：动画的 `animation-duration` 在滚动时间线下其实没意义，进度完全由滚动位置决定，但 `animation-name`、`linear` 这些还是要写。我一开始没写 `linear`，用了默认的缓动曲线，结果进度条跟滚动条对不齐——你滚到一半，进度条显示六成多，因为 ease 在中间段更快。滚动驱动的进度类动画几乎都该用 `linear`，让它和滚动严格成线性关系，缓动留给入场那种一次性动画。\n\n如果用元素进入视口的进度来驱动，写法是换成 `view()`，还能用 `animation-range` 精确控制“从元素刚露头到完全离开”这段区间：\n\n```css\n.cover {\n  animation: reveal linear both;\n  animation-timeline: view();\n  animation-range: entry 0% cover 40%;\n}\n```\n\n`animation-range` 这个属性是我后来才真正用顺的。`entry` 表示元素开始进入视口，`cover` 表示它和视口重叠的阶段。把范围卡在 `entry 0% cover 40%`，意思是元素刚冒头时动画开始、覆盖到四成时就播完，这样图片在视线中央时已经定住，不会一边看一边还在动。光靠 `view()` 默认范围很难做到这种“提前收尾”的效果。\n\n如果浏览器支持不足，可以保留 JavaScript 版本作为降级：\n\n```javascript\nfunction updateProgress() {\n  const scrollTop = window.scrollY;\n  const maxScroll =\n    document.documentElement.scrollHeight - window.innerHeight;\n  const progress = maxScroll > 0 ? scrollTop / maxScroll : 0;\n\n  document.querySelector('.progress').style.transform =\n    `scaleX(${Math.min(progress, 1)})`;\n}\n```\n\n这里加了 `maxScroll > 0` 的判断，避免页面高度不足一屏时出现除以 0。滚动动画里的边界很多，越是看似简单的进度条，越要处理这些基础情况。\n\n## 元素进入视口可以用 IntersectionObserver\n\n如果需求只是“元素进入视口后执行一次动画”，不一定要监听滚动位置。`IntersectionObserver` 更合适：\n\n```javascript\nconst observer = new IntersectionObserver((entries) => {\n  entries.forEach((entry) => {\n    if (!entry.isIntersecting) {\n      return;\n    }\n\n    entry.target.classList.add('is-visible');\n    observer.unobserve(entry.target);\n  });\n});\n\ndocument.querySelectorAll('.fade-in').forEach((element) => {\n  observer.observe(element);\n});\n```\n\n对应 CSS 可以很简单：\n\n```css\n.fade-in {\n  opacity: 0;\n  transform: translateY(16px);\n  transition: opacity 300ms ease, transform 300ms ease;\n}\n\n.fade-in.is-visible {\n  opacity: 1;\n  transform: translateY(0);\n}\n```\n\n这种方式适合内容卡片、图片、章节标题的入场动画。它不需要你手动计算滚动距离，也更容易控制“只触发一次”。\n\n实战里我会再调两个参数。一个是 `rootMargin`，比如设成 `0px 0px -10% 0px`，让元素进到视口下方 10% 才算“可见”，避免它刚露出一条边就开始淡入、用户其实还没看到。另一个是 `threshold`，默认 0 是“露一个像素就触发”，如果想等元素露出一半再动，就给 `threshold: 0.5`：\n\n```javascript\nconst observer = new IntersectionObserver(callback, {\n  rootMargin: '0px 0px -10% 0px',\n  threshold: 0.1,\n});\n```\n\n还有个坑是触发以后，要记得 `unobserve`。我见过一个列表页因为忘了取消观察，几百个元素的回调一直挂着，滚动时 CPU 莫名其妙偏高。入场动画是一次性的，触发完就该把元素从观察列表里摘掉，上面例子里那行 `observer.unobserve(entry.target)` 不是可有可无。\n\n如果动画必须跟着滚动进度连续变化，再考虑滚动时间线或滚动监听。\n\n## 适合哪些场景\n\n滚动驱动动画适合：\n\n- 页面阅读进度条\n- 图片视差效果\n- 内容进入视口时变化\n- 横向滚动展示\n- 长页面中的分段动画\n\n这些效果的共同点是：动画和滚动位置强相关。\n\n我会按需求强度选择实现方式：\n\n- 只需要进入视口后出现：优先 `IntersectionObserver`\n- 需要跟随页面整体进度：考虑 CSS 滚动时间线或简单滚动计算\n- 需要复杂分段、编排、多元素联动：考虑成熟动画库或明确的状态机\n\n不要把所有滚动效果都写成全局 `scroll` 监听。页面里滚动相关逻辑一多，后期很难判断哪个监听器影响哪个元素。\n\n## 性能上关注 transform 和 opacity\n\n滚动动画里，优先使用 `transform` 和 `opacity`。它们通常比直接改 `top`、`left`、`width` 更适合动画。\n\n例如让图片轻微上移：\n\n```css\n.hero-image {\n  transform: translateY(var(--offset));\n}\n```\n\n尽量不要在滚动过程中频繁修改会触发布局的属性。布局、绘制、合成是浏览器渲染里的不同阶段，动画越靠近合成层，越容易保持流畅。\n\n不过也不要滥用 `will-change`：\n\n```css\n.animated {\n  will-change: transform;\n}\n```\n\n它可以提示浏览器提前优化，但如果给大量元素都加上，反而会增加内存压力。只给确实会频繁动画的少量元素使用。\n\n我现在的做法是动态加 `will-change`：动画开始前用 JS 加上，动画结束后去掉，而不是写死在 CSS 里常驻。常驻的 `will-change` 等于让浏览器一直为这些元素准备独立的合成层，几十上百个元素都这样，移动端内存很快就吃紧，反而更卡。\n\n判断一个属性是否“便宜”，可以记个粗略的分级：`transform` 和 `opacity` 通常只走合成，最便宜；`color`、`background` 这类只重绘，中等；`width`、`height`、`top`、`margin` 会触发布局重排，最贵。滚动动画基本只该碰第一档。验证的时候打开 DevTools 的 Rendering 面板，勾上 Paint flashing，如果滚动时大片区域闪绿，说明有不该重绘的地方在动，顺着找一般能揪出某个改了 `top` 或 `height` 的样式。\n\n调试时我还会录一段 Performance，看 Main 线程有没有长任务。滚动动画如果每一帧都跑复杂 JS，哪怕只改 `transform`，也可能因为脚本计算太重而掉帧。不要只盯 CSS 属性，滚动链路里脚本、布局、绘制、合成任何一环超预算，用户看到的都是卡。\n\n一个实用标准是：先在低端手机或浏览器性能降速模式下看。如果只在自己的高性能电脑上顺滑，很可能只是机器替你掩盖了问题。\n\n## 使用时要保持克制\n\n滚动动画很容易做得过多。\n\n如果一个页面里每滚动一点都有大量元素移动、旋转、缩放，用户会觉得累，性能也可能变差。\n\n比较好的原则是：\n\n- 用在关键视觉反馈上\n- 动画幅度不要太夸张\n- 不要影响正文阅读\n- 保留无动画时的基本可用性\n\n动画是增强体验，不应该成为阅读障碍。\n\n还要考虑用户的系统偏好。有些用户开启了减少动态效果，页面应该尊重这个设置：\n\n```css\n@media (prefers-reduced-motion: reduce) {\n  .progress,\n  .fade-in,\n  .hero-image {\n    animation: none;\n    transition: none;\n    transform: none;\n  }\n}\n```\n\n这不是可有可无的细节。对一部分用户来说，大量滚动动画会造成眩晕或不适。\n\n## 兼容和降级\n\n滚动驱动动画的 CSS 新能力在 2023 年已经可以进入现代浏览器的渐进增强方案，但仍然需要关注浏览器支持。实际项目里不要假设所有用户都能使用最新语法。\n\n比较稳的策略是：\n\n- 内容默认可见，不依赖动画才出现\n- CSS 新特性只作为增强\n- 关键反馈提供 JavaScript 或静态降级\n- 不把核心操作藏在动画里\n\n比如阅读进度条没有显示，用户仍然能正常阅读文章；卡片没有淡入动画，内容也应该直接可见。这样新能力即使不生效，也不会影响核心体验。\n\n具体到代码，我会用 `@supports` 做特性探测，而不是嗅探浏览器版本：\n\n```css\n@supports (animation-timeline: scroll()) {\n  .progress {\n    animation: grow linear;\n    animation-timeline: scroll();\n  }\n}\n```\n\n支持的浏览器走 CSS 滚动时间线，不支持的就落到默认样式或 JS 版本。这里有个特别要小心的反面教材：千万别把元素的初始隐藏状态（`opacity: 0`）写在动画之外，再指望动画把它显出来。万一动画那段 CSS 不生效，元素就永远是透明的，内容直接消失了。我吃过这个亏，正确的顺序是默认可见，动画只负责“锦上添花”，而不是“雪中送炭”。\n\n\n滚动驱动动画让 CSS 能直接表达“动画跟随滚动进度”这类关系。\n\n它可以减少一部分滚动监听代码，让进度条、视差和页面动效更清晰。实际项目中仍然要关注浏览器支持，并为关键内容保留稳定的基础体验。\n\n我现在做滚动动画时，会先判断它是“进入视口触发”还是“跟随滚动进度”。前者通常交给 `IntersectionObserver`，后者再考虑 CSS 滚动时间线或滚动计算。选对模型，比一开始就堆动画代码更重要。\n"},"recommendedPosts":[{"title":"跨文档 View Transitions：多页应用也能做出丝滑过渡了","date":"2026-04-04","excerpt":"同文档 View Transitions 之外，跨文档（MPA）过渡终于可用。记录我给一个传统多页站点加上页面切换动画的过程、view-transition-name 的配对和降级处理。","tags":["前端基础","CSS","交互与动效"],"slug":"view-transitions-cross-document-2026","category":"2026","likeCount":10,"postId":"54351a26-388d-b51e-ff65-3ae7fde9bbd4","viewCount":1019,"readTime":null,"coverImage":null},{"title":"原生 CSS 嵌套和 @scope：写了这么多年 Sass 之后的几点真实感受","date":"2026-03-22","excerpt":"原生嵌套和 @scope 落地后，我在项目里逐步去掉一部分 Sass 的过程：嵌套的坑、@scope 的下边界 donut scope、以及现在还不能完全替代预处理器的地方。","tags":["前端基础","CSS"],"slug":"css-scope-nesting-2026","category":"2026","likeCount":37,"postId":"a4a7366f-5967-f614-350b-d223d0a045f3","viewCount":836,"readTime":null,"coverImage":null},{"title":"Tailwind CSS v4 迁移记：新引擎、CSS 变量主题和那些不再需要的配置","date":"2026-01-24","excerpt":"从 v3 升到 v4 的真实迁移过程：Oxide 引擎带来的速度变化、配置从 JS 搬到 CSS、@theme 和 CSS 变量怎么用，以及几个升级时踩的坑。","tags":["前端基础","CSS","构建与模块化"],"slug":"tailwind-v4-migration-2026","category":"2026","likeCount":34,"postId":"c8484300-10ec-bf7b-ef1b-6a237cb4f4f2","viewCount":963,"readTime":null,"coverImage":null}],"prevPost":{"title":"CSS 可维护性实践：样式失控通常不是技术问题，而是边界问题","date":"2023-09-14","excerpt":"讨论前端项目里 CSS 为什么容易失控，并结合 cascade layers、设计变量和组件边界梳理样式治理思路。","tags":["前端基础","构建与模块化"],"slug":"frontend-css-maintainability-2023","category":"2023","likeCount":5,"postId":"b08d9bf6-fad0-4d3c-a643-691eb1da0315","viewCount":2303,"readTime":null,"coverImage":null},"nextPost":{"title":"CSS Grid 真正好用的地方：把二维布局写清楚","date":"2023-08-20","excerpt":"从一次后台仪表盘布局重写出发，聊聊 CSS Grid 在页面骨架、卡片列表、区域命名和响应式布局里的真实用法。","tags":["前端基础","CSS"],"slug":"mastering-css-grid","category":"2023","likeCount":7,"postId":"d85d9915-5fb3-ed85-72bc-53f6fc206b79","viewCount":3305,"readTime":null,"coverImage":null}},"__N_SSG":true}