{"pageProps":{"postData":{"title":"移动端 H5 拍照功能怎么做","date":"2023-01-31","excerpt":"从一次工单现场拍照真机测试翻车的经历出发，聊聊 H5 调相机、图片压缩、方向修正、权限、上传重试和 WebView 兼容。","tags":["前端基础","移动与小程序"],"slug":"frontend-h5-camera-2023","category":"2023","likeCount":5,"postId":"48297c21-7c45-137c-2464-53bed13af648","viewCount":2454,"readTime":"7分钟","coverImage":null,"content":"\n很多移动端页面都会遇到拍照需求。\n\n比如上传头像、拍证件照、提交工单图片、上传现场照片。在 H5 页面里，实现拍照最常见的方式是使用文件选择框，而不是直接控制手机摄像头。\n\n这类需求看起来简单：点按钮、拍照、上传。但真实项目里还会遇到兼容性、图片过大、方向旋转、权限失败、用户取消、上传重试等问题。\n\n我做过一次工单现场拍照，最开始以为半天能搞定，结果真机测试一圈问题全出来了：iPhone 拍出来方向不对，安卓低端机压缩大图时页面卡死，企业微信 WebView 里 `capture` 表现和浏览器不一样，用户取消拍照还被当成上传失败弹了 toast。这个需求后来让我记住了，H5 拍照难点不在“调起相机”，而在“拍完之后稳定处理”。\n\n## 使用 input 调用相机\n\n可以用 `input type=\"file\"` 让用户选择或拍摄图片：\n\n```html\n<input type=\"file\" accept=\"image/*\" capture=\"environment\">\n```\n\n几个属性的含义：\n\n- `type=\"file\"` 表示选择文件\n- `accept=\"image/*\"` 表示只接收图片\n- `capture=\"environment\"` 表示优先使用后置摄像头\n\n如果希望优先使用前置摄像头，可以写：\n\n```html\n<input type=\"file\" accept=\"image/*\" capture=\"user\">\n```\n\n不过不同手机和浏览器对 `capture` 的支持不完全一致。有的会直接打开相机，有的会弹出相册和拍照选项，有的可能忽略前后摄像头偏好。实际项目一定要在目标机型和 WebView 里测试。\n\n如果业务运行在微信、企业微信、钉钉或自家 App WebView 里，还要确认容器有没有自己的相机能力。有些容器的 JS SDK 能拿到更稳定的拍照结果，也能处理权限提示；但这会带来平台绑定。我的习惯是先用标准 input 做基础方案，再按目标容器决定是否接 SDK 增强。\n\n## 读取用户拍摄的图片\n\n用户拍照或选择图片后，可以通过 `change` 事件拿到文件。\n\n```html\n<input id=\"camera\" type=\"file\" accept=\"image/*\" capture=\"environment\">\n<img id=\"preview\" alt=\"图片预览\">\n```\n\n```javascript\nconst input = document.querySelector(\"#camera\");\nconst preview = document.querySelector(\"#preview\");\n\ninput.addEventListener(\"change\", (event) => {\n  const file = event.target.files[0];\n\n  if (!file) {\n    return;\n  }\n\n  const url = URL.createObjectURL(file);\n  preview.src = url;\n\n  preview.onload = () => {\n    URL.revokeObjectURL(url);\n  };\n});\n```\n\n`URL.createObjectURL` 适合做本地预览，速度快，也不需要把图片转成 base64。预览完成后调用 `URL.revokeObjectURL`，可以释放临时地址占用的资源。\n\n预览时不要忘了加载失败态。移动端相册里可能有损坏图片、云端未下载完成的图片，`img.onerror` 要能给用户明确提示，而不是留一个空白预览框。\n\n## 什么时候用 FileReader\n\n如果后续逻辑需要 base64，可以使用 `FileReader`：\n\n```javascript\nconst reader = new FileReader();\n\nreader.onload = function (event) {\n  const base64 = event.target.result;\n  preview.src = base64;\n};\n\nreader.readAsDataURL(file);\n```\n\n但 base64 通常会让体积变大，不适合大图上传。除非接口明确要求 base64，或者你需要把图片嵌入到某段文本数据里，否则更推荐直接上传 `File` 或 `Blob`。\n\n我现在基本不主动把照片转 base64。base64 会增加大约三分之一体积，还会占用更多内存。低端手机上一张几 MB 的图转成字符串，很容易让页面卡住。能传二进制，就传二进制。\n\n## 上传前要校验文件\n\n移动端拍出来的图片可能很大，一张照片几 MB 很常见。如果不做限制，上传会慢，也容易失败。\n\n上传前至少校验类型和大小：\n\n```javascript\nfunction validateImage(file) {\n  const allowTypes = [\"image/jpeg\", \"image/png\", \"image/webp\"];\n  const maxSize = 5 * 1024 * 1024;\n\n  if (!allowTypes.includes(file.type)) {\n    return \"只支持 JPG、PNG 或 WebP 图片\";\n  }\n\n  if (file.size > maxSize) {\n    return \"图片不能超过 5MB\";\n  }\n\n  return \"\";\n}\n```\n\n注意不要只依赖 `accept`。它只能提示系统选择器过滤文件，并不能作为安全校验。服务端仍然要重新校验文件类型、大小和内容。\n\n## 使用 FormData 上传\n\n最常见的上传方式是 `FormData`：\n\n```javascript\nasync function uploadImage(file) {\n  const formData = new FormData();\n  formData.append(\"file\", file);\n\n  const response = await fetch(\"/api/upload\", {\n    method: \"POST\",\n    body: formData,\n  });\n\n  if (!response.ok) {\n    throw new Error(\"上传失败\");\n  }\n\n  return response.json();\n}\n```\n\n不要手动给这个请求设置 `Content-Type: multipart/form-data`。浏览器会自动带上 boundary，手动设置反而可能导致服务端无法解析。\n\n上传错误要统一处理。网络断开、文件过大、服务端校验失败、登录过期，给用户的提示不应该都叫“上传失败”。我会把错误归一成稳定结构，再在页面展示明确行动：重试、重新选择、重新登录，或者联系管理员。\n\n```javascript\nfunction normalizeUploadError(error) {\n  return {\n    code: error?.code || \"UPLOAD_ERROR\",\n    message: error?.message || \"图片上传失败，请稍后重试\",\n  };\n}\n```\n\n如果要展示上传进度，`fetch` 目前并没有稳定的上传进度事件，最直接的方式仍然是 `XMLHttpRequest`：\n\n```javascript\nfunction uploadWithProgress(file, onProgress) {\n  return new Promise((resolve, reject) => {\n    const xhr = new XMLHttpRequest();\n    const formData = new FormData();\n    formData.append(\"file\", file);\n\n    xhr.upload.onprogress = (event) => {\n      if (event.lengthComputable) {\n        onProgress(Math.round((event.loaded / event.total) * 100));\n      }\n    };\n\n    xhr.onload = () => {\n      if (xhr.status >= 200 && xhr.status < 300) {\n        resolve(JSON.parse(xhr.responseText));\n      } else {\n        reject({\n          code: \"UPLOAD_HTTP_ERROR\",\n          message: \"图片上传失败\",\n          status: xhr.status,\n        });\n      }\n    };\n\n    xhr.onerror = () => reject({ code: \"UPLOAD_NETWORK_ERROR\", message: \"网络异常\" });\n    xhr.open(\"POST\", \"/api/upload\");\n    xhr.send(formData);\n  });\n}\n```\n\n多图上传时不要并发全开。五六张现场照同时压缩、同时上传，低端手机很容易卡死。我通常会把压缩和上传都排队，最多同时处理 2 个，并把每张图的状态拆开：待压缩、压缩中、待上传、上传中、失败、成功。这样用户知道哪张失败了，也能只重试失败的那张。\n\n## 图片压缩和方向问题\n\n移动端照片经常有两个问题：体积太大，方向不对。\n\n体积太大可以在前端用 canvas 压缩：\n\n```javascript\nfunction compressImage(file, maxWidth = 1280, quality = 0.8) {\n  return new Promise((resolve, reject) => {\n    const img = new Image();\n    const url = URL.createObjectURL(file);\n\n    img.onload = () => {\n      const scale = Math.min(1, maxWidth / img.width);\n      const canvas = document.createElement(\"canvas\");\n      canvas.width = Math.round(img.width * scale);\n      canvas.height = Math.round(img.height * scale);\n\n      const context = canvas.getContext(\"2d\");\n      context.drawImage(img, 0, 0, canvas.width, canvas.height);\n\n      canvas.toBlob(\n        (blob) => {\n          URL.revokeObjectURL(url);\n          blob ? resolve(blob) : reject(new Error(\"图片压缩失败\"));\n        },\n        \"image/jpeg\",\n        quality\n      );\n    };\n\n    img.onerror = reject;\n    img.src = url;\n  });\n}\n```\n\n方向问题通常和 EXIF 信息有关。部分浏览器会自动处理，部分场景不会。如果业务对证件照、现场照方向要求严格，需要在目标设备上测试，必要时读取 EXIF 并修正方向。\n\n压缩也要控制节奏。一次性把多张大图都丢进 canvas，主线程会明显卡。多图上传时可以排队处理，或者限制一次最多选择几张。对现场工单这种场景，用户宁愿看到“正在压缩第 2/5 张”，也不愿意页面直接假死。\n\n压缩不要只固定一个 `quality`。不同手机拍出来的照片差异很大，有的 0.8 就能压到 1MB 内，有的还会超过限制。我会用一个简单循环，在最大宽度和质量之间做几次尝试：\n\n```javascript\nasync function compressToLimit(file, maxSize = 1024 * 1024) {\n  let quality = 0.85;\n  let width = 1600;\n  let blob = file;\n\n  for (let i = 0; i < 4; i += 1) {\n    blob = await compressImage(file, width, quality);\n    if (blob.size <= maxSize) {\n      return blob;\n    }\n    quality -= 0.15;\n    width = Math.round(width * 0.8);\n  }\n\n  return blob;\n}\n```\n\n这不是追求压得越小越好，而是把上传成功率控制住。证件照、工单现场照这类图片还要保留足够细节，压缩后最好让用户看到预览，而不是后台偷偷把图糊掉。\n\n## 用户取消也要处理\n\n用户打开相机后可能取消拍照，这不是错误。代码里要允许 `files[0]` 不存在，并且不要弹出“上传失败”。\n\n如果同一张照片连续选择两次，部分浏览器可能不会触发 `change`。可以在处理完成后清空 input：\n\n```javascript\ninput.value = \"\";\n```\n\n这样用户再次选择同一个文件时，也能触发事件。\n\n\n那次工单拍照之后，我再也不敢只在桌面浏览器里验证 H5 相机功能。\n\n`input type=\"file\"` 配合 `accept` 和 `capture` 是最常见也最稳的起点，但它不能保证每个 WebView 都按你想的方式打开相机，也不能替你处理大图压缩、方向、取消、重试和权限提示。\n\n拍照功能真正被用户感知的，不是“相机有没有调起来”，而是拍完以后能不能预览清楚、压缩不卡、方向不歪、上传失败能重试、取消时不乱报错。只要目标环境里有微信、企业微信、钉钉或自家 App WebView，真机矩阵就不能省。\n"},"recommendedPosts":[{"title":"小程序开发实践：那些前端容易踩到的坑","date":"2018-08-16","excerpt":"从 Web 前端切到小程序时踩过的坑：模板逻辑、setData、页面通信、分包和接口错误处理都和浏览器不太一样。","tags":["前端基础","移动与小程序"],"slug":"frontend-miniprogram-practice-2018","category":"2018","likeCount":6,"postId":"5b95f214-db06-fc5b-c8fe-b2a639d60c89","viewCount":1004,"readTime":null,"coverImage":null},{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":26,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":313,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":49,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":324,"readTime":null,"coverImage":null}],"prevPost":{"title":"Zustand 状态设计：小 store 也要有边界","date":"2023-02-10","excerpt":"围绕 Zustand 的 store 拆分、选择器、派生状态和持久化，讨论轻量状态管理的实践边界。","tags":["React","前端基础","组件与状态"],"slug":"frontend-zustand-state-design-2023","category":"2023","likeCount":7,"postId":"eafcfed2-0a05-f6ad-29d1-5aa0c91999e1","viewCount":1446,"readTime":null,"coverImage":null},"nextPost":{"title":"移动端适配入门：做前端页面时，别把响应式理解成只改宽度","date":"2023-01-17","excerpt":"移动端适配别只盯 rem 公式，真机视口、安全区、动态地址栏和表单键盘才是上线后最容易翻车的地方。","tags":["前端基础"],"slug":"frontend-mobile-adaptation-2023","category":"2023","likeCount":6,"postId":"89a606b4-2eea-20c9-6ed3-83e44ee37071","viewCount":1285,"readTime":null,"coverImage":null}},"__N_SSG":true}