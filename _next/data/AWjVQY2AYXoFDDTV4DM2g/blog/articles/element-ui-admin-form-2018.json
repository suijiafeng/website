{"pageProps":{"postData":{"title":"Element UI 后台表单整理：真正麻烦的是校验和回显","date":"2018-03-19","excerpt":"记录 Element UI 后台表单开发中的常见问题，包括表单校验、编辑回显、异步选项、提交防抖和错误提示。","tags":["Vue","前端基础"],"slug":"element-ui-admin-form-2018","category":"2018","likeCount":8,"postId":"a352dd0c-968a-a250-b67d-2d19aa4c4806","viewCount":4411,"readTime":"7分钟","coverImage":null,"content":"\n后台系统里最常写的不是图表，也不是复杂动画，而是表单。\n\n我做了两年多管理后台，掐指一算写过的表单没有一百也有八十：新增、编辑、筛选、批量、各种弹窗里的小表单。Element UI 把输入框、选择器、日期、弹窗都准备好了，搭个表单的骨架确实快，半小时就能把界面摆出来。但每次返工、每次被测试提 bug，几乎都不在“组件长什么样”上，而在校验、回显、异步数据、提交状态这些看不见的地方。界面摆完只是开了个头，后面这些细节才是真正吃工时的。\n\n这里记几件我在表单页里反复踩过的坑，技术背景是 2018 年的 Vue 2 + Element UI 2.x。\n\n## 新增和编辑最好共用一套结构\n\n常见表单：\n\n```js\ndata() {\n  return {\n    form: {\n      name: '',\n      status: 1,\n      categoryId: '',\n      description: ''\n    }\n  }\n}\n```\n\n新增时使用默认值，编辑时回显接口数据：\n\n```js\nasync fetchDetail(id) {\n  const detail = await getDetail(id)\n\n  this.form = {\n    name: detail.name || '',\n    status: detail.status,\n    categoryId: detail.categoryId || '',\n    description: detail.description || ''\n  }\n}\n```\n\n不要直接 `this.form = detail`。后端返回字段可能比表单多，也可能缺字段。前端应该明确表单需要哪些值。\n\n我吃过 `this.form = detail` 的亏。详情接口顺手多返回了 `createTime`、`creator`、`updateTime` 一堆只读字段，直接赋给 `form` 之后，提交时整个 `form` 原样 POST 回去，后端校验“creator 不允许修改”直接 400。更隐蔽的是缺字段：某个开关 `enabled` 在旧数据里压根没存，detail 里就没这个 key，`this.form = detail` 之后 `form.enabled` 是 `undefined`，`el-switch` 绑上去既不是开也不是关，显示成一个诡异的中间态。所以我现在一律手动挑字段、给默认值，多一个不要，少一个补上。\n\n另外提一句默认值的写法。我会把初始结构抽成一个工厂函数，新增和重置都调它，避免引用共享导致的串数据：\n\n```js\nfunction createDefaultForm() {\n  return {\n    name: '',\n    status: 1,\n    categoryId: '',\n    description: ''\n  }\n}\n\ndata() {\n  return {\n    form: createDefaultForm()\n  }\n}\n```\n\n千万别把默认对象写成模块级常量再直接赋值，那样所有实例共用同一个引用，一个表单改了另一个跟着变，这种 bug 极难查。\n\n## 校验规则要贴近业务\n\nElement UI 校验：\n\n```js\nrules: {\n  name: [\n    { required: true, message: '请输入名称', trigger: 'blur' },\n    { min: 2, max: 20, message: '名称长度为 2 到 20 个字符', trigger: 'blur' }\n  ],\n  categoryId: [\n    { required: true, message: '请选择分类', trigger: 'change' }\n  ]\n}\n```\n\n校验规则不要只写“必填”。长度、格式、边界值最好在前端先挡一层，用户体验会好很多。\n\n`trigger` 选 `blur` 还是 `change` 是有讲究的。输入框（`el-input`）我一律用 `blur`，让用户输完离开焦点再校验，边输边报红字很烦人；选择器、单选、日期这类“选一下就定了”的，用 `change`，选完立刻给反馈。早期我图省事全写 `blur`，结果下拉框选完不触发校验，用户得点一下别处才报错，体验很别扭。\n\n复杂一点的业务规则，内置的 `required` / `min` / `max` 不够用，就上自定义 `validator`。比如确认密码要和密码一致，或者某个字段格式特殊：\n\n```js\nconst validatePhone = (rule, value, callback) => {\n  if (!value) {\n    callback(new Error('请输入手机号'))\n  } else if (!/^1[3-9]\\d{9}$/.test(value)) {\n    callback(new Error('手机号格式不正确'))\n  } else {\n    callback() // 一定要无参调用，否则校验一直 pending\n  }\n}\n\nrules: {\n  phone: [{ validator: validatePhone, trigger: 'blur' }]\n}\n```\n\n自定义 validator 我踩过的坑就是忘了在校验通过时调 `callback()`——不调的话这条规则永远处于未完成状态，整个表单 `validate` 的 Promise/回调一直不返回，提交按钮点了没反应，还以为是接口卡住了。校验通过那条分支的 `callback()` 千万别漏。\n\n但前端校验不能替代后端校验。接口仍然要返回统一错误：\n\n```json\n{\n  \"code\": 40001,\n  \"message\": \"名称已存在\"\n}\n```\n\n页面收到后展示：\n\n```js\ncatch (error) {\n  this.$message.error(error.message)\n}\n```\n\n## 异步选项要处理加载状态\n\n很多表单选择项来自接口：\n\n```js\nasync fetchCategories() {\n  this.categoryLoading = true\n\n  try {\n    this.categories = await getCategories()\n  } finally {\n    this.categoryLoading = false\n  }\n}\n```\n\n模板：\n\n```html\n<el-select v-model=\"form.categoryId\" :loading=\"categoryLoading\">\n  <el-option\n    v-for=\"item in categories\"\n    :key=\"item.id\"\n    :label=\"item.name\"\n    :value=\"item.id\"\n  />\n</el-select>\n```\n\n编辑页要注意：详情接口和选项接口谁先返回都可能。不要因为选项还没加载出来，就误以为回显失败。\n\n这个竞态我专门栽过一次，现象很迷惑：编辑页打开，分类下拉框显示的是 categoryId 的数字（比如 `5`），而不是分类名“电子产品”。第一反应以为是回显代码写错了，查了半天才明白——`el-select` 是靠 `value` 去 `options` 里匹配 `label` 来显示文字的，如果详情接口先回来、`form.categoryId = 5` 已经设上了，但分类选项接口还没回来，`options` 是空数组，匹配不到，就只能把原始 value `5` 显示出来。等选项接口一回来，文字又自己变正常了，所以偶现、难复现。\n\n解法是保证两个接口都拿到了再渲染。我一般用 `Promise.all` 并行请求，全部 resolve 之后再标记 ready：\n\n```js\nasync init(id) {\n  const [detail] = await Promise.all([\n    getDetail(id),\n    this.fetchCategories() // 选项先备好\n  ])\n  this.fillForm(detail)\n  this.ready = true\n}\n```\n\n并行而不是串行，是因为这俩请求互不依赖，`Promise.all` 能省一个来回的等待时间，编辑页打开能快不少。\n\n## 提交要防重复\n\n```js\nasync submit() {\n  this.$refs.form.validate(async valid => {\n    if (!valid) return\n\n    if (this.submitting) return\n    this.submitting = true\n\n    try {\n      await saveForm(this.form)\n      this.$message.success('保存成功')\n      this.$router.back()\n    } catch (error) {\n      this.$message.error(error.message)\n    } finally {\n      this.submitting = false\n    }\n  })\n}\n```\n\n按钮：\n\n```html\n<el-button type=\"primary\" :loading=\"submitting\" @click=\"submit\">\n  保存\n</el-button>\n```\n\n后台接口慢一点时，重复点击很常见。前端应该挡住重复提交，后端也要保证幂等或唯一性校验。\n\n我专门强调“后端也要”，是因为前端这层 `submitting` 标志只能挡住“同一个页面连点”，挡不住网络重发、用户开两个 tab 同时提交、或者直接拿 Postman 打接口。真实事故是这样的：表单提交后跳转有点慢，用户以为没点上又点了一下，结果库里多了两条一模一样的数据。前端 loading 加上之后好了很多，但偶尔还是有重复——最后是后端在关键业务字段上加了唯一索引才彻底根治。前端防抖是为了体验，后端幂等才是兜底，两者缺一不可。\n\n还有个小细节：`validate` 的回调是异步的，`submitting` 的判断要放在 `validate` 校验通过之后、真正发请求之前那一段，别放在 `validate` 外面，否则校验没过也把按钮 loading 上了，转个不停还提交不了，看着像卡死。\n\n## 重置表单要小心\n\nElement UI 的 `resetFields` 会回到初始值。编辑页如果在数据回显前就渲染表单，初始值可能是空对象，后续重置会变成空。\n\n更稳的做法是等详情加载完成后再显示表单：\n\n```html\n<el-form v-if=\"ready\" ref=\"form\" :model=\"form\" :rules=\"rules\">\n  ...\n</el-form>\n```\n\n或者自己维护初始值副本。\n\n我现在的标准做法是：不依赖 `resetFields`，而是自己用前面那个 `createDefaultForm()` 工厂函数重置。`resetFields` 重置到的是“表单首次渲染时的值”，这个时机非常微妙——你回显的数据如果是在表单渲染之后才填进去的，它记的初始值就是回显前的空值，一点重置全没了；而工厂函数的语义是确定的，永远回到一份干净的默认结构，不受渲染时机影响：\n\n```js\nresetForm() {\n  this.form = createDefaultForm()\n  // 顺手清掉残留的校验红字\n  this.$nextTick(() => this.$refs.form && this.$refs.form.clearValidate())\n}\n```\n\n这里 `clearValidate` 也别漏。直接换 `form` 数据后，上一次校验留下的红色错误提示不会自动消失，得手动清一下；放在 `$nextTick` 里是为了等数据先更新到 DOM，再清校验状态，顺序反了清不干净。\n\n## 弹窗表单要在关闭时重置\n\n后台里很多表单是放在 `el-dialog` 里的，新增/编辑共用一个弹窗。这种最容易出的问题是：上次编辑的数据残留到下次新增。用户编辑了 A，关掉弹窗，再点“新增”，弹窗里还显示着 A 的内容。\n\n原因是 `el-dialog` 默认只是 `v-show` 切换显示，组件没销毁，`form` 数据还在。我的处理是监听弹窗的关闭事件，在 `close` 时重置：\n\n```html\n<el-dialog :visible.sync=\"visible\" @close=\"resetForm\">\n```\n\n或者更省心的办法，给 dialog 加 `destroy-on-close`，关闭时直接销毁内部内容，下次打开是全新的。不过 `destroy-on-close` 会让每次打开都重新渲染，表单复杂时有一点点开销，看场景取舍。我一般简单表单用 `@close=\"resetForm\"`，内部组件重、想图省事就上 `destroy-on-close`。\n\n表单的价值不在于把控件堆起来，而在于处理那些看不见但常出问题的细节：字段结构、回显、校验、异步选项、提交防重和统一错误提示。把这些做好，能显著减少后续的返工和用户痛点。\n"},"recommendedPosts":[{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2023-09-17","excerpt":"无限级菜单看着是递归组件，实际牵着路由、高亮、权限、图标和接口数据清洗，边界要先想清楚。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2023","category":"2023","likeCount":5,"postId":"73d4dc9e-8fc7-3ce9-8dcb-31a0122f0b98","viewCount":3765,"readTime":null,"coverImage":null},{"title":"刚开始用 Vue3 时，最容易踩的几个坑","date":"2023-06-07","excerpt":"从我把 Vue2 项目迁到 Vue3 时踩过的坑出发，聊聊响应式、props、watch、组合式 API、生命周期清理和 Pinia 使用误区。","tags":["Vue","前端基础"],"slug":"vue3-common-mistakes-2023","category":"2023","likeCount":6,"postId":"06bb42c9-56d9-0865-c906-88f606e068a9","viewCount":1090,"readTime":null,"coverImage":null},{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"从后台项目里反复写 loading/data/error 三件套的经历出发，聊聊请求库为什么会从 HTTP 客户端走向状态管理、缓存和统一错误处理。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2371,"readTime":null,"coverImage":null}],"prevPost":{"title":"Nuxt 初体验：Vue 项目需要 SEO 时，SSR 不是一句配置","date":"2018-04-03","excerpt":"记录 Nuxt 在内容站和官网项目中的使用体验，包括 SSR、路由、asyncData、head、部署和接口错误处理。","tags":["Vue","组件与状态"],"slug":"nuxt-ssr-first-look-2018","category":"2018","likeCount":5,"postId":"2367e95b-64c8-074e-c0e8-7c90f3e02298","viewCount":3688,"readTime":null,"coverImage":null},"nextPost":{"title":"CSS 半透明边框：rgba、hsla 和 background-clip","date":"2018-03-19","excerpt":"做卡片和弹窗边框时，我更关心透明效果会不会压住背景、能不能维护；rgba、hsla、background-clip 各有适用边界。","tags":["前端基础","CSS"],"slug":"frontend-css-transparent-border-2018","category":"2018","likeCount":6,"postId":"4c4d50af-88eb-8833-bdbe-92a1aff44118","viewCount":3144,"readTime":null,"coverImage":null}},"__N_SSG":true}