{"pageProps":{"postData":{"title":"Vue 2 项目尝试 TypeScript：类型安全和开发习惯都要磨合","date":"2018-05-20","excerpt":"记录 Vue 2 项目中尝试 TypeScript 的体验，包括组件写法、接口类型、Vuex 类型和迁移边界。","tags":["Vue","TypeScript"],"slug":"typescript-vue2-first-try-2018","category":"2018","likeCount":9,"postId":"8e886c02-9281-1ffe-1b4f-d989fbdc15fd","viewCount":5504,"readTime":"7分钟","coverImage":null,"content":"\n在 Vue 2 项目里引入 TypeScript，第一感觉不是“更安全”，而是“写法变重了”。\n\n原来一个 `.vue` 文件里直接写 `data`、`methods`、`computed`，现在要考虑类型、装饰器、this 推断、接口返回结构。小项目里可能觉得麻烦，但项目一大，类型能帮忙挡住不少低级错误。\n\n我是在一个已经跑了大半年的后台项目里动这个念头的。当时的导火索很具体：一个同事把接口返回的 `status` 字段当成了数字判断，可后端某次改成了字符串 `\"1\"`，前端没人发现，灰度上线后列表筛选整个失灵。这种问题用肉眼 review 很难抓，跑一遍也未必触发，但只要那个字段有类型，编译期就会红一片。那次之后我才下决心试 TypeScript，而不是因为它“看起来更专业”。\n\n需要先说清楚 2018 年这套组合的现实：Vue 2 对 TypeScript 的支持是“能用，但不顺手”。`vue` 官方提供了 `Vue.extend` 和 `vue-class-component`，再加上 `vue-property-decorator` 做语法糖，但 `.vue` 单文件组件里 `<script lang=\"ts\">` 的类型推断要靠 `vetur` 插件撑着，IDE 时不时给你飘红或者干脆不识别。心理预期摆正了再上手，磨合期会少很多怨气。\n\n## 先从接口类型开始\n\n最值得先加类型的地方是接口数据。\n\n```ts\nexport interface User {\n  id: number\n  name: string\n  role: 'admin' | 'editor' | 'guest'\n}\n```\n\n请求函数：\n\n```ts\nexport function getUsers(): Promise<User[]> {\n  return request({\n    url: '/users',\n    method: 'get'\n  })\n}\n```\n\n这样组件里使用时，至少知道字段是什么：\n\n```ts\ngetUsers().then(users => {\n  users.forEach(user => {\n    console.log(user.name)\n  })\n})\n```\n\n比起一上来改所有组件，先给接口加类型收益更明确。\n\n这里有个细节我后来才想通：`role` 用联合字面量 `'admin' | 'editor' | 'guest'` 比单纯的 `string` 有用得多。因为业务里经常 `if (user.role === 'admim')` 这种拼错，`string` 类型完全拦不住，但联合类型会直接告诉你这个值不在可选范围内。我们项目里光这一条就抓出过两三处隐藏 bug，都是手抖把权限角色拼错导致某个菜单永远不显示。\n\n另一个要提醒的是，接口类型最好和后端的字段“当下真实情况”对齐，而不是和接口文档对齐。文档经常滞后，后端偷偷加了字段或改了类型很常见。我的做法是把类型定义当成一份“前端这边对契约的理解”，每次联调发现不一致就立刻改类型，让编译器帮你记住这些差异，而不是靠脑子。\n\n如果返回结构是嵌套的，别偷懒写一层就完事。比如分页接口：\n\n```ts\nexport interface PageResult<T> {\n  list: T[]\n  total: number\n  pageSize: number\n  pageNum: number\n}\n\nexport function getUserPage(params: { pageNum: number; pageSize: number }): Promise<PageResult<User>> {\n  return request({\n    url: '/users',\n    method: 'get',\n    params\n  })\n}\n```\n\n泛型 `PageResult<T>` 写一次，所有分页接口复用。这是 TypeScript 真正开始还利息的地方——以前每个列表页都要在脑子里记一遍“返回的是 `list` 还是 `records`”，现在编译器替你记。\n\n## 组件写法要统一\n\nVue 2 + TypeScript 常见类组件写法：\n\n```ts\nimport Vue from 'vue'\nimport Component from 'vue-class-component'\n\n@Component\nexport default class UserList extends Vue {\n  loading = false\n  users: User[] = []\n\n  async created() {\n    this.loading = true\n    this.users = await getUsers()\n    this.loading = false\n  }\n}\n```\n\n这种写法更接近传统类，但和 Vue 原来的 Options API 不同。团队如果有人不熟，维护起来会有成本。\n\n配合 `vue-property-decorator` 还能把 `props`、`watch`、`emit` 都写成装饰器：\n\n```ts\nimport { Vue, Component, Prop, Watch } from 'vue-property-decorator'\n\n@Component\nexport default class UserCard extends Vue {\n  @Prop({ type: Number, required: true }) readonly userId!: number\n\n  detail: User | null = null\n\n  @Watch('userId', { immediate: true })\n  onUserIdChange(id: number) {\n    this.loadDetail(id)\n  }\n\n  async loadDetail(id: number) {\n    this.detail = await getUserDetail(id)\n  }\n}\n```\n\n`@Prop` 那行的 `userId!: number` 里那个感叹号是“确定赋值断言”，告诉编译器这个值一定会被外部传进来、不用我在 `data` 里给初值。少了它 TypeScript 会抱怨属性没初始化。这种小语法 2018 年第一次写的时候卡了我半天，文档里也没怎么强调。\n\n我踩过的一个真坑是装饰器和 Babel 的兼容。`vue-class-component` 依赖 `experimentalDecorators`，`tsconfig.json` 里这两项必须打开，否则编译直接报语法错误：\n\n```json\n{\n  \"compilerOptions\": {\n    \"experimentalDecorators\": true,\n    \"emitDecoratorMetadata\": true,\n    \"strict\": true,\n    \"noImplicitThis\": true\n  }\n}\n```\n\n也可以保留 Options API，只给关键数据补类型。不要为了 TypeScript 把所有写法都推倒。\n\n我后来在团队里定的规矩是：新写的、逻辑复杂的组件用类组件，老的、改动小的就用 `Vue.extend` 包一层补个类型，别强求统一。两种风格并存确实不够漂亮，但比为了“整齐”去重写一堆稳定运行的页面划算得多。技术债要还，但不必一次性还。\n\n## this 类型是常见痛点\n\nVue 2 里 `this` 类型推断并不总是舒服。尤其是 mixin、Vuex、插件挂载属性时，经常要补类型声明。\n\n例如挂载 `$http`：\n\n```ts\ndeclare module 'vue/types/vue' {\n  interface Vue {\n    $http: typeof request\n  }\n}\n```\n\n这些声明文件要集中放好，不要散在业务代码里。我一般专门建一个 `src/types/` 目录，里面放 `vue-shim.d.ts`、`global.d.ts` 之类的声明，业务代码不直接写 `declare module`。否则同一个属性在两个地方声明，类型一冲突排查起来非常痛苦。\n\n还有个新手必踩的坑：刚把 `.js` 改成 `.ts` 时，import 一个 `.vue` 文件 IDE 会飘红说找不到模块。这是因为缺了 SFC 的类型声明，得手动补上：\n\n```ts\n// src/types/vue-shim.d.ts\ndeclare module '*.vue' {\n  import Vue from 'vue'\n  export default Vue\n}\n```\n\n这段几乎是每个 Vue 2 + TS 项目的标配，没有它整个工程的导入路径都会报错，但偏偏没人会主动告诉你。第一次配环境我就卡在这里找了一晚上。\n\nVuex 的类型是另一个磨人的地方。Vue 2 时代的 Vuex 3 对 TypeScript 几乎是“无视”的，`this.$store.state.user.name` 这种链路拿不到任何提示。我当时的折中是不去硬啃官方那套泛型 module 写法，而是给 state 单独写接口，再在取值的地方手动断言：\n\n```ts\ninterface RootState {\n  user: {\n    profile: User | null\n    token: string\n  }\n}\n\nconst profile = (this.$store.state as RootState).user.profile\n```\n\n不优雅，但比引入一堆 `vuex-class` 装饰器再去和团队解释成本低。类型这东西，能拦住错误就算赚到，不一定要追求满分姿势。\n\n## 不要滥用 any\n\n刚迁移时很容易写：\n\n```ts\nconst data: any = await request('/api/user')\n```\n\n这能让报错消失，但也让 TypeScript 失去意义。\n\n如果暂时无法准确描述，可以先用 `unknown`：\n\n```ts\nconst data: unknown = await request('/api/user')\n```\n\n然后在边界处做判断。\n\n`unknown` 和 `any` 的区别是：`any` 会让类型检查彻底失效，沿着这个变量传下去的所有东西都不再检查；`unknown` 则强制你在用之前先收窄类型，相当于给自己留了个“这里还没处理好”的记号。我的经验是项目里可以全局搜索 `as any` 和 `: any`，把它们当成待还的债，每次重构顺手消一两个。一旦放任不管，半年后整个项目就退化成“穿着 TS 外衣的 JS”，类型报错全靠 `any` 压下去，那还不如不上。\n\nteam 里我立过一条软规矩：业务代码禁止裸 `any`，确实需要的地方要写注释说明原因，比如对接一个类型实在描述不清的第三方 SDK。这条规矩不靠 lint 强制，靠 code review 时互相盯，效果反而比 `no-explicit-any` 一刀切好——因为总有真该用 `any` 的场景，规则太死大家就开始用各种花招绕过它。\n\n## 迁移要分阶段\n\n老项目不要一次性全改。更稳的顺序：\n\n1. 新文件允许写 TypeScript\n2. 接口返回加类型\n3. 工具函数加类型\n4. Vuex state 和 mutation 加类型\n5. 再考虑组件整体迁移\n\n如果一开始就要求所有 `.vue` 文件改成 TS，很容易把团队拖进语法细节里。\n\n这个顺序不是拍脑袋排的，而是按“收益除以风险”排的。接口和工具函数是纯逻辑、没有 `this` 和组件生命周期的干扰，类型最好写、最不容易出幺蛾子，而且一旦加上，整个调用链都受益。组件迁移放最后，是因为它牵扯模板、装饰器、`this` 推断、第三方组件库的类型缺失，是这套体系里最容易让人崩溃的部分。先把容易的果子摘了，团队尝到甜头，后面推动组件迁移阻力会小很多。\n\n`allowJs` 这个配置在迁移期很关键，它允许 `.ts` 和 `.js` 共存，让你能一个文件一个文件慢慢改，而不是改了一个就整个项目编译不过：\n\n```json\n{\n  \"compilerOptions\": {\n    \"allowJs\": true,\n    \"checkJs\": false\n  }\n}\n```\n\n`checkJs` 先关掉，等纯 JS 文件都收拾差不多了再考虑打开。我们项目这套迁移前后拖了快三个月，中间一直是混合状态在正常上线发版的，没有出现“为了上 TS 停一个迭代”的情况。这点对说服老板和团队很重要——技术升级如果要停业务，多半推不动。\n\n在 Vue 2 中渐进引入 TypeScript 更实际：先从接口与工具函数下手，收获类型带来的信心，再逐步覆盖组件与状态。别把 TypeScript 当成形式，按风险与收益分阶段推进，团队更容易接受也更稳妥。\n"},"recommendedPosts":[{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":53,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":278,"readTime":null,"coverImage":null},{"title":"TypeScript 7 Beta 用 Go 重写之后：我更关心反馈链路有没有变短","date":"2026-04-28","excerpt":"从大型前端项目的类型检查、编辑器响应和 CI 等待说起，聊聊我会怎么评估 TypeScript 7 Beta 的性能收益和升级风险。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"frontend-typescript-7-go-2026","category":"2026","likeCount":25,"postId":"c582b9f3-9184-63db-3ced-7b4c1e26ca12","viewCount":309,"readTime":null,"coverImage":null},{"title":"TanStack Router 的类型安全路由：把跳转和参数错误挪到编译期","date":"2026-02-21","excerpt":"用过一段时间 TanStack Router 后的复盘：文件式与代码式路由、search params 的类型校验、loader 数据预取，以及它和 React Router 的取舍。","tags":["React","TypeScript","前端基础"],"slug":"tanstack-router-type-safe-2026","category":"2026","likeCount":25,"postId":"ba186679-ff87-187d-4d34-cff3f06231d3","viewCount":677,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 组件单元测试：先测行为，不要一上来测实现","date":"2018-06-02","excerpt":"Vue 单测刚落地时，我先从工具函数和组件行为下手，再补异步更新、接口错误和历史 bug，避免只写一堆脆弱快照。","tags":["Vue","构建与模块化"],"slug":"jest-vue-unit-test-2018","category":"2018","likeCount":6,"postId":"9677401b-1003-a8e8-a367-4d5ed3530d19","viewCount":4588,"readTime":null,"coverImage":null},"nextPost":{"title":"CSS 美化 checkbox：保留语义，比完全模拟更稳","date":"2018-05-10","excerpt":"我后来做复选框样式时，重点从“好看”转到“别破坏原生能力”：选中、焦点、禁用和可访问性都要保住。","tags":["前端基础","CSS"],"slug":"frontend-checkbox-style-2018","category":"2018","likeCount":8,"postId":"ca5257a1-b6ec-8261-35de-3cc00a9edc3f","viewCount":2686,"readTime":null,"coverImage":null}},"__N_SSG":true}