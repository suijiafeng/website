{"pageProps":{"postData":{"title":"CSS 美化 checkbox：保留语义，比完全模拟更稳","date":"2018-05-10","excerpt":"我后来做复选框样式时，重点从“好看”转到“别破坏原生能力”：选中、焦点、禁用和可访问性都要保住。","tags":["前端基础","CSS"],"slug":"frontend-checkbox-style-2018","category":"2018","likeCount":8,"postId":"ca5257a1-b6ec-8261-35de-3cc00a9edc3f","viewCount":2686,"readTime":"8分钟","coverImage":null,"content":"\n浏览器默认的 checkbox 样式比较朴素，而且各家浏览器长得还不一样。Chrome 是一个圆角小方块，Firefox 又是另一种描边，IE 上更是一言难尽。设计稿里给的往往是统一的蓝色对勾、圆角、加大尺寸，默认控件根本对不上。\n\n很多项目会希望把复选框做得更符合设计稿，比如换颜色、加图标、调整大小。我早些年接手一个后台表单，设计师要求复选框选中是品牌蓝，hover 还要有个浅色背景。当时第一反应是干脆用 div 加 JS 自己画一个，结果上线没多久就被测试提了一堆 bug：键盘 Tab 切不过去、表单提交收不到值、无障碍测试直接挂掉。后来推倒重来，老老实实保留原生 input，反而又快又稳。\n\n美化 checkbox 有很多方法，但最重要的是不要丢掉它原本的表单语义。这句话是我踩过坑之后才真正理解的。\n\n真正稳定的 checkbox 不是“看起来像复选框”，而是鼠标能点、键盘能切换、表单能提交、读屏软件能理解。一个控件好不好用，鼠标用户往往感觉不到差别，但只用键盘的人、用读屏软件的人马上就会发现问题。这部分代价在写的时候看不见，等真出问题时返工成本却很高。\n\n## 推荐保留原生 input\n\n比较稳的方式是保留 `input type=\"checkbox\"`，再用 `label` 扩大点击区域。\n\n```html\n<label class=\"checkbox\">\n  <input type=\"checkbox\">\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">我已阅读并同意协议</span>\n</label>\n```\n\n这样用户点击文字时，也能切换复选框状态。\n\n如果不想把 `input` 放在 `label` 里面，也可以用 `for` 和 `id` 关联：\n\n```html\n<input id=\"agree\" class=\"checkbox-input\" type=\"checkbox\">\n<label class=\"checkbox-label\" for=\"agree\">\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">我已阅读并同意协议</span>\n</label>\n```\n\n这种写法适合表单结构比较复杂、控件和文案需要分开布局的场景。比如一个设置页，左边是开关列表，右边是说明文字，DOM 结构上很难把 input 和文案塞进同一个 label 里，这时候用 `for`/`id` 关联就很灵活。\n\n这里提醒一个我反复踩过的小坑：用 `for`/`id` 时，页面上 `id` 必须唯一。后台系统经常用循环渲染一堆复选框，如果偷懒把 `id=\"agree\"` 写死，循环出来就全是同一个 id，点第二个会去切换第一个，调试半天才发现。我的习惯是用数据里的唯一字段拼 id，比如 `id={`agree-${item.id}`}`，别图省事写常量。\n\n## 隐藏原生样式\n\n可以把原生 input 视觉上隐藏，但不要从可访问性树里完全移除。\n\n```css\n.checkbox input {\n  position: absolute;\n  opacity: 0;\n}\n\n.checkbox {\n  display: inline-flex;\n  align-items: center;\n  cursor: pointer;\n}\n\n.checkbox-box {\n  width: 18px;\n  height: 18px;\n  margin-right: 8px;\n  border: 1px solid #999;\n  border-radius: 3px;\n}\n```\n\n这样 input 还在页面里，只是不可见。\n\n更推荐使用“视觉隐藏”方式，让它仍然可以获得焦点：\n\n```css\n.checkbox input {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  white-space: nowrap;\n  border: 0;\n}\n```\n\n不要使用 `display: none` 或 `visibility: hidden` 来隐藏表单控件。这样会让它无法聚焦，也更容易破坏可访问性和表单行为。\n\n这个坑我亲自踩过。当时为了图省事直接 `display: none`，视觉上没问题，鼠标点 label 也能切换。但有同事反馈用键盘 Tab 完全跳不到这个复选框，焦点直接越过去了——因为 `display: none` 的元素根本不在 tab 顺序里。换成上面那套“视觉隐藏”（也叫 sr-only）的写法，input 在布局上缩成 1px、裁剪掉、但仍然存在于可访问性树和焦点链里，键盘和读屏才能正常工作。\n\n补一句，上面用了 `clip: rect(0 0 0 0)`，这是当时兼容性最好的做法。新一点的写法会用 `clip-path: inset(50%)`，但 2018 年我还是更信任 `clip`，老浏览器上不会翻车。两个一起写也行：\n\n```css\n.checkbox input {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  margin: -1px;\n  padding: 0;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  clip-path: inset(50%);\n  white-space: nowrap;\n  border: 0;\n}\n```\n\n## 使用 :checked 控制选中态\n\n可以用相邻选择器根据选中状态改变样式：\n\n```css\n.checkbox input:checked + .checkbox-box {\n  border-color: #1677ff;\n  background: #1677ff;\n}\n\n.checkbox input:checked + .checkbox-box::after {\n  content: \"\";\n  display: block;\n  width: 5px;\n  height: 9px;\n  margin: 2px auto;\n  border: solid #fff;\n  border-width: 0 2px 2px 0;\n  transform: rotate(45deg);\n}\n```\n\n这样不需要额外 JavaScript，就能实现选中效果。我特别喜欢这个方案的一点是：状态的真相永远在 input 上，CSS 只是被动跟着 `:checked` 走。不用手动同步、不用监听 change 再 toggle class，少了一整类“状态不一致”的 bug。\n\n这里用相邻选择器 `+` 是有讲究的：它要求 `.checkbox-box` 紧跟在 input 后面。如果中间插了别的元素，`+` 就选不中了，得换成通用兄弟选择器 `~`。我一般保持 input 紧贴着 box，结构稳定，选择器也最简单。\n\n那个对勾用的是 border 画法——一个旋转 45 度的方块，只留右边和下边两条边。比起放一张对勾图片或者 icon font，纯 CSS 画对勾在高分屏上永远清晰，也不会有额外请求。尺寸要自己微调，`width`、`height`、`border-width` 三个值配合 box 的大小调，调到对勾居中、比例顺眼为止。这块没有公式，就是对着屏幕拧。\n\n为了让焦点状态也清楚，可以补上 `:focus-visible`：\n\n```css\n.checkbox input:focus-visible + .checkbox-box {\n  outline: 2px solid rgba(22, 119, 255, 0.35);\n  outline-offset: 2px;\n}\n```\n\n键盘用户通过 Tab 切到复选框时，需要知道当前焦点在哪里。这个细节在表单里很重要。\n\n为什么用 `:focus-visible` 而不是 `:focus`？因为 `:focus` 鼠标点一下也会触发，那个蓝色描边框就会在鼠标用户点完之后还赖着不走，不少设计师看到会让你“把那个框去掉”。`:focus-visible` 只在浏览器判断为键盘操作时才出现，鼠标点击不显示，正好两边都照顾到。2018 年它还比较新，老浏览器不支持也无所谓——退化成完全没有焦点框，至少不会比原来更差，而且可以加一段 polyfill。千万别为了“好看”直接写 `outline: none` 把焦点框一刀切掉，那等于把键盘用户晾在原地，完全不知道焦点跑哪去了。\n\n如果担心 `:focus-visible` 兼容，可以做个兜底：先给 `:focus` 一个保守的样式，再用 `:focus:not(:focus-visible)` 把鼠标态的去掉。\n\n## 处理禁用状态\n\n业务表单里经常会遇到禁用项，例如某些协议未开放、权限不足或依赖条件未满足。\n\n```html\n<label class=\"checkbox\">\n  <input type=\"checkbox\" disabled>\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">仅管理员可操作</span>\n</label>\n```\n\n```css\n.checkbox input:disabled + .checkbox-box {\n  border-color: #d9d9d9;\n  background: #f5f5f5;\n}\n\n.checkbox input:disabled ~ .checkbox-text {\n  color: #999;\n}\n\n.checkbox:has(input:disabled) {\n  cursor: not-allowed;\n}\n```\n\n说实话 2018 年 `:has()` 基本没法用，浏览器支持度几乎为零，我都是当它不存在。这里写出来更多是表达思路。真要落地，老老实实给外层手动加类名最稳。\n\n如果要兼容不支持 `:has()` 的环境，可以给外层手动加一个类名：\n\n```html\n<label class=\"checkbox checkbox-disabled\">\n  <input type=\"checkbox\" disabled>\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">仅管理员可操作</span>\n</label>\n```\n\n```css\n.checkbox-disabled {\n  cursor: not-allowed;\n}\n```\n\n## 不建议只用 div 模拟\n\n有些实现会完全不用 `input`，只用 `div` 或 `i` 模拟复选框。\n\n这种方式虽然看起来灵活，但会带来问题：\n\n- 表单语义丢失\n- 键盘操作不自然\n- 可访问性变差\n- 表单提交需要额外处理\n- 状态同步更容易出错\n\n除非有很特殊的需求，否则不建议这样做。\n\n如果确实必须自定义成非原生元素，至少要补齐 `role`、`aria-checked` 和键盘事件。但这会让实现复杂很多：\n\n```html\n<div role=\"checkbox\" aria-checked=\"false\" tabindex=\"0\" class=\"fake-checkbox\">\n  接收通知\n</div>\n```\n\n```javascript\nconst checkbox = document.querySelector('.fake-checkbox');\n\nfunction toggle() {\n  const checked = checkbox.getAttribute('aria-checked') === 'true';\n  checkbox.setAttribute('aria-checked', String(!checked));\n}\n\ncheckbox.addEventListener('click', toggle);\ncheckbox.addEventListener('keydown', (event) => {\n  if (event.key === ' ' || event.key === 'Enter') {\n    event.preventDefault();\n    toggle();\n  }\n});\n```\n\n这段代码只是补了最基础的交互。和原生 checkbox 相比，它没有更简单，只是把浏览器已经做好的事情重新做了一遍。而且这还远没补齐：原生 checkbox 在表单里会自动带上 name/value 参与提交，`<div>` 不会，你得额外塞一个隐藏 input 去同步状态；原生的还支持 `indeterminate`（半选）这种第三态，自己模拟又是一摊代码；读屏软件对原生 checkbox 的播报也是现成的，自定义的得自己把 `aria-checked` 维护对。\n\n我后来给团队定了条规矩：除非设计稿要的效果原生 input 配 CSS 实在做不出来（这种情况极少），否则一律保留原生控件。省下来的不只是开发时间，更是后面无障碍、表单提交、键盘交互这些隐性维护成本。\n\n## 别忘了半选状态\n\n做“全选”这类功能时，经常需要第三种状态：部分选中。原生 checkbox 支持 `indeterminate`，但它只能通过 JS 设置，没法用 HTML 属性写：\n\n```javascript\nconst all = document.querySelector(\"#select-all\");\nconst items = document.querySelectorAll(\".item-checkbox\");\n\nfunction syncAll() {\n  const checkedCount = [...items].filter((i) => i.checked).length;\n  all.checked = checkedCount === items.length;\n  all.indeterminate = checkedCount > 0 && checkedCount < items.length;\n}\n\nitems.forEach((i) => i.addEventListener(\"change\", syncAll));\n```\n\n视觉上可以用 `:indeterminate` 伪类画一条横杠：\n\n```css\n.checkbox input:indeterminate + .checkbox-box {\n  border-color: #1677ff;\n  background: #1677ff;\n}\n\n.checkbox input:indeterminate + .checkbox-box::after {\n  content: \"\";\n  display: block;\n  width: 10px;\n  height: 2px;\n  margin: 8px auto;\n  background: #fff;\n  transform: none;\n}\n```\n\n这又是一个“原生帮你做好了”的例子——`indeterminate` 不影响实际的 `checked` 值，提交时该是 false 还是 false，状态语义清清楚楚。自己用 div 模拟的话，光是把全选、半选、未选三态和子项联动调对就够喝一壶的。\n\n## 一套完整的基础样式\n\n综合起来，一个可维护的版本可以这样写：\n\n```html\n<label class=\"checkbox\">\n  <input name=\"agreement\" type=\"checkbox\">\n  <span class=\"checkbox-box\" aria-hidden=\"true\"></span>\n  <span class=\"checkbox-text\">我已阅读并同意协议</span>\n</label>\n```\n\n```css\n.checkbox {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  cursor: pointer;\n}\n\n.checkbox input {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  white-space: nowrap;\n  border: 0;\n}\n\n.checkbox-box {\n  width: 18px;\n  height: 18px;\n  /* 本来想用 flex 的 gap 来留间距，但 2018 年 gap 在 flex 上几乎没浏览器支持\n     （Chrome 要到 2020、Safari 更晚），只能老实用 margin 顶着 */\n  margin-right: 8px;\n  border: 1px solid #999;\n  border-radius: 3px;\n  transition: border-color 0.2s ease, background-color 0.2s ease;\n}\n\n.checkbox input:checked + .checkbox-box {\n  border-color: #1677ff;\n  background: #1677ff;\n}\n\n.checkbox input:checked + .checkbox-box::after {\n  content: \"\";\n  display: block;\n  width: 5px;\n  height: 9px;\n  margin: 2px auto;\n  border: solid #fff;\n  border-width: 0 2px 2px 0;\n  transform: rotate(45deg);\n}\n\n.checkbox input:focus-visible + .checkbox-box {\n  outline: 2px solid rgba(22, 119, 255, 0.35);\n  outline-offset: 2px;\n}\n```\n\n美化复选框要保留它的语义与交互：保留原生 input，把视觉交给旁边的样式元素，让 `:checked`、`:disabled` 等伪类自然驱动表现。这样既能满足设计，又不会破坏键盘、读屏和表单行为。简单、稳妥、可维护，胜过把控件完全自造一遍。\n"},"recommendedPosts":[{"title":"跨文档 View Transitions：多页应用也能做出丝滑过渡了","date":"2026-04-04","excerpt":"同文档 View Transitions 之外，跨文档（MPA）过渡终于可用。记录我给一个传统多页站点加上页面切换动画的过程、view-transition-name 的配对和降级处理。","tags":["前端基础","CSS","交互与动效"],"slug":"view-transitions-cross-document-2026","category":"2026","likeCount":10,"postId":"54351a26-388d-b51e-ff65-3ae7fde9bbd4","viewCount":1094,"readTime":null,"coverImage":null},{"title":"原生 CSS 嵌套和 @scope：写了这么多年 Sass 之后的几点真实感受","date":"2026-03-22","excerpt":"原生嵌套和 @scope 落地后，我在项目里逐步去掉一部分 Sass 的过程：嵌套的坑、@scope 的下边界 donut scope、以及现在还不能完全替代预处理器的地方。","tags":["前端基础","CSS"],"slug":"css-scope-nesting-2026","category":"2026","likeCount":37,"postId":"a4a7366f-5967-f614-350b-d223d0a045f3","viewCount":891,"readTime":null,"coverImage":null},{"title":"Tailwind CSS v4 迁移记：新引擎、CSS 变量主题和那些不再需要的配置","date":"2026-01-24","excerpt":"从 v3 升到 v4 的真实迁移过程：Oxide 引擎带来的速度变化、配置从 JS 搬到 CSS、@theme 和 CSS 变量怎么用，以及几个升级时踩的坑。","tags":["前端基础","CSS","构建与模块化"],"slug":"tailwind-v4-migration-2026","category":"2026","likeCount":5,"postId":"c8484300-10ec-bf7b-ef1b-6a237cb4f4f2","viewCount":1011,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 2 项目尝试 TypeScript：类型安全和开发习惯都要磨合","date":"2018-05-20","excerpt":"记录 Vue 2 项目中尝试 TypeScript 的体验，包括组件写法、接口类型、Vuex 类型和迁移边界。","tags":["Vue","TypeScript"],"slug":"typescript-vue2-first-try-2018","category":"2018","likeCount":9,"postId":"8e886c02-9281-1ffe-1b4f-d989fbdc15fd","viewCount":5504,"readTime":null,"coverImage":null},"nextPost":{"title":"Ajax、JSONP 和跨域：接口联调时最常被问的老问题","date":"2018-05-09","excerpt":"结合接口联调经验，聊聊 XMLHttpRequest、Ajax、JSONP、CORS、预检请求和统一错误处理怎么连起来。","tags":["JavaScript","前端基础"],"slug":"ajax-jsonp-cors-interview-2018","category":"2018","likeCount":5,"postId":"e2cded49-c9c7-f85d-8697-13b49a2d98dc","viewCount":4657,"readTime":null,"coverImage":null}},"__N_SSG":true}