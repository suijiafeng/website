{"pageProps":{"postData":{"title":"前端性能优化应该先看哪里：别一上来就想着大招","date":"2021-12-10","excerpt":"从资源体积、渲染成本和交互反馈出发，讨论前端性能优化更合理的思考顺序。","tags":["前端基础"],"slug":"frontend-performance-basics-2021","category":"2021","likeCount":9,"postId":"13d22c9f-6242-1147-177e-978f1f8c2b31","viewCount":2561,"readTime":"10分钟","coverImage":null,"content":"\n前端性能优化是一个很容易让人焦虑的话题。\n\n因为它涉及的面太广：资源加载、接口响应、渲染卡顿、动画流畅度、打包体积、缓存策略，几乎每一层都能谈性能。也正因为这样，很多人一提优化就容易直接奔着“大招”去，反而忽略最基础的判断顺序。\n\n## 性能问题首先要先定位，不要凭感觉改\n\n页面慢有很多种慢：\n\n- 首屏白屏时间长\n- 内容出来了但交互卡\n- 列表滚动不流畅\n- 某个操作点击后响应慢\n\n这些问题背后的原因很可能完全不同。如果不先区分是哪一类，再多优化动作也可能打偏。\n\n例如首屏慢更可能和资源加载、构建产物、缓存策略有关；交互卡则可能和渲染、事件处理、DOM 更新频率有关。\n\n我吃过一次很典型的亏。当时有个列表页用户反馈“卡”，我想当然以为是渲染问题，花了大半天把列表拆成虚拟滚动，结果上线后体感几乎没变。后来打开 Performance 面板录一段才发现，真正的瓶颈是每次滚动都同步调了一个排序函数，几千条数据排一遍，主线程被占满，渲染再快也没用。那次之后我养成了习惯：先录制再下结论，别让“看起来像”左右判断。\n\n我现在排查时基本是固定几步走：\n\n```js\n// 1. 先看到底是网络慢还是脚本慢，打开 Performance 面板录一段操作\n//    看火焰图里哪一块时间最长：Loading / Scripting / Rendering / Painting\n\n// 2. 怀疑某段同步逻辑很重时，直接用 performance.now() 圈一下\nconst t0 = performance.now()\ndoHeavyWork()\nconsole.log('耗时', (performance.now() - t0).toFixed(2), 'ms')\n\n// 3. 想看长任务（阻塞主线程超过 50ms 的）就挂个 observer\nconst observer = new PerformanceObserver((list) => {\n  for (const entry of list.getEntries()) {\n    console.warn('Long Task', entry.duration, entry)\n  }\n})\nobserver.observe({ entryTypes: ['longtask'] })\n```\n\n光是 `longtask` 这个 observer 就帮我抓出过好几个“偷偷”阻塞主线程的逻辑，比如某个第三方埋点 SDK 在初始化时同步解析了一大坨配置。光看代码很难发现，录一段就一目了然。\n\n## 先看影响最大的地方\n\n很多性能瓶颈不是细枝末节，而是几个比较明显的问题：\n\n- 脚本体积过大\n- 图片资源过重\n- 重复请求过多\n- 页面一次性渲染数据太多\n\n这些问题带来的影响往往比某几个微优化大得多。\n\n拿脚本体积来说，我接手过一个项目首屏 JS 打出来 2MB 多，光下载加解析就好几秒。后来用 `rollup-plugin-visualizer`（webpack 那边是 `webpack-bundle-analyzer`）出了个图，一看全是 moment 带着几十种语言包、还有整包引入的 lodash。处理起来其实不复杂：\n\n```js\n// lodash 别整包引，按需引或者用 lodash-es 配合 tree-shaking\n// 错误示范\nimport _ from 'lodash'\n_.debounce(fn, 200)\n\n// 正确示范\nimport debounce from 'lodash-es/debounce'\ndebounce(fn, 200)\n\n// moment 太重就换 dayjs，API 几乎一致，体积差一个数量级\nimport dayjs from 'dayjs'\ndayjs().format('YYYY-MM-DD')\n```\n\n想知道下载下来的代码到底有多少根本没跑到，不用猜，Chrome DevTools 自带的 Coverage 面板就能量。打开方式：DevTools 里按 `Cmd/Ctrl+Shift+P` 调出命令菜单，输入 `Coverage`、选 `Show Coverage`，点左上角圆形录制按钮刷新页面。跑完它会按文件列出每个 JS/CSS 的总字节数和「Unused Bytes」百分比，红色是未执行、蓝色是执行过的。我在一些项目首页上常看到首屏阶段某些 JS 未使用比例到 70%、80%——这些就是白下载、白解析的字节，是拆包和懒加载最该下手的地方。\n\n光是这两处替换，加上把不影响首屏的路由组件改成动态 `import()` 懒加载，首屏包就瘦了一半还多。这种“大头”改一次，比抠十个小函数的收益都大。\n\n```js\n// 路由级懒加载，非首屏页面不必塞进首屏包里\nconst Settings = () => import('./views/Settings.vue')\n```\n\n体积砍下来之后，还可以在加载顺序上做文章。浏览器渲染首屏要走一条关键渲染路径：拿到 HTML→构建 DOM→解析 CSS 构建 CSSOM→合成渲染树→布局→绘制，CSS 和首屏要用的 JS 会阻塞这条路径上的渲染。能帮上忙的是几个资源提示，但各管各的、别混用：\n\n```html\n<!-- preload：这个资源首屏一定要用，请提前以高优先级拉，别等解析到才发现 -->\n<link rel=\"preload\" href=\"/fonts/main.woff2\" as=\"font\" type=\"font/woff2\" crossorigin />\n<!-- preconnect：提前和这个域名把 DNS/TCP/TLS 握手做完，省掉首次请求的连接耗时 -->\n<link rel=\"preconnect\" href=\"https://cdn.example.com\" />\n<!-- prefetch：这个资源这次用不上，但下一个页面很可能要，浏览器空闲时低优先级偷偷拉 -->\n<link rel=\"prefetch\" href=\"/next-page.js\" />\n```\n\n我吃过的亏是 `preload` 用过了头——一股脑把十几个资源都标成 preload，等于谁都不优先，反而把真正该先到的字体和首屏图挤后面了。它是“纠正优先级”的工具，不是“调高所有优先级”。\n\n所以我通常更推荐先处理大头，再考虑更细的技巧。否则很容易在局部花很多时间，却没明显改善体验。\n\n## 用户感知比指标名字更重要\n\n性能当然可以用很多指标衡量，但最终页面快不快，还是体现在用户感知上。\n\n例如：\n\n- 页面是不是很久都没内容\n- 点击后有没有及时反馈\n- 输入时会不会掉帧\n- 切换页面有没有明显卡顿\n\n如果一个优化动作不能改善这些体验，只是让某个数字看起来更漂亮，那它的价值要再判断。\n\n这两年大家慢慢都在用 Core Web Vitals 那套指标，我觉得它好就好在每个指标都对应一种真实感受，而不是凭空造出来的数字：\n\n- LCP（最大内容绘制）对应“主要内容多久能看到”，Google 给的“良好”阈值是 2.5s 以内\n- FID（首次输入延迟）对应“第一次点了之后多久有反应”，良好阈值是 100ms 以内\n- CLS（累计布局偏移）对应“内容会不会突然乱跳”，良好阈值是 0.1 以内\n\n这几个值也不用靠工具才能看到，LCP 在控制台贴段 `PerformanceObserver` 就能当场打出来。把下面这段贴进任意页面的控制台，然后随便滚一下或等首屏画完：\n\n```js\nnew PerformanceObserver((list) => {\n  const entries = list.getEntries()\n  const last = entries[entries.length - 1] // LCP 取最后一次报告的候选\n  console.log('LCP:', last.startTime.toFixed(0), 'ms', last.element)\n}).observe({ type: 'largest-contentful-paint', buffered: true })\n```\n\n`buffered: true` 会把页面加载早期已经发生的 LCP 候选也补报回来，所以即便你贴晚了一点也能拿到值。控制台会打印类似 `LCP: 1832 ms <img...>` 这样一行，后面那个 DOM 元素就是被判定为「最大内容」的那个——很多时候你会惊讶它指向的并不是你以为的主图。\n\n这几个值不用死记，但记住量级有用：LCP 卡在两三秒以上、首次交互延迟上到一两百毫秒，用户是能明显感觉到的。它们衡量的也分别是不同阶段——LCP 关心首屏内容多久画出来，FID 关心页面可交互之后第一次点下去跟不跟手，这俩经常被混着说成“快慢”，其实是两码事：首屏内容出来了，不代表主线程腾出来能响应点击。\n\nCLS 我特别有体会。有个页面图片没写宽高，加载完后下面的内容被往下顶，用户正要点的按钮瞬间错位，点歪了。修法很简单，给图片占好位就行：\n\n```html\n<!-- 提前声明尺寸，浏览器就能预留位置，不会加载完才撑开 -->\n<img src=\"banner.jpg\" width=\"1200\" height=\"400\" alt=\"\" />\n```\n\n```css\n/* 不确定具体像素时用 aspect-ratio 锁住比例也行 */\n.cover { width: 100%; aspect-ratio: 3 / 1; }\n```\n\n这个改动在任何“速度”指标上都体现不出来，但用户能实实在在感觉到页面稳了。这就是我说的，要盯感知，别只盯数字。\n\n图片本身也是首屏大头里最值得收拾的一类。我现在的几条常规动作：尺寸上别拿一张 2000px 宽的原图去填一个 300px 的位置，按实际显示尺寸出图；格式上能上 WebP/AVIF 就上，同等画质下比 JPEG 小一大截；首屏以下的图统统加 `loading=\"lazy\"`，让浏览器等快滚到了再去拉：\n\n```html\n<img src=\"thumb.webp\" width=\"300\" height=\"200\" loading=\"lazy\" alt=\"\" />\n```\n\n`loading=\"lazy\"` 是浏览器原生的，不用上 IntersectionObserver 那套手写懒加载，但首屏内的图别加，加了反而拖慢 LCP。\n\n如果项目已经上线，光看本地 Lighthouse 不够。我的做法是把几个关键性能条目轻量上报到后端，至少能知道真实用户那边到底慢在哪。最简版不需要任何依赖：\n\n```js\nconst report = (name, value, extra = {}) => {\n  const body = JSON.stringify({\n    name,\n    value: Math.round(value),\n    path: location.pathname,\n    ua: navigator.userAgent,\n    ...extra,\n  })\n\n  navigator.sendBeacon('/rum', body)\n}\n\nnew PerformanceObserver((list) => {\n  const entries = list.getEntries()\n  const last = entries[entries.length - 1]\n  report('LCP', last.startTime, {\n    tag: last.element?.tagName,\n    url: last.url,\n  })\n}).observe({ type: 'largest-contentful-paint', buffered: true })\n\nnew PerformanceObserver((list) => {\n  for (const entry of list.getEntries()) {\n    if (entry.duration > 50) {\n      report('LONG_TASK', entry.duration)\n    }\n  }\n}).observe({ entryTypes: ['longtask'] })\n\nwindow.addEventListener('visibilitychange', () => {\n  if (document.visibilityState === 'hidden') {\n    const nav = performance.getEntriesByType('navigation')[0]\n    if (nav) {\n      report('TTFB', nav.responseStart - nav.requestStart)\n    }\n  }\n})\n```\n\n这段代码不追求替代完整监控系统，但它能回答几个很实际的问题：LCP 慢的是图片还是文字块，长任务集中在哪些页面，TTFB 是不是后端或 CDN 在拖后腿。`sendBeacon` 的好处是页面卸载时也能比较稳定地把数据发出去，不会像普通 `fetch` 那样容易被导航打断。\n\n真正上线时还要做采样，比如只上报 1% 或 5% 用户，避免日志量爆炸；也要把用户标识脱敏，别把业务数据塞进性能日志。性能数据一旦来自真实用户，就不能再只凭自己电脑上的体感判断。\n\n## 前端性能不只是下载速度\n\n不少人一提性能，就只盯着资源体积。\n\n资源大小当然重要，但它只是前端性能的一部分。页面真正展现给用户，还要经过解析、执行、渲染和交互——下载慢会卡、执行太重会卡、渲染不合理同样会卡，性能优化不能只盯网络层。\n\n我遇到过一个很反直觉的例子：接口返回的 JSON 不到 100KB，下载几乎瞬间完成，但页面就是要卡个一两秒才出来。后来发现是拿到数据后在前端做了一堆嵌套循环去拼层级关系，几千条数据 O(n²) 地跑，主线程直接卡死。网络这一层一点问题都没有，问题全在 JS 执行上。把那段逻辑改成用 Map 建索引、一次遍历搞定后，卡顿立刻消失。\n\n```js\n// 改之前：每条都去整个数组里 find 父节点，n×n\nlist.forEach(item => {\n  item.parent = list.find(p => p.id === item.parentId)\n})\n\n// 改之后：先建索引，再一次遍历\nconst map = new Map(list.map(item => [item.id, item]))\nlist.forEach(item => {\n  item.parent = map.get(item.parentId)\n})\n```\n\n下载快不代表页面就快，解析、执行、渲染每一步都可能是瓶颈。\n\n## “少做事”通常比“更聪明地做事”更划算\n\n性能优化里一个很朴素但很有效的原则是：能少做就少做。\n\n例如：\n\n- 不必要的请求别发\n- 不必要的渲染别做\n- 不必要的监听别挂\n- 不必要的大资源别下\n\n这类优化看起来不炫，但往往最稳定。因为它不是在原有复杂度上继续补救，而是直接减少了系统要处理的工作量。\n\n最常见的“多做事”就是高频事件没节流。`scroll`、`resize`、`input` 这些一秒能触发几十上百次，回调里要是还干重活，主线程很快就顶不住。我现在写这类交互几乎是条件反射地包一层防抖或节流：\n\n```js\n// 搜索框输入：用户停下来再发请求，防抖\ninput.addEventListener('input', debounce(e => {\n  search(e.target.value)\n}, 300))\n\n// 滚动联动：固定频率执行就够了，节流\nwindow.addEventListener('scroll', throttle(() => {\n  updateActiveAnchor()\n}, 100))\n```\n\n还有个容易被忽略的“少做事”是事件监听记得解绑。早期我在组件里随手 `addEventListener`，组件卸载又没移除，切来切去监听越挂越多，回调还引用着已经没用的闭包，既漏内存又白白消耗。后来养成了挂和卸成对写的习惯：\n\n```js\nfunction onResize() { /* ... */ }\n\nonMounted(() => window.addEventListener('resize', onResize))\nonUnmounted(() => window.removeEventListener('resize', onResize)) // 别忘了这行\n```\n\n这些都不是什么高级技巧，但它们真正减少了运行时要处理的工作量，比事后补救扎实得多。\n\n渲染层面同理，最常见的“多做事”是没必要的重排（reflow）。改了几何相关的属性——宽高、位置、字体大小，或者读取 `offsetTop`、`offsetWidth` 这类会触发同步布局的值——浏览器就得重新算一遍布局；只改颜色、背景这类不影响布局的，则只触发重绘（repaint），便宜得多。我栽过的典型坑是在循环里一边读 `offsetWidth` 一边改 style，读写交替会逼浏览器反复重算布局（layout thrashing），几百次循环能卡出明显掉帧。修法是把读和写分开批处理，或者改用 `transform`/`opacity` 做动画——这俩能走合成线程，根本不碰布局和重绘。\n\n## 首屏、交互和长列表，是三类完全不同的问题\n\n按用户感知去拆，前端性能至少能分出三类，解决方式完全不同，混在一起看只会得到一堆不痛不痒的微优化：\n\n- 首屏慢：更偏资源加载、构建产物、缓存和关键渲染路径\n- 交互慢：更偏事件处理、状态更新、同步计算和主线程占用\n- 长列表卡：更偏渲染数量、滚动处理和节点更新策略\n\n具体到长列表，我踩过的最大的坑是“一次性把上万条数据全渲染成 DOM 节点”。哪怕数据已经在内存里了，浏览器创建和布局这么多节点本身就要花掉好几秒，滚动还持续掉帧。真正有效的做法是只渲染可视区域那一屏，也就是虚拟滚动：\n\n```js\n// 核心思路：根据滚动位置算出当前该渲染哪几条，其余用占位高度撑住\nconst itemHeight = 40\nconst visibleCount = Math.ceil(containerHeight / itemHeight)\n\nfunction onScroll(scrollTop) {\n  const start = Math.floor(scrollTop / itemHeight)\n  const end = start + visibleCount + 2 // 上下各留一点缓冲，避免边缘空白\n  renderList(allData.slice(start, end), start * itemHeight)\n}\n```\n\n实际项目里我一般不自己手写，`vue-virtual-scroller`、`react-window` 这类成熟库已经把边界情况处理得很好了。但原理一定要懂，不然遇到不定高度行、横向虚拟滚动这些场景就抓瞎。判断要不要上虚拟滚动也有个朴素标准：列表稳定就几十条，分页就够了；动辄成百上千还要平滑滚动，才值得引入这套复杂度。\n\n## 把性能前移到日常和 CI\n\n性能很少靠一次专项行动救回来，更多是靠日常判断慢慢维持。所以我做了一件成本很低但收益不错的事：给构建产物体积加了个监控，每次打包后看一眼主包大小，超阈值 CI 直接报警，谁不小心整包引了个大库提交时就能发现，不用等用户抱怨慢了才回头查。\n\n```json\n// package.json，用 size-limit 卡一下首屏包体积\n{\n  \"size-limit\": [\n    { \"path\": \"dist/assets/index-*.js\", \"limit\": \"200 KB\" }\n  ]\n}\n```\n\n最后还有一点：优化动作要对着真实用户路径来。后台系统里用户最常抱怨的可能不是首页多慢，而是筛选后表格更新太卡；内容站点里最关键的也许不是动效，而是文章首屏和图片。先搞清楚用户最常停在哪个页面、影响体验的到底是进入速度还是操作反馈、慢是偶发还是稳定复现，优化才不会花了时间却没改善用户真正感知到的慢。那次“虚拟滚动改完几乎没效果”的教训说到底就是这个——不录 Performance 不下结论，先定位再找大头，已经是我现在的肌肉记忆。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":29,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":359,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":53,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":364,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":53,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":278,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue3 Composition API 组织方式：别把 setup 写成新的杂物间","date":"2022-01-14","excerpt":"从状态、计算属性、副作用和 composable 拆分出发，梳理 Vue3 Composition API 的代码组织方式。","tags":["Vue","前端基础","组件与状态"],"slug":"frontend-vue3-composition-patterns-2022","category":"2022","likeCount":5,"postId":"3df92a46-9a3e-0682-3399-c1fa7d3294c1","viewCount":3427,"readTime":null,"coverImage":null},"nextPost":{"title":"前端代码评审在看什么：不是挑格式，而是提前拦住后续成本","date":"2021-11-22","excerpt":"从协作和维护角度讨论前端代码评审的重点，说明 review 为什么不只是找语法问题。","tags":["前端基础"],"slug":"frontend-code-review-2021","category":"2021","likeCount":7,"postId":"ee5aa082-328e-2198-376e-f5dd296eba81","viewCount":3909,"readTime":"10分钟","coverImage":null}},"__N_SSG":true}