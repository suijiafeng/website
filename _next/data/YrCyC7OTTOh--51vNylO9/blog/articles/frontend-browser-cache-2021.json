{"pageProps":{"postData":{"title":"浏览器缓存为什么重要：前端性能优化里最容易被忽略的一环","date":"2021-03-04","excerpt":"从静态资源加载体验出发，梳理浏览器缓存的基本作用、常见策略和前端项目里需要理解的几个关键点。","tags":["前端基础"],"slug":"frontend-browser-cache-2021","category":"2021","likeCount":6,"postId":"9bd813f3-cbaf-2924-c6da-bbf1d8a45793","viewCount":2004,"readTime":"10分钟","coverImage":null,"content":"\n做前端性能优化时，很多人先想到的是图片压缩、代码拆包、接口并发控制。\n\n这些当然都重要，但浏览器缓存其实是一个更基础的性能杠杆。因为只要缓存策略合理，很多资源根本不需要重复下载。\n\n用户第二次打开页面快不快，很大程度上就看缓存有没有配好。\n\n我自己印象最深的一次，是接手一个老项目时翻 Network 面板，发现首页一次性发了 80 多个请求，其中一半 vendor 脚本每次刷新都重新走网络，总共一两兆。当时第一反应是想着拆包、按需加载，折腾了两天体积只压下来一点点。后来才意识到，问题根本不在体积，而在于这些根本不怎么变的库每次都在重新下载——给它们配上一年的强缓存，二次访问的资源加载时间直接从一千多毫秒掉到几十毫秒。那次之后我才真正把缓存当成性能优化的第一优先级，而不是收尾时顺手做的小事。\n\n## 缓存到底在解决什么\n\n浏览器访问一个页面时，要下载 HTML、CSS、JavaScript、图片、字体等资源。\n\n如果每次进入页面都把这些内容重新拉一遍，不仅浪费带宽，也会让首屏时间明显变长。\n\n缓存的意义很直接：\n\n- 减少重复请求\n- 降低资源下载时间\n- 提升二次访问体验\n- 减轻服务端压力\n\n尤其是静态资源体积较大时，缓存带来的收益会非常明显。\n\n## 前端最常接触的是强缓存和协商缓存\n\n虽然缓存细节涉及 HTTP 协议，但从前端视角看，最常碰到的还是两类：\n\n- 强缓存\n- 协商缓存\n\n强缓存的特点是浏览器直接使用本地副本，不再向服务端发起完整资源请求。只要缓存还没过期，就不用走网络。它由响应头里的 `Cache-Control` 和 `Expires` 控制，现在基本都用前者：\n\n```http\nCache-Control: max-age=31536000, immutable\n```\n\n`max-age` 是相对时间，单位秒，比 `Expires` 那种绝对时间戳靠谱（`Expires` 依赖客户端本地时钟，用户电脑时间不准就会失效）。`immutable` 是我后来才补上的一个属性，意思是告诉浏览器\"这文件在有效期内绝对不会变，连刷新时的条件请求都别发\"。没加它之前，用户按 F5 刷新，Chrome 还是会对强缓存资源发一轮 304 校验，加了之后这一轮请求也省掉了——对带 hash 的静态资源特别合适。\n\n协商缓存则是浏览器先问服务端：\"我本地这个资源还能不能继续用？\"如果服务端确认没变，就返回一个 `304 Not Modified`，body 是空的，告诉浏览器继续用旧副本。它靠两组头配合：\n\n- `Last-Modified` / `If-Modified-Since`：基于文件修改时间，精度只到秒\n- `ETag` / `If-None-Match`：基于内容指纹，更准\n\n我踩过一个坑：只靠 `Last-Modified` 时，有个文件被重新部署但内容没变，修改时间却更新了，结果明明可以复用却被判定成\"变了\"，全量重下。后来发现同时存在 ETag 时浏览器会优先用 ETag，这种\"内容没变但时间戳变了\"的场景就稳了。所以多数 web server（Nginx、Apache）默认两个一起发，是有道理的。\n\n开发中最重要的不是背术语，而是知道两者带来的实际差别：强缓存命中时 Network 里状态是灰色的 `200 (from disk cache / from memory cache)`，根本不发请求；协商缓存命中时是 `304`，请求还是发了，只是省掉了 body 的传输。前者更省网络，后者更适合确认资源是否更新。\n\n这些都可以自己动手在 DevTools 里看到，比记文字踏实得多。打开任意一个配了缓存的站点（比如某个 CDN 上的库），按 F12 切到 Network 面板，**不勾选** \"Disable cache\"，然后按 F5 刷新，重点看几列：\n\n- **强缓存命中**：那个静态资源的 Status 列是灰色的 `200`，Size 列不再显示字节数，而是显示 `(memory cache)` 或 `(disk cache)`——内存缓存通常是同一次会话里刚用过的资源，关掉标签页就没了；磁盘缓存能跨会话保留。点开这条请求，Headers 里看不到 `If-None-Match` 之类的条件请求头，因为它压根没发网络请求。\n- **协商缓存命中**：Status 列是 `304 Not Modified`，Size 列显示一个很小的字节数（只有响应头的大小，没有 body）。点开这条请求，在 **Request Headers** 里能看到浏览器自动带上的 `If-None-Match`（值就是上次拿到的 ETag）或 `If-Modified-Since`；**Response Headers** 里服务端回的是 `304`，确认\"没变，接着用\"。\n\n也可以绕开浏览器，直接用 `curl -I` 看响应头的真实样子（`-I` 只发 HEAD 请求、只看头）：\n\n```bash\ncurl -I https://unpkg.com/vue@3.4.0/dist/vue.global.js\n```\n\n返回里能看到类似这样的几行，强缓存和协商缓存的\"凭据\"都在这儿：\n\n```http\nHTTP/2 200\ncache-control: public, max-age=31536000, immutable\netag: \"a1b2c3d4...\"\nlast-modified: Wed, 03 Jan 2024 10:00:00 GMT\n```\n\n`cache-control` 决定强缓存能管多久，`etag` / `last-modified` 则是协商缓存比对用的指纹。想验证 304 的话，把拿到的 ETag 用条件请求再发一次：\n\n```bash\ncurl -I https://unpkg.com/vue@3.4.0/dist/vue.global.js \\\n  -H 'If-None-Match: \"a1b2c3d4...\"'\n# 内容没变时，第一行会变成：HTTP/2 304\n```\n\n## 为什么静态资源特别适合做缓存\n\n静态资源有一个天然优势：很多时候它们不会频繁变化。\n\n例如：\n\n- logo 图片\n- 打包后的 JS 文件\n- 样式文件\n- 字体文件\n\n如果这些资源每次都重新下载，用户体验会非常浪费。\n\n所以前端工程化里常见做法是给资源文件名加 hash。这样一旦内容变化，文件名也会变化；内容没变时，文件名保持不变，就可以大胆使用更长时间的缓存策略。\n\n以 webpack 为例，输出配置一般长这样：\n\n```js\n// webpack.config.js\nmodule.exports = {\n  output: {\n    filename: 'js/[name].[contenthash:8].js',\n    chunkFilename: 'js/[name].[contenthash:8].chunk.js',\n  },\n};\n```\n\n这里一定要用 `contenthash` 而不是 `hash` 或 `chunkhash`。我早期图省事用了 `[hash]`，结果它是整个 build 的 hash，只要改一个组件，所有文件名全变，用户缓存全失效，等于白做。`contenthash` 是按单个文件内容算的，改 A 不影响 B 的文件名，缓存复用率才真正立起来。\n\n还有一个容易忽略的细节：webpack 默认会把模块 id 算进 chunk 内容，导致你加一个无关的新模块时，老 chunk 的 contenthash 也跟着变。生产构建里我一般会显式固定 runtime 和模块 id 的生成方式（比如 `optimization.moduleIds: 'deterministic'`、把 runtime 单独抽成一个 chunk），让\"内容没动的文件 hash 也别动\"这件事真正可控，否则发一次版用户缓存哗啦啦失效一大片，缓存配得再长也没用。\n\n## 仅仅“开缓存”还不够，关键是更新机制要可靠\n\n很多缓存问题不是因为没缓存，而是因为缓存更新失控。\n\n最典型的是：\n\n- 资源已经发布了新版本，但用户还在用旧文件\n- HTML 更新了，引用的静态资源还是老地址\n- 强缓存太久，线上修复无法及时生效\n\n这就是为什么缓存策略必须和构建产物命名、发布流程一起考虑。\n\n如果资源文件名不变，又给了很长缓存时间，后面想让用户拿到新版本就会变得麻烦。反过来，如果资源名带内容 hash，就能兼顾\"缓存时间长\"和\"版本更新准\"这两个目标。\n\n我们线上真出过一次\"缓存太久压不下来\"的事故。某个公共配置 JS 当时没加 hash，文件名固定，又被配了 `max-age=86400`（一天）。结果上线一个紧急修复，老用户因为强缓存还没过期，整整一天都拿的是旧文件，客服那边投诉一直没断。强缓存命中期间浏览器压根不发请求，你在服务端怎么改都没用，唯一能\"召回\"用户的办法就是换 URL。从那以后我定的规矩很简单：\n\n- 带 contenthash 的文件：`Cache-Control: max-age=31536000, immutable`，放心缓一年\n- 没法加 hash、但又需要随时能改的文件：宁可 `no-cache` 走协商缓存，也不要配较长的 max-age\n\n`no-cache` 这个名字特别有迷惑性，它不是\"不缓存\"，而是\"每次都要先找服务端校验一下再用\"，真正完全不存的是 `no-store`。我面试时也常拿这个问，能分清这俩的人其实没想象中多。\n\n## 开发环境和生产环境看缓存的方式不一样\n\n开发阶段经常会看到浏览器里资源不断重新加载，于是有人会误以为缓存策略没生效。\n\n其实开发环境往往有热更新、禁用缓存或本地代理等因素，行为和线上并不一样。\n\n真正需要关心的是生产环境：\n\n- 页面 HTML 怎么缓存\n- 打包出来的 JS / CSS 怎么缓存\n- 图片和字体怎么缓存\n\n通常 HTML 不会缓存太激进，因为它要负责把最新资源入口给到浏览器；而带 hash 的静态资源可以更大胆地长期缓存。\n\n落到 Nginx 配置上，我一般是这样分开处理的：\n\n```nginx\n# 入口 HTML：每次校验，保证用户拿到最新入口\nlocation = /index.html {\n  add_header Cache-Control \"no-cache\";\n}\n\n# 带 hash 的静态资源：一年强缓存\nlocation ~* \\.(js|css|woff2|png|jpg|svg)$ {\n  add_header Cache-Control \"public, max-age=31536000, immutable\";\n}\n```\n\n逻辑闭环是这样的：HTML 走 `no-cache`，每次刷新都向服务端确认有没有变（命中就 304，几乎不耗流量）；一旦发版，HTML 里引用的脚本文件名（hash）变了，浏览器自然就去拉新文件；而那些 hash 没变的老文件，继续吃一年强缓存。这套组合拳配好之后，\"发版后用户拿不到新页面\"和\"用户反复下重复文件\"这两个问题基本就同时解决了。\n\n补充一个开发环境的真实困惑：本地用 webpack-dev-server 时所有资源都是内存里实时编译、强制不缓存的，所以你在本地永远复现不出线上的缓存问题。要验证缓存策略，最靠谱的办法是 `npm run build` 出产物，用一个真正的静态服务器（比如 `npx serve` 或本地起个 Nginx）配上对应的响应头跑一遍，再去 Network 面板看状态码。光在 dev 环境点点点是看不出门道的。\n\n## 缓存不是万能的，但它非常划算\n\n缓存不解决所有问题：首访用户照样要下载一遍，体积过大的脚本也不会因为缓存就消失。但它性价比极高——不用改业务代码，就能显著改善重复访问体验。所以前端做性能优化别只盯着打包体积和接口耗时，资源怎么被复用、有没有重复下载、版本更新稳不稳，同样关键。这几件事最后都由前端拍板：文件名是否稳定可控、页面是否引用了正确版本、发版后用户多久能拿到新资源。\n\n## 真正排查缓存问题时，要连发布链路一起看\n\n缓存之所以让人头疼，是因为它表现出来的现象往往不只发生在浏览器这一层。\n\n例如：\n\n- 资源文件名有没有随着内容变化而变化\n- HTML 引用的是不是最新打包产物\n- CDN 节点是否还在分发旧文件\n- 用户本地是否命中了老入口页\n\n只盯着浏览器有没有重新请求，并不足以解释很多线上现象。前端理解缓存的真正价值，不是会背几个响应头，而是能在资源复用、版本更新和发布稳定性之间做出更清楚的判断。\n\n我自己排查这类问题有个固定顺序，基本能少走很多弯路：\n\n1. 先在浏览器无痕窗口里复现，排除是不是本地缓存太顽固——无痕也复现，说明问题在服务端或 CDN，不在用户本地。\n2. 看 HTML 的响应头到底命中没命中缓存。最常见的元凶就是 HTML 被某一层（CDN、Nginx）顺手配了强缓存，导致用户拿到的还是指向旧脚本的老入口。\n3. 对比 HTML 里引用的脚本文件名和最新构建产物的文件名是否一致，确认入口指对了。\n4. 直接拿脚本的完整 URL 去请求 CDN 各个节点，看是不是个别节点没刷新、还在分发旧文件——CDN 这层经常需要手动触发刷新（purge），构建发布脚本里最好把它纳进去。\n\n说到底，缓存不是一个孤立的浏览器话题，它串起了构建产物命名、服务端响应头、CDN 分发和发布流程一整条链路。前端能把这条链路在脑子里走通，比记住十个响应头的语义都管用。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":29,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":359,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":53,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":364,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":53,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":278,"readTime":null,"coverImage":null}],"prevPost":{"title":"表单校验不是正则堆砌：前端表单体验真正容易出问题的地方","date":"2021-03-17","excerpt":"从输入体验、错误提示和提交时机出发，讨论前端表单校验为什么不只是写几个正则表达式。","tags":["前端基础"],"slug":"frontend-form-validation-2021","category":"2021","likeCount":5,"postId":"8e1ff5cc-bb05-dd24-dbfa-b9c2699e51c1","viewCount":2384,"readTime":"10分钟","coverImage":null},"nextPost":{"title":"CSS 定位为什么总让人绕晕：relative、absolute、fixed 到底怎么配合","date":"2021-02-03","excerpt":"定位问题一乱，页面就容易靠试错修；我把包含块、文档流和 sticky 失效条件放在一起记。","tags":["前端基础"],"slug":"frontend-css-position-2021","category":"2021","likeCount":5,"postId":"2774d790-6069-63bc-d284-6ba763e2da45","viewCount":1756,"readTime":"10分钟","coverImage":null}},"__N_SSG":true}