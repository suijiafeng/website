{"pageProps":{"postData":{"title":"CSS transition 入门：实现简单的滑入滑出效果","date":"2018-12-20","excerpt":"从滑入滑出和展开收起两个小需求出发，记录 transition 真正卡人的点：状态起点、重排、性能和可访问性。","tags":["前端基础","CSS"],"slug":"frontend-css-transition-slide-2018","category":"2018","likeCount":5,"postId":"2ff304f8-e478-f10a-7dba-6fbb62801faa","viewCount":1205,"readTime":"8分钟","coverImage":null,"content":"\nCSS `transition` 是最常用的动画能力之一。\n\n它适合处理简单状态变化，比如 hover、展开收起、透明度变化、滑入滑出。\n\n理解它的关键是：先有状态变化，再有过渡动画。\n\n也就是说，`transition` 本身不会创造状态。它只负责把一个已经发生的样式变化，变得更平滑。\n\n这句话我刚学的时候没当回事，结果吃了不少苦头。最常见的就是：元素一插入页面就想让它淡入，直接给初始 opacity 写 0、加个 transition，再用 JS 把它改成 1，结果发现根本不过渡，啪一下就出来了。原因是浏览器还没来得及把“opacity 为 0”渲染成一帧，就被同一帧里改成 1，它看到的只有最终值，没有起点可言。后来才懂，要么先把元素挂上去、强制读一次 `offsetHeight` 触发一次布局（俗称强制重排），再改 class；要么用 `requestAnimationFrame` 把改 class 推到下一帧。状态的“前一帧”必须真实存在过，transition 才有的可过渡。\n\n## transition 做了什么\n\n如果一个元素的样式从 A 变成 B，默认会瞬间完成。\n\n加上 `transition` 后，浏览器会在 A 和 B 之间生成平滑过渡。\n\n```css\n.box {\n  width: 100px;\n  height: 100px;\n  background: #1677ff;\n  transition: transform 0.3s ease;\n}\n\n.box.active {\n  transform: translateX(100px);\n}\n```\n\n当 `.box` 增加 `active` 类时，就会向右平滑移动。\n\n这里的两个状态很明确：\n\n- 默认状态：`transform: none`\n- 激活状态：`transform: translateX(100px)`\n\n如果没有这两个稳定状态，动画就会变成“调参数”。写过渡前先把状态想清楚，后面会省很多事。\n\n我的习惯是先把默认态和激活态的样式都写死、确认两个静止画面都对，再加 `transition`。很多时候动画“怪”不是 transition 写错了，而是两个端点本身就没对——比如激活态忘了归零 transform，或者默认态压根没设初始值。把动画拆成“两张静止图 + 一段过渡”来想，调试会清醒很多。\n\n## transition 的几个组成部分\n\n`transition` 是简写属性，常见格式如下：\n\n```css\n.box {\n  transition: transform 0.3s ease 0s;\n}\n```\n\n它包含：\n\n- `transition-property`：要过渡的属性\n- `transition-duration`：动画持续时间\n- `transition-timing-function`：速度曲线\n- `transition-delay`：延迟时间\n\n多数情况下写前三个就够了。\n\n如果多个属性需要不同时间，可以分开写：\n\n```css\n.card {\n  opacity: 0;\n  transform: translateY(8px);\n  transition:\n    opacity 0.2s ease,\n    transform 0.3s ease;\n}\n\n.card.show {\n  opacity: 1;\n  transform: translateY(0);\n}\n```\n\n关于速度曲线多说一句。`ease` 是默认值，开头快、结尾慢，大部分场景够用。但侧边栏滑入这种，我更喜欢 `ease-out`——进场快、收尾缓，感觉更跟手。需要更精细的时候直接上 `cubic-bezier()`，比如 `cubic-bezier(0.4, 0, 0.2, 1)` 这条曲线我用了很多年，进出都比较自然。`linear` 一般只留给加载条这种匀速的东西，用在位移上会显得很机械、很廉价。\n\n不要直接写 `transition: all 0.3s ease;`。它看起来省事，但后续任何属性变化都可能被意外动画化，排查起来很麻烦。我真遇到过：某个卡片写了 `transition: all`，结果某天加了个 `:hover` 改 `background-color`，连带着旁边 JS 动态改的 `width` 也跟着慢悠悠地动，页面看着像卡了。还有更隐蔽的，hover 时连 `box-shadow`、`color` 全在过渡，性能也白白浪费。明确写出要过渡哪几个属性，是对未来的自己负责。\n\n## 实现滑入滑出\n\n可以用 `transform` 做侧边栏滑入：\n\n```css\n.panel {\n  position: fixed;\n  top: 0;\n  right: 0;\n  width: 280px;\n  height: 100vh;\n  background: #fff;\n  transform: translateX(100%);\n  transition: transform 0.3s ease;\n}\n\n.panel.open {\n  transform: translateX(0);\n}\n```\n\n用 JavaScript 切换类名：\n\n```javascript\nconst panel = document.querySelector(\".panel\");\nconst overlay = document.querySelector(\".overlay\");\n\nfunction openPanel() {\n  panel.classList.add(\"open\");\n  overlay.classList.add(\"show\");\n  panel.setAttribute(\"aria-hidden\", \"false\");\n}\n\nfunction closePanel() {\n  panel.classList.remove(\"open\");\n  overlay.classList.remove(\"show\");\n  panel.setAttribute(\"aria-hidden\", \"true\");\n}\n```\n\n这样面板就能从右侧滑入和滑出。\n\n完整结构可以这样组织：\n\n```html\n<button type=\"button\" class=\"open-button\">打开菜单</button>\n<div class=\"overlay\"></div>\n<aside class=\"panel\" aria-hidden=\"true\">\n  <button type=\"button\" class=\"close-button\">关闭</button>\n  <nav>菜单内容</nav>\n</aside>\n```\n\n遮罩也可以用透明度过渡：\n\n```css\n.overlay {\n  position: fixed;\n  inset: 0;\n  pointer-events: none;\n  opacity: 0;\n  background: rgba(15, 23, 42, 0.45);\n  transition: opacity 0.2s ease;\n}\n\n.overlay.show {\n  pointer-events: auto;\n  opacity: 1;\n}\n```\n\n注意 `pointer-events`。否则遮罩虽然透明了，仍然可能挡住页面点击。我们组就出过这个线上 bug：弹窗关掉之后，遮罩 opacity 是 0 了看不见，但 DOM 还在、还是铺满全屏的，用户点页面任何地方都没反应，以为页面死了。加上 `pointer-events: none` 让透明遮罩不拦截事件，问题就没了。\n\n不过这里又藏着一个细节：`pointer-events` 不参与过渡（它没有中间态），所以遮罩淡出时如果立刻把 `pointer-events` 设成 none，体验上没问题；但反过来淡入时，最好让它在动画一开始就能点。我一般把 `pointer-events` 和 `.show` 类绑在一起切，不去单独动它。\n\n关闭动画还有个常被忽略的点：很多人关闭时直接把面板 `display: none`，结果滑出动画根本没机会播，啪一下就没了。如果要让滑出动画也跑完，得在 `transitionend` 事件里再做收尾，而不是同步隐藏：\n\n```javascript\nfunction closePanel() {\n  panel.classList.remove(\"open\");\n  overlay.classList.remove(\"show\");\n  panel.setAttribute(\"aria-hidden\", \"true\");\n  // 想等动画播完再做别的，可以监听 transitionend\n  panel.addEventListener(\"transitionend\", function handler(e) {\n    if (e.propertyName !== \"transform\") return;\n    panel.removeEventListener(\"transitionend\", handler);\n    // 这里再做真正的清理，比如卸载内容\n  });\n}\n```\n\n注意 `transitionend` 会为每个过渡属性各触发一次，所以要用 `e.propertyName` 过滤，否则同一个回调会跑好几遍。\n\n## 优先使用 transform 和 opacity\n\n并不是所有 CSS 属性都适合做动画。\n\n通常更推荐过渡：\n\n- `transform`\n- `opacity`\n\n它们更容易被浏览器优化。\n\n像 `width`、`height`、`top`、`left` 这类属性可能触发布局计算，复杂页面里要谨慎使用。\n\n例如滑入面板不要用 `right` 从 `-280px` 变成 `0`，更推荐用 `transform`：\n\n```css\n.bad-panel {\n  right: -280px;\n  transition: right 0.3s ease;\n}\n\n.good-panel {\n  transform: translateX(100%);\n  transition: transform 0.3s ease;\n}\n```\n\n前者更容易影响布局计算，后者更容易走合成层，动画也更稳。\n\n为什么差这么多？因为 `transform` 和 `opacity` 这两个属性，浏览器可以完全交给 GPU 在合成阶段处理，不需要重新算布局（layout）、也不用重绘（paint），每一帧都很便宜。而 `left`、`width`、`top` 这些会触发 layout，整棵子树都得重新计算位置，列表一长动画就掉帧。我在低端安卓机上测过同一个抽屉效果，用 `right` 过渡明显一卡一卡，换成 `transform` 立马顺滑，差距非常直观。\n\n如果想给浏览器一点提示，可以加 `will-change: transform`，让它提前把元素提升到独立合成层。但这东西别乱撒——每个 `will-change` 都占显存，整页到处写反而拖慢。我一般只在确实要动、而且动的瞬间才加，动完了去掉。\n\n```css\n.panel {\n  transform: translateX(100%);\n  transition: transform 0.3s ease;\n  will-change: transform;\n}\n```\n\n## 展开收起的常见坑\n\n很多人会尝试给 `height: auto` 加过渡：\n\n```css\n.content {\n  height: 0;\n  transition: height 0.3s ease;\n}\n\n.content.open {\n  height: auto;\n}\n```\n\n这通常不会得到预期效果，因为浏览器无法在具体数值和 `auto` 之间直接插值。\n\n如果内容高度可控，可以用 `max-height`：\n\n```css\n.content {\n  max-height: 0;\n  overflow: hidden;\n  transition: max-height 0.3s ease;\n}\n\n.content.open {\n  max-height: 320px;\n}\n```\n\n用 `max-height` 这招也有副作用要知道：动画时长是按 `max-height` 的变化量算的，如果你给了一个远大于真实高度的值（比如内容只有 100px 你写了 max-height 1000px），那么收起时前面 900px 是“空跑”，动画看起来会先停顿一下再动，体感很怪。所以 `max-height` 尽量贴着内容的最大可能高度给。\n\n如果内容高度不固定，可以用 JavaScript 读取真实高度，再设置到内联样式。这个时候要注意读写分离，避免频繁触发布局：\n\n```javascript\nfunction expand(el) {\n  el.style.height = \"auto\";          // 先撑开\n  const target = el.scrollHeight;    // 读真实高度\n  el.style.height = \"0px\";           // 再压回 0\n  el.offsetHeight;                   // 强制重排，制造起点\n  el.style.height = target + \"px\";   // 触发过渡\n}\n\nel.addEventListener(\"transitionend\", () => {\n  if (el.classList.contains(\"open\")) {\n    el.style.height = \"auto\";        // 展开完成后还原 auto，适应内容变化\n  }\n});\n```\n\n这段代码看着绕，核心就是“读到真实高度 → 制造一个 0 的起点 → 过渡到具体数值 → 结束后还原 auto”。中间那行 `el.offsetHeight` 是故意写的，逼浏览器把 0 这一帧落实，否则又会回到开头说的“没起点不过渡”问题。\n\n## 尊重用户的动画偏好\n\n有些用户会在系统里开启“减少动态效果”。页面动画应该尊重这个设置：\n\n```css\n@media (prefers-reduced-motion: reduce) {\n  .panel,\n  .overlay,\n  .content {\n    transition: none;\n  }\n}\n```\n\n动画是体验增强，不应该成为用户完成操作的障碍。前庭功能敏感的用户看到大幅度的滑动、缩放会眩晕甚至不适，系统里那个“减少动态效果”就是为他们准备的。我现在写组件库会默认带上这段媒体查询，几乎不花什么成本，却是基本的体面。\n\n要注意的是别简单粗暴地把 `transition: none` 写完就完事——如果你的关闭逻辑依赖 `transitionend` 来做收尾，禁用过渡后这个事件可能压根不触发，导致面板关不掉。这种情况下，要么把动画时长改成接近 0（比如 `0.01ms`）保证事件还会发，要么在 JS 里判断 `prefers-reduced-motion` 走另一条同步关闭的分支。我吃过这个亏，禁用动画后弹窗反而卡死打不开，排查了好一会儿才定位到是 `transitionend` 没来。\n\n过渡动画适合把明确的状态变化变得更顺滑。实现时先想清楚两端状态，优先用 `transform`/`opacity`，并处理遮罩、焦点和“减少动画”的无障碍设置。写动画不是为了炫技，而是为了提升体验——把这些细节照顾到，动画才既好看又靠谱。\n"},"recommendedPosts":[{"title":"跨文档 View Transitions：多页应用也能做出丝滑过渡了","date":"2026-04-04","excerpt":"同文档 View Transitions 之外，跨文档（MPA）过渡终于可用。记录我给一个传统多页站点加上页面切换动画的过程、view-transition-name 的配对和降级处理。","tags":["前端基础","CSS","交互与动效"],"slug":"view-transitions-cross-document-2026","category":"2026","likeCount":92,"postId":"54351a26-388d-b51e-ff65-3ae7fde9bbd4","viewCount":968,"readTime":null,"coverImage":null},{"title":"原生 CSS 嵌套和 @scope：写了这么多年 Sass 之后的几点真实感受","date":"2026-03-22","excerpt":"原生嵌套和 @scope 落地后，我在项目里逐步去掉一部分 Sass 的过程：嵌套的坑、@scope 的下边界 donut scope、以及现在还不能完全替代预处理器的地方。","tags":["前端基础","CSS"],"slug":"css-scope-nesting-2026","category":"2026","likeCount":36,"postId":"a4a7366f-5967-f614-350b-d223d0a045f3","viewCount":800,"readTime":null,"coverImage":null},{"title":"Tailwind CSS v4 迁移记：新引擎、CSS 变量主题和那些不再需要的配置","date":"2026-01-24","excerpt":"从 v3 升到 v4 的真实迁移过程：Oxide 引擎带来的速度变化、配置从 JS 搬到 CSS、@theme 和 CSS 变量怎么用，以及几个升级时踩的坑。","tags":["前端基础","CSS","构建与模块化"],"slug":"tailwind-v4-migration-2026","category":"2026","likeCount":34,"postId":"c8484300-10ec-bf7b-ef1b-6a237cb4f4f2","viewCount":933,"readTime":null,"coverImage":null}],"prevPost":{"title":"进程和线程：写前端也绕不开的操作系统基础","date":"2019-01-09","excerpt":"从浏览器、Node 服务和面试常题出发，整理进程、线程、协程、上下文切换、并发和并行这些基础概念。","tags":["其他"],"slug":"os-process-thread-basics-2019","category":"2019","likeCount":7,"postId":"e38de104-d0ac-0d30-b15f-ccabae6dbcdb","viewCount":2568,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue 里 computed 和 watch 到底怎么选：别把两种能力写成一回事","date":"2018-11-16","excerpt":"回到 Vue 日常开发最容易混淆的一组能力，通过派生状态、异步副作用和错误处理示例讲清 computed 和 watch 的职责边界。","tags":["Vue","前端基础"],"slug":"vue-computed-watch-2018","category":"2018","likeCount":6,"postId":"afe7173c-7cea-829f-39ab-d16d3ae725af","viewCount":1249,"readTime":null,"coverImage":null}},"__N_SSG":true}