{"pageProps":{"postData":{"title":"Vue 动态组件与 keep-alive：多 tab 后台怎么切回来还是原样","date":"2018-10-28","excerpt":"多 tab 后台真正难的不是把页签画出来，而是“切回来还是原样”。这里把 `component :is`、`keep-alive`、缓存生命周期、关 tab 泄漏和数据串号这些坑放回同一个需求里看。","tags":["Vue","前端基础"],"slug":"vue-dynamic-components-tabs-2018","category":"2018","likeCount":5,"postId":"b06627c9-7590-7fea-b88b-35f40b59f88d","viewCount":2611,"readTime":"11分钟","coverImage":null,"content":"\n多 tab 后台最麻烦的从来不是把页签那排 UI 画出来，而是让用户切出去再切回来时，列表筛选、页码、滚动位置和编辑态都还在原地。只要组件一销毁一重建，这些状态就会一起清零。\n\n这类需求表面像交互升级，本质上却会直接撞上组件缓存策略。你到底是在切视图，还是在切实例；切出去的页面是暂存，还是彻底销毁；再切回来时走的是激活还是重建，这些判断全会影响最终体验。\n\n根子在于：每次切 tab，Vue 都把上一个路由组件销毁了，再切回来又重新 `create` 一遍，所有组件内 state 自然清零。要解决它，绕不开两个东西——动态组件和 `keep-alive`。技术栈是 Vue 2.5 加 vue-router 3.0，今年后台项目的标配。\n\n## 第一天：先拿 component :is 试了个错\n\n我最初的思路很直：tab 不就是「同一个壳子里换内容」吗，Vue 文档里现成的动态组件写法：\n\n```html\n<component :is=\"currentComponent\" />\n```\n\n`currentComponent` 是个字符串或组件对象，它一变，Vue 就卸掉旧组件、挂上新组件：\n\n```js\nexport default {\n  components: { UserList, UserDetail, OrderForm },\n  data() {\n    return {\n      currentComponent: 'UserList'\n    }\n  },\n  methods: {\n    switchTo(name) {\n      this.currentComponent = name\n    }\n  }\n}\n```\n\n原型搭出来当天就被自己推翻了。这种写法最大的特点是不走路由：URL 不变，运营想把「某个客户的订单页」链接甩给同事打不开，浏览器前进后退失灵，刷新一下状态全没。后台 tab 大多数情况要的是另一种——路由级别的页面缓存，地址栏要变、要能分享链接、要能前进后退。\n\n但这一天不算白费，我把动态组件的适用场景想清楚了：一个配置向导分好几步、每步一个组件；或者弹窗里的分步表单，纯前端交互、跟路由无关。这类场景用 `<component :is>` 反而比硬塞进路由清爽。**动态组件和路由缓存是两条腿，搞混了后面会很乱。**这个需求走后一条。\n\n## 第一周：keep-alive 让组件「离开但不死」\n\n路由级缓存的钥匙是 `keep-alive`。把 `router-view` 包进去，路由组件切走时就不销毁，而是被「冻」在内存里：\n\n```html\n<keep-alive>\n  <router-view />\n</keep-alive>\n```\n\n包上之后组件的生命周期会变。原来切走是 `beforeDestroy / destroyed`，现在变成 `deactivated`；切回来不再是 `created / mounted`，而是 `activated`：\n\n```js\nexport default {\n  name: 'UserList',\n  created() {\n    // 只在第一次进入时执行一次\n    console.log('UserList created')\n  },\n  activated() {\n    console.log('页面被重新激活')\n  },\n  deactivated() {\n    console.log('页面离开但未销毁，state 都还在')\n  }\n}\n```\n\n第一版包完自测，筛选条件保住了，我美滋滋提测。第二天产品试用，当场提回来一个问题：从详情页改完客户资料返回列表，列表显示的还是旧数据。原因是我习惯性把列表请求写在 `created` 里，而 `keep-alive` 之下 `created` 只跑一次，state 是「保住了」，但保得太彻底，连该刷新的数据一起冻住了。\n\n琢磨了一晚，我把「请求时机」分成两类，后来成了这个项目的固定套路：要保留状态、不需要刷新的，请求留在 `created`，靠 `keep-alive` 把 state 冻住，回来啥都不动；要保留筛选条件、但数据可能变了的，请求挪到 `activated`，但筛选条件、页码这些查询参数本身存在组件 data 里不动，`activated` 时拿着这些参数重新拉数据。\n\n```js\nexport default {\n  name: 'UserList',\n  data() {\n    return {\n      query: { keyword: '', status: '', page: 1 }, // 这些被 keep-alive 保住\n      list: []\n    }\n  },\n  activated() {\n    // 用保留下来的 query 重新拉一次，数据是新的、条件是旧的\n    this.fetchList()\n  },\n  methods: {\n    async fetchList() {\n      const { data } = await this.$http.get('/users', { params: this.query })\n      this.list = data.items\n    }\n  }\n}\n```\n\n这样运营回到列表，筛选条件原封不动，但能看到刚改的最新数据，两头都满足。\n\n滚动位置是下一个投诉点。`keep-alive` 不会自动帮你恢复滚动条——DOM 虽然没销毁，但滚动是挂在外层容器上的。我先去翻了 vue-router 的 `scrollBehavior`，想着框架总该管这事，结果发现它主要针对整页滚动、且依赖浏览器的前进后退导航，我们这种「布局固定、中间内容区自己滚」的后台结构帮不上忙。最后还是自己动手，`deactivated` 时记下 `scrollTop`，`activated` 时还原：\n\n```js\ndeactivated() {\n  this.savedScrollTop = this.$refs.scrollWrap.scrollTop\n},\nactivated() {\n  // 等 DOM 渲染完再设置，否则可能被覆盖\n  this.$nextTick(() => {\n    this.$refs.scrollWrap.scrollTop = this.savedScrollTop || 0\n  })\n}\n```\n\n缓存下来之后还撞过一个和第三方图表库相关的坑：看板页面里嵌了个 ECharts，`created` 里 `echarts.init` 一次没问题，但从别的 tab 切回来时，容器的宽高在缓存期间可能被浏览器窗口变化影响过，图表却不知道，画出来的坐标轴挤成一团。后来在 `activated` 里补一句 `chart.resize()` 就解决了——**任何在隐藏状态下可能和 DOM 尺寸打交道的第三方库，只要挂在 `keep-alive` 下面，都得在 `activated` 里手动踩一脚它的重新计算逻辑，它自己是不会知道自己被\"冻\"过的。**\n\n## 第二周：别什么页面都缓存\n\n功能齐了，新问题从测试那边来：后台挂一天，页面越开越多，浏览器内存只增不减，老机器上卡到飞起。`keep-alive` 不加约束的话，所有路由组件都会被缓存，开一个存一个，永不释放。而且有些页面本来就不该缓存——比如「新建订单」，每次进来都该是干净的空表单，你缓存它干嘛。\n\n用 `include` 白名单控制：\n\n```html\n<keep-alive :include=\"cachedViews\">\n  <router-view />\n</keep-alive>\n```\n\n```js\ndata() {\n  return {\n    cachedViews: ['UserList', 'OrderList', 'FollowupRecord']\n  }\n}\n```\n\n这里我结结实实撞了一个坑：配好了 `include`，列表页死活不缓存，切走回来照样重置。翻源码加打印查了半天，原因是——`include` 匹配的是组件的 `name` 选项，不是路由的 name，也不是文件名，而我的列表组件压根没写 `name`。\n\n```js\nexport default {\n  name: 'UserList', // 没有这行，include 永远匹配不上它\n  // ...\n}\n```\n\n从那以后我们组的路由组件强制写 `name`，还进了 code review 清单。`include` 可以是数组、逗号分隔的字符串、也可以是正则；后台动态 tab 一般用数组，因为要动态增删。另外提一句，给 `include` 传组件实例匹配要等 Vue 2.6，现在大部分项目还在 2.5，老老实实用 name 字符串就好。\n\n顺便说 `keep-alive` 还有个 `max` 属性（2.5.0 加的），超过上限就按 LRU 把最久没访问的实例挤出去：\n\n```html\n<keep-alive :include=\"cachedViews\" :max=\"10\">\n  <router-view />\n</keep-alive>\n```\n\n它适合当兜底保险——就算白名单逻辑哪天漏了，内存也不至于无限涨。但别拿它当主策略：哪个页面被挤掉是按访问顺序算的，用户感知不到规律，「我明明开着的 tab 怎么又重置了」这种反馈更难查。我们的做法是 `include` 精确控制为主，`max` 设个宽松的上限保底。\n\n## 把缓存和 tab 状态打通\n\n真正的后台 tab 系统，缓存列表不能写死，得跟着用户开关 tab 动态变。我用 Vuex 管这份 state：\n\n```js\n// store/modules/tabsView.js\nconst state = {\n  visitedTabs: [],   // 顶部显示的 tab\n  cachedViews: []    // 给 keep-alive 用的 include\n}\n\nconst mutations = {\n  ADD_TAB(state, view) {\n    if (state.visitedTabs.some(v => v.path === view.path)) return\n    state.visitedTabs.push({ name: view.name, title: view.meta.title, path: view.path })\n    // meta.keepAlive 为 true 的才进缓存\n    if (view.meta.keepAlive && state.cachedViews.indexOf(view.name) === -1) {\n      state.cachedViews.push(view.name)\n    }\n  },\n  REMOVE_TAB(state, view) {\n    state.visitedTabs = state.visitedTabs.filter(v => v.path !== view.path)\n    const i = state.cachedViews.indexOf(view.name)\n    if (i > -1) state.cachedViews.splice(i, 1)\n  }\n}\n```\n\n路由里用 `meta` 标记哪些页面需要缓存，缓存策略跟着路由配置走，不散落在各处：\n\n```js\n{\n  path: '/user/list',\n  name: 'UserList',\n  component: () => import('@/views/user/list'),\n  meta: { title: '客户列表', keepAlive: true }\n}\n```\n\n布局组件里监听路由变化往里塞 tab：\n\n```js\nwatch: {\n  $route(route) {\n    this.$store.commit('tabsView/ADD_TAB', route)\n  }\n},\ncomputed: {\n  cachedViews() {\n    return this.$store.state.tabsView.cachedViews\n  }\n}\n```\n\n美化 tab 切换时我还想给页面加个淡入过渡，又撞了个小坑：`<transition>` 和 `<keep-alive>` 的嵌套顺序是有讲究的，transition 要包在外层、keep-alive 在里层，反过来包动画会丢：\n\n```html\n<transition name=\"fade\" mode=\"out-in\">\n  <keep-alive :include=\"cachedViews\">\n    <router-view />\n  </keep-alive>\n</transition>\n```\n\n## 最隐蔽的 bug：关 tab 不清缓存\n\n上线前最后一轮自测，测试同学报了个灵异现象：把「客户列表」tab 关掉，重新从菜单点进去，看到的居然是关闭之前的筛选状态——她明明关了，页面却像没关过一样。\n\n这是整个需求里最隐蔽的一个 bug。用户点 tab 上的小叉关页面，我只做了从 `visitedTabs` 移除、跳到相邻 tab，UI 上看页面是「关了」，但 `cachedViews` 里没删，组件实例还冻在内存里。后果有两个：一是内存一直涨，等于隐形内存泄漏；二更坑——用户重新打开同一个页面，`keep-alive` 发现缓存里有，直接复活旧实例，于是看到的是上次关闭前的脏状态。用户一脸懵：我不是关了吗，咋还是老样子。\n\n所以关 tab 必须两边一起清：\n\n```js\ncloseTab(view) {\n  this.$store.commit('tabsView/REMOVE_TAB', view)\n  // 如果关的是当前页，跳到最后一个还开着的 tab\n  if (this.$route.path === view.path) {\n    const tabs = this.$store.state.tabsView.visitedTabs\n    const last = tabs[tabs.length - 1]\n    this.$router.push(last ? last.path : '/')\n  }\n}\n```\n\n**「UI 上消失」和「缓存里清掉」是两件事，keep-alive 用得越深，越要把这两件事绑在一起做。**\n\n## 「刷新当前页」：keep-alive 反而碍事了\n\n产品验收时又加了个高频小需求：tab 右键菜单里加个「刷新」，把当前页彻底重来一遍。听着简单，难点恰恰在于 `keep-alive` 把组件冻住了——你 `this.$router.push` 到自己的路由，组件根本不会重新创建。\n\n我试过 `this.$forceUpdate()`，只是强制重渲染、不重置 data，没用；试过给 `router-view` 临时换 `key`，能刷但状态管理很别扭。最后稳妥的是「先把它踢出缓存，跳个空中转路由，再跳回来」：\n\n```js\n// 中转路由 /redirect/:path\n{\n  path: '/redirect/:path(.*)',\n  component: {\n    beforeRouteEnter(to, from, next) {\n      next(vm => vm.$router.replace('/' + to.params.path))\n    },\n    render: h => h() // 不渲染任何东西\n  }\n}\n```\n\n```js\nrefreshCurrentTab() {\n  const { name, fullPath } = this.$route\n  // 从缓存移除，强制下次重新创建\n  this.$store.commit('tabsView/REMOVE_CACHE', name)\n  this.$nextTick(() => {\n    this.$router.replace('/redirect' + fullPath)\n    // 重新加回缓存\n    this.$store.commit('tabsView/ADD_CACHE', name)\n  })\n}\n```\n\n中转路由那一下让组件经历了一次真正的销毁和重建。上周我特意翻了 vue-element-admin 那套模板对答案，它也是这么干的，心里踏实了。\n\n这个中转路由方案还有个小细节容易漏掉：`REMOVE_CACHE` 和 `ADD_CACHE` 之间隔了一个 `$nextTick`，是故意的。如果直接同步把组件名从 `cachedViews` 里删掉又立刻加回去，`keep-alive` 内部的 patch 逻辑还没来得及响应这次变化，组件可能压根没有被真正销毁，刷新等于白刷。等一个 tick，确保上一轮渲染确实把旧实例摘掉了，再把名字加回白名单，新实例才会干干净净地重新走一遍 `created`。\n\n## 上线前最后一惊：编辑页数据串号\n\n以为可以收工的时候，QA 报了个吓出一身冷汗的 bug。场景是：打开「编辑客户 A」，填了一半切走，又打开「编辑客户 B」——A 没填完的数据出现在了 B 的表单上。轻则数据错乱，重则把 A 的内容存到 B 头上，真上线就是事故。\n\n根因是 `keep-alive` 缓存的粒度是「组件」，不是「组件 + 参数」。「编辑客户 A」和「编辑客户 B」走的是同一个路由组件（`/user/edit/:id`），缓存了它，Vue 就复用同一个组件实例，不同 `:id` 共享一份 data。两条出路：\n\n一是干脆别缓存编辑页，进来就请求，离开就销毁，最省心：\n\n```js\n{\n  path: '/user/edit/:id',\n  name: 'UserEdit',\n  component: () => import('@/views/user/edit'),\n  meta: { title: '编辑客户', keepAlive: false }\n}\n```\n\n二是如果非要缓存，就在 `beforeRouteUpdate` 里按参数重新加载——因为同路由切 `:id` 不会触发 `created`，只会触发 `beforeRouteUpdate`：\n\n```js\nbeforeRouteUpdate(to, from, next) {\n  this.fetchDetail(to.params.id)\n  next()\n}\n```\n\n还有个更暴力但有效的隔离手段：给 `router-view` 套 `key`，让不同参数被当成不同组件，逼着它重建：\n\n```html\n<keep-alive :include=\"cachedViews\">\n  <router-view :key=\"$route.fullPath\" />\n</keep-alive>\n```\n\n不过加了 `fullPath` 作 key，缓存基本就失效了（每个 path 都是新实例），这招和缓存是互斥的，按需取舍。我们最终的选择是：列表、查询页缓存、用 name 匹配；编辑、详情页不缓存、靠 `beforeRouteUpdate` 处理参数变化。QA 回归通过，上线。\n\n## 三周下来，一张表收尾\n\n| 需求 | 方案 | 注意点 |\n| --- | --- | --- |\n| 同壳换内容、不走路由 | `<component :is>` | 刷新即丢状态 |\n| tab 切走保留状态 | `keep-alive` 包 `router-view` | 组件必须有 name |\n| 控制谁缓存 | `include` + `meta.keepAlive` | 匹配 name 非路由名 |\n| 兜底内存上限 | `:max` 属性 | LRU 淘汰无规律，只当保险 |\n| 关 tab | 同步删 `cachedViews` | 不删会内存泄漏+脏状态复活 |\n| 刷新当前页 | 踢缓存 + redirect 中转路由 | 比 forceUpdate 可靠 |\n| 编辑页防数据串 | 不缓存 / `beforeRouteUpdate` | 缓存粒度是组件非参数 |\n| 切换加过渡 | transition 外、keep-alive 里 | 顺序反了动画丢失 |\n\n上线一周了，运营再没因为筛选条件丢失骂过街，产品在周会上还专门表扬了「刷新」那个小按钮。`keep-alive` 确实能把后台 tab 体验救回来，但它不是开了就万事大吉的开关，真正花功夫的是缓存范围的控制、关闭时的清理、刷新的实现，以及哪些页面不该碰它。\n\n判断标准其实很简单：这个页面用户离开后再回来，希望看到原样还是看到干净的？想看原样的（列表、查询、看板）就缓存；想要干净的（新建、编辑、带参数详情）就别缓存。**缓存从来不是越多越好，它是用内存和复杂度换体验，得算账。**\n\n这几周攒下来的判断标准，我后来也用同样的思路套在了别的中后台项目上，屡试不爽。\n"},"recommendedPosts":[{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2023-09-17","excerpt":"无限级菜单看着是个递归组件，实际牵着路由高亮、权限过滤、图标映射和接口数据清洗。这篇把该在数据层收干净的边界，和只留给递归模板的部分拆清楚。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2023","category":"2023","likeCount":5,"postId":"73d4dc9e-8fc7-3ce9-8dcb-31a0122f0b98","viewCount":3793,"readTime":null,"coverImage":null},{"title":"刚开始用 Vue3 时，最容易踩的几个坑","date":"2023-06-07","excerpt":"响应式规则、props 单向流、watch 边界这些 Vue3 坑大多不是语法问题，而是组件逻辑边界没理清。这篇把自己从 Vue2 迁过来踩过的几个坑连同验证方法一起记下来。","tags":["Vue","前端基础"],"slug":"vue3-common-mistakes-2023","category":"2023","likeCount":6,"postId":"06bb42c9-56d9-0865-c906-88f606e068a9","viewCount":1097,"readTime":null,"coverImage":null},{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"请求库的边界正从单纯的 HTTP 客户端往请求状态管理扩展：loading/data/error 三件套重复、竞态覆盖、缓存 key 设计，这些问题不是换个库名就能解决的，引入新方案的工程代价也要算清。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2388,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 里 computed 和 watch 怎么选：一次 code review 现场记","date":"2018-11-16","excerpt":"review 新同事的表单页代码，一个组件里堆了九个 watch，互相触发、彼此依赖。我们坐下来逐条过了一下午，把 computed 和 watch 的职责边界从案例里一点点抠出来。","tags":["Vue","前端基础"],"slug":"vue-computed-watch-2018","category":"2018","likeCount":5,"postId":"2a417d20-70e0-5149-0664-ebdc34b2c6fa","viewCount":4261,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue history 模式部署：本地能打开，不代表线上刷新不 404","date":"2018-10-16","excerpt":"记录一次 Vue Router history 模式刷新 404 的部署排查：Nginx 回退、二级目录、静态资源路径和缓存怎么配才不互相打架。","tags":["Vue","构建与模块化"],"slug":"nginx-vue-history-deploy-2018","category":"2018","likeCount":5,"postId":"3631e00d-daf5-37dd-a18e-012349096bf3","viewCount":5423,"readTime":null,"coverImage":null}},"__N_SSG":true}