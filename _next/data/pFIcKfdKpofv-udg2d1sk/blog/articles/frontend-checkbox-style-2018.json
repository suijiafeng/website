{"pageProps":{"postData":{"title":"自定义 checkbox 该怎么做：保留原生 input 才是正路","date":"2018-05-10","excerpt":"自定义 checkbox 最容易犯的错，不是样式没画对，而是把原生控件整个扔掉。这里把键盘焦点、表单取值和无障碍一起踩过一遍之后，重新说明为什么保留原生 `input` 才是正路。","tags":["前端基础","CSS"],"slug":"frontend-checkbox-style-2018","category":"2018","likeCount":7,"postId":"d10f3144-7f22-bb1e-7827-d748c73a6d80","viewCount":4796,"readTime":"10分钟","coverImage":null,"content":"\n自定义 checkbox 最常见的误区，不是颜色没对上，而是为了还原设计稿，先把原生控件整个扔掉。这样确实能更快画出一个“长得像”的方块，但键盘焦点、表单取值、可访问性这些原生能力也会一起被你扔掉。\n\n我这次就是这么翻车的。设计稿里的品牌蓝复选框和浏览器默认样式差得太远，我下意识就走了最偷懒的路：拿 `div` 加 JS 自己画一个，点击切换 class，选中态补个对勾，两个小时搞定，像素级还原。\n\n它看起来很像，真正的问题却都在提测之后才开始出现。\n\n## 第一张 bug 单：键盘 Tab 切不过去\n\nQA 的操作习惯是填表单不碰鼠标，Tab 一路切过去。切到我的\"复选框\"——焦点直接越过去了。div 默认不在 tab 顺序里，我压根没想过这件事。\n\n第二张：表单提交收不到值。div 不是表单控件，没有 name 和 value，`form` 序列化时它就是个透明人。我当时是在 JS 里手动收集状态的，但这个表单后来被另一个同事复用，他按常规表单写法取值，取了个空。\n\n第三张最扎心：公司有个大客户对系统有无障碍要求，验收用读屏软件过了一遍，我的复选框在读屏里就是一段普通文本，不播报\"复选框\"，不播报选中状态，空格键按下去也没反应。无障碍测试直接挂掉。\n\n那几天我一边修一边意识到一个以前从没认真想过的问题：**一个控件好不好用，鼠标用户往往感觉不到差别，但只用键盘的人、用读屏软件的人马上就会发现问题。** 我做的那个东西只是\"看起来像复选框\"——真正稳定的 checkbox，是鼠标能点、键盘能切换、表单能提交、读屏软件能理解。原生 `<input type=\"checkbox\">` 把这些全做好了，是我亲手把它扔掉的。\n\n修补了两天越修越多之后，我决定推倒重来：保留原生 input，只动样式。下面是这次返工的完整记录。\n\n## 返工第一步：结构上保留原生 input\n\n比较稳的方式是保留 `input type=\"checkbox\"`，再用 `label` 扩大点击区域：\n\n```html\n<label class=\"checkbox\">\n  <input type=\"checkbox\">\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">我已阅读并同意协议</span>\n</label>\n```\n\n这样用户点击文字时，也能切换复选框状态——这是 label 白送的行为，div 版里我可是自己写的事件委托。\n\n如果不想把 `input` 放在 `label` 里面，也可以用 `for` 和 `id` 关联：\n\n```html\n<input id=\"agree\" class=\"checkbox-input\" type=\"checkbox\">\n<label class=\"checkbox-label\" for=\"agree\">\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">我已阅读并同意协议</span>\n</label>\n```\n\n这种写法适合表单结构比较复杂、控件和文案需要分开布局的场景。比如一个设置页，左边是开关列表，右边是说明文字，DOM 结构上很难把 input 和文案塞进同一个 label，用 `for`/`id` 关联就很灵活。\n\n这里有个我在这次返工里踩的小坑：用 `for`/`id` 时，页面上 `id` 必须唯一。后台系统经常循环渲染一堆复选框，我偷懒把 `id=\"agree\"` 写死在了循环模板里，出来全是同一个 id，点第二个切换的是第一个，调了半天才发现。后来的习惯是拿数据里的唯一字段拼 id，比如 `:id=\"'agree-' + item.id\"`，别图省事写常量。\n\n## 返工第二步：把原生样式藏起来，但别藏死\n\n设计稿的样式要画在旁边的 `.checkbox-box` 上，原生 input 要在视觉上隐藏。最直接的写法：\n\n```css\n.checkbox input {\n  position: absolute;\n  opacity: 0;\n}\n\n.checkbox {\n  display: inline-flex;\n  align-items: center;\n  cursor: pointer;\n}\n\n.checkbox-box {\n  width: 18px;\n  height: 18px;\n  margin-right: 8px;\n  border: 1px solid #999;\n  border-radius: 3px;\n}\n```\n\ninput 还在页面里，只是不可见。更讲究的做法是所谓\"视觉隐藏\"（sr-only），让它布局上缩到 1 像素、裁剪掉，但仍然可以获得焦点：\n\n```css\n.checkbox input {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  white-space: nowrap;\n  border: 0;\n}\n```\n\n**千万不要用 `display: none` 或 `visibility: hidden` 来隐藏表单控件。** 这个坑我在返工时又踩了一遍：第一版隐藏我图省事直接 `display: none`，视觉上没问题，鼠标点 label 也能切换，结果 QA 复测——键盘 Tab 又跳不到复选框了，焦点直接越过去。因为 `display: none` 的元素根本不在 tab 顺序里，等于把 div 版的老 bug 原样复刻了回来。换成上面那套视觉隐藏写法，input 仍然存在于可访问性树和焦点链里，键盘和读屏才能正常工作。\n\n补一句，上面用了 `clip: rect(0 0 0 0)`，这是眼下兼容性最好的做法。新一点的写法用 `clip-path: inset(50%)`，但我们要兼容到 IE11，我还是更信任 `clip`，老浏览器上不会翻车。两个一起写也行：\n\n```css\n.checkbox input {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  margin: -1px;\n  padding: 0;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  clip-path: inset(50%);\n  white-space: nowrap;\n  border: 0;\n}\n```\n\n顺带说一下另一条路：`appearance: none`（加 `-webkit-` 前缀）可以把原生控件的默认外观整个抹掉，然后直接在 input 本身上画样式，连旁边的 span 都省了。我试了半天最后放弃——各家浏览器对 `appearance` 的支持和抹掉之后剩下什么都不一致，IE 干脆不认，伪元素在 input 上的表现也各家有各家的脾气。**在 checkbox 这件事上，\"隐藏原生 + 兄弟元素画皮\"仍然是兼容性最稳的方案**，Element UI 的 checkbox 拆开看也是这个套路：label 套着一个视觉隐藏的 input 和一个画样式的 span。\n\n## 返工第三步：`:checked` 驱动选中态，一行 JS 都不用\n\n设计稿的蓝色选中态，用相邻选择器根据选中状态改变样式：\n\n```css\n.checkbox input:checked + .checkbox-box {\n  border-color: #409eff;\n  background: #409eff;\n}\n\n.checkbox input:checked + .checkbox-box::after {\n  content: \"\";\n  display: block;\n  width: 5px;\n  height: 9px;\n  margin: 2px auto;\n  border: solid #fff;\n  border-width: 0 2px 2px 0;\n  transform: rotate(45deg);\n}\n```\n\n不需要额外 JavaScript 就有选中效果。对比我那个 div 版——监听 click、toggle class、再想办法把状态同步给表单——高下立判。我特别喜欢这个方案的一点是：**状态的真相永远在 input 上，CSS 只是被动跟着 `:checked` 走。** 不用手动同步、不用监听 change 再 toggle class，少了一整类\"状态不一致\"的 bug。div 版被 QA 抓到过一个\"快速连点后视觉状态和实际提交值相反\"的 bug，就属于这一类，原生方案里它根本不可能发生。\n\n这里用相邻选择器 `+` 是有讲究的：它要求 `.checkbox-box` 紧跟在 input 后面，中间插了别的元素就选不中了，那时得换通用兄弟选择器 `~`。我一般保持 input 紧贴着 box，结构稳定，选择器也最简单。\n\n那个对勾用的是 border 画法——一个旋转 45 度的方块，只留右边和下边两条边。比起放一张对勾图片或者 icon font，纯 CSS 画的对勾在高分屏上永远清晰，也不多一次请求。尺寸要自己微调，`width`、`height`、`border-width` 三个值配合 box 的大小调，调到对勾居中、比例顺眼为止。这块没有公式，我就是对着屏幕拧了二十分钟，设计师坐我旁边说\"再往下一像素\"。\n\n## 返工第四步：焦点态是这次返工里最容易被忽略的一环\n\n被那三张 bug 单教育之后，我给焦点状态补了课。键盘用户 Tab 切到复选框时，需要知道当前焦点在哪里，所以选中框上要有可见的焦点样式：\n\n```css\n.checkbox input:focus-visible + .checkbox-box {\n  outline: 2px solid rgba(64, 158, 255, 0.35);\n  outline-offset: 2px;\n}\n```\n\n为什么用 `:focus-visible` 而不是 `:focus`？因为 `:focus` 鼠标点一下也会触发，那个描边框会在鼠标用户点完之后赖着不走——我们设计师看到第一版就说\"把那个框去掉\"。`:focus-visible` 只在浏览器判断为键盘操作时才出现，鼠标点击不显示，两边都照顾到。它现在还很新，规范都没完全定稿，老浏览器不支持也无所谓——退化成没有焦点框，至少不会比原来更差，讲究的话可以加官方那个 focus-visible polyfill。\n\n如果担心兼容，可以做个兜底：先给 `:focus` 一个保守样式，再用 `:focus:not(:focus-visible)` 把鼠标态的去掉。\n\n**但千万别为了\"好看\"直接写 `outline: none` 把焦点框一刀切掉**——那等于把键盘用户晾在原地，完全不知道焦点跑哪去了。我在 div 版里就是这么干的（div 加了 tabindex 之后出现焦点框，我嫌丑给 none 掉了），现在想起来脸热。\n\n## 返工第五步：禁用态\n\n业务表单里经常有禁用项：协议未开放、权限不足、依赖条件未满足。原生 input 一个 `disabled` 属性就位：\n\n```html\n<label class=\"checkbox\">\n  <input type=\"checkbox\" disabled>\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">仅管理员可操作</span>\n</label>\n```\n\n```css\n.checkbox input:disabled + .checkbox-box {\n  border-color: #d9d9d9;\n  background: #f5f5f5;\n}\n\n.checkbox input:disabled ~ .checkbox-text {\n  color: #999;\n}\n```\n\n有个想要而不可得的：外层 label 的 `cursor: not-allowed` 按理想写法应该是\"选中包含 disabled input 的 label\"，CSS 选择器里有个 `:has()` 能表达这个意思——但它现在浏览器支持度几乎为零，我都当它不存在，写出来只是表达思路：\n\n```css\n.checkbox:has(input:disabled) {\n  cursor: not-allowed;\n}\n```\n\n真要落地，老老实实给外层手动加类名最稳：\n\n```html\n<label class=\"checkbox checkbox-disabled\">\n  <input type=\"checkbox\" disabled>\n  <span class=\"checkbox-box\"></span>\n  <span class=\"checkbox-text\">仅管理员可操作</span>\n</label>\n```\n\n```css\n.checkbox-disabled {\n  cursor: not-allowed;\n}\n```\n\n## 回头看那个 div 版：它到底缺了多少东西\n\n返工完我把 div 版的代码翻出来对着看，想搞清楚如果坚持走那条路，到底要补多少东西才能及格。答案是：`role`、`aria-checked`、`tabindex`，外加键盘事件，一样都不能少：\n\n```html\n<div role=\"checkbox\" aria-checked=\"false\" tabindex=\"0\" class=\"fake-checkbox\">\n  接收通知\n</div>\n```\n\n```javascript\nvar checkbox = document.querySelector('.fake-checkbox');\n\nfunction toggle() {\n  var checked = checkbox.getAttribute('aria-checked') === 'true';\n  checkbox.setAttribute('aria-checked', String(!checked));\n}\n\ncheckbox.addEventListener('click', toggle);\ncheckbox.addEventListener('keydown', function(event) {\n  if (event.key === ' ' || event.key === 'Enter') {\n    event.preventDefault();\n    toggle();\n  }\n});\n```\n\n而这还只是补了最基础的交互。和原生 checkbox 相比，它没有更简单，只是把浏览器已经做好的事情重新做了一遍——而且还远没补齐：原生 checkbox 在表单里自动带上 name/value 参与提交，`<div>` 不会，得额外塞一个隐藏 input 去同步状态；原生还支持 `indeterminate`（半选）这种第三态，自己模拟又是一摊代码；读屏软件对原生 checkbox 的播报是现成的，自定义的得自己把 `aria-checked` 维护对。表单语义丢失、键盘操作不自然、可访问性变差、提交要额外处理、状态同步容易出错——我那三张 bug 单，张张都在这个清单里。\n\n这次之后我给团队定了条规矩：**除非设计稿要的效果原生 input 配 CSS 实在做不出来（这种情况极少），否则一律保留原生控件。** 省下来的不只是开发时间，更是无障碍、表单提交、键盘交互这些隐性维护成本。\n\n## 顺手补齐：全选的半选状态\n\n改版这套表单里正好有个\"全选\"，需要第三种状态：部分选中。原生 checkbox 支持 `indeterminate`，但它只能通过 JS 设置，没法用 HTML 属性写：\n\n```javascript\nvar all = document.querySelector('#select-all');\nvar items = document.querySelectorAll('.item-checkbox');\n\nfunction syncAll() {\n  // NodeList 在 IE11 上没有 forEach/filter，先转数组\n  var list = Array.prototype.slice.call(items);\n  var checkedCount = list.filter(function(i) { return i.checked }).length;\n\n  all.checked = checkedCount === list.length;\n  all.indeterminate = checkedCount > 0 && checkedCount < list.length;\n}\n```\n\n给每个子项挂监听时也是同一个兼容问题——`NodeList` 直接 `.forEach` 在 IE11 上会报错，保险起见我统一走转换后的数组：\n\n```javascript\nArray.prototype.slice.call(items).forEach(function(i) {\n  i.addEventListener('change', syncAll);\n});\n```\n\n视觉上用 `:indeterminate` 伪类画一条横杠：\n\n```css\n.checkbox input:indeterminate + .checkbox-box {\n  border-color: #409eff;\n  background: #409eff;\n}\n\n.checkbox input:indeterminate + .checkbox-box::after {\n  content: \"\";\n  display: block;\n  width: 10px;\n  height: 2px;\n  margin: 8px auto;\n  background: #fff;\n  transform: none;\n}\n```\n\n这又是一个\"原生帮你做好了\"的例子——`indeterminate` 不影响实际的 `checked` 值，提交时该是 false 还是 false，状态语义清清楚楚。要是用 div 模拟，光把全选、半选、未选三态和子项联动调对就够喝一壶的。\n\n另外这套\"隐藏原生 + 兄弟元素画皮 + 伪类驱动\"的思路不止管 checkbox——同一套结构把 `type` 换成 `radio`、对勾换成圆点、`border-radius` 拉满，就是自定义单选框；设计稿里那组单选按钮我十分钟就照搬完了。**学会一个模式，收获一整个表单控件家族。**\n\n## 最终交付的版本\n\n综合起来，最后合进主干的基础版本长这样：\n\n```html\n<label class=\"checkbox\">\n  <input name=\"agreement\" type=\"checkbox\">\n  <span class=\"checkbox-box\" aria-hidden=\"true\"></span>\n  <span class=\"checkbox-text\">我已阅读并同意协议</span>\n</label>\n```\n\n```css\n.checkbox {\n  position: relative;\n  display: inline-flex;\n  align-items: center;\n  cursor: pointer;\n}\n\n.checkbox input {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0 0 0 0);\n  white-space: nowrap;\n  border: 0;\n}\n\n.checkbox-box {\n  width: 18px;\n  height: 18px;\n  /* 本来想用 flex 的 gap 来留间距，可惜 gap 目前只在 grid 里能用，\n     flex 上还没浏览器支持，只能老实用 margin 顶着 */\n  margin-right: 8px;\n  border: 1px solid #999;\n  border-radius: 3px;\n  transition: border-color 0.2s ease, background-color 0.2s ease;\n}\n\n.checkbox input:checked + .checkbox-box {\n  border-color: #409eff;\n  background: #409eff;\n}\n\n.checkbox input:checked + .checkbox-box::after {\n  content: \"\";\n  display: block;\n  width: 5px;\n  height: 9px;\n  margin: 2px auto;\n  border: solid #fff;\n  border-width: 0 2px 2px 0;\n  transform: rotate(45deg);\n}\n\n.checkbox input:focus-visible + .checkbox-box {\n  outline: 2px solid rgba(64, 158, 255, 0.35);\n  outline-offset: 2px;\n}\n```\n\n`.checkbox-box` 上那个 `aria-hidden=\"true\"` 是无障碍验收时顾问建议加的：这个 span 纯粹是画皮，状态语义全在 input 上，明确告诉读屏软件忽略它，别播报出冗余内容。\n\n五月初，这版重新提测：QA 的键盘流一路 Tab 到底，表单取值正常，读屏播报\"复选框，未选中\"，无障碍验收一次通过。设计师对着像素稿量了一遍，也挑不出毛病——好看和好用，从来就不是二选一。\n\n回到这次折腾给我留下的东西。美化 checkbox 的方法有很多，但最重要的一条是**不要丢掉它原本的表单语义**：保留原生 input，把视觉交给旁边的样式元素，让 `:checked`、`:disabled`、`:indeterminate` 这些伪类自然驱动表现。这样既能满足设计稿，又不破坏键盘、读屏和表单行为。这部分代价在写的时候看不见，等真出问题时返工成本却很高——我的三张 bug 单和四天返工可以作证。简单、稳妥、可维护，胜过把控件完全自造一遍。\n"},"recommendedPosts":[{"title":"跨文档 View Transitions：多页应用也能做出丝滑过渡了","date":"2026-04-04","excerpt":"同文档 View Transitions 之外，跨文档（MPA）过渡终于可用。记录我给一个传统多页站点加上页面切换动画的过程、view-transition-name 的配对和降级处理。","tags":["前端基础","CSS","交互与动效"],"slug":"view-transitions-cross-document-2026","category":"2026","likeCount":10,"postId":"54351a26-388d-b51e-ff65-3ae7fde9bbd4","viewCount":1094,"readTime":null,"coverImage":null},{"title":"原生 CSS 嵌套和 @scope：写了这么多年 Sass 之后的几点真实感受","date":"2026-03-22","excerpt":"原生嵌套和 @scope 落地后，我在项目里逐步去掉一部分 Sass 的过程：嵌套的坑、@scope 的下边界 donut scope、以及现在还不能完全替代预处理器的地方。","tags":["前端基础","CSS"],"slug":"css-scope-nesting-2026","category":"2026","likeCount":37,"postId":"a4a7366f-5967-f614-350b-d223d0a045f3","viewCount":891,"readTime":null,"coverImage":null},{"title":"Tailwind CSS v4 迁移记：新引擎、CSS 变量主题和那些不再需要的配置","date":"2026-01-24","excerpt":"从 v3 升到 v4 的真实迁移过程：Oxide 引擎带来的速度变化、配置从 JS 搬到 CSS、@theme 和 CSS 变量怎么用，以及几个升级时踩的坑。","tags":["前端基础","CSS","构建与模块化"],"slug":"tailwind-v4-migration-2026","category":"2026","likeCount":5,"postId":"c8484300-10ec-bf7b-ef1b-6a237cb4f4f2","viewCount":1011,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 2 项目初尝 TypeScript：一个字段类型引发的冲动，和我按住没全上的理由","date":"2018-05-20","excerpt":"一次字段类型变更穿过所有检查，逼着我认真评估 TypeScript 在 Vue 2 项目里的真实收益和成本。类型能拦错，接入和推断也确实磨人，结论不是全盘拥抱，而是按场景上。","tags":["Vue","TypeScript"],"slug":"typescript-vue2-first-try-2018","category":"2018","likeCount":8,"postId":"4b142bd1-dc56-6897-15ca-b846e512b454","viewCount":2580,"readTime":null,"coverImage":null},"nextPost":{"title":"组内技术摸底被连环追问 Ajax、JSONP 和 CORS 预检：我是怎么一步步露馅的","date":"2018-05-09","excerpt":"跨域这件事天天在用，但 XMLHttpRequest 的状态机、CORS 预检的触发条件、JSONP 的并发和清理，很多人说不清背后的原理。这篇把 Ajax、JSONP、CORS 从状态码到响应头逐层拆开，顺带把请求层的错误分类也理清楚。","tags":["JavaScript","前端基础"],"slug":"ajax-jsonp-cors-interview-2018","category":"2018","likeCount":6,"postId":"0f3d16ad-dc69-0f81-1930-9d2384b5c469","viewCount":4036,"readTime":null,"coverImage":null}},"__N_SSG":true}