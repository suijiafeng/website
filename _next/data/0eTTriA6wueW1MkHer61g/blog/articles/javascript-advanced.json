{"pageProps":{"postData":{"title":"Next.js路由传参全攻略","date":"2024-09-07","excerpt":"深入探讨 Next.js 中各种路由传参方法，包括动态路由、查询字符串、Link 组件、App Router 参数读取，以及不同场景下的边界处理。","tags":["React","前端基础"],"slug":"javascript-advanced","category":"2024","likeCount":7,"postId":"90a82100-3dee-7a79-0ace-e30e41291732","viewCount":2115,"readTime":"8分钟","coverImage":null,"content":"\nNext.js 的路由传参看起来很简单：路径里放一个 `id`，页面里读出来就行。但真正做项目时，问题往往不在“怎么拿到参数”，而在“这个参数适合放在哪里”“什么时候能拿到”“拿到以后如何校验”“错误时页面应该怎么处理”。\n\n2024 年的 Next.js 项目里，很多团队同时接触两套路由体系：老项目常见 `pages` Router，新项目更多使用 `app` Router。两者都能传参，但读取方式、服务端渲染时机、客户端可用 API 都不完全一样。本文按实际开发场景梳理一次。\n\n## 先区分三类参数\n\n我通常会先把路由参数分成三类：\n\n- 路径参数：资源身份，例如 `/posts/123` 里的 `123`\n- 查询参数：筛选、排序、分页，例如 `/posts?page=2&tag=react`\n- 状态参数：不适合暴露在 URL 里的临时状态，例如弹窗内部数据、复杂对象、敏感信息\n\n路径参数应该稳定、可分享、可被搜索引擎理解。查询参数适合表达同一个页面下的不同视图状态。状态参数则更适合放在组件状态、全局 store、服务端 session 或数据库里。\n\n一个常见误区是把复杂对象塞进 query：\n\n```js\nrouter.push(`/checkout?payload=${encodeURIComponent(JSON.stringify(order))}`)\n```\n\n这在 demo 里能跑，但生产环境很容易踩坑：URL 太长、数据被用户篡改、敏感字段泄露、刷新后状态不一致。更稳妥的方式是传递一个短 ID，再让目标页面根据 ID 读取服务端数据。\n\n## Pages Router：动态路由\n\n在 `pages` Router 中，Next.js 支持通过文件名创建动态路由：\n\n```jsx\n// pages/posts/[id].js\nimport { useRouter } from 'next/router'\n\nexport default function Post() {\n  const router = useRouter()\n  const { id } = router.query\n\n  return <p>Post: {id}</p>\n}\n```\n\n访问 `/posts/1` 时，`id` 参数会是 `\"1\"`。\n\n需要注意的是，`router.query` 在客户端首次渲染时可能还没准备好，尤其是静态优化页面。更稳一点的写法是判断 `router.isReady`：\n\n```jsx\nimport { useRouter } from 'next/router'\n\nexport default function Post() {\n  const router = useRouter()\n\n  if (!router.isReady) {\n    return <p>Loading...</p>\n  }\n\n  const id = String(router.query.id || '')\n\n  if (!/^\\d+$/.test(id)) {\n    return <p>Invalid post id</p>\n  }\n\n  return <p>Post: {id}</p>\n}\n```\n\n如果页面依赖这个参数请求数据，推荐在服务端阶段处理：\n\n```jsx\nexport async function getServerSideProps({ params }) {\n  const id = String(params.id || '')\n\n  if (!/^\\d+$/.test(id)) {\n    return { notFound: true }\n  }\n\n  const post = await getPostById(id)\n\n  if (!post) {\n    return { notFound: true }\n  }\n\n  return { props: { post } }\n}\n```\n\n这样做的好处是错误路径不会先渲染一个半成品页面，再在客户端跳转或报错。\n\n## 查询字符串：筛选、搜索和分页\n\n查询字符串适合表达页面状态，例如：\n\n```text\n/posts?tag=react&page=2&sort=latest\n```\n\n在 `pages` Router 中可以这样读取：\n\n```jsx\nimport { useRouter } from 'next/router'\n\nexport default function Posts() {\n  const router = useRouter()\n  const tag = String(router.query.tag || 'all')\n  const page = Number(router.query.page || 1)\n\n  return (\n    <div>\n      <p>Tag: {tag}</p>\n      <p>Page: {Number.isFinite(page) ? page : 1}</p>\n    </div>\n  )\n}\n```\n\n这里有两个边界要处理：\n\n第一，query 可能是字符串，也可能是字符串数组。比如用户访问 `/posts?tag=react&tag=nextjs`，`tag` 就可能变成数组。\n\n第二，URL 是用户可编辑的，不能假设 `page` 一定是合法数字。\n\n我习惯封装一个小函数：\n\n```js\nfunction firstQueryValue(value) {\n  if (Array.isArray(value)) return value[0]\n  return value\n}\n\nfunction parsePage(value) {\n  const page = Number(firstQueryValue(value) || 1)\n  return Number.isInteger(page) && page > 0 ? page : 1\n}\n```\n\n这样页面里的逻辑会清楚很多，也能避免到处散落 `Array.isArray`。\n\n## Link 组件传参\n\n使用 `Link` 组件时可以传递动态路径或 query：\n\n```jsx\nimport Link from 'next/link'\n\nexport default function Home() {\n  return (\n    <Link\n      href={{\n        pathname: '/about',\n        query: { name: 'John' },\n      }}\n    >\n      About John\n    </Link>\n  )\n}\n```\n\n更推荐把资源身份放进路径：\n\n```jsx\n<Link href={`/posts/${post.id}`}>{post.title}</Link>\n```\n\n筛选条件再放 query：\n\n```jsx\n<Link\n  href={{\n    pathname: '/posts',\n    query: { tag: 'react', page: 1 },\n  }}\n>\n  React posts\n</Link>\n```\n\n路径越语义化，后续做 SEO、分享、日志分析都会更轻松。\n\n## 编程式导航\n\n表单提交、按钮点击、权限判断后跳转，通常会用 `router.push()` 或 `router.replace()`：\n\n```jsx\nimport { useRouter } from 'next/router'\n\nexport default function SearchBox() {\n  const router = useRouter()\n\n  function submit(keyword) {\n    router.push({\n      pathname: '/search',\n      query: { q: keyword.trim(), page: 1 },\n    })\n  }\n\n  return <button onClick={() => submit('next router')}>Search</button>\n}\n```\n\n`push` 会新增浏览器历史记录，适合用户主动导航；`replace` 会替换当前历史记录，适合登录后重定向、修正非法参数、默认值补齐。\n\n例如用户访问 `/posts?page=-1`，可以替换成第一页：\n\n```js\nrouter.replace({\n  pathname: router.pathname,\n  query: { ...router.query, page: 1 },\n})\n```\n\n这种场景用 `replace` 比 `push` 更合适，否则用户点返回时可能又回到非法 URL。\n\n## Catch-all 路由\n\n使用 `...` 可以捕获多个路径段：\n\n```jsx\n// pages/docs/[...slug].js\nimport { useRouter } from 'next/router'\n\nexport default function Docs() {\n  const router = useRouter()\n  const slug = router.query.slug\n\n  const path = Array.isArray(slug) ? slug.join('/') : ''\n\n  return <p>Docs path: {path}</p>\n}\n```\n\n访问 `/docs/react/server-components` 时，`slug` 是：\n\n```js\n['react', 'server-components']\n```\n\n如果要让 `/docs` 也匹配，可以使用 optional catch-all：\n\n```text\npages/docs/[[...slug]].js\n```\n\n这类路由适合文档站、分类页、CMS 页面。但也要注意边界：不要让 catch-all 路由吞掉本来应该 404 的路径。服务端读取到 slug 后，最好到内容源里查一次，不存在就返回 `notFound`。\n\n## App Router：params 和 searchParams\n\n新项目里更常见的是 `app` Router。它把路径参数和查询参数拆得更清楚：\n\n```tsx\n// app/posts/[id]/page.tsx\ntype PageProps = {\n  params: Promise<{ id: string }>\n  searchParams: Promise<{ preview?: string }>\n}\n\nexport default async function PostPage({ params, searchParams }: PageProps) {\n  const { id } = await params\n  const { preview } = await searchParams\n\n  if (!/^\\d+$/.test(id)) {\n    return <p>Invalid post id</p>\n  }\n\n  return (\n    <article>\n      <h1>Post {id}</h1>\n      {preview === '1' ? <p>Preview mode</p> : null}\n    </article>\n  )\n}\n```\n\n在不同 Next.js 版本中，`params` 和 `searchParams` 的类型写法会有差异；但核心思路不变：路径参数从 `params` 读，查询参数从 `searchParams` 读。\n\n客户端组件中可以使用 `useParams` 和 `useSearchParams`：\n\n```tsx\n'use client'\n\nimport { useParams, useSearchParams } from 'next/navigation'\n\nexport default function PostToolbar() {\n  const params = useParams<{ id: string }>()\n  const searchParams = useSearchParams()\n  const mode = searchParams.get('mode') || 'read'\n\n  return <p>Post {params.id}, mode: {mode}</p>\n}\n```\n\n这里要注意：`useSearchParams` 只能在客户端组件中使用。如果一个页面只是根据 query 请求数据，优先在服务端页面组件里读取 `searchParams`，不要为了读 query 就把整个页面变成客户端组件。\n\n## App Router 中的跳转\n\n`app` Router 中的编程式导航来自 `next/navigation`：\n\n```tsx\n'use client'\n\nimport { useRouter, useSearchParams } from 'next/navigation'\n\nexport default function SortButton() {\n  const router = useRouter()\n  const searchParams = useSearchParams()\n\n  function changeSort(sort: string) {\n    const nextParams = new URLSearchParams(searchParams.toString())\n    nextParams.set('sort', sort)\n    nextParams.set('page', '1')\n\n    router.push(`/posts?${nextParams.toString()}`)\n  }\n\n  return <button onClick={() => changeSort('latest')}>Latest</button>\n}\n```\n\n`URLSearchParams` 比手动拼字符串更稳，能自动处理编码问题。\n\n如果用户输入关键词，一定要让 URL 编码由标准 API 处理：\n\n```js\nconst params = new URLSearchParams()\nparams.set('q', 'React Server Components')\n\nrouter.push(`/search?${params.toString()}`)\n```\n\n不要手写：\n\n```js\nrouter.push('/search?q=' + keyword)\n```\n\n遇到空格、中文、`&`、`?` 时，手写拼接很容易出错。\n\n## API 路由中的参数\n\nNext.js 的 API 路由也支持动态参数：\n\n```js\n// pages/api/users/[id].js\nexport default function handler(req, res) {\n  const { id } = req.query\n\n  if (!/^\\d+$/.test(String(id))) {\n    return res.status(400).json({ error: { message: 'Invalid user id' } })\n  }\n\n  return res.status(200).json({ id, name: `User ${id}` })\n}\n```\n\n如果使用 App Router 的 Route Handler：\n\n```ts\n// app/api/users/[id]/route.ts\nimport { NextResponse } from 'next/server'\n\nexport async function GET(\n  request: Request,\n  { params }: { params: Promise<{ id: string }> }\n) {\n  const { id } = await params\n\n  if (!/^\\d+$/.test(id)) {\n    return NextResponse.json(\n      { error: { message: 'Invalid user id' } },\n      { status: 400 }\n    )\n  }\n\n  return NextResponse.json({ id, name: `User ${id}` })\n}\n```\n\n这里我会坚持一个习惯：所有 API 错误都返回统一结构。比如：\n\n```json\n{\n  \"error\": {\n    \"message\": \"Invalid user id\"\n  }\n}\n```\n\n项目变大后，这能显著降低前端调用方的判断成本。否则有的接口返回 `{ message }`，有的返回 `{ error }`，有的直接返回字符串，最后错误提示组件会写得很痛苦。\n\n## 什么时候不要用 URL 传参\n\n不是所有信息都适合通过路由传递。\n\n下面这些情况要谨慎：\n\n- token、手机号、邮箱等敏感信息\n- 体积很大的对象\n- 只在当前交互流程中有意义的临时状态\n- 用户不能篡改的数据\n- 会频繁变化但不需要分享的状态\n\n比如结算页不要把完整订单对象放进 URL。更合理的是：\n\n```text\n/checkout?orderId=202409070001\n```\n\n目标页面拿到 `orderId` 后，从服务端读取订单详情，并做权限校验。这样即使用户改 URL，也不会越权看到别人的订单。\n\n## 实战建议\n\n我的经验是先问三个问题：\n\n第一，这个参数是否代表资源身份？如果是，放路径里，例如 `/posts/[id]`。\n\n第二，这个参数是否代表可分享的页面视图？如果是，放 query，例如 `/posts?tag=react&page=2`。\n\n第三，这个参数是否敏感、复杂或不可信？如果是，不要直接放 URL，改用 ID、服务端状态或客户端状态。\n\n然后再做两件事：所有入口统一校验，所有错误统一返回。\n\n路由传参不是语法题，而是应用信息架构的一部分。参数设计得清楚，页面刷新、分享、回退、SEO、日志分析都会顺很多；参数设计得随意，后面通常会在权限、缓存、埋点和错误处理上慢慢还债。\n\n## 小结\n\nNext.js 提供了丰富而灵活的路由传参方式：动态路由适合资源身份，查询字符串适合筛选和分页，`Link` 和编程式导航适合不同交互场景，catch-all 路由适合文档和 CMS，App Router 则把 `params` 与 `searchParams` 分得更明确。\n\n真正重要的是边界意识：参数来自用户，必须校验；URL 可分享，也可被篡改；API 错误要统一；复杂对象不要轻易塞进 query。把这些基础打牢，路由层会稳定很多。\n"},"randomPosts":[{"title":"Vue 表单组件拆分经验：可复用输入框不是把所有 props 都塞进去","date":"2019-04-20","excerpt":"结合 Vue 表单场景，讨论输入组件、字段容器、校验提示和提交错误处理怎么拆，避免复用组件变成万能配置层。","tags":["Vue","组件与状态"],"slug":"vue-form-components-2019","category":"2019","likeCount":5,"postId":"d0db4543-db2c-450f-def4-3bbe57b7eec9","viewCount":4062,"readTime":null,"coverImage":null},{"title":"ES2026 新特性整理：关注表达式、不可变数据和资源管理","date":"2026-04-24","excerpt":"整理 ES2026 中值得前端关注的新能力，说明新标准对日常 JavaScript 编码方式的影响。","tags":["前端基础","JavaScript"],"slug":"frontend-es2026-features-2026","category":"2026","likeCount":46,"postId":"3037b660-aed7-21f1-aebb-e09c0cc3c0ec","viewCount":342,"readTime":null,"coverImage":null},{"title":"JavaScript 数组常用方法整理：写业务代码时最常用的是哪几个","date":"2020-08-19","excerpt":"从我写业务列表、表单和状态更新的经验出发，整理 JavaScript 数组方法的真实使用场景，以及怎么用方法表达意图。","tags":["前端基础"],"slug":"frontend-javascript-arrays-2020","category":"2020","likeCount":5,"postId":"6214405a-8a37-c7e8-f7b4-1d51d7f4d889","viewCount":3353,"readTime":null,"coverImage":null}],"prevPost":{"title":"Next.js：React应用的未来","date":"2024-09-08","excerpt":"探索 Next.js 的核心特性，从 SSR、SSG 到 App Router、Server Components、缓存和部署取舍，理解它为什么成为现代 React 应用的重要选择。","tags":["React","组件与状态"],"slug":"react-nextjs","category":"2024","likeCount":9,"postId":"853faa2e-1bd8-1497-d1df-183fdb9dc0ad","viewCount":4673,"readTime":null,"coverImage":null},"nextPost":{"title":"掌握JavaScript异步编程","date":"2024-09-07","excerpt":"探索 JavaScript 中的异步编程技术，从回调函数到 Promise，再到 async/await、并发控制、请求取消和统一错误处理。","tags":["前端基础"],"slug":"javascript-async-await","category":"2024","likeCount":8,"postId":"2fc17dce-93a5-8a87-c22b-7adeceb84e60","viewCount":3692,"readTime":null,"coverImage":null}},"__N_SSG":true}