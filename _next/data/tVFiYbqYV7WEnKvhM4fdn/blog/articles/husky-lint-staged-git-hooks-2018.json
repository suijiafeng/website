{"pageProps":{"postData":{"title":"Husky 和 lint-staged：把低级问题挡在提交之前","date":"2018-08-23","excerpt":"把 Husky 和 lint-staged 接到团队项目后，我更在意三件事：提交别太慢、规则别乱飘、坏代码别轻易进仓库。","tags":["构建与模块化"],"slug":"husky-lint-staged-git-hooks-2018","category":"2018","likeCount":7,"postId":"05cd071c-41d5-33c2-9362-df2a59f49ddb","viewCount":1895,"readTime":"8分钟","coverImage":null,"content":"\n我们组今年初接手一个三年前的老项目，第一周我就被各种鸡毛蒜皮的提交折腾得够呛。有人 `var` 和 `let` 混着用，有人缩进是 tab 有人是空格，还有人把 `console.log` 直接提交进了主分支。这些都不是什么大 bug，但每次 code review 都要花时间挑这些毛病，review 的人和被 review 的人都烦。\n\n最离谱的一次，我合并完一个同事的分支，本地一跑就报 ESLint 错误，结果 CI 因为这个挂了红，把后面排队等部署的几个分支全堵住了。一个没删干净的未使用变量，让整条流水线停了二十多分钟。那天我就下决心，必须把这类低级问题挡在提交之前，而不是等 CI 红了才发现。\n\nCI 当然能兜底，但 CI 的反馈链路太长了：推上去、排队、装依赖、跑完整流程，快则三五分钟，慢则十几分钟。等你看到红叉，注意力早就切到别的事情上去了，再切回来修，上下文丢得七七八八。本地的 Git hook 不一样，它在 `git commit` 那一刻就给你反馈，错就错在眼前，改起来也最便宜。Husky 和 lint-staged 就是干这个的：前者负责把脚本挂到 Git 的钩子上，后者负责让检查只针对这次改动的文件，而不是整个仓库。\n\n## 为什么不能直接 `eslint .` 了事\n\n最朴素的想法是 pre-commit 里直接全量 lint 一遍。我一开始也是这么干的，写了个：\n\n```json\n{\n  \"husky\": {\n    \"hooks\": {\n      \"pre-commit\": \"eslint src/ && prettier --check src/\"\n    }\n  }\n}\n```\n\n结果第二天就被同事在群里 @ 了。我们那个项目 `src` 下有近两千个文件，全量 ESLint 一遍要四十多秒。改一行代码、提交一次、等四十秒，谁受得了？没两天大家就学会了 `git commit --no-verify`，等于这道关形同虚设。\n\n问题的关键在于：**这次提交只动了两三个文件，凭什么要检查全部？** lint-staged 解决的就是这件事——它从 Git 暂存区（staged）拿到本次要提交的文件列表，把这个列表喂给你配置的命令，只检查这些文件。\n\n```json\n{\n  \"lint-staged\": {\n    \"*.{js,vue}\": [\n      \"eslint --fix\",\n      \"prettier --write\",\n      \"git add\"\n    ]\n  }\n}\n```\n\n改成这样之后，提交一两个文件基本是秒级反馈。这里有个容易踩的细节：数组里的命令是按顺序执行的，lint-staged 会把匹配到的文件路径**追加在每条命令后面**。也就是说上面实际执行的是 `eslint --fix file1.js file2.vue`，而不是 `eslint --fix` 然后再读配置去扫目录。所以千万别在命令里再写上路径或 glob，否则就变成全量了，这个坑我亲手踩过。\n\n最后那行 `git add` 也很关键。因为 `eslint --fix` 和 `prettier --write` 会就地改写文件，如果不重新 `git add`，被自动修复后的内容不会进入这次提交，提交进去的还是修复前的版本。\n\n这里还藏着一个我栽过的“部分暂存”坑。假设一个文件你只 `git add` 了其中几行（用 `git add -p` 那种），剩下的改动还在工作区没暂存。这时 lint-staged 跑 `--fix` 改的是磁盘上的整个文件，连你没打算提交的那部分一起格式化了，最后 `git add` 又把整个文件塞进暂存区——你以为只提交了那几行，结果一大坨没准备好的改动也跟着上去了。lint-staged 后来的版本对这种情况做了 stash 处理来保护未暂存的改动，但我用的 7.x 上这块并不完善，所以那阵子我养成了习惯：要做部分提交时，先确认没有混在一起的脏改动，宁可分两次干净地提。工具再贴心，也架不住边界情况，自己心里得有数。\n\n> 顺带说一下版本背景：我写这套配置时用的是 husky 0.14、lint-staged 7.x。husky 这个版本还要求 hook 配置放在 package.json 的 `scripts` 里（比如 `precommit` 这种命名约定）。后来 husky 0.15/1.0 引入了 `husky.hooks` 这种独立配置块，后面这套写法以这个为准。如果你用的还是更老的 husky，记得查一下对应的配置方式，两代差别不小。\n\n## Husky 是怎么把脚本塞进 Git 的\n\nGit 本身就支持 hook，机制很简单：`.git/hooks/` 目录下放着一堆脚本，命名固定，比如 `pre-commit`、`commit-msg`、`pre-push`。你 `git commit` 的时候，Git 会去执行 `.git/hooks/pre-commit`，如果这个脚本退出码非零，提交就被中止。\n\n但 `.git` 目录不会被纳入版本控制，你没法把 hook 脚本提交到仓库让别人共享。Husky 干的事情就是：在 `npm install` 的时候，通过一个 postinstall 钩子，自动往 `.git/hooks/` 里写入桥接脚本，让 Git 的钩子转而去读 package.json 里你的配置。这样团队每个人 clone + install 之后就自动装上了同一套 hook。\n\n配置本身很简单：\n\n```json\n{\n  \"husky\": {\n    \"hooks\": {\n      \"pre-commit\": \"lint-staged\",\n      \"commit-msg\": \"commitlint -E HUSKY_GIT_PARAMS\"\n    }\n  }\n}\n```\n\n`pre-commit` 跑 lint-staged，这是主力。我后来还加了 `commit-msg` 配合 commitlint 校验提交信息格式（团队约定 Angular 那套 `feat:` / `fix:` 前缀），`HUSKY_GIT_PARAMS` 是 husky 注入的环境变量，指向 Git 存放提交信息的临时文件。\n\n提交信息规范化这件事，单看好像有点强迫症，但它的回报在后面。我们统一了前缀之后，用 `conventional-changelog` 这类工具就能直接从提交记录生成版本变更日志，发版时不用再手工扒拉这次改了啥；`fix:` 和 `feat:` 还能驱动语义化版本号自动 bump。也就是说 `commit-msg` 这道关换来的不只是好看的 git log，而是后面一整条自动化链路的输入。当然规则别一上来就卡死，我们最初只校验类型前缀和不超长，body 之类的留作可选，门槛太高同样会逼出 `--no-verify`。\n\n提交时这些会自动跑。检查失败提交就被拦下，开发者修完重新 `git add`、再 commit 即可。第一次给团队装上的时候，有个同事提交被拦了，还以为 Git 坏了，跑来问我。所以上线这种东西最好提前在群里说一声，并写清楚失败了怎么办。\n\n## 一份我现在还在用的完整配置\n\n把上面的拼起来，再加上 TypeScript 的处理，这是我们项目里实际跑的版本：\n\n```json\n{\n  \"scripts\": {\n    \"lint\": \"eslint \\\"src/**/*.{js,ts,vue}\\\"\",\n    \"test\": \"jest\"\n  },\n  \"husky\": {\n    \"hooks\": {\n      \"pre-commit\": \"lint-staged\",\n      \"commit-msg\": \"commitlint -E HUSKY_GIT_PARAMS\"\n    }\n  },\n  \"lint-staged\": {\n    \"*.{js,vue}\": [\n      \"eslint --fix\",\n      \"prettier --write\",\n      \"git add\"\n    ],\n    \"*.ts\": [\n      \"eslint --fix\",\n      \"prettier --write\",\n      \"tsc-files --noEmit\",\n      \"git add\"\n    ],\n    \"*.{json,md,css,scss}\": [\n      \"prettier --write\",\n      \"git add\"\n    ]\n  }\n}\n```\n\n这里有个我交过学费的点：**对 TypeScript 做类型检查不能直接用 `tsc --noEmit`**。`tsc` 接收单个文件名时会忽略 tsconfig.json，导致路径别名、全局类型都不认，报一堆假错。当时我对着这堆莫名其妙的报错查了半天才反应过来。解决办法是用 `tsc-files` 这类工具，它会保留 tsconfig 的语义，只是把检查范围限定在指定文件。如果嫌麻烦，干脆别在 pre-commit 做类型检查，丢给 CI 全量跑——类型检查本来也不快。\n\n## 规则要轻，别把 pre-commit 当 CI 用\n\npre-commit 的核心诉求是**快**，快到大家感觉不到它存在。我的经验是这道关只放这些：\n\n- `eslint --fix`：能自动修的直接修\n- `prettier --write`：格式问题机器解决，人别操心\n- 改动文件相关的类型检查（如果做得到增量）\n- 跟改动强相关的少量单元测试\n\n而下面这些，我试过往 pre-commit 塞，最后全部撤了：\n\n- 全量构建（webpack 打包一次几十秒到几分钟，提交一次等这么久没人能忍）\n- 全量 E2E（动辄几分钟，还可能 flaky，本地环境也未必齐全）\n- 慢的集成测试、跨服务测试\n\n有阵子我图省事，把 `jest`（全量）挂在了 pre-commit，结果随着用例越来越多，提交越来越慢，同事的 `--no-verify` 又回来了。后来我改成只用 jest 的 `--findRelatedTests`，让它只跑跟本次改动文件相关的测试：\n\n```json\n{\n  \"lint-staged\": {\n    \"*.{js,ts}\": [\n      \"eslint --fix\",\n      \"jest --bail --findRelatedTests\",\n      \"git add\"\n    ]\n  }\n}\n```\n\n`--findRelatedTests` 会根据模块依赖图找出受影响的测试文件，`--bail` 让它一失败就停。这样提交一个文件通常只跑几个相关用例，几秒钟搞定。慢的、全量的，统统留给 CI。\n\n一句话：**提交前的检查要快，CI 要完整，两者职责不同，别混。**\n\n## 关于 `--no-verify` 和绕过这件事\n\nGit 给了逃生口：\n\n```bash\ngit commit --no-verify\n```\n\n加上它就跳过所有 pre-commit / commit-msg 钩子。我不主张完全禁止它——有时候你就是要提交一个半成品 WIP 临时存一下，或者紧急修复线上故障，没工夫等检查。逃生口该留。\n\n但如果你发现团队里**天天有人在 `--no-verify`**，那不是人的问题，是配置的问题。要么 hook 太慢，要么规则太激进（比如把一些有争议的、还在讨论的 lint 规则设成了 error 级别，逼着大家就范）。这种时候正确的做法是回头优化规则和速度，而不是发文件三令五申禁止绕过。工具应该让正确的路成为最省事的路，靠纪律去对抗工具的别扭，长期一定输。\n\n我们当时就专门花了半天，把几条争议大的规则从 error 降到 warn，把全量测试换成增量，`--no-verify` 的使用频率肉眼可见地降下来了。\n\n## 本地 hook 不是安全边界，CI 才是\n\n这点必须说清楚，因为我见过有人装了 husky 就以为高枕无忧，把 CI 的检查给砍了。**千万别。**\n\n本地 hook 有太多可以绕开的方式：有人没跑 `npm install`，hook 根本没装上；有人用的 Git 客户端（某些 GUI 工具）不走标准 hook；有人直接 `--no-verify`；甚至有人本地 Node 版本不对，hook 静默失败。本地 hook 是**便利**，不是**保障**。\n\n真正的关卡在 CI。CI 在一个干净、统一、你控制的环境里跑完整检查，谁也绕不过去：\n\n```bash\nnpm ci\nnpm run lint\nnpm test\nnpm run build\n```\n\n我们的分支保护规则要求这条流水线必须绿才能合并。本地 hook 负责把九成的低级问题在提交那一刻就拦掉、给开发者最快的反馈，CI 负责最终把关、对所有人一视同仁。两者叠加，既快又稳。\n\n把低级格式问题交给 husky + lint-staged，能让 code review 把时间花在设计与逻辑上。原则很简单：检查要快、规则要小且明确、能自动修的就自动修。别试图把 CI 的全部检查挪到 pre-commit，职责分清楚才能长期奏效。\n"},"recommendedPosts":[{"title":"AI 辅助编程技巧与踩坑：一些我现在不会再省略的步骤","date":"2026-05-05","excerpt":"不聊工具评测，记录几次和 AI 协作写代码后留下来的实用习惯：先给上下文，先讲约束，先读代码和拆计划，每轮改完都要测试。","tags":["AI","构建与模块化"],"slug":"ai-assisted-coding-tips-pitfalls-2026","category":"2026","likeCount":42,"postId":"70a2ea27-98a3-a396-d498-03c47cd4e067","viewCount":653,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":51,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":267,"readTime":null,"coverImage":null},{"title":"TypeScript 7 Beta 用 Go 重写之后：我更关心反馈链路有没有变短","date":"2026-04-28","excerpt":"从大型前端项目的类型检查、编辑器响应和 CI 等待说起，聊聊我会怎么评估 TypeScript 7 Beta 的性能收益和升级风险。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"frontend-typescript-7-go-2026","category":"2026","likeCount":25,"postId":"c582b9f3-9184-63db-3ced-7b4c1e26ca12","viewCount":297,"readTime":null,"coverImage":null}],"prevPost":{"title":"Babel 7 升级记录：语法转换越来越像基础设施","date":"2018-09-03","excerpt":"记录 Babel 7 升级中的包名变化、preset-env、polyfill 边界和老项目迁移时容易忽略的问题。","tags":["JavaScript","构建与模块化"],"slug":"babel7-upgrade-notes-2018","category":"2018","likeCount":6,"postId":"ff85b64c-e1bb-d60f-032d-6f1a714a1441","viewCount":2369,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue CLI 3 项目体验：脚手架变轻以后，配置反而要更清楚","date":"2018-08-20","excerpt":"从实际项目角度记录 Vue CLI 3 的使用体验，包括插件化配置、vue.config.js、环境变量、打包目录和团队协作中的取舍。","tags":["Vue","构建与模块化"],"slug":"vue-cli3-project-experience-2018","category":"2018","likeCount":8,"postId":"8d0b58ce-06ab-30c2-f8e3-142a48be97cb","viewCount":1711,"readTime":null,"coverImage":null}},"__N_SSG":true}