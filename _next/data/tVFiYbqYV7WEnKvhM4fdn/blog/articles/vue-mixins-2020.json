{"pageProps":{"postData":{"title":"Vue Mixins 用久了为什么容易乱：复用逻辑不等于把代码拼在一起","date":"2020-10-17","excerpt":"回顾 Vue Mixins 在项目里的使用体验，通过命名冲突、隐式来源和替代方案说明逻辑复用应该保持清楚边界。","tags":["Vue","组件与状态"],"slug":"vue-mixins-2020","category":"2020","likeCount":6,"postId":"d2a578f4-7ad3-2ca3-6b38-4f861e259bf4","viewCount":1224,"readTime":"6分钟","coverImage":null,"content":"\nVue 里做逻辑复用时，Mixins 是很自然会接触到的一种方式。\n\n刚开始用时它很顺手：提一段公共逻辑，多个组件混进来就能复用。可一旦项目做久，很多团队都会感受到同一个问题：Mixin 好写，但不太好管。\n\n## Mixin 最大的问题是“隐式”\n\n组件引入一个 Mixin 后，表面上只是多了一行配置，实际上它可能带来：\n\n- data\n- methods\n- computed\n- 生命周期\n\n这些能力会直接混进当前组件里。\n\n功能上当然成立，但阅读代码的人很容易看不出来某个字段到底来自哪里。\n\n例如一个列表 Mixin 可能是这样：\n\n```javascript\nexport default {\n  data() {\n    return {\n      loading: false,\n      list: [],\n    };\n  },\n  methods: {\n    async fetchList() {\n      this.loading = true;\n      const result = await this.requestList();\n      this.loading = false;\n\n      if (!result.ok) {\n        this.errorMessage = result.message;\n        return;\n      }\n\n      this.list = result.data;\n    },\n  },\n};\n```\n\n组件里只看到：\n\n```javascript\nexport default {\n  mixins: [listMixin],\n};\n```\n\n但模板里却可以直接用 `loading`、`list`、`fetchList`。第一次读代码的人很难马上知道它们来自 Mixin、组件本身，还是另一个 Mixin。\n\n## 一开始省事，后面成本会上来\n\nMixin 常常在项目前期特别讨喜，因为它能很快解决：\n\n- 表单通用逻辑\n- 列表加载逻辑\n- 权限判断逻辑\n\n可随着 Mixin 越来越多，组件就开始出现两个典型问题：\n\n- 命名冲突\n- 来源不清\n\n这时调试体验会明显变差。\n\n命名冲突尤其隐蔽：\n\n```javascript\nconst pageMixin = {\n  data() {\n    return {\n      page: 1,\n    };\n  },\n  methods: {\n    reset() {\n      this.page = 1;\n    },\n  },\n};\n\nexport default {\n  mixins: [pageMixin],\n  data() {\n    return {\n      page: 10,\n    };\n  },\n  methods: {\n    reset() {\n      this.page = 10;\n    },\n  },\n};\n```\n\n不同选项的合并策略不完全一样。生命周期会合并执行，方法同名可能被覆盖，数据也会合并。项目里 Mixin 多了以后，行为来源会越来越难判断。\n\n把合并规则说细一点，因为冲突时全靠它：\n\n- 生命周期钩子：不会覆盖，会合并成数组依次执行，Mixin 的先跑，组件自己的后跑。所以一个组件可能有好几个 `created` 在排队跑，调试时打断点会反复进。\n- `data`：递归合并，键冲突时以组件自身的为准（上面例子里 `page` 最终是 10）。\n- `methods` / `computed` / `components`：合并成对象，键冲突时组件自身覆盖 Mixin。\n- `watch`：和钩子一样合并成数组都执行。\n\n最坑的是「组件覆盖 Mixin」这条规则的隐蔽性。我遇到过：一个 Mixin 提供了 `reset` 方法做完整重置，某个组件里碰巧也写了个 `reset` 只重置一半，于是这个组件的重置「看着对、其实漏了」，而别的组件没事。排查时根本不会怀疑 Mixin，因为代码里压根看不到那行被覆盖的方法。最后是全局搜 `reset` 才发现撞名。多个 Mixin 之间同名方法的合并顺序按数组先后,后面的覆盖前面的,这又给「为什么是这个行为」加了一层迷雾。\n\n## 复用逻辑不能只看“抽没抽出来”\n\n真正稳定的复用，不只是把代码搬到另一个文件，而是要让依赖关系仍然清楚。\n\n如果一个组件用了多个 Mixin，后面想知道：\n\n- 哪个方法是谁提供的\n- 哪个生命周期里做了什么\n- 为什么这个字段会突然存在\n\n这些问题都会变得不那么直观。\n\n所以判断一个 Mixin 是否合适，可以看几个问题：\n\n- 它暴露了哪些字段和方法\n- 是否依赖组件必须实现某些方法\n- 是否会改组件已有状态\n- 是否包含异步请求或副作用\n- 多个 Mixin 一起用时是否会冲突\n\n如果这些问题需要靠口头约定才能说清楚，说明抽象已经不够透明。\n\n## 把逻辑拆成普通函数\n\n很多时候，普通函数比 Mixin 更清楚。\n\n```javascript\nexport async function requestJson(url) {\n  try {\n    const response = await fetch(url);\n\n    if (!response.ok) {\n      return { ok: false, message: '请求失败，请稍后重试' };\n    }\n\n    return { ok: true, data: await response.json() };\n  } catch (error) {\n    return { ok: false, message: '网络异常，请检查连接' };\n  }\n}\n```\n\n组件里显式调用：\n\n```javascript\nimport { requestJson } from '@/services/request';\n\nexport default {\n  data() {\n    return {\n      list: [],\n      errorMessage: '',\n    };\n  },\n  async created() {\n    const result = await requestJson('/api/list');\n\n    if (!result.ok) {\n      this.errorMessage = result.message;\n      return;\n    }\n\n    this.list = result.data;\n  },\n};\n```\n\n这样依赖关系很直观：请求函数来自哪里、返回什么结构、组件自己维护哪些状态，都能看见。\n\n跟 Mixin 比，差别就在「显式」两个字。`requestJson` 是从一个明确的路径 import 进来的，鼠标点过去就能跳到定义；返回值结构 `{ ok, data, message }` 写在那一目了然；组件自己的 `list`、`errorMessage` 也实打实声明在 data 里。这三件事 Mixin 全是隐式的——你得知道有这么个 Mixin、得去翻它内部、还得脑补合并后的结果。普通函数把这些都摆到了明面上。\n\n代价是普通函数没法帮你管组件的状态（loading、list 这些响应式数据还得组件自己声明），也接不进生命周期。这恰恰是它的边界：纯逻辑、纯计算、纯请求，普通函数最合适；一旦涉及「要往组件实例上挂响应式状态」或「要挂生命周期」，普通函数就力不从心了，这也是为什么后面会聊到组件组合和组合式函数。\n\n## 用组件组合替代一部分 Mixin\n\n有些 Mixin 其实是在复用 UI 行为，比如加载、空状态、错误状态。它们更适合抽成组件。\n\n```vue\n<StateView :loading=\"loading\" :error=\"errorMessage\" :empty=\"list.length === 0\">\n  <UserList :items=\"list\" />\n</StateView>\n```\n\n`StateView` 负责展示状态，页面负责拿数据。这样比把 `loading`、`errorMessage`、`fetchList` 全部混进组件更清楚。\n\n我以前真有过一个 `listMixin`，里面塞了 loading 状态、空状态判断、错误文案、重试按钮逻辑，混进十几个列表页。一开始爽，后来要改空状态的样式，得确认这十几个页面都能接受同一套改动，改完还得一个个验。后来把展示层抽成 `StateView` 组件，状态长什么样集中在一个组件里维护，改一处全生效，页面只管把 `loading`/`error`/`list` 当 props/slot 传进去。这就是「UI 复用用组件，逻辑复用用函数」的分工——Mixin 的问题恰恰是它把这两类东西糊在一起,既改不动状态展示，又看不清逻辑来源。\n\n判断一段 Mixin 该往哪拆，我的简单标准是：它主要在复用「长什么样」就抽组件，主要在复用「怎么算、怎么请求」就抽函数。两样都占的，先拆成这两半再说。\n\n## Vue 3 之后的组合式思路\n\nVue 3 正式发布之后，组合式函数给了我一个更明确的替代方向。如果项目使用 Vue 3，可以把复用逻辑显式整理成 composable：\n\n```javascript\nexport function useList(requestList) {\n  const loading = ref(false);\n  const list = ref([]);\n  const errorMessage = ref('');\n\n  async function fetchList() {\n    loading.value = true;\n    errorMessage.value = '';\n\n    const result = await requestList();\n\n    loading.value = false;\n\n    if (!result.ok) {\n      errorMessage.value = result.message;\n      return;\n    }\n\n    list.value = result.data;\n  }\n\n  return {\n    loading,\n    list,\n    errorMessage,\n    fetchList,\n  };\n}\n```\n\n调用方能清楚看到返回了什么，不像 Mixin 那样把字段隐式塞进组件实例。\n\n即使是 Vue 2 项目，也可以借鉴这种思路：让复用逻辑通过函数参数和返回值表达依赖，而不是依赖组件实例上“刚好存在”的字段。\n\n## Mixin 仍然可以用，但要小\n\nMixin 不是完全不能用。适合它的通常是短小、稳定、没有太多隐式依赖的逻辑。\n\n比如统一页面标题、简单埋点、极少量生命周期增强，可以保留。但如果一个 Mixin 同时带来数据、请求、校验、权限、跳转，就应该拆开。\n\n更务实的治理方式是：\n\n- 新逻辑少写 Mixin\n- 老 Mixin 不急着全量重构\n- 修改某个组件时顺手显式化依赖\n- 把通用请求和格式化先移到普通函数\n- 把 UI 状态抽成组件\n\n不要为了“去 Mixin”做大范围重写，风险通常不值得。\n\n\nMixin 当然能用，但它更适合短期、简单、边界清楚的逻辑复用。\n\n一旦项目越来越复杂，光“能复用”已经不够，来源和边界同样重要。代码复用如果把可读性搭进去，后面通常会还回来更多维护成本。\n\n复用逻辑的目标不是少写几行，而是让变化更好管理。能用普通函数说清楚的，就不要先交给 Mixin 隐式合并。\n"},"recommendedPosts":[{"title":"Vue 3 与 Pinia 状态管理实践：从 Vuex 迁移时真正要改的不是 API","date":"2022-04-18","excerpt":"结合 Vue 3 项目实践，讨论 Pinia 的状态组织、模块拆分、持久化边界、异步 action、SSR 和从 Vuex 迁移时的注意点。","tags":["Vue","前端基础","组件与状态"],"slug":"vue3-pinia-state-management-2022","category":"2022","likeCount":7,"postId":"a80f5fc3-79c3-d9e7-38e2-39ecfedb927f","viewCount":3379,"readTime":null,"coverImage":null},{"title":"组件 props 和 events 设计：少传大对象，少暴露内部细节","date":"2022-03-11","excerpt":"从 props 粒度、事件命名、双向绑定和插槽边界出发，讨论组件 API 如何更稳定。","tags":["前端基础","组件与状态","Vue"],"slug":"frontend-component-props-events-2022","category":"2022","likeCount":6,"postId":"674160e2-6d95-3167-ea47-f96214583df8","viewCount":3232,"readTime":null,"coverImage":null},{"title":"Vue3 Composition API 组织方式：别把 setup 写成新的杂物间","date":"2022-01-14","excerpt":"从状态、计算属性、副作用和 composable 拆分出发，梳理 Vue3 Composition API 的代码组织方式。","tags":["Vue","前端基础","组件与状态"],"slug":"frontend-vue3-composition-patterns-2022","category":"2022","likeCount":5,"postId":"3df92a46-9a3e-0682-3399-c1fa7d3294c1","viewCount":3421,"readTime":null,"coverImage":null}],"prevPost":{"title":"第一次用 Vite 跑 Vue 项目，最强烈的感受不是新，而是终于不用一直等","date":"2020-10-24","excerpt":"从一次真实的 Vue 项目迁移尝试出发，聊聊我第一次接触 Vite 时的直接感受，以及我后来怎样判断它的收益和边界。","tags":["Vue","构建与模块化"],"slug":"vue-vite-intro-2020","category":"2020","likeCount":5,"postId":"54e20394-58ff-95ad-0811-8c5dbb588074","viewCount":1692,"readTime":null,"coverImage":null},"nextPost":{"title":"Flex 布局入门：为什么它会成为前端布局的常用方案","date":"2020-10-08","excerpt":"从我实际写页面布局的经验出发，解释 Flex 为什么适合一维布局，以及主轴、收缩、间距和常见坑应该怎么理解。","tags":["前端基础"],"slug":"frontend-flex-layout-2020","category":"2020","likeCount":7,"postId":"60c8adf7-a756-c887-d019-a692d86f7a6b","viewCount":3049,"readTime":null,"coverImage":null}},"__N_SSG":true}