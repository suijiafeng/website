{"pageProps":{"postData":{"title":"Vue 生命周期别背顺序了：先弄清楚每个阶段该做什么","date":"2019-11-23","excerpt":"把 Vue 生命周期从死记硬背拉回到工程实践，讨论数据请求、DOM 操作、更新时机、keep-alive 和资源清理应该放在哪里。","tags":["Vue","组件与状态"],"slug":"vue-lifecycle-2019","category":"2019","likeCount":5,"postId":"11946d43-16f0-646e-ea58-e6095bbba5db","viewCount":3392,"readTime":"9分钟","coverImage":null,"content":"\n学 Vue 时，生命周期几乎是绕不过去的一章。\n\n很多人一开始会背：\n\n- `created`\n- `mounted`\n- `updated`\n- `destroyed`\n\n顺序当然要知道，但真正写项目时，更重要的问题其实是：哪些逻辑该放在哪个阶段。\n\n## 生命周期不是为了背名词\n\n生命周期存在的意义，是让你知道组件此刻处于什么状态。\n\n数据是不是已经初始化了，DOM 是不是已经挂载了，页面是不是刚更新完，这些信息会直接决定你在这个阶段能做什么。\n\n可以把生命周期理解成组件运行过程中的几个检查点。每个检查点能做的事不同，能拿到的资源也不同。\n\n## 先建立阶段感\n\n一个常见的 Vue 2 组件，大致会经历：\n\n- `beforeCreate`：实例刚开始初始化，数据和方法还没准备好\n- `created`：数据、方法、计算属性已经准备好，DOM 还没挂载\n- `mounted`：DOM 已经进入页面，可以访问真实节点\n- `updated`：响应式数据导致 DOM 更新完成\n- `beforeDestroy` / `destroyed`：组件准备销毁或已经销毁\n\n如果使用 `keep-alive`，还会遇到：\n\n- `activated`：缓存组件重新激活\n- `deactivated`：缓存组件被切走\n\n记顺序只是开始，关键是知道每个阶段的能力边界。不过顺序本身也不用死背，写个最小组件在每个钩子里打日志，跑一遍就清清楚楚：\n\n```js\nnew Vue({\n  el: '#app',\n  data: { msg: 'hi' },\n  template: '<p>{{ msg }}</p>',\n  beforeCreate() { console.log('beforeCreate', this.$el); },\n  created()      { console.log('created', this.$el); },\n  beforeMount()  { console.log('beforeMount', this.$el); },\n  mounted()      { console.log('mounted', this.$el); },\n  beforeUpdate() { console.log('beforeUpdate'); },\n  updated()      { console.log('updated'); },\n  beforeDestroy(){ console.log('beforeDestroy'); },\n  destroyed()    { console.log('destroyed'); },\n});\n```\n\n挂载阶段控制台会按这个顺序打印：`beforeCreate → created → beforeMount → mounted`。之后每次改 `msg` 触发的是 `beforeUpdate → updated`；组件销毁时是 `beforeDestroy → destroyed`（据说正在路上的 Vue 3 会把这两个钩子改名成 `beforeUnmount → unmounted`，语义更准，但眼下我们用的还是 Vue 2 这套）。\n\n更值得注意的是 `this.$el`：`beforeCreate` 和 `created` 里打印它是 `undefined`，到了 `mounted` 才是真实的 `<p>` 节点。这就直接验证了下面要说的——`created` 阶段拿不到 DOM，`mounted` 才能碰。\n\n还有一组容易被忽略的：`beforeMount`（挂载前，模板已编译但还没插入 DOM）和 `beforeUpdate`（数据变了、DOM 还没更新）。这俩平时用得少，但 `beforeUpdate` 偶尔有用——比如更新前想记录一下滚动位置，等 `updated` 再恢复，做「内容刷新但视口不跳」的效果。父子组件的钩子触发顺序也常被问：挂载时是父 beforeCreate/created/beforeMount → 子完整挂载 → 父 mounted，也就是「父先开始，子先完成，父后 mounted」。我调试嵌套组件初始化时序时，脑子里就靠这条顺序定位「为什么父组件 mounted 里拿子组件 ref 是有值的」。\n\n## created 更适合处理数据准备\n\n到了 `created`，组件的数据和方法已经准备好了，但真实 DOM 还没挂到页面上。\n\n所以这个阶段更适合：\n\n- 初始化数据\n- 发起请求\n- 做纯逻辑处理\n\n如果你只是要拿数据、准备状态，很多时候放在这里就够了。\n\n例如列表页初始化请求：\n\n```javascript\nexport default {\n  data() {\n    return {\n      loading: false,\n      list: [],\n      errorMessage: '',\n      destroyed: false,\n    };\n  },\n  created() {\n    this.fetchList();\n  },\n  beforeDestroy() {\n    this.destroyed = true;\n  },\n  methods: {\n    async fetchList() {\n      this.loading = true;\n      this.errorMessage = '';\n\n      const result = await this.requestList();\n\n      if (this.destroyed) {\n        return;\n      }\n\n      this.loading = false;\n\n      if (!result.ok) {\n        this.errorMessage = result.message;\n        return;\n      }\n\n      this.list = result.data;\n    },\n    async requestList() {\n      try {\n        const response = await fetch('/api/list');\n\n        if (!response.ok) {\n          return { ok: false, message: '列表加载失败，请稍后重试' };\n        }\n\n        return { ok: true, data: await response.json() };\n      } catch (error) {\n        return { ok: false, message: '网络异常，请检查连接' };\n      }\n    },\n  },\n};\n```\n\n这里把接口结果统一成 `{ ok, data, message }`，组件就不需要在多个地方写不同的异常文案。`destroyed` 标记用于避免组件销毁后继续写状态。\n\n这个 `destroyed` 标记不是多余的防御。我真踩过：列表页发请求还没回来，用户手快切走了组件，请求回来时 `this.list = ...` 往一个已销毁的实例上写，控制台报一串看不懂的警告，偶现还难复现。加了销毁标记直接 return 就干净了。更狠一点的做法是在 `beforeDestroy` 里 `cancelToken` 取消请求（用 axios 的话），从源头掐断。\n\n关于「请求放 created 还是 mounted」这个老问题，我的结论是：纯数据请求放 `created` 就行，能早一点发出去，不用等 DOM。网上有说放 mounted 的，理由是服务端渲染时 created 在服务端也会跑可能重复请求——但纯客户端项目没这顾虑，created 更早。如果请求结果要立刻操作 DOM（比如拿到数据后要算某个元素高度），那才需要等到 mounted 之后、甚至 `$nextTick` 之后。\n\n## mounted 才适合碰 DOM\n\n如果你要做的是：\n\n- 获取元素尺寸\n- 操作第三方 DOM 库\n- 依赖页面真实节点\n\n那就更适合放在 `mounted`。\n\n因为只有到了这个阶段，组件的 DOM 才真正进了页面。\n\n例如初始化第三方图表：\n\n```javascript\nexport default {\n  mounted() {\n    this.chart = createChart(this.$refs.chart);\n    this.chart.setOption(this.chartOption);\n  },\n  beforeDestroy() {\n    if (this.chart) {\n      this.chart.dispose();\n      this.chart = null;\n    }\n  },\n};\n```\n\n如果 DOM 依赖某个状态变化后的最新渲染结果，可以用 `$nextTick`：\n\n```javascript\nmethods: {\n  async openPanel() {\n    this.visible = true;\n\n    await this.$nextTick();\n    this.$refs.input.focus();\n  },\n},\n```\n\n`mounted` 代表初次挂载完成，`nextTick` 代表这次响应式更新对应的 DOM 已经刷新。两者解决的问题不同。\n\n这里要提醒一个经典误区：`mounted` 并不保证子组件全部挂载完。Vue 父组件的 `mounted` 触发时，它自己的 DOM 是好的，子组件通常也挂好了（因为子先 mounted），但如果子组件内部还有异步渲染（比如 `v-if` 依赖一个异步数据），那部分 DOM 在父的 mounted 里还拿不到。我吃过的亏是：在父 mounted 里直接量一个依赖异步数据撑开的容器高度，量到的是 0。解法就是把测量挪到数据回来后的 `$nextTick` 里，而不是赖在 mounted。\n\n`$nextTick` 还有个细节：它既能 `await`，也能传回调。我现在统一用 `await this.$nextTick()`，配合 async 方法读起来顺，避免回调嵌套。\n\n## updated 不是补丁桶\n\n很多人一发现界面更新后要再做点事，就会想到 `updated`。它当然能用，但也最容易被滥用。\n\n因为一旦组件更新频繁，这个钩子也会跟着频繁触发。\n\n所以如果你只是想在某个字段变化后做动作，很多时候用 `watch` 会更明确，而不是把逻辑一股脑塞进 `updated`。\n\n例如不要在 `updated` 里判断一堆字段：\n\n```javascript\nupdated() {\n  if (this.keyword !== this.previousKeyword) {\n    this.fetchList();\n  }\n}\n```\n\n更清楚的方式是直接监听目标：\n\n```javascript\nwatch: {\n  keyword() {\n    this.fetchList();\n  },\n},\n```\n\n`updated` 更适合处理“任意更新后都要同步的 DOM 任务”，而不是业务状态变化的分发中心。即便要用，也要非常克制，避免在里面继续改响应式数据造成更新循环。\n\n## destroyed 别只想着“组件被删了”\n\n这个阶段真正要关注的是清理。\n\n例如：\n\n- 解绑事件\n- 清理定时器\n- 销毁第三方实例\n\n这些事情如果不做，页面看起来可能没马上出问题，但后面很容易留下隐患。\n\n典型清理包括：\n\n```javascript\nexport default {\n  mounted() {\n    window.addEventListener('resize', this.handleResize);\n    this.timer = window.setInterval(this.poll, 5000);\n  },\n  beforeDestroy() {\n    window.removeEventListener('resize', this.handleResize);\n    window.clearInterval(this.timer);\n  },\n};\n```\n\n如果组件里有订阅、WebSocket、第三方实例、定时器、全局事件，基本都应该有对应清理。\n\n这块是内存泄漏的重灾区。最隐蔽的是事件解绑——`addEventListener` 和 `removeEventListener` 必须传同一个函数引用。我见过有人这么写：\n\n```javascript\nmounted() {\n  window.addEventListener('resize', this.handleResize.bind(this));\n},\nbeforeDestroy() {\n  window.removeEventListener('resize', this.handleResize.bind(this));\n}\n```\n\n看着对称，其实两个 `.bind(this)` 生成的是两个不同的函数，根本解不掉，监听器一直挂着。正确做法是把方法定义在 `methods` 里（自动绑定 this），两边传同一个 `this.handleResize`。这种泄漏在单页应用里反复进出页面会越积越多，表现是越用越卡，特别难定位。\n\n定时器也是，`setInterval` 不清，组件销毁了它还在跑，里面如果引用了已销毁实例的数据，又是一串报错。`$on` 注册的自定义事件、第三方库（图表、地图、富文本编辑器）的实例 `dispose`/`destroy`，全都得在 `beforeDestroy` 里收尾。我的习惯是：mounted 里建了什么，beforeDestroy 里就对称地拆什么，一一对应着写，漏不掉。\n\n## keep-alive 下要注意激活和停用\n\n被 `keep-alive` 包住的组件不会每次切换都销毁，所以 `created` 和 `mounted` 不会反复执行。\n\n如果你希望页面每次回到前台都刷新，可以用 `activated`：\n\n```javascript\nexport default {\n  activated() {\n    this.refreshIfNeeded();\n  },\n  deactivated() {\n    this.pauseVideo();\n  },\n};\n```\n\n这类页面最容易出问题的地方是：你以为组件重新创建了，实际上它只是从缓存里恢复。列表滚动位置、筛选条件、定时器状态，都要根据业务决定是保留还是重置。\n\n我在带 keep-alive 的列表-详情场景里踩过一个很典型的坑：从列表点进详情，改了某条数据状态，返回列表，列表还是旧的，因为列表组件被缓存了、`created` 不再执行。当时第一反应是「数据没刷新是不是接口缓存了」，查了半天才想起来是 keep-alive。解法就是把刷新逻辑从 `created` 挪到 `activated`，每次激活时按需拉一下。\n\n但又不能无脑每次 activated 都重新请求，否则用户只是切个 tab 看一眼又回来，列表整个重刷、滚动位置丢了，体验更差。所以我一般加个「脏标记」：详情页改了数据就往一个共享状态里打个标，列表 `activated` 时看到标记才刷新，没标记就用缓存。`activated`/`deactivated` 配合定时器也要小心——`deactivated` 时该暂停轮询，否则后台缓存的页面还在偷偷发请求，用户根本看不到。这些都是 keep-alive 把「销毁/重建」换成「停用/激活」之后必须重新审视的点。\n\n## 生命周期不是业务架构\n\n生命周期只是时机，不应该成为业务逻辑的唯一组织方式。\n\n如果 `created` 里有几十行请求、权限、格式化和状态拼装，问题不在生命周期，而在逻辑没有拆分。可以把请求放到 service，把格式化放到工具函数，把复杂状态拆到子组件或组合函数。\n\n生命周期里最好只做调度：\n\n```javascript\ncreated() {\n  this.initPage();\n},\nmethods: {\n  async initPage() {\n    await this.loadUser();\n    await this.loadList();\n  },\n},\n```\n\n它告诉读代码的人“什么时候开始”，而不是把所有细节都塞在钩子里。\n\n\nVue 生命周期真正难的地方，不是顺序，而是时机感。\n\n什么时候该拿数据，什么时候该碰 DOM，什么时候该做清理，只要这些边界分清楚，生命周期就不再是一串需要背诵的名字，而是你组织组件逻辑的坐标点。\n\n专业一点的写法是：`created` 准备数据，`mounted` 处理真实 DOM，`watch` 响应具体状态，`updated` 谨慎使用，销毁阶段彻底清理资源，`keep-alive` 单独考虑激活和停用。\n"},"recommendedPosts":[{"title":"Vue 3 与 Pinia 状态管理实践：从 Vuex 迁移时真正要改的不是 API","date":"2022-04-18","excerpt":"结合 Vue 3 项目实践，讨论 Pinia 的状态组织、模块拆分、持久化边界、异步 action、SSR 和从 Vuex 迁移时的注意点。","tags":["Vue","前端基础","组件与状态"],"slug":"vue3-pinia-state-management-2022","category":"2022","likeCount":7,"postId":"a80f5fc3-79c3-d9e7-38e2-39ecfedb927f","viewCount":3379,"readTime":null,"coverImage":null},{"title":"组件 props 和 events 设计：少传大对象，少暴露内部细节","date":"2022-03-11","excerpt":"从 props 粒度、事件命名、双向绑定和插槽边界出发，讨论组件 API 如何更稳定。","tags":["前端基础","组件与状态","Vue"],"slug":"frontend-component-props-events-2022","category":"2022","likeCount":6,"postId":"674160e2-6d95-3167-ea47-f96214583df8","viewCount":3232,"readTime":null,"coverImage":null},{"title":"Vue3 Composition API 组织方式：别把 setup 写成新的杂物间","date":"2022-01-14","excerpt":"从状态、计算属性、副作用和 composable 拆分出发，梳理 Vue3 Composition API 的代码组织方式。","tags":["Vue","前端基础","组件与状态"],"slug":"frontend-vue3-composition-patterns-2022","category":"2022","likeCount":5,"postId":"3df92a46-9a3e-0682-3399-c1fa7d3294c1","viewCount":3421,"readTime":null,"coverImage":null}],"prevPost":{"title":"Redis 缓存基础：接口快了，也要小心缓存穿透和雪崩","date":"2019-11-23","excerpt":"从接口性能和面试常题出发，整理 Redis 缓存、过期时间、缓存穿透、击穿、雪崩和前端缓存体验。","tags":["数据可视化与存储"],"slug":"redis-cache-basics-2019","category":"2019","likeCount":6,"postId":"a27bccd1-eb63-44dc-9ee2-cecfe331d99d","viewCount":2744,"readTime":null,"coverImage":null},"nextPost":{"title":"Cookie、localStorage 和 sessionStorage：别什么都往本地存","date":"2019-11-18","excerpt":"从登录态、用户偏好和临时数据出发，整理 Cookie、localStorage、sessionStorage 的区别、安全边界和使用场景。","tags":["前端基础","JavaScript"],"slug":"browser-storage-cookie-localstorage-2019","category":"2019","likeCount":5,"postId":"351202e8-84fb-d783-8edc-9391c62170e8","viewCount":2865,"readTime":null,"coverImage":null}},"__N_SSG":true}