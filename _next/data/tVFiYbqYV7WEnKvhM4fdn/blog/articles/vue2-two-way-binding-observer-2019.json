{"pageProps":{"postData":{"title":"Vue 2 双向绑定原理：Object.defineProperty 背后的响应式","date":"2019-04-16","excerpt":"从 Vue 2 响应式踩坑出发，聊聊 Object.defineProperty、Observer、Dep、Watcher 和数组变更限制。","tags":["Vue","JavaScript"],"slug":"vue2-two-way-binding-observer-2019","category":"2019","likeCount":6,"postId":"de420d4e-a55c-c76b-b77e-4a557221a378","viewCount":2595,"readTime":"8分钟","coverImage":null,"content":"\nVue 用起来很顺手，改了 data，页面就更新。\n\n面试里经常会问：Vue 双向绑定是怎么实现的？如果只回答“数据劫持加发布订阅”，通常还不够。至少要知道 `Object.defineProperty` 做了什么，依赖怎么收集，为什么有些数组和对象改动不会触发更新。\n\n## Object.defineProperty\n\n基础例子：\n\n```js\nvar obj = {}\nvar value = 'Tom'\n\nObject.defineProperty(obj, 'name', {\n  get: function() {\n    console.log('get name')\n    return value\n  },\n  set: function(newValue) {\n    console.log('set name')\n    value = newValue\n  }\n})\n\nconsole.log(obj.name)\nobj.name = 'Jerry'\n```\n\n通过 getter 和 setter，可以拦截属性读取和设置。\n\nVue 2 的响应式核心就建立在这个能力上。这里有个常被忽略的点：`Object.defineProperty` 是逐个属性劫持的。Vue 初始化时要递归遍历 data 里的每一个属性，给每个都装上 getter/setter。属性层级越深、对象越大，初始化成本越高。所以 data 别一上来就塞一个几百个字段的大对象，尤其是那种从后端直接拿来当 state 用的庞大响应体，能不响应式的部分（比如纯展示、不会变的常量配置）可以 `Object.freeze` 掉，Vue 检测到冻结对象就不去劫持了，省一笔开销。这也是为什么 Vue 3 后来要换 Proxy——Proxy 能直接代理整个对象，不用一开始就递归劫持每个 key。\n\n## 简单响应式\n\n```js\nfunction defineReactive(obj, key, value) {\n  Object.defineProperty(obj, key, {\n    get: function() {\n      return value\n    },\n    set: function(newValue) {\n      if (newValue === value) return\n\n      value = newValue\n      updateView()\n    }\n  })\n}\n\nfunction observe(obj) {\n  Object.keys(obj).forEach(key => {\n    defineReactive(obj, key, obj[key])\n  })\n}\n```\n\n使用：\n\n```js\nvar data = {\n  name: 'Tom'\n}\n\nobserve(data)\n\ndata.name = 'Jerry'\n```\n\n这个版本非常简化，但能说明：数据变化时，setter 里可以通知视图更新。\n\n## 依赖收集\n\n真实框架不能每次数据变化都更新整个页面。它需要知道哪些地方用到了这个数据。\n\n可以简单理解为：\n\n- getter 负责收集依赖\n- setter 负责通知依赖更新\n\n```js\nclass Dep {\n  constructor() {\n    this.subs = []\n  }\n\n  depend(watcher) {\n    this.subs.push(watcher)\n  }\n\n  notify() {\n    this.subs.forEach(watcher => watcher.update())\n  }\n}\n```\n\nWatcher 可以理解为一个观察者：\n\n```js\nclass Watcher {\n  update() {\n    console.log('update view')\n  }\n}\n```\n\n面试里不一定要完整写 Vue，但要能说清楚：读取数据时收集 watcher，修改数据时通知 watcher。\n\n把 Dep 和 defineReactive 串起来才完整。每个响应式属性身上挂一个自己的 `Dep` 实例，getter 里把「当前正在求值的 watcher」收进去，setter 里通知这个 Dep 下所有 watcher：\n\n```js\nfunction defineReactive(obj, key, value) {\n  const dep = new Dep()\n\n  Object.defineProperty(obj, key, {\n    get() {\n      if (Dep.target) {        // 当前有 watcher 在求值\n        dep.depend(Dep.target)\n      }\n      return value\n    },\n    set(newValue) {\n      if (newValue === value) return\n      value = newValue\n      dep.notify()             // 通知这个属性的所有依赖\n    }\n  })\n}\n```\n\n`Dep.target` 是个全局变量，指向「此刻正在读数据的那个 watcher」。Watcher 求值前把自己挂到 `Dep.target`，触发 getter 就被收集，求值完再置空。这就是依赖收集的关键机制：靠一次「主动读取」把数据和用它的地方关联起来。\n\n```js\nlet uid = 0\nclass Watcher {\n  constructor(vm, expOrFn, cb) {\n    this.vm = vm\n    this.getter = expOrFn\n    this.cb = cb\n    this.value = this.get()\n  }\n  get() {\n    Dep.target = this          // 把自己挂上去\n    const value = this.getter.call(this.vm, this.vm)\n    Dep.target = null          // 用完置空，避免误收集\n    return value\n  }\n  update() {\n    const oldValue = this.value\n    this.value = this.get()\n    this.cb.call(this.vm, this.value, oldValue)\n  }\n}\n```\n\n面试时能讲到「getter 里 `Dep.target` 收集、setter 里 `notify`、Watcher 求值前后挂载/清空 target」这条链路，基本就说明你是真懂而不是背的。我自己当年也是手撸了一遍这个迷你版才彻底想通——为什么只是读一下数据就能建立依赖关系，关键就在 `Dep.target` 这个全局指针。\n\n顺带提一句更新是异步批量的。setter 通知 watcher 后，Vue 不会立刻同步更新 DOM，而是把 watcher 推进一个队列去重，在下一个 tick 统一刷新。所以你连续改十次同一个数据，视图也只更新一次。这也是 `this.$nextTick` 存在的原因：改完数据想拿到更新后的 DOM，得等下一个 tick。\n\n## 为什么新增属性不响应\n\n```js\nvar vm = new Vue({\n  data: {\n    user: {\n      name: 'Tom'\n    }\n  }\n})\n\nvm.user.age = 18\n```\n\n`age` 是后面新增的，初始化时没有被 `defineProperty` 劫持，所以不是响应式。\n\n要用：\n\n```js\nVue.set(vm.user, 'age', 18)\n```\n\n或者一开始就在 data 里声明完整结构。\n\n这也是写 Vue 表单时常见的坑。后端返回什么字段就直接塞进去，后面新增字段不更新，排查会很烦。\n\n我吃过最闷的一次是动态表单：根据配置渲染若干字段，用 `this.formData[field] = ''` 初始化，结果新加的字段输入框打字没反应。盯着代码看半天，逻辑没毛病，就是不响应。后来才反应过来 `formData` 里这些 key 是后加的，没被劫持。改成 `this.$set(this.formData, field, '')`（组件里 `Vue.set` 的别名）就好了。\n\n要批量加多个字段，别一个个 `$set`，那样性能差，直接整体替换一个新对象更干脆：\n\n```js\nthis.formData = { ...this.formData, ...newFields }\n```\n\n整体赋值会触发 `formData` 这个属性自己的 setter，新对象会被重新 observe 一遍，所有字段都是响应式的。\n\n删属性同理，`delete obj.key` 不会触发更新，得用 `Vue.delete` 或 `this.$delete`。\n\n## 数组的限制\n\n直接改数组索引：\n\n```js\nvm.list[0] = newValue\n```\n\n可能不会触发更新。\n\n直接改长度：\n\n```js\nvm.list.length = 0\n```\n\n也可能不响应。\n\n推荐：\n\n```js\nVue.set(vm.list, 0, newValue)\n```\n\n或者使用会被 Vue 包装过的方法：\n\n```js\nvm.list.push(item)\nvm.list.splice(index, 1, newValue)\n```\n\nVue 2 对数组做了方法拦截，但无法像 Proxy 那样天然拦截所有操作。\n\n具体怎么拦截的，面试也常追问。Vue 重写了数组原型上 7 个会改变原数组的方法：`push`、`pop`、`shift`、`unshift`、`splice`、`sort`、`reverse`。做法是造一个继承自 `Array.prototype` 的拦截原型，把这 7 个方法包一层，在原方法执行后手动触发 `dep.notify()`，再把响应式数组的原型指向这个拦截原型：\n\n```js\nconst arrayProto = Array.prototype\nconst arrayMethods = Object.create(arrayProto)\n\n;['push', 'pop', 'shift', 'unshift', 'splice', 'sort', 'reverse'].forEach(method => {\n  const original = arrayProto[method]\n  Object.defineProperty(arrayMethods, method, {\n    value(...args) {\n      const result = original.apply(this, args)\n      // 对新增的元素做 observe，再通知更新\n      // ...这里拿到该数组的 __ob__.dep.notify()\n      return result\n    }\n  })\n})\n```\n\n而 `arr[index] = x` 和 `arr.length = 0` 不在这 7 个方法里，自然拦不到。这就是为什么改索引、改长度不响应——不是 Vue 偷懒，是 `defineProperty` 给每个数组下标都装 getter/setter 既不现实也太贵，于是退而求其次只拦方法。`splice` 既能改索引又能改长度，所以官方推荐用它兜底。\n\n“`defineProperty` 只能拦已有 key、Proxy 能拦全部”这点可以直接在控制台跑一段对照看清楚。先看 `defineProperty`：\n\n```js\nconst obj = {};\nObject.defineProperty(obj, 'name', {\n  get() { console.log('get name'); return this._name; },\n  set(v) { console.log('set name =', v); this._name = v; },\n});\n\nobj.name = 'Jerry'; // 打印 set name = Jerry —— 已有 key，拦得到\nobj.age = 18;       // 什么都不打印 —— 新增 key，没装过 getter/setter，拦不到\n```\n\n`obj.name = 'Jerry'` 会打印 `set name = Jerry`，而 `obj.age = 18` 一声不响——因为 `age` 是后加的，初始化时没给它定义 setter。Vue 2 的“新增属性不响应”和“数组下标赋值不响应”，本质都是这一条。\n\n换成 Proxy，代理的是整个对象，新增 key、改数组下标都逃不掉：\n\n```js\nconst arr = new Proxy(['a'], {\n  set(target, key, value) {\n    console.log('set', key, '=', value);\n    return Reflect.set(target, key, value);\n  },\n});\n\narr[0] = 'x';   // 打印 set 0 = x   —— 下标赋值，Proxy 拦得到\narr.push('b');  // 打印 set 1 = b, set length = 2 —— push 也是底层在写下标和 length\n```\n\n`arr[0] = 'x'` 打印 `set 0 = x`，连 `push` 内部对下标和 `length` 的写入都会逐条触发 set。这正是 Vue 3 用 Proxy 之后 `Vue.set`/`$set` 能退休的原因：拦截层级从“单个 key”升到了“整个对象”。\n\n## v-model 不是魔法\n\n输入框：\n\n```html\n<input v-model=\"name\">\n```\n\n可以理解为：\n\n```html\n<input :value=\"name\" @input=\"name = $event.target.value\">\n```\n\n所谓双向绑定，就是数据到视图、视图事件再改数据。\n\n理解这一点后，自定义组件的 v-model 也好理解：\n\n```html\n<my-input :value=\"name\" @input=\"name = $event\" />\n```\n\n也就是说自定义组件想支持 `v-model`，默认得接收一个 `value` prop、并在变化时 `$emit('input', 新值)`：\n\n```js\nexport default {\n  props: ['value'],\n  methods: {\n    onChange(e) {\n      this.$emit('input', e.target.value)\n    }\n  }\n}\n```\n\n如果是 checkbox、单选这类语义，默认的 `value`/`input` 不合适，可以用 `model` 选项改掉：\n\n```js\nexport default {\n  model: { prop: 'checked', event: 'change' },\n  props: ['checked'],\n}\n```\n\n注意 prop 是单向的，组件内部不能直接改 `this.value`，得通过 emit 让父级去改，再流回来。这也是 Vue「单向数据流」的体现，所谓双向只是「下行绑定 + 上行事件」的语法糖，本质上数据流向始终是清晰单向的。这点想明白了，很多「子组件改了 prop 报警告」的困惑也就解开了。\n\n## 为什么后来要换 Proxy\n\n聊响应式绕不开这个对比。`Object.defineProperty` 的几个硬伤前面都遇到了：检测不到新增/删除属性、数组下标和 length 改动无感、初始化要递归劫持每个 key。这些不是 bug，是 API 本身的能力边界。\n\nVue 3 换成 `Proxy` 后，代理的是整个对象，新增删除属性、数组索引操作都能拦到，`Vue.set`/`Vue.delete` 这些补丁基本可以退休了，深层对象也改成读到才递归（惰性），初始化更快。代价是 Proxy 不兼容 IE11，而 2019 年还有不少项目得管 IE，所以那会儿大多数线上项目还是 Vue 2。理解 `defineProperty` 这套机制，不光是为了应付面试，也是为了搞清楚「为什么有些改动不更新」——多数时候问题不在 Vue，而在数据压根没被做成响应式。\n\n\nVue 2 响应式绕不开 `Object.defineProperty`、依赖收集和派发更新。\n\n面试问双向绑定，通常会继续问新增属性、数组索引、`Vue.set`、v-model 原理。项目里理解这些限制很重要，因为很多“不更新”的问题不是 Vue 坏了，而是数据一开始没有被做成响应式。\n"},"recommendedPosts":[{"title":"Vue 项目里的 axios 封装：别让每个页面自己处理接口错误","date":"2018-02-24","excerpt":"从 Vue 项目实践出发，整理 axios 请求封装、拦截器、错误处理、登录失效和 loading 状态的处理方式。","tags":["Vue","JavaScript","前端基础"],"slug":"vue-axios-request-wrapper-2018","category":"2018","likeCount":5,"postId":"7bdbf934-979b-7903-8458-6bae8834f951","viewCount":4630,"readTime":null,"coverImage":null},{"title":"看 ES2026 和 ESNext 新特性时，我更关心它什么时候能进业务代码","date":"2026-04-24","excerpt":"从不可变数据、资源管理和新语法落地说起，记录我如何判断 JavaScript 新特性是否适合进入真实项目。","tags":["前端基础","JavaScript"],"slug":"frontend-es2026-features","category":"2026","likeCount":10,"postId":"0ac5cfee-fe01-706f-b371-6fe7cc8af036","viewCount":1078,"readTime":null,"coverImage":null},{"title":"用 Biome 替掉 ESLint + Prettier：一次工具链瘦身的得与失","date":"2026-04-18","excerpt":"把项目的 lint 和格式化从 ESLint+Prettier 换成 Biome 的过程：速度提升有多明显、规则迁移缺了什么、以及哪些场景我还是留着 ESLint。","tags":["前端基础","构建与模块化","JavaScript"],"slug":"biome-toolchain-2026","category":"2026","likeCount":6,"postId":"37e39142-8bf2-d042-6270-2228c894603c","viewCount":1274,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 表单组件拆分经验：可复用输入框不是把所有 props 都塞进去","date":"2019-04-20","excerpt":"结合 Vue 表单场景，讨论输入组件、字段容器、校验提示和提交错误处理怎么拆，避免复用组件变成万能配置层。","tags":["Vue","组件与状态"],"slug":"vue-form-components-2019","category":"2019","likeCount":5,"postId":"d0db4543-db2c-450f-def4-3bbe57b7eec9","viewCount":4095,"readTime":null,"coverImage":null},"nextPost":{"title":"MySQL 索引基础：为什么加了索引，查询还是可能慢","date":"2019-04-11","excerpt":"从接口查询慢和面试常题出发，整理 MySQL 索引、B+ 树、联合索引、最左前缀、回表和索引失效。","tags":["数据可视化与存储"],"slug":"mysql-index-basics-2019","category":"2019","likeCount":7,"postId":"e75192d2-bf1d-0564-fdc7-b789cbc2934c","viewCount":1840,"readTime":null,"coverImage":null}},"__N_SSG":true}