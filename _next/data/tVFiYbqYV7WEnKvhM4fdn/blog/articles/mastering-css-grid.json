{"pageProps":{"postData":{"title":"CSS Grid 真正好用的地方：把二维布局写清楚","date":"2023-08-20","excerpt":"从一次后台仪表盘布局重写出发，聊聊 CSS Grid 在页面骨架、卡片列表、区域命名和响应式布局里的真实用法。","tags":["前端基础","CSS"],"slug":"mastering-css-grid","category":"2023","likeCount":7,"postId":"d85d9915-5fb3-ed85-72bc-53f6fc206b79","viewCount":3320,"readTime":"7分钟","coverImage":null,"content":"\nCSS Grid 是现代 CSS 里最适合做二维布局的工具。和 Flexbox 更偏向一维排列不同，Grid 可以同时处理行和列，因此非常适合页面骨架、卡片列表、仪表盘、图片墙和复杂表单。\n\n很多人学 Grid 时只记住了 `display: grid` 和 `repeat(3, 1fr)`，但真正写项目时，还需要理解轨道、间距、区域命名、自适应列和内容溢出这些细节。\n\n我真正开始频繁用 Grid，是做一个后台仪表盘。左边筛选区固定宽度，右边图表区自适应，下面还有一排统计卡片。早期用 Flexbox 加一堆百分比和媒体查询，越写越绕；换成 Grid 后，页面骨架一下清楚很多。Grid 的价值不只是“能排成网格”，而是能把二维结构直接写出来。\n\n## Grid 适合解决什么问题\n\n如果你只需要让几个按钮横向排列，Flexbox 通常更简单。如果你需要控制“这一块占两列，那一块固定宽度，主内容自适应”，Grid 会更自然。\n\n可以简单这样判断：\n\n- 一行或一列内部对齐：优先 Flexbox\n- 同时控制行和列：优先 Grid\n- 页面整体布局：优先 Grid\n- 小组件内部图标和文字排列：优先 Flexbox\n\n两者不是竞争关系。真实项目里经常是外层用 Grid 做结构，内层用 Flexbox 做对齐。\n\n我现在会把它们分工得很明确：页面级、区域级用 Grid；按钮组、标题和操作区、图标文字对齐用 Flexbox。不要为了证明自己掌握 Grid，把所有局部对齐也硬写成网格。\n\n## 创建一个基础网格\n\n要使用 Grid，先在容器上设置 `display: grid`，再定义列和间距：\n\n```css\n.cards {\n  display: grid;\n  grid-template-columns: repeat(3, 1fr);\n  gap: 20px;\n}\n```\n\n这段代码创建了三列等宽网格。`1fr` 表示占用一份剩余空间，`repeat(3, 1fr)` 是重复三次的简写。\n\n这里有个很值得动手验证的点：`grid-template-columns` 写的是 `repeat(3, 1fr)`，但浏览器算完之后会把它换算成具体像素。假设容器内容宽 660px、`gap: 20px`，那扣掉两道 20px 的缝，剩 620px 三等分，每列 206.667px 左右。在控制台读 `getComputedStyle(document.querySelector('.cards')).gridTemplateColumns`，返回的不是 `\"repeat(3, 1fr)\"` 而是算好的像素值，类似 `\"206.667px 206.667px 206.667px\"`——这是确认列宽分配是否如预期的最快办法。更直观的是在 DevTools 的 Elements 面板里，grid 容器旁会出现一个 `grid` 徽章，点开能在页面上叠加显示行线、列线和每条轨道的尺寸标注，`gap` 的真实间距也会标出来。顺带，`CSS.supports('display', 'grid')` 在现代浏览器里返回 `true`。\n\n现在更推荐使用 `gap`，而不是旧写法 `grid-gap`。旧写法仍然能工作，但 `gap` 同时适用于 Grid 和 Flexbox，语义更统一。\n\n## 用 minmax 做响应式卡片\n\n固定三列在桌面端没问题，但移动端会挤压内容。更实用的写法是 `repeat + minmax`：\n\n```css\n.cards {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));\n  gap: 20px;\n}\n```\n\n它的含义是：每张卡片最小 240px，空间足够时自动增加列数，空间不够时自动换行。\n\n这类写法非常适合文章卡片、商品卡片、相册列表。很多时候不需要写媒体查询，就能得到比较自然的响应式布局。\n\n不过 `minmax(240px, 1fr)` 里的最小值要按内容决定，不是随便写。卡片里如果有按钮组、标题和价格，240px 可能刚好；如果是复杂图表卡片，最小宽度可能要 320px。断点应该由内容撑不撑得住来决定。\n\n## auto-fit 和 auto-fill 的区别\n\n`auto-fit` 和 `auto-fill` 很像，但表现不完全一样。\n\n`auto-fill` 会尽可能创建可用列，即使某些列没有内容；`auto-fit` 会把空列折叠掉，让已有项目占满空间。\n\n大多数卡片列表更适合 `auto-fit`：\n\n```css\ngrid-template-columns: repeat(auto-fit, minmax(240px, 1fr));\n```\n\n如果你希望保留空轨道，比如做固定网格占位，才更可能使用 `auto-fill`。\n\n这俩的区别同样能眼见为实。拿一个 `width: 800px` 的容器，里面只放 2 个子项，列定义都用 `minmax(200px, 1fr)`：用 `auto-fill` 时，容器能塞下 4 列（800/200），多出来的 2 条空轨道会保留，于是两个子项各停在 200px 宽、右边空一大片；换成 `auto-fit`，空轨道被折叠成 0 宽，两个子项把 800px 平分、各撑到 400px。读 `getComputedStyle(el).gridTemplateColumns` 也能看出差别——`auto-fill` 那次会返回四段尺寸（含空轨道），`auto-fit` 返回的空轨道是 `0px`。\n\n## 网格区域让页面结构更清楚\n\n通过 `grid-template-areas`，可以为每个区域命名，直观地控制页面布局。\n\n```css\n.layout {\n  display: grid;\n  grid-template-columns: 240px 1fr;\n  grid-template-rows: auto 1fr auto;\n  grid-template-areas:\n    \"header header\"\n    \"sidebar main\"\n    \"footer footer\";\n  min-height: 100vh;\n}\n\n.header { grid-area: header; }\n.sidebar { grid-area: sidebar; }\n.main { grid-area: main; }\n.footer { grid-area: footer; }\n```\n\nHTML 结构可以保持清楚：\n\n```html\n<div class=\"layout\">\n  <header class=\"header\">Header</header>\n  <aside class=\"sidebar\">Sidebar</aside>\n  <main class=\"main\">Main Content</main>\n  <footer class=\"footer\">Footer</footer>\n</div>\n```\n\n这种写法特别适合后台系统、文档站和博客详情页。看 CSS 就能知道整体布局，不需要靠一堆嵌套 div 猜结构。\n\n## 响应式布局中重新排列区域\n\nGrid 的一个优势是可以在不同屏幕下重新安排区域。\n\n```css\n@media (max-width: 768px) {\n  .layout {\n    grid-template-columns: 1fr;\n    grid-template-areas:\n      \"header\"\n      \"main\"\n      \"sidebar\"\n      \"footer\";\n  }\n}\n```\n\n桌面端侧边栏在左侧，移动端可以放到正文后面。这样不需要改 HTML 结构，只调整 CSS 布局。\n\n不过要注意可访问性和阅读顺序。CSS 可以改变视觉顺序，但键盘焦点和屏幕阅读器通常仍然按 DOM 顺序走。不要为了视觉布局，把重要内容在 DOM 中放到不合理的位置。\n\n这点在移动端尤其明显。视觉上把侧边栏放到正文下面没问题，但 DOM 里如果侧边栏在正文前，键盘用户仍然会先经过一堆导航。布局可以重排，内容语义顺序还是要先想好。\n\n## 跨列与跨行\n\nCSS Grid 允许元素跨越多行或多列，可以使用 `grid-column` 和 `grid-row`。\n\n```css\n.featured-card {\n  grid-column: span 2;\n  grid-row: span 2;\n}\n```\n\n这段代码会让重点卡片占据更大的区域，常见于资讯首页、作品集和数据看板。\n\n也可以明确指定起止线：\n\n```css\n.hero {\n  grid-column: 1 / -1;\n}\n```\n\n`1 / -1` 表示从第一条网格线跨到最后一条网格线，常用于让标题、横幅或分组头占满整行。\n\n## 对齐：容器和项目要分清\n\nGrid 里有两组容易混淆的对齐属性。\n\n控制每个网格项在自己格子里的位置：\n\n```css\n.grid {\n  justify-items: center;\n  align-items: center;\n}\n```\n\n控制整个网格在容器里的位置：\n\n```css\n.grid {\n  justify-content: center;\n  align-content: start;\n}\n```\n\n简单记法是：`items` 管项目，`content` 管整张网格。单个项目还可以用 `justify-self` 和 `align-self` 单独调整。\n\n## 内容溢出要特别注意\n\nGrid 里一个常见坑是内容过长导致列被撑开。比如卡片标题是一串很长的英文或链接，可能把布局撑破。\n\n可以给网格项加上 `min-width: 0`：\n\n```css\n.card {\n  min-width: 0;\n}\n\n.card-title {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  white-space: nowrap;\n}\n```\n\n这在后台表格、卡片列表和两列布局里很常见。如果发现 `1fr` 没有按预期收缩，先检查子元素是否需要 `min-width: 0`。\n\n这个坑我踩过很多次。一个两列布局左边固定、右边 `1fr`，右侧表格怎么都把页面撑出横向滚动，最后发现是右侧 grid item 默认 `min-width: auto`，内容不愿意收缩。给右侧容器补上 `min-width: 0`，布局立刻正常。Grid 很强，但内容溢出规则要记住。\n\n## Grid 和 Flexbox 结合使用\n\nGrid 适合整体结构，Flexbox 适合局部对齐。\n\n```css\n.dashboard {\n  display: grid;\n  grid-template-columns: 260px 1fr;\n  gap: 24px;\n}\n\n.toolbar {\n  display: flex;\n  align-items: center;\n  justify-content: space-between;\n  gap: 12px;\n}\n```\n\n外层仪表盘使用 Grid 控制侧栏和内容区，工具栏内部使用 Flexbox 控制标题、筛选器和按钮。这种组合比强行只用一种布局方式更自然。\n\n近几年容器查询也让 Grid 更好用了。过去很多卡片布局只能看 viewport 宽度，现在可以根据组件容器宽度调整列数。一个组件放在主内容区是三列，放在侧栏是一列，这种场景用容器查询比全局媒体查询自然很多。\n\n```css\n.card-section {\n  container-type: inline-size;\n}\n\n@container (min-width: 720px) {\n  .card-section__grid {\n    grid-template-columns: repeat(3, 1fr);\n  }\n}\n```\n\n\n我后来判断要不要用 Grid，基本不看“能不能排出来”，而是看布局是不是天然有行和列。\n\n页面骨架、仪表盘、卡片墙、复杂表单，这些用 Grid 往往更顺；按钮组、标题栏、图标和文字对齐，还是 Flexbox 更轻。把两者分工清楚，比强行押一个技术更重要。\n\n`fr`、`minmax`、`auto-fit`、区域命名、跨行跨列和溢出处理这些东西掌握以后，很多原本靠嵌套和媒体查询硬撑的布局，会变得直观很多。Grid 最舒服的地方，是它让二维结构直接出现在 CSS 里，而不是藏在一堆宽度计算里。\n"},"recommendedPosts":[{"title":"跨文档 View Transitions：多页应用也能做出丝滑过渡了","date":"2026-04-04","excerpt":"同文档 View Transitions 之外，跨文档（MPA）过渡终于可用。记录我给一个传统多页站点加上页面切换动画的过程、view-transition-name 的配对和降级处理。","tags":["前端基础","CSS","交互与动效"],"slug":"view-transitions-cross-document-2026","category":"2026","likeCount":10,"postId":"54351a26-388d-b51e-ff65-3ae7fde9bbd4","viewCount":1068,"readTime":null,"coverImage":null},{"title":"原生 CSS 嵌套和 @scope：写了这么多年 Sass 之后的几点真实感受","date":"2026-03-22","excerpt":"原生嵌套和 @scope 落地后，我在项目里逐步去掉一部分 Sass 的过程：嵌套的坑、@scope 的下边界 donut scope、以及现在还不能完全替代预处理器的地方。","tags":["前端基础","CSS"],"slug":"css-scope-nesting-2026","category":"2026","likeCount":37,"postId":"a4a7366f-5967-f614-350b-d223d0a045f3","viewCount":872,"readTime":null,"coverImage":null},{"title":"Tailwind CSS v4 迁移记：新引擎、CSS 变量主题和那些不再需要的配置","date":"2026-01-24","excerpt":"从 v3 升到 v4 的真实迁移过程：Oxide 引擎带来的速度变化、配置从 JS 搬到 CSS、@theme 和 CSS 变量怎么用，以及几个升级时踩的坑。","tags":["前端基础","CSS","构建与模块化"],"slug":"tailwind-v4-migration-2026","category":"2026","likeCount":35,"postId":"c8484300-10ec-bf7b-ef1b-6a237cb4f4f2","viewCount":994,"readTime":null,"coverImage":null}],"prevPost":{"title":"滚动驱动动画入门：让页面动画跟着滚动走","date":"2023-08-24","excerpt":"滚动驱动动画能少写监听，但我会先看兼容、调试手段和性能预算，再决定用 CSS 时间线还是 JS。","tags":["前端基础","CSS"],"slug":"frontend-scroll-linked-animation-2023","category":"2023","likeCount":5,"postId":"922a678a-2fd4-856a-3b20-7dcd1c0141e6","viewCount":1607,"readTime":null,"coverImage":null},"nextPost":{"title":"WebSocket 重连和心跳：实时不等于一直在线","date":"2023-08-18","excerpt":"围绕 WebSocket 连接状态、心跳、重连退避、消息确认和降级策略，整理实时前端的实践细节。","tags":["前端基础","JavaScript"],"slug":"frontend-websocket-reconnect-2023","category":"2023","likeCount":74,"postId":"1f13fc9c-8ed7-c5a9-8271-2007a4d109c8","viewCount":954,"readTime":null,"coverImage":null}},"__N_SSG":true}