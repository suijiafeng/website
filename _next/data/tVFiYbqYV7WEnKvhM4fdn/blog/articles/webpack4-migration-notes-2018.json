{"pageProps":{"postData":{"title":"webpack 4 迁移笔记：从配置堆里把构建速度找回来","date":"2018-03-05","excerpt":"记录 webpack 4 迁移中的常见变化，包括 mode、optimization、splitChunks、loader 版本和构建排查思路。","tags":["构建与模块化"],"slug":"webpack4-migration-notes-2018","category":"2018","likeCount":5,"postId":"4c50bfa0-bf5f-2755-fc7e-694f4942eaed","viewCount":4881,"readTime":"6分钟","coverImage":null,"content":"\nwebpack 4 正式发布后，我把一个老项目从 webpack 3 迁了过去，最先感受到的不是新功能，而是配置少了一些。\n\n`mode`、默认优化、`splitChunks`、更清楚的生产开发区分，让很多以前手写的配置变得没那么必要。但迁移不是把版本号一改就结束。真正麻烦的地方在 loader、plugin、脚手架模板和团队已有配置之间的磨合。\n\n我这次迁移的项目是公司一个跑了快两年的后台系统，webpack 配置文件加起来六七百行，里面叠了好几任同事的\"祖传优化\"，谁也不敢动。借着升级 4 的机会才算把它重新梳理了一遍。说实话迁移本身花的时间不多，真正费劲的是搞清楚那些历史配置当初为什么加、现在还要不要留。下面这些都是我踩过坑、记在便签上的东西。\n\n## mode 是第一道分界线\n\nwebpack 4 最重要的变化之一是 `mode`：\n\n```js\nmodule.exports = {\n  mode: 'production'\n}\n```\n\n开发环境：\n\n```js\nmodule.exports = {\n  mode: 'development'\n}\n```\n\n它会影响默认优化行为。以前很多项目自己配 Uglify、DefinePlugin、Scope Hoisting，现在一部分默认能力可以交给 webpack。\n\n`mode: 'production'` 默认就开了 `UglifyJsPlugin`（压缩）、`Scope Hoisting`（作用域提升）、并把 `process.env.NODE_ENV` 设成 `\"production\"`；`mode: 'development'` 则关掉压缩、开启更友好的 sourcemap 和命名。我老项目里原本手动 `new webpack.DefinePlugin({ 'process.env.NODE_ENV': '\"production\"' })` 那一行，迁移后直接删掉了，因为 mode 已经帮你做了——留着不会报错，但属于冗余。\n\n有个坑要提醒：如果你**只**写了 `mode` 而没删旧的 `UglifyJsPlugin`，可能会出现压缩跑两遍、或者新旧插件版本打架的诡异情况。我的做法是先删掉明显重复的优化配置，让默认行为跑起来，再根据产物分析决定是否补配置。另外别忘了 webpack 4 如果不写 `mode`，构建时会甩一条黄字警告 `The 'mode' option has not been set`，并默认按 production 处理——这条警告我一开始没在意，结果开发环境莫名其妙被压缩了，debug 时傻眼，后来才反应过来是漏配了 mode。\n\n## splitChunks 替代 CommonsChunkPlugin\n\n老项目里常见：\n\n```js\nnew webpack.optimize.CommonsChunkPlugin({\n  name: 'vendor'\n})\n```\n\nwebpack 4 里更推荐使用 `optimization.splitChunks`：\n\n```js\nmodule.exports = {\n  optimization: {\n    splitChunks: {\n      chunks: 'all'\n    }\n  }\n}\n```\n\n刚迁移时不要急着写复杂规则。先用默认策略，看主包和 vendor 包是否合理。如果某个大库进入首屏主包，再针对性拆分。\n\n很多项目分包策略写得很复杂，但没人知道为什么。这样的配置后面最难维护。\n\n从 `CommonsChunkPlugin` 迁到 `splitChunks`，思路上有个根本变化值得说清楚。老的 `CommonsChunkPlugin` 是\"自上而下\"指定的：你得手动声明哪些是公共 chunk、谁的优先级高，配置之间还存在顺序依赖，多写几个 `CommonsChunkPlugin` 实例经常互相打架。`splitChunks` 是\"自动按规则提取\"的：你定的是一组条件（体积阈值、被引用次数、来源目录），webpack 自己去判断哪些模块满足条件该被拆出来。理解了这个差别，就不会再用写 `CommonsChunkPlugin` 的老思维去硬套新配置。\n\n迁移时我遇到一个具体问题：老项目用 `CommonsChunkPlugin` 单独抽了一个 `manifest`（运行时代码）chunk 来稳定 vendor 的 hash。webpack 4 里这个职责改由 `optimization.runtimeChunk` 接管：\n\n```js\nmodule.exports = {\n  optimization: {\n    runtimeChunk: 'single',\n    splitChunks: { chunks: 'all' },\n  },\n};\n```\n\n不把 runtime 单独抽出来的话，每次改业务代码，vendor 包的 hash 也可能跟着变，长缓存就白做了。这个点不写在 `splitChunks` 里，单独是一项配置，很容易迁移时漏掉。\n\n## loader 版本经常是坑\n\n迁移过程中，报错最多的往往不是 webpack 本身，而是 loader。\n\n比如 Babel 版本、`babel-loader` 版本、`vue-loader` 版本、`css-loader` 配置都有可能不兼容。遇到构建错误时，不要只盯着 webpack 版本，先看错误栈里实际报错的 loader。\n\n升级步骤可以保守一点：\n\n1. 升 webpack 和 webpack-cli\n2. 保持业务代码不动\n3. 逐个升级 loader\n4. 跑开发环境\n5. 跑生产构建\n6. 对比 dist 产物\n\n一次升级太多依赖，失败后很难定位。\n\n具体到这次，让我栽得最狠的是 `extract-text-webpack-plugin`。这个老牌的抽 CSS 插件在 webpack 4 下直接不兼容了，构建报一堆 `Tapable.plugin is deprecated` 之类的错。官方那时候的态度是别再修它了，改用 `mini-css-extract-plugin`。换的时候配置写法也变了，`ExtractTextPlugin.extract({...})` 那套要整个换成在 `module.rules` 里用 `MiniCssExtractPlugin.loader` 替代 `style-loader`：\n\n```js\nconst MiniCssExtractPlugin = require('mini-css-extract-plugin');\n\nmodule.exports = {\n  module: {\n    rules: [\n      {\n        test: /\\.css$/,\n        use: [MiniCssExtractPlugin.loader, 'css-loader'],\n      },\n    ],\n  },\n  plugins: [new MiniCssExtractPlugin({ filename: '[name].[contenthash:8].css' })],\n};\n```\n\n类似的还有 `vue-loader` 必须升到 15 并补 `VueLoaderPlugin`、`html-webpack-plugin` 也得升到兼容 4 的版本。这几个插件的版本卡点没事先列清楚，升级过程就是不停地\"改一个错冒出下一个错\"，特别消磨耐心。我后来的教训是：动手前先去翻一遍 webpack 4 的官方 migration guide，把已知不兼容的 plugin 列个清单，比边撞边查高效得多。\n\n## 构建速度要用数据看\n\n很多人觉得“升级后快了”或者“好像慢了”，但最好还是记录时间：\n\n```bash\ntime npm run build\n```\n\n也可以打开构建统计：\n\n```bash\nwebpack --profile --json > stats.json\n```\n\n再用分析工具看模块体积。\n\n我更关心两个指标：\n\n- 冷启动构建时间\n- 生产包体积\n\n如果构建快了但包体变大很多，也不算成功。\n\n`stats.json` 那个文件可以直接拖到官方的 webpack analyse 工具或者 `webpack-bundle-analyzer` 里看：\n\n```bash\nnpm install --save-dev webpack-bundle-analyzer\n```\n\n```js\nconst BundleAnalyzerPlugin = require('webpack-bundle-analyzer').BundleAnalyzerPlugin;\n\nmodule.exports = {\n  plugins: [new BundleAnalyzerPlugin({ analyzerMode: 'static' })],\n};\n```\n\n生成的矩形树图一眼就能看出谁占体积大头。我这次靠它抓到两个典型问题：一是 `lodash` 整包被打进来了（其实只用到几个方法，换成按需引入 `lodash/get` 就好），二是 `moment` 把全部语言包都带上了，体积比业务代码还大。这俩处理完，生产包直接小了一截。\n\n说到构建速度，webpack 4 配合 `cache-loader` 或者 `HardSourceWebpackPlugin` 做二次构建缓存，热更新会快很多。但缓存类插件我是有顾虑的——它偶尔会因为缓存脏了导致改了代码却不生效，排查起来反而浪费时间。所以我只在确实嫌慢的大项目上才开，开了也会在 CI 上禁用，保证线上构建始终是干净的。\n\n## 不要把所有优化都写进配置\n\nwebpack 4 默认优化已经比以前好不少。很多配置不是不能写，而是不应该无理由写。\n\n比如手动 vendor 分包：\n\n```js\ncacheGroups: {\n  vendor: {\n    test: /[\\\\/]node_modules[\\\\/]/,\n    name: 'vendors',\n    chunks: 'all'\n  }\n}\n```\n\n这种规则很常见，但它不一定适合所有项目。后台系统、官网、移动 H5 的加载模型不一样，分包策略也应该不一样。\n\n我的原则是：先默认，后分析，再微调。\n\n## Babel 和 browserslist 顺手统一\n\n迁移这种事难得动一次构建配置，我一般会顺带把 Babel 的目标也理一理。老项目里经常是 `.babelrc` 里硬编码了一堆 preset 和 target，各处还不一致。借这次机会我把目标浏览器统一收口到 `package.json` 的 `browserslist` 字段：\n\n```json\n{\n  \"browserslist\": [\n    \"> 1%\",\n    \"last 2 versions\",\n    \"not dead\"\n  ]\n}\n```\n\n好处是 `babel-preset-env`、`autoprefixer`、`postcss` 这些工具都会读同一份 `browserslist`，目标浏览器只在一个地方维护，不会出现\"Babel 编译到 ES5 了但 autoprefixer 还在按很老的浏览器加前缀\"这种各说各话的情况。这块和 webpack 4 没直接关系，但既然都把构建翻出来重整了，一次性收口最划算，省得以后再单独碰一回。\n\n把迁移当成一次清理和学习配置的机会：先保证能跑，再逐步删冗余并用数据驱动优化。别把所有优化一次性堆进配置，理解每项改动的原因比盲目追新更重要。\n"},"recommendedPosts":[{"title":"AI 辅助编程技巧与踩坑：一些我现在不会再省略的步骤","date":"2026-05-05","excerpt":"不聊工具评测，记录几次和 AI 协作写代码后留下来的实用习惯：先给上下文，先讲约束，先读代码和拆计划，每轮改完都要测试。","tags":["AI","构建与模块化"],"slug":"ai-assisted-coding-tips-pitfalls-2026","category":"2026","likeCount":42,"postId":"70a2ea27-98a3-a396-d498-03c47cd4e067","viewCount":653,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":51,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":267,"readTime":null,"coverImage":null},{"title":"TypeScript 7 Beta 用 Go 重写之后：我更关心反馈链路有没有变短","date":"2026-04-28","excerpt":"从大型前端项目的类型检查、编辑器响应和 CI 等待说起，聊聊我会怎么评估 TypeScript 7 Beta 的性能收益和升级风险。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"frontend-typescript-7-go-2026","category":"2026","likeCount":25,"postId":"c582b9f3-9184-63db-3ced-7b4c1e26ca12","viewCount":297,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 后台登录与权限：token 只是开始，路由守卫才是日常","date":"2018-03-08","excerpt":"从 Vue 后台登录权限落地经验出发，聊聊 token 存储、路由守卫、权限菜单、按钮控制和接口错误处理怎么串起来。","tags":["Vue","组件与状态"],"slug":"vue-jwt-login-permission-2018","category":"2018","likeCount":9,"postId":"36cc8414-41fd-dbbe-aef8-ce23959c2ce4","viewCount":3067,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue 项目里的 axios 封装：别让每个页面自己处理接口错误","date":"2018-02-24","excerpt":"从 Vue 项目实践出发，整理 axios 请求封装、拦截器、错误处理、登录失效和 loading 状态的处理方式。","tags":["Vue","JavaScript","前端基础"],"slug":"vue-axios-request-wrapper-2018","category":"2018","likeCount":5,"postId":"7bdbf934-979b-7903-8458-6bae8834f951","viewCount":4630,"readTime":null,"coverImage":null}},"__N_SSG":true}