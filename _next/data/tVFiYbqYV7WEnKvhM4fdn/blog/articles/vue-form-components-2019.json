{"pageProps":{"postData":{"title":"Vue 表单组件拆分经验：可复用输入框不是把所有 props 都塞进去","date":"2019-04-20","excerpt":"结合 Vue 表单场景，讨论输入组件、字段容器、校验提示和提交错误处理怎么拆，避免复用组件变成万能配置层。","tags":["Vue","组件与状态"],"slug":"vue-form-components-2019","category":"2019","likeCount":5,"postId":"d0db4543-db2c-450f-def4-3bbe57b7eec9","viewCount":4095,"readTime":"8分钟","coverImage":null,"content":"\n做表单页面时，很多团队都会很快开始抽输入组件。\n\n这方向没问题。文本框、下拉框、日期选择器这些东西确实值得复用。问题通常出在复用方式上：刚开始只是想统一样式，后面慢慢开始加校验、加联动、加提示、加布局控制，最后组件越做越胖。\n\n我经历过一个特别典型的演变。最早一个 `AppInput` 组件就三十行，干干净净。半年后它涨到了八百多行，props 列表能滚一屏。原因不是谁乱写，而是每个需求都\"合理\"：A 页面要在失焦时校验，加个 `validateOnBlur`；B 页面要前置图标，加个 `prefixIcon`；C 页面手机号要中间四位打码，加个 `maskRule`；运营活动页要个特殊的金色边框，加个 `theme`。每一次都只多一个 prop，看着无害，攒到最后这个组件成了团队里没人敢动的\"祖传屎山\"——改一个分支怕影响另外五个调用方，加测试又覆盖不全。这篇想说的就是怎么从一开始避免走到这一步。\n\n## 先统一外观，再考虑行为\n\n表单组件一开始最值得统一的，通常是这些：\n\n- label 展示\n- 错误提示\n- 间距\n- 基础交互样式\n\n如果一上来就把业务校验、字段联动、接口逻辑一起塞进去，组件很快就会失去边界。\n\n一个更稳的拆法是：基础输入组件只负责输入，字段容器负责 label 和错误展示，页面负责业务规则。\n\n```vue\n<!-- BaseInput.vue -->\n<template>\n  <input\n    class=\"base-input\"\n    :value=\"value\"\n    :placeholder=\"placeholder\"\n    @input=\"$emit('input', $event.target.value)\"\n    @blur=\"$emit('blur')\"\n  >\n</template>\n\n<script>\nexport default {\n  name: 'BaseInput',\n  props: {\n    value: {\n      type: String,\n      default: '',\n    },\n    placeholder: {\n      type: String,\n      default: '',\n    },\n  },\n};\n</script>\n```\n\n它不关心“这是手机号还是邮箱”，也不关心提交接口。它只是一个稳定的输入控件。\n\n字段容器可以单独处理表单布局：\n\n```vue\n<!-- FormField.vue -->\n<template>\n  <label class=\"form-field\">\n    <span class=\"form-label\">{{ label }}</span>\n    <slot />\n    <span v-if=\"error\" class=\"form-error\" role=\"alert\">{{ error }}</span>\n  </label>\n</template>\n\n<script>\nexport default {\n  name: 'FormField',\n  props: {\n    label: String,\n    error: String,\n  },\n};\n</script>\n```\n\n这样输入控件和字段布局互不绑定，组合起来也更灵活。\n\n这套两层结构里，`FormField` 用 `<slot />` 把内容交出去是关键。它只管\"左边一个 label、中间一块内容、下面一行错误\"，至于中间塞的是输入框、下拉框、还是一组单选按钮，它一概不管。所以下面这些写法它都接得住：\n\n```vue\n<FormField label=\"姓名\" :error=\"errors.name\">\n  <BaseInput v-model=\"form.name\" />\n</FormField>\n\n<FormField label=\"城市\" :error=\"errors.city\">\n  <BaseSelect v-model=\"form.city\" :options=\"cities\" />\n</FormField>\n\n<FormField label=\"性别\" :error=\"errors.gender\">\n  <label><input type=\"radio\" value=\"m\" v-model=\"form.gender\"> 男</label>\n  <label><input type=\"radio\" value=\"f\" v-model=\"form.gender\"> 女</label>\n</FormField>\n```\n\n布局的一致性（label 对齐、错误样式、间距）由 `FormField` 统一保证，内容的多样性由 slot 放开。这种\"用插槽换配置项\"的思路，是我后来对抗组件膨胀最常用的一招——能用结构表达的差异，就别用 prop 去配。一旦你发现自己在给组件加 `type=\"text|select|radio\"` 这种 prop，再配一堆 `if/else` 决定渲染什么，基本就是该用插槽的信号了。\n\n## props 多到一定程度，复用就开始走样\n\n一旦出现下面这种趋势，就要警觉了：\n\n- `showError`\n- `validateOnBlur`\n- `customRule`\n- `layoutMode`\n- `extraTip`\n- `beforeChange`\n\n参数越来越多，看起来像组件更通用了，实际上往往说明它开始同时服务太多场景。\n\n比如一个组件同时负责输入、布局、校验、接口、权限、联动，它最后会变成这样：\n\n```vue\n<SmartInput\n  v-model=\"form.phone\"\n  label=\"手机号\"\n  layout-mode=\"horizontal\"\n  :validate-on-blur=\"true\"\n  :remote-rule=\"checkPhone\"\n  :show-extra-tip=\"true\"\n  :disabled-by-permission=\"true\"\n/>\n```\n\n看起来调用很短，实际所有复杂度都藏进了组件内部。后面任何业务差异都会继续加 props。\n\n更清楚的方式是把差异留在页面：\n\n```vue\n<FormField label=\"手机号\" :error=\"errors.phone\">\n  <BaseInput\n    v-model=\"form.phone\"\n    placeholder=\"请输入手机号\"\n    @blur=\"validatePhone\"\n  />\n</FormField>\n```\n\n页面知道业务规则，组件负责稳定能力。这个边界会更耐用。\n\n判断一个 prop 该不该加，我有个朴素的标准：问自己\"这个 prop 描述的是组件的能力，还是某个页面的业务？\"。`placeholder`、`disabled`、`maxlength` 这种是能力，加；`remote-rule=\"checkPhone\"`、`disabled-by-permission` 这种是业务，不加——`checkPhone` 是这个页面的事，权限判断是这个角色的事，组件不该知道这些。\n\n还有一类伪装得很好的 prop 叫\"行为开关\"，比如 `validateOnBlur`、`showError`、`autoFormat`。它们危险在于：每加一个布尔开关，组件内部的分支就翻倍，`true/false` 的组合爆炸到后面根本测不过来。我的处理是，能让调用方自己用事件和插槽控制的行为，就别做成内部开关。比如\"失焦时校验\"，与其加 `validateOnBlur` 让组件内部决定要不要校验，不如组件老老实实 `@blur` 抛出去，让页面决定 blur 之后干什么。控制权交给页面，组件就永远只有一种行为，简单且可预测。\n\n## v-model 只是输入协议\n\n在 Vue 2 里，自定义组件支持 `v-model`，通常对应 `value` 和 `input`：\n\n```javascript\nexport default {\n  props: {\n    value: String,\n  },\n  methods: {\n    handleInput(event) {\n      this.$emit('input', event.target.value);\n    },\n  },\n};\n```\n\n这是一种输入协议，不代表组件应该顺手处理所有业务状态。\n\n如果一个输入组件内部私自维护一份值，又和父组件值同步，就很容易出现状态不同步。除非有明确原因，否则让父组件成为唯一数据来源会更简单。\n\n我栽过这个跟头。当时给一个金额输入框做\"自动格式化\"，想让用户输入 `1000` 显示成 `1,000`。图省事在组件内部 `data` 里存了个 `innerValue`，输入时格式化 `innerValue`，再 emit 纯数字给父组件。看着没问题，直到出现这个场景：父组件因为别的逻辑把金额重置成 `0`，但组件内部的 `innerValue` 还显示着 `1,000`，因为我没处理\"外部值变化时同步内部值\"。于是页面数据是 0、输入框显示 1,000，对不上。\n\n补救要么加 watch 监听 `value` 回写 `innerValue`，要么干脆不存内部状态——用 computed 把格式化做成纯展示：\n\n```javascript\nexport default {\n  props: { value: { type: [Number, String], default: '' } },\n  computed: {\n    // 展示用，永远从 value 派生，不存第二份真相\n    displayValue() {\n      return this.value === '' ? '' : Number(this.value).toLocaleString();\n    }\n  },\n  methods: {\n    handleInput(e) {\n      // 去掉千分位逗号，emit 纯数字\n      const raw = e.target.value.replace(/,/g, '');\n      this.$emit('input', raw);\n    }\n  }\n};\n```\n\n只要值的\"唯一真相\"始终在父组件，组件内部的所有东西都从它派生，就不会出现两份状态打架。\"单一数据源\"听起来是句正确的废话，但表单里 90% 的状态不同步 bug，根子都在偷偷存了第二份。\n\n## 校验逻辑放在哪里\n\n基础校验可以放在页面或表单层：\n\n```javascript\nfunction validateProfile(form) {\n  const errors = {};\n\n  if (!form.name.trim()) {\n    errors.name = '请输入姓名';\n  }\n\n  if (!/^1\\d{10}$/.test(form.phone)) {\n    errors.phone = '请输入有效手机号';\n  }\n\n  return errors;\n}\n```\n\n页面提交时使用：\n\n```javascript\nmethods: {\n  async submit() {\n    this.errors = validateProfile(this.form);\n\n    if (Object.keys(this.errors).length > 0) {\n      return;\n    }\n\n    const result = await this.saveProfile(this.form);\n\n    if (!result.ok) {\n      this.submitError = result.message;\n      return;\n    }\n\n    this.submitError = '';\n  },\n},\n```\n\n校验规则和业务字段强相关时，放在页面或表单模块里更直观。输入组件只需要把输入和失焦事件抛出来。\n\n把校验写成这样一个纯函数（输入 form、输出 errors 对象）有几个实打实的好处。一是好测——它不依赖组件、不依赖 DOM，单测里直接喂数据断言结果就行：\n\n```javascript\ntest('手机号格式错误时报错', () => {\n  const errors = validateProfile({ name: '张三', phone: '123' });\n  expect(errors.phone).toBe('请输入有效手机号');\n});\n```\n\n二是好复用——同一份规则，新建页和编辑页都能用，不用在两个组件里各抄一遍。三是触发时机灵活，整体提交时校验全部，单字段失焦时只挑那个字段的错误展示，规则本身不用动。\n\n实际项目里我会把校验再分两层：能在本地同步判断的（必填、格式、长度、两次密码一致）放纯函数里，提交前一把校验完，体验最好，用户不用等网络;需要查后端的（用户名是否已存在、手机号是否已注册）单独做成异步校验，挂在失焦或防抖之后，别和同步校验混在一起。混在一起的后果是，同步规则也得 await，本来即时的反馈被网络拖慢了。这两类校验性质不同，分开处理代码也清楚。当然 2019 年很多团队直接用 Element UI 的 `el-form` + `rules`，那套 `async-validator` 也能用，但我个人在规则复杂、联动多的表单里，更倾向手写纯函数，因为声明式 rules 一旦遇到\"A 填了 B 才必填\"这种跨字段联动就开始别扭。\n\n## 提交错误要统一\n\n表单最容易出现错误处理分散的问题。建议接口层返回统一结构：\n\n```javascript\nasync function postJson(url, payload) {\n  try {\n    const response = await fetch(url, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify(payload),\n    });\n\n    if (!response.ok) {\n      return { ok: false, message: '保存失败，请稍后重试' };\n    }\n\n    return { ok: true, data: await response.json() };\n  } catch (error) {\n    return { ok: false, message: '网络异常，请检查连接' };\n  }\n}\n```\n\n页面只处理 `ok`、`message` 和 `data`，不要在每个表单里重复写一套 `try...catch` 文案。\n\n不过统一文案有个边界要小心：后端的字段级校验错误别被这层\"友好文案\"吃掉。比如提交时后端返回 `{ field: 'email', message: '该邮箱已被注册' }`，这种带具体字段的错误应该回填到对应输入框下面，而不是笼统弹一句\"保存失败\"。我的做法是约定后端在 422 这类校验失败时返回结构化的字段错误，前端这层包装把它透传出去：\n\n```javascript\nif (response.status === 422) {\n  const body = await response.json();\n  // 把字段级错误带回去，让页面回填到对应 FormField\n  return { ok: false, fieldErrors: body.errors };\n}\n```\n\n页面拿到 `fieldErrors` 后合并进 `this.errors`，错误就精准落到了出问题的那个字段下面。统一错误处理是为了消灭重复的样板代码，不是为了把所有错误都碾成同一句话——该精确的地方还得精确。\n\n提交按钮的 loading 和防重复点击也顺手在这层兜了：提交期间禁用按钮、置 `submitting = true`，无论成功失败都在 `finally` 里复位，避免用户狂点导致重复创建数据。这种细节不做，测试环境发现不了，一上线遇到网络慢的用户就出脏数据。\n\n## 什么时候值得继续抽象\n\n抽象不是不能加，而是要等重复足够明确。\n\n比较适合继续抽象的内容：\n\n- 统一的字段间距\n- 统一的错误展示\n- 统一的输入尺寸\n- 统一的禁用和加载状态\n- 稳定的输入协议\n\n不适合过早塞进基础组件的内容：\n\n- 某个页面的接口请求\n- 某个业务的联动规则\n- 某个角色的权限判断\n- 某个活动页的特殊文案\n\n组件越基础，越应该稳定；业务越多变，越应该留在业务层。\n\n\nVue 表单组件做得稳不稳，关键不在于抽没抽，而在于抽象边界是不是克制。\n\n输入组件最适合统一的是外观和基础交互，不该顺手把整个业务逻辑也吞进去。复用是为了减轻页面压力，不是为了再造一个更复杂的配置层。\n\n稳妥的顺序是：先抽基础输入，再抽字段布局，最后根据真实重复抽表单能力。不要一开始就造一个能配置所有场景的万能组件。\n"},"recommendedPosts":[{"title":"Vue 3 与 Pinia 状态管理实践：从 Vuex 迁移时真正要改的不是 API","date":"2022-04-18","excerpt":"结合 Vue 3 项目实践，讨论 Pinia 的状态组织、模块拆分、持久化边界、异步 action、SSR 和从 Vuex 迁移时的注意点。","tags":["Vue","前端基础","组件与状态"],"slug":"vue3-pinia-state-management-2022","category":"2022","likeCount":7,"postId":"a80f5fc3-79c3-d9e7-38e2-39ecfedb927f","viewCount":3379,"readTime":null,"coverImage":null},{"title":"组件 props 和 events 设计：少传大对象，少暴露内部细节","date":"2022-03-11","excerpt":"从 props 粒度、事件命名、双向绑定和插槽边界出发，讨论组件 API 如何更稳定。","tags":["前端基础","组件与状态","Vue"],"slug":"frontend-component-props-events-2022","category":"2022","likeCount":6,"postId":"674160e2-6d95-3167-ea47-f96214583df8","viewCount":3232,"readTime":null,"coverImage":null},{"title":"Vue3 Composition API 组织方式：别把 setup 写成新的杂物间","date":"2022-01-14","excerpt":"从状态、计算属性、副作用和 composable 拆分出发，梳理 Vue3 Composition API 的代码组织方式。","tags":["Vue","前端基础","组件与状态"],"slug":"frontend-vue3-composition-patterns-2022","category":"2022","likeCount":5,"postId":"3df92a46-9a3e-0682-3399-c1fa7d3294c1","viewCount":3421,"readTime":null,"coverImage":null}],"prevPost":{"title":"数据库事务和 ACID：转账问题为什么总被拿来面试","date":"2019-05-07","excerpt":"从转账、订单和库存场景出发，整理事务、ACID、隔离级别、脏读、不可重复读、幻读和前端理解接口一致性的方式。","tags":["数据可视化与存储"],"slug":"database-transaction-acid-2019","category":"2019","likeCount":5,"postId":"65ac459b-9fd7-1492-7f1e-225dfda367d3","viewCount":4117,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue 2 双向绑定原理：Object.defineProperty 背后的响应式","date":"2019-04-16","excerpt":"从 Vue 2 响应式踩坑出发，聊聊 Object.defineProperty、Observer、Dep、Watcher 和数组变更限制。","tags":["Vue","JavaScript"],"slug":"vue2-two-way-binding-observer-2019","category":"2019","likeCount":6,"postId":"de420d4e-a55c-c76b-b77e-4a557221a378","viewCount":2595,"readTime":null,"coverImage":null}},"__N_SSG":true}