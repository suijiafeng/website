{"pageProps":{"postData":{"title":"前端主题切换怎么做：从深色模式到多主题","date":"2022-10-05","excerpt":"介绍前端主题切换的常见方案，说明 CSS 变量、全局 class、组件库主题和设计 token 分层之间的关系。","tags":["前端基础","CSS"],"slug":"frontend-theme-switching-2022","category":"2022","likeCount":5,"postId":"90af5be6-8a98-9853-df01-8c166e347755","viewCount":2611,"readTime":"12分钟","coverImage":null,"content":"\n主题切换是很多前端项目都会遇到的需求。\n\n最常见的是浅色模式和深色模式，也有一些后台系统会支持品牌色切换、多套皮肤或节日主题。\n\n它看起来是视觉问题，实际会影响 CSS 组织方式、组件封装和长期维护成本。\n\n主题系统做得好，后面增加深色模式、多品牌、节日皮肤都比较自然；做得不好，就会变成到处覆盖样式、到处写特殊判断。\n\n## 主题切换的核心是什么\n\n主题切换不是简单地把背景色改成黑色。\n\n真正需要管理的是一组设计变量：\n\n- 主色\n- 背景色\n- 文本色\n- 边框色\n- 阴影\n- 禁用态颜色\n- 成功、警告、错误等状态色\n\n如果这些颜色散落在各个 CSS 文件里，后面改主题会非常痛苦。\n\n所以主题切换的第一步不是写切换按钮，而是先定义 token。不要让组件直接依赖具体颜色，而是依赖语义变量。\n\n我现在会把 token 粗略分成三层：\n\n- 基础色阶：`blue-500`、`gray-100` 这类颜色原料\n- 语义 token：`color-text-primary`、`color-bg-panel` 这类业务语义\n- 组件 token：`button-bg`、`table-border` 这类组件细节\n\n基础色阶适合设计侧管理，业务组件更适合用语义 token。否则深色模式下继续写 `gray-100` 会很别扭，因为这个名字描述的是颜色本身，不描述它承担的角色。\n\n例如不要在组件里写：\n\n```css\n.button {\n  background: #1677ff;\n}\n```\n\n更好的方式是：\n\n```css\n.button {\n  background: var(--color-primary);\n}\n```\n\n这样组件关心的是“主色”，而不是某个固定色值。\n\n我自己是吃过亏才认真做这件事的。早期接手一个后台项目，色值全是硬编码，光是 `#1677ff` 这一个蓝色在工程里就出现了三十多处，还混着 `#1677FF`、`rgb(22,119,255)` 几种写法。后来要把主色从蓝改成品牌绿，我用编辑器全局替换都不敢一把梭，怕误伤注释和图片占位色，最后是一处一处肉眼确认改的，改完还漏了两个 hover 态，上线被设计师抓出来。那次之后我就定了规矩：业务代码里不允许出现裸色值，CI 里加了一条简单的正则检查，提交带 `#` 开头六位十六进制颜色的 CSS 就报警，逼着大家走变量。这条规则看着粗暴，但它确实把“随手写个颜色”的坏习惯掐掉了。\n\n## 推荐使用 CSS 变量\n\nCSS 变量很适合做主题系统。\n\n例如：\n\n```css\n:root {\n  --color-primary: #1677ff;\n  --color-bg: #ffffff;\n  --color-text: #1f2329;\n  --color-border: #dcdfe6;\n}\n\n.theme-dark {\n  --color-primary: #4096ff;\n  --color-bg: #141414;\n  --color-text: #f0f0f0;\n  --color-border: #303030;\n}\n```\n\n组件里只使用变量：\n\n```css\n.panel {\n  background: var(--color-bg);\n  color: var(--color-text);\n  border: 1px solid var(--color-border);\n}\n```\n\n切换主题时，只需要给根节点切换 class。\n\n变量命名最好按语义，而不是按颜色本身：\n\n```css\n:root {\n  --color-bg-page: #ffffff;\n  --color-bg-panel: #f8fafc;\n  --color-text-primary: #0f172a;\n  --color-text-secondary: #64748b;\n  --color-border-subtle: #e2e8f0;\n}\n```\n\n`--color-gray-100` 这类名字也有价值，但它更像基础色阶；组件更适合使用语义 token。否则深色模式下，`gray-100` 到底是浅灰还是深灰，会变得很别扭。\n\n我后来的做法是分两层：底下一层是“原始色板”，纯描述颜色本身，比如 `--gray-50` 到 `--gray-900`、`--blue-500`；上面一层是语义 token，引用色板：\n\n```css\n:root {\n  --gray-50: #f8fafc;\n  --gray-900: #0f172a;\n  --blue-500: #1677ff;\n\n  /* 语义层引用色板 */\n  --color-bg-page: var(--gray-50);\n  --color-text-primary: var(--gray-900);\n  --color-primary: var(--blue-500);\n}\n\n.theme-dark {\n  /* 深色模式只重新映射语义层，色板基本不动 */\n  --color-bg-page: var(--gray-900);\n  --color-text-primary: var(--gray-50);\n}\n```\n\n这样切主题时改的是“语义指向哪个色阶”，而不是改色值本身。色板像调色盘，语义层像贴在上面的标签。组件永远只碰语义层。这套分层一开始会觉得啰嗦，但当主题数量从 2 套涨到 4 套时，它的好处会非常明显——你不用再为每套主题重抄一遍所有色值，只维护那张映射表就行。\n\n## 用 class 控制当前主题\n\n实际项目里可以把主题 class 放在 `html` 或 `body` 上：\n\n```javascript\nfunction setTheme(theme) {\n  document.documentElement.classList.remove(\"theme-light\", \"theme-dark\");\n  document.documentElement.classList.add(`theme-${theme}`);\n  localStorage.setItem(\"theme\", theme);\n}\n```\n\n页面初始化时再读取用户选择：\n\n```javascript\nconst savedTheme = localStorage.getItem(\"theme\") || \"light\";\nsetTheme(savedTheme);\n```\n\n这样刷新页面后，主题也能保持一致。\n\n还可以结合系统偏好：\n\n```javascript\nfunction getInitialTheme() {\n  const savedTheme = localStorage.getItem('theme');\n\n  if (savedTheme) {\n    return savedTheme;\n  }\n\n  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;\n\n  return prefersDark ? 'dark' : 'light';\n}\n```\n\n如果用户没有主动选择，就跟随系统；一旦用户手动选择，就以用户选择为准。\n\n还要注意系统主题可能会变化。用户没有手动选择时，可以监听 `prefers-color-scheme`：\n\n```javascript\nconst media = window.matchMedia('(prefers-color-scheme: dark)');\n\nmedia.addEventListener('change', () => {\n  if (!localStorage.getItem('theme')) {\n    setTheme(media.matches ? 'dark' : 'light');\n  }\n});\n```\n\n如果用户已经手动选过主题，就不要再被系统变化覆盖。这个优先级要一开始想清楚，不然后面会出现“我明明选了浅色，系统一变又给我切回深色”的问题。\n\n这里有个细节我一开始没处理好：用户选的是“跟随系统”这个选项，还是明确选了“深色”，这两件事不一样。我最早只存 `light`/`dark`，结果用户白天选了浅色，晚上系统自动切深色，他期待页面也跟着变，但因为 `localStorage` 里压着一个 `light`，页面就一直是浅的，他还以为坏了。后来我把存储值改成三态：`light`、`dark`、`system`。选了 `system` 才去实时监听 `matchMedia`：\n\n```javascript\nconst media = window.matchMedia('(prefers-color-scheme: dark)');\n\nmedia.addEventListener('change', (e) => {\n  if (localStorage.getItem('theme') !== 'system') return;\n  applyTheme(e.matches ? 'dark' : 'light');\n});\n```\n\n顺带提一句，`matchMedia(...).addListener` 这个老 API 已经废弃了，要用 `addEventListener('change', ...)`，旧写法在新版 Safari 上会有兼容告警。\n\n## 避免首屏闪烁\n\n主题切换里常见问题是页面先显示浅色，随后 JavaScript 运行后再切到深色，用户会看到明显闪烁。\n\n解决思路是尽早在 HTML 加载阶段设置主题 class。可以在页面头部放一小段内联脚本：\n\n```html\n<script>\n  const theme = localStorage.getItem('theme') ||\n    (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');\n\n  document.documentElement.classList.add('theme-' + theme);\n</script>\n```\n\n这段脚本要尽量小，执行越早越好。它的目的不是承载业务逻辑，而是在 CSS 加载前给页面一个正确主题状态。\n\n另外可以给浏览器一个原生控件的主题提示：\n\n```css\n:root {\n  color-scheme: light;\n}\n\n.theme-dark {\n  color-scheme: dark;\n}\n```\n\n`color-scheme` 会影响滚动条、表单控件等浏览器原生 UI 的默认配色。它不能替代业务主题，但能减少“页面是深色，输入框还是亮白”的违和感。\n\n注意一定要内联，不能用 `<script src>` 外链。外链脚本要等额外的网络请求回来才执行，闪烁照样发生。我在一个 Next.js 项目里就为这个折腾过一阵——SSR 出来的 HTML 在服务端根本不知道用户的 `localStorage`，所以默认渲染浅色，到了客户端水合后才切深色，深色用户每次刷新都会“闪白一下”。最后的办法就是在 `<head>` 里塞一段 `dangerouslySetInnerHTML` 的内联脚本，赶在框架水合之前先把 class 打到 `documentElement` 上。这段脚本会在每个页面 HTML 里重复出现，所以越短越好，别在里面写花活，几行能跑完最理想。\n\n还有个容易忽略的点：如果用 `prefers-color-scheme` 决定初值，最好把 `<meta name=\"color-scheme\" content=\"light dark\">` 也加上，这样浏览器的滚动条、表单控件这些原生 UI 也会跟着深色，不然会出现页面是深色、滚动条却是亮色的割裂感。\n\n## 跨标签页同步\n\n如果用户打开了多个标签页，在一个标签页切换主题，其他标签页最好也同步。\n\n可以监听 `storage` 事件：\n\n```javascript\nwindow.addEventListener('storage', (event) => {\n  if (event.key !== 'theme') {\n    return;\n  }\n\n  setTheme(event.newValue || 'light');\n});\n```\n\n注意，`storage` 事件通常不会在当前写入的标签页触发，所以当前页切换主题时仍然要立即更新 DOM。\n\n## 组件库主题要单独处理\n\n如果项目使用 Ant Design、Element Plus 这类组件库，主题切换不能只改业务 CSS。\n\n还要看组件库本身支持哪种方式：\n\n- 是否支持 CSS 变量\n- 是否提供暗黑主题\n- 是否支持运行时切换 token\n- 是否需要单独引入主题样式文件\n\n最理想的情况是：业务样式和组件库主题都由同一套变量或 token 驱动。\n\n以 Element Plus 为例，它本身有一套 CSS 变量。如果业务样式用自己的变量，组件库用另一套变量，就要建立映射关系，而不是各写各的。\n\n```css\n:root {\n  --color-primary: #1677ff;\n  --el-color-primary: var(--color-primary);\n}\n```\n\n这样业务按钮、组件库按钮至少能跟随同一个主色变化。\n\n如果组件库支持运行时 token，比如通过配置提供主题算法，也要确认它和业务 CSS 变量是不是同一份来源。我遇到过业务样式已经切到深色，但组件库弹窗仍然是浅色的情况，本质就是两套主题状态没有同步。\n\n实际接 Element Plus 的深色模式时还有几个坑。它的暗色样式要单独引 `element-plus/theme-chalk/dark/css-vars.css`，并且要求深色 class 必须加在 `html` 上、名字固定是 `dark`，不是随便起的 `theme-dark`。我一开始用自己的 `theme-dark`，结果业务样式都变黑了，组件库还是亮的，对着控制台找了半天才发现是 class 名对不上。后来我干脆让两个 class 一起挂：\n\n```javascript\nfunction setTheme(theme) {\n  const el = document.documentElement;\n  el.classList.toggle('dark', theme === 'dark');        // 给组件库用\n  el.classList.toggle('theme-dark', theme === 'dark');  // 给业务样式用\n}\n```\n\n另外要小心 `--el-color-primary` 这种映射只覆盖了主色，组件库还会基于主色派生出一堆 `--el-color-primary-light-3`、`light-5`、`light-7` 之类的浅色变体（用于 hover、disabled 等态）。如果你只覆盖了主色没覆盖这些派生色，换品牌色后会发现按钮 hover 还是原来那个蓝。这些派生色要么手动算一遍补上，要么用组件库提供的 SCSS 变量在构建期生成，运行时硬靠 CSS 变量覆盖全部派生色是比较费劲的。\n\n## 不建议写两套完全独立样式\n\n有些项目会写 `light.css` 和 `dark.css` 两套完整文件。\n\n短期看很直接，长期容易出现问题：\n\n- 两套样式内容重复\n- 新组件容易漏写暗黑样式\n- 修改一个细节要改两处\n- 主题之间差异越来越难控制\n\n如果只是颜色不同，CSS 变量通常更容易维护。\n\n当然，也不是所有差异都适合用变量解决。如果主题之间布局结构、插图资源、动效规则都完全不同，那可能已经不是“主题切换”，而是不同页面皮肤或不同产品形态。这个时候硬塞进一套变量里，反而会让系统复杂。\n\n## 图片、阴影和图表也要考虑\n\n深色模式里最容易漏的是非 CSS 颜色：\n\n- 图片和 logo 是否适合深色背景\n- 图表颜色是否仍然可读\n- 阴影在深色背景下是否看得见\n- 代码高亮主题是否同步切换\n\n浅色模式下常用的阴影，在深色模式里可能几乎不可见。可以把阴影也做成变量：\n\n```css\n:root {\n  --shadow-panel: 0 8px 24px rgb(15 23 42 / 12%);\n}\n\n.theme-dark {\n  --shadow-panel: 0 8px 24px rgb(0 0 0 / 40%);\n}\n```\n\n图表库也要接入主题，否则页面整体变暗，图表仍然是亮色，会显得很突兀。\n\n图表还有一个细节：不要只换背景色，坐标轴、网格线、tooltip、legend、数据色板都要一起换。尤其是数据可视化页面，深色模式下图表对比度不够，会直接影响读数，而不是单纯“不好看”。\n\n图表这块我趟过一个坑。CSS 变量是给 DOM 用的，而 ECharts、Canvas 这类图表的颜色是写在 JS 配置里的，`getComputedStyle` 拿不到“当前生效的变量值”那么顺手，更麻烦的是切主题时图表不会自己重画。我的做法是切主题后主动读出变量再重设配置：\n\n```javascript\nfunction getVar(name) {\n  return getComputedStyle(document.documentElement)\n    .getPropertyValue(name).trim();\n}\n\nfunction applyChartTheme(chart) {\n  chart.setOption({\n    backgroundColor: getVar('--color-bg-panel'),\n    textStyle: { color: getVar('--color-text-primary') },\n  });\n}\n```\n\n然后在主题切换的回调里把页面上所有图表实例遍历一遍调用它。要点是“切主题”这个动作得有个统一的事件出口，让图表、代码高亮、第三方组件都能挂上去重渲染，而不是各自去监听 class 变化。\n\n图片我一般用两招：纯色 logo 直接做成 SVG 用 `currentColor` 或变量上色，跟着文字色走；带阴影、有底色的位图就准备两份，用 `<picture>` 配合媒体查询切换：\n\n```html\n<picture>\n  <source srcset=\"/logo-dark.png\" media=\"(prefers-color-scheme: dark)\">\n  <img src=\"/logo-light.png\" alt=\"logo\">\n</picture>\n```\n\n不过 `<picture>` 只认系统偏好，跟我前面说的“用户手动选主题”那套对不上，手动切换为主的项目还是得在 JS 里换 `src`。\n\n## 可访问性和对比度\n\n主题切换不是只看“好不好看”，还要看文字对比度是否足够。\n\n深色模式里常见问题是正文灰度太低，长时间阅读很吃力；浅色模式里则可能是浅灰文字过淡。设计 token 时要把主文本、次文本、禁用文本分清楚。\n\n不要只通过颜色表达状态。错误、成功、警告最好同时有文字、图标或明确上下文，避免用户只能靠颜色判断。\n\n判断对比度别只靠肉眼，用浏览器 DevTools 的对比度检查或在线工具量一下，正文至少要到 WCAG AA 的 4.5:1。深色模式下我踩过的典型反例是“纯黑底配纯白字”，对比度拉满反而刺眼、发虚，主流做法是底色用接近 `#121212` 的深灰、正文用略带灰的白，眼睛舒服很多。\n\n## 切换动画别想当然加\n\n很多人喜欢给切换加个过渡，在根节点写 `* { transition: background-color .3s; }`。我劝你别这么干。给所有元素加全局过渡，首屏加载和路由切换时所有颜色都会“渐变进场”，看着像页面没加载好；而且元素一多，这个过渡的性能开销很可观。\n\n更稳的做法是只给确实需要的容器加过渡，并且尊重用户的“减弱动效”偏好：\n\n```css\n@media (prefers-reduced-motion: no-preference) {\n  .panel {\n    transition: background-color .2s ease, color .2s ease;\n  }\n}\n```\n\n切换那一瞬间如果想完全关掉过渡（比如初始化设主题时不希望看到动画），可以在切换前给 `html` 临时加一个 `no-transition` 类，下一帧再移除，避开首次设值的闪变。\n\n## 小结\n\n主题切换的关键是提前抽象颜色变量，而不是到处写固定色值。\n\n简单项目可以用 `class + CSS 变量` 实现。复杂项目还要结合组件库主题能力，让业务样式和组件库样式保持一致。\n\n我现在做主题系统，会先设计语义 token，再处理主题 class、持久化、系统偏好和首屏防闪。切换按钮只是最后一步，真正的工作是让颜色、组件、图表和状态都能被同一套主题规则驱动。\n"},"recommendedPosts":[{"title":"跨文档 View Transitions：多页应用也能做出丝滑过渡了","date":"2026-04-04","excerpt":"同文档 View Transitions 之外，跨文档（MPA）过渡终于可用。记录我给一个传统多页站点加上页面切换动画的过程、view-transition-name 的配对和降级处理。","tags":["前端基础","CSS","交互与动效"],"slug":"view-transitions-cross-document-2026","category":"2026","likeCount":92,"postId":"54351a26-388d-b51e-ff65-3ae7fde9bbd4","viewCount":968,"readTime":null,"coverImage":null},{"title":"原生 CSS 嵌套和 @scope：写了这么多年 Sass 之后的几点真实感受","date":"2026-03-22","excerpt":"原生嵌套和 @scope 落地后，我在项目里逐步去掉一部分 Sass 的过程：嵌套的坑、@scope 的下边界 donut scope、以及现在还不能完全替代预处理器的地方。","tags":["前端基础","CSS"],"slug":"css-scope-nesting-2026","category":"2026","likeCount":36,"postId":"a4a7366f-5967-f614-350b-d223d0a045f3","viewCount":800,"readTime":null,"coverImage":null},{"title":"Tailwind CSS v4 迁移记：新引擎、CSS 变量主题和那些不再需要的配置","date":"2026-01-24","excerpt":"从 v3 升到 v4 的真实迁移过程：Oxide 引擎带来的速度变化、配置从 JS 搬到 CSS、@theme 和 CSS 变量怎么用，以及几个升级时踩的坑。","tags":["前端基础","CSS","构建与模块化"],"slug":"tailwind-v4-migration-2026","category":"2026","likeCount":34,"postId":"c8484300-10ec-bf7b-ef1b-6a237cb4f4f2","viewCount":933,"readTime":null,"coverImage":null}],"prevPost":{"title":"Web Worker 实践：把耗时任务从主线程里挪出去","date":"2022-10-11","excerpt":"结合前端性能优化场景，说明 Web Worker 适合处理哪些任务、如何通信、如何处理错误，以及不适合滥用的边界。","tags":["前端基础","JavaScript","性能与缓存"],"slug":"frontend-web-worker-practice-2022","category":"2022","likeCount":6,"postId":"ed2150e0-8302-7f20-9ca6-16c9a6ae46df","viewCount":2401,"readTime":null,"coverImage":null},"nextPost":{"title":"前端缓存基础：强缓存、协商缓存和资源更新到底该怎么理解","date":"2022-10-03","excerpt":"介绍前端开发中最常见的浏览器缓存机制，帮助你理解资源为什么有时更新了却没生效。","tags":["前端基础","性能与缓存"],"slug":"frontend-http-cache-2022","category":"2022","likeCount":5,"postId":"893c3552-ee46-d6a3-fb1d-42e18cf3373c","viewCount":3163,"readTime":null,"coverImage":null}},"__N_SSG":true}