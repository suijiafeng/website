{"pageProps":{"postData":{"title":"刚开始用 Vue3 时，最容易踩的几个坑","date":"2023-06-07","excerpt":"整理 Vue3 入门和迁移时常见的响应式、props、watch、组合式 API、生命周期清理和 Pinia 使用误区。","tags":["Vue","前端基础"],"slug":"vue3-common-mistakes-2023","category":"2023","likeCount":6,"postId":"06bb42c9-56d9-0865-c906-88f606e068a9","viewCount":1085,"readTime":"6分钟","coverImage":null,"content":"\nVue3 已经很成熟，但从 Vue2 或 Options API 迁移过来时，还是很容易写出一些“能跑但不太对”的代码。\n\n很多坑不是语法不会，而是对组合式 API 和响应式规则理解不够清楚。Vue3 的写法更灵活，也意味着组件逻辑的边界更需要开发者自己管理。\n\n我从 Vue2 迁到 Vue3 时，最大的不适应不是 `.value`，而是逻辑组织方式变了。Options API 会把 data、methods、watch 分栏放好；Composition API 更自由，但自由也意味着你可以把请求、表单、弹窗、权限全写进一个 setup 里。写得顺手不代表边界清楚。\n\n## 基本类型不要直接用 reactive\n\n`reactive` 适合对象、数组、Map、Set 这类引用类型。如果是数字、字符串、布尔值，应该使用 `ref`。\n\n```javascript\nimport { reactive, ref } from \"vue\";\n\nconst user = reactive({\n  name: \"张三\",\n  age: 18,\n});\n\nconst count = ref(0);\nconst keyword = ref(\"\");\nconst loading = ref(false);\n```\n\n不要这样写：\n\n```javascript\nconst count = reactive(0);\n```\n\n基本类型不能被 `reactive` 正常包装。一个简单判断是：单个值用 `ref`，结构化对象用 `reactive`。当然，对象也可以用 `ref`，这取决于你希望整体替换对象，还是更常修改对象内部字段。\n\n如果对象需要整体替换，比如接口返回的详情对象，我更倾向用 `ref<User | null>(null)`。如果是表单这种长期修改内部字段的对象，用 `reactive` 会更顺。不是 ref 和 reactive 谁更高级，而是看更新方式。\n\n## ref 在脚本里要用 .value\n\n模板里使用 `ref` 会自动解包：\n\n```vue\n<template>\n  <button>{{ count }}</button>\n</template>\n```\n\n但在 `<script setup>` 里读写时，需要使用 `.value`：\n\n```javascript\nconst count = ref(0);\n\nfunction add() {\n  count.value += 1;\n}\n```\n\n刚开始写 Vue3 时，很容易忘记 `.value`。如果发现状态没有更新，先检查是在模板里还是脚本里使用。\n\n## 解构 reactive 会丢失响应式\n\n这是 Vue3 里很常见的坑。\n\n```javascript\nconst state = reactive({\n  name: \"张三\",\n  age: 18,\n});\n\nconst { name } = state;\n```\n\n这样解构出来的 `name` 只是普通值，不再跟随 `state.name` 更新。\n\n如果确实需要解构，可以用 `toRefs`：\n\n```javascript\nimport { reactive, toRefs } from \"vue\";\n\nconst state = reactive({\n  name: \"张三\",\n  age: 18,\n});\n\nconst { name, age } = toRefs(state);\n```\n\n`toRefs` 会把对象属性转换成 ref，解构后仍然保留响应式。\n\n## props 不要直接修改\n\n子组件接收的 `props` 应该当作只读数据。\n\n如果子组件需要修改值，通常有两种方式：\n\n- 通过 `emit` 通知父组件修改\n- 在子组件里复制一份本地状态\n\n例如：\n\n```javascript\nconst props = defineProps({\n  modelValue: String,\n});\n\nconst emit = defineEmits([\"update:modelValue\"]);\n\nfunction updateValue(value) {\n  emit(\"update:modelValue\", value);\n}\n```\n\n直接修改 `props` 会让数据流变混乱，也容易出现警告。\n\n如果确实需要本地编辑副本，可以这样处理：\n\n```javascript\nconst localValue = ref(props.modelValue);\n\nwatch(\n  () => props.modelValue,\n  (value) => {\n    localValue.value = value;\n  }\n);\n```\n\n但要注意：本地副本意味着父子状态可能短暂不一致，需要明确什么时候同步、什么时候提交。\n\n这个问题在弹窗表单里特别常见。父组件传一条记录进来，子组件复制成本地表单，用户点取消时不应该污染父数据，点确定时再 emit 提交。边界想清楚，props 只读就不会显得麻烦。\n\n## watch 不要滥用\n\n`watch` 很有用，但不应该把所有逻辑都塞进去。\n\n如果一个值可以由其他值计算出来，优先考虑 `computed`：\n\n```javascript\nconst fullName = computed(() => `${firstName.value} ${lastName.value}`);\n```\n\n`watch` 更适合处理副作用，比如请求接口、写入缓存、同步外部状态。\n\n比如搜索关键词变化后请求列表：\n\n```javascript\nwatch(keyword, async (value, oldValue, onCleanup) => {\n  const controller = new AbortController();\n\n  onCleanup(() => {\n    controller.abort();\n  });\n\n  const response = await fetch(`/api/search?q=${value}`, {\n    signal: controller.signal,\n  });\n\n  result.value = await response.json();\n});\n```\n\n这里使用 `onCleanup` 取消旧请求，避免上一次请求比下一次更晚返回，导致旧数据覆盖新数据。\n\n如果请求逻辑在多个页面重复出现，不要每个 `watch` 里都手写取消和错误处理。可以抽成 composable 或请求库策略，统一处理 loading、error、abort。否则同一个搜索框，不同页面会有不同的竞态 bug。\n\n## watch 对象时要明确监听目标\n\n另一个常见问题是直接深度监听整个对象：\n\n```javascript\nwatch(\n  form,\n  () => {\n    saveDraft(form);\n  },\n  { deep: true }\n);\n```\n\n这很方便，但也容易导致触发过于频繁，尤其是表单字段很多时。\n\n如果只关心某几个字段，更推荐明确写出依赖：\n\n```javascript\nwatch(\n  () => [form.title, form.category],\n  () => {\n    saveDraft({\n      title: form.title,\n      category: form.category,\n    });\n  }\n);\n```\n\n监听目标越明确，后续维护越容易判断为什么会触发。\n\n## computed 不要产生副作用\n\n`computed` 应该用来计算派生值，不应该在里面改状态或发请求。\n\n不要这样写：\n\n```javascript\nconst total = computed(() => {\n  reportLog();\n  return price.value * count.value;\n});\n```\n\n`computed` 可能因为依赖变化和缓存策略被多次求值，把副作用放进去会让行为变得难预测。发请求、埋点、写缓存这类事情应该放在事件处理或 `watch` 里。\n\n## 组合式函数要保持清楚边界\n\nVue3 里可以把逻辑封装成组合式函数。\n\n例如：\n\n```javascript\nfunction useLoading() {\n  const loading = ref(false);\n\n  function setLoading(value) {\n    loading.value = value;\n  }\n\n  return {\n    loading,\n    setLoading,\n  };\n}\n```\n\n但不要把所有业务都塞进一个巨大的 `usePage`。如果一个组合式函数同时处理请求、表单、权限、弹窗、路由和埋点，它只是把组件复杂度换了个文件。\n\n好的组合式函数应该职责明确，例如只处理请求、表单、分页、弹窗或权限中的一类问题。\n\n我不太喜欢万能的 `useTablePage`。一开始它能帮你快速做列表，后来加权限、批量操作、导出、缓存、路由同步，参数会越来越多。组合式函数最好小而专注，可以组合使用，而不是变成另一个配置平台。\n\n## 生命周期里注意清理副作用\n\n组合式 API 让事件监听和定时器更容易散落在函数里，清理就更重要。\n\n```javascript\nimport { onMounted, onUnmounted } from \"vue\";\n\nonMounted(() => {\n  window.addEventListener(\"resize\", handleResize);\n});\n\nonUnmounted(() => {\n  window.removeEventListener(\"resize\", handleResize);\n});\n```\n\n定时器、订阅、WebSocket、全局事件都应该在组件卸载时清理。否则页面切换后，旧逻辑可能还在运行。\n\n还有一个迁移期常见问题：在模块顶层直接调用 Pinia store 或依赖组件上下文的 composable。很多 composable 只能在 setup 或已经创建 app 后调用，放到模块顶层可能在初始化顺序上出问题。能延后到函数内部，就不要过早执行。\n\n## 模板里 ref 自动解包，不代表到处都自动\n\nVue 的自动解包很方便，但也容易让新人误判。模板里 `count` 可以直接用，脚本里要 `count.value`；`reactive` 对象里的 ref 会被解包，但数组、Map 里又不是同一套表现。遇到不确定的响应式问题，先把数据结构简化，不要把 ref、reactive、toRefs 混成一团。\n\n## 小结\n\nVue3 的核心不是把 `data`、`methods` 换成 `ref`、`reactive`，而是重新组织组件逻辑。\n\n基本类型用 `ref`，复杂对象用 `reactive`；脚本里记得 `.value`；解构时注意响应式；props 保持单向数据流；能用 `computed` 就不要滥用 `watch`；组合式函数要有明确职责。这些习惯能减少很多后期维护问题。\n"},"recommendedPosts":[{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"介绍现代请求库从简单 HTTP 客户端走向请求状态管理、缓存、分页、取消请求和统一错误处理的趋势。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2362,"readTime":null,"coverImage":null},{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2022-09-17","excerpt":"介绍 Vue3 和 Element Plus 中封装无限级菜单组件的核心数据结构、递归渲染、权限过滤和动态路由边界。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2022","category":"2022","likeCount":7,"postId":"3462b2e8-f24c-3869-fb44-dc017a7c3f2f","viewCount":4229,"readTime":null,"coverImage":null},{"title":"Vue 3 与 Pinia 状态管理实践：从 Vuex 迁移时真正要改的不是 API","date":"2022-04-18","excerpt":"结合 Vue 3 项目实践，讨论 Pinia 的状态组织、模块拆分、持久化边界、异步 action、SSR 和从 Vuex 迁移时的注意点。","tags":["Vue","前端基础","组件与状态"],"slug":"vue3-pinia-state-management-2022","category":"2022","likeCount":7,"postId":"a80f5fc3-79c3-d9e7-38e2-39ecfedb927f","viewCount":3357,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端打包优化经验：包体积不是只靠压缩就能降下来的","date":"2023-07-02","excerpt":"总结前端项目中常见的打包优化思路，结合后台项目包体积膨胀的排查经历，讨论依赖、拆包、资源和缓存策略。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"frontend-bundling-2023","category":"2023","likeCount":5,"postId":"ef208b58-4020-7c41-1b0f-a4516cc7cbaa","viewCount":1927,"readTime":null,"coverImage":null},"nextPost":{"title":"前端性能排查思路：页面卡顿时，不要一上来就怀疑框架","date":"2023-05-18","excerpt":"总结前端页面卡顿时更有效的排查顺序，结合 Performance 面板、Web Vitals 和真实用户指标定位资源、请求和主线程问题。","tags":["前端基础","性能与缓存"],"slug":"frontend-performance-debugging-2023","category":"2023","likeCount":5,"postId":"cb9fc5df-ed94-c896-b1cd-8d229034c998","viewCount":2513,"readTime":null,"coverImage":null}},"__N_SSG":true}