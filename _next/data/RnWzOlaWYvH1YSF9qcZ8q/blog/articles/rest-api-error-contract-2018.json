{"pageProps":{"postData":{"title":"前后端接口错误约定：message 不能每个接口都不一样","date":"2018-09-18","excerpt":"从前后端联调角度整理 REST 接口错误结构、HTTP 状态码、业务 code、表单错误和前端统一处理方式。","tags":["前端基础","构建与模块化"],"slug":"rest-api-error-contract-2018","category":"2018","likeCount":8,"postId":"d49c5469-8e60-2730-dfcc-7be800f0caf2","viewCount":2927,"readTime":"9分钟","coverImage":null,"content":"\n前后端联调时，最消耗我精力的从来不是正常数据，而是错误。正常数据无非是把字段渲染出来，结构对不上大不了改个映射。可错误处理是散落在每一个 `.catch` 里的，一旦后端各个接口对\"出错\"的表达方式不一致，前端就会被逼着写一堆 if-else 去猜测对方到底想说什么。\n\n去年接一个后台管理系统，对接的后端是三个人分模块写的，没有统一规范。我大概数了一下，光是\"失败\"这件事，我就遇到过这些花样：\n\n```json\n{ \"message\": \"名称已存在\" }\n{ \"msg\": \"stock not enough\" }\n{ \"error\": \"参数错误\", \"errorCode\": 1001 }\n\"系统繁忙，请稍后再试\"\n{ \"success\": false, \"data\": { \"tip\": \"验证码错误\" } }\n```\n\n最离谱的是有个接口，业务出错时 HTTP 返回 200，body 里塞个 `code: -1`；而真正服务器异常的时候直接给我一坨 500 的 HTML 错误页，`JSON.parse` 当场抛异常，连 message 都拿不到。我当时在每个页面里写的 `error.response.data.message`，碰到这些就全线崩溃，要么显示 `undefined`，要么干脆白屏。\n\n那阵子我才真正理解一件事：**接口错误如果没有约定，烂账最后一定会全部转嫁到前端的 catch 块里。** 后来我跟后端一起把错误契约拉出来定死，前端只维护一个统一的转换层，整个项目的报错体验和代码量才回到正轨。这篇就把当时定下来的东西和踩过的坑整理一遍。\n\n## 先把\"成功\"的壳子统一\n\n错误结构要统一，前提是成功结构也统一。如果连成功长什么样都不固定，前端连\"这次到底成没成\"都要现猜。我们最后定的是经典的三段式：\n\n```json\n{\n  \"code\": 0,\n  \"data\": {},\n  \"message\": \"ok\"\n}\n```\n\n`code === 0` 表示业务成功，`data` 放真正的数据，`message` 给个可读文案（成功一般用不上，但留着方便兜底）。请求层只认 `code`，不认别的：\n\n```js\n// 在 axios 响应拦截器里做第一道判断\nservice.interceptors.response.use(\n  response => {\n    const result = response.data\n    if (result.code !== 0) {\n      // 业务失败，主动 reject，让它走到 catch\n      return Promise.reject({\n        isBizError: true,\n        code: result.code,\n        message: result.message || '请求失败',\n        errors: result.errors || null\n      })\n    }\n    // 业务成功，直接把 data 剥出来给页面，页面不用再写 result.data.data\n    return result.data\n  },\n  error => Promise.reject(error)\n)\n```\n\n这里有个我踩过的坑：**别让请求层\"吞掉\"业务失败然后返回个空对象**。早期为了图省事，我把业务失败的时候直接 `return null`，结果页面里 `then` 照样执行，拿到 null 又去访问属性，错误被掩盖得更深。失败就要 reject，让它干干净净走到 catch，这是底线。\n\n## 错误也要有固定形状\n\n成功是一种形状，失败也得是固定的几种形状。我们分了两类。\n\n第一类是普通业务错误，一句话能说清的：\n\n```json\n{\n  \"code\": 40001,\n  \"data\": null,\n  \"message\": \"名称已存在\"\n}\n```\n\n第二类是表单字段错误，这类不能只给一句话，得告诉前端\"哪个字段错了\"，否则用户根本不知道去改哪：\n\n```json\n{\n  \"code\": 40002,\n  \"message\": \"表单校验失败\",\n  \"errors\": {\n    \"name\": \"名称不能为空\",\n    \"email\": \"邮箱格式不正确\"\n  }\n}\n```\n\n`errors` 用字段名做 key，这样前端可以精确地把红字落到对应的输入框下面，而不是笼统弹一句\"表单校验失败\"让用户自己找。Element UI 的表单里，我是这么把后端错误回填上去的：\n\n```js\nasync function submit() {\n  try {\n    await api.createUser(this.form)\n    this.$message.success('创建成功')\n  } catch (err) {\n    // 字段级错误：回填到对应表单项\n    if (err.errors) {\n      Object.keys(err.errors).forEach(field => {\n        const item = this.$refs.form.fields.find(f => f.prop === field)\n        if (item) {\n          item.validateState = 'error'\n          item.validateMessage = err.errors[field]\n        }\n      })\n      return\n    }\n    // 普通业务错误：弹个 toast\n    this.$message.error(err.message)\n  }\n}\n```\n\n定 `errors` 这个字段的时候，我和后端为格式吵过一架。他们一开始想给数组 `[{ field, message }]`，理由是\"将来一个字段可能有多条错误\"。我坚持用对象，因为表单回填本来就是按字段名查的，数组每次都得 `find` 一遍。最后折中：单条错误就用对象 `{ field: message }`，确实需要多条的极少数接口才升级成 `{ field: [msg1, msg2] }`，前端做下兼容。规范这东西，能简单就别提前为想象中的需求买单。\n\n## HTTP 状态码和业务 code 各管各的\n\n这是当时争议最大、也是收益最大的一条约定：**HTTP 状态码表达协议层，业务 code 表达业务层，两者不能混用。**\n\nHTTP 状态码回答的是\"这次请求在传输和鉴权层面发生了什么\"：\n\n- `200` 请求正常到达并处理（哪怕业务失败，只要服务正常响应了，也算 200）—— 这点后面要修正，往下看\n- `401` 没登录 / token 失效\n- `403` 登录了但没权限\n- `404` 资源不存在\n- `500` 服务端自己挂了\n\n业务 code 回答的是\"在业务规则层面发生了什么\"：名称重复、库存不足、验证码错误、订单状态不允许操作……这些是后端业务逻辑主动判定的结果，跟 HTTP 协议没半毛钱关系。\n\n这里有个经典的两难，我们反复掂量过：**业务失败到底该用 HTTP 200 还是 4xx？** 两种流派：\n\n- **全 200 派**：只要服务正常处理了请求，HTTP 就是 200，对错全看 body 里的 `code`。好处是前端逻辑统一，坏处是网关、监控、浏览器开发者工具的 Network 面板全是绿的，出了业务问题在监控曲线上完全看不出来。\n- **语义 HTTP 派**：业务失败用对应的 4xx（比如校验失败用 422，权限不够用 403）。好处是符合 REST 语义、监控告警能直接用状态码做，坏处是 4xx 的细分粒度有限，业务错误五花八门根本不够分。\n\n我们最后的取舍是**分层混用**：鉴权和资源这类天然属于协议层的，老老实实用 401/403/404；纯业务规则的失败（名称重复、库存不足）统一走 `400`，再用 body 里的业务 `code` 细分。这样监控可以靠\"4xx/5xx 比例\"做粗粒度告警，前端又能靠业务 code 做精细处理，两边都不亏。\n\n最不能接受的就是两个极端：所有错误都返回 200（监控瞎了），或者所有业务错误都甩 500（运维半夜被告警叫醒，结果只是用户输错了验证码）。500 应该是\"程序员的锅\"专用，业务规则失败不配占用 500。\n\n## 核心：前端只维护一个转换层\n\n前面所有约定，落到前端就是一件事——**把后端千奇百怪（以及未来可能再变）的错误，收敛成一个稳定的内部结构**。我把这个函数叫 `normalizeError`，整个项目所有报错都先过它：\n\n```js\nfunction normalizeError(error) {\n  // 0. 我们自己在拦截器里 reject 的业务错误，已经是规范结构，直接放行\n  if (error && error.isBizError) {\n    return {\n      status: 200,\n      code: error.code,\n      message: error.message,\n      errors: error.errors || null\n    }\n  }\n\n  // 1. 服务器有响应（4xx / 5xx）\n  if (error.response) {\n    const status = error.response.status\n    let body = error.response.data\n\n    // 防 500 返回 HTML 的坑：body 不是对象就别硬解\n    if (typeof body !== 'object' || body === null) {\n      body = {}\n    }\n\n    return {\n      status,\n      code: body.code != null ? body.code : status,\n      message: body.message || body.msg || defaultMessageByStatus(status),\n      errors: body.errors || null\n    }\n  }\n\n  // 2. 请求发出去了但没收到响应（断网、超时、跨域被拦）\n  if (error.request) {\n    return {\n      status: 0,\n      code: 'NETWORK_ERROR',\n      message: '网络异常，请检查网络后重试',\n      errors: null\n    }\n  }\n\n  // 3. 连请求都没发出去（代码自己抛的异常）\n  return {\n    status: -1,\n    code: 'CLIENT_ERROR',\n    message: error.message || '客户端异常',\n    errors: null\n  }\n}\n\nfunction defaultMessageByStatus(status) {\n  const map = {\n    400: '请求参数有误',\n    401: '登录已失效',\n    403: '没有操作权限',\n    404: '请求的资源不存在',\n    500: '服务异常，请稍后重试',\n    502: '网关异常',\n    503: '服务暂时不可用'\n  }\n  return map[status] || '请求失败'\n}\n```\n\n`body.message || body.msg` 这种兼容写法，其实是无奈之举。规范定了之后老接口不可能一夜全改，前端做一层兼容，能让新老接口平滑过渡——这是转换层最大的价值：**脏活只脏在一个地方。** 后端哪天又冒出个 `error` 字段，我只改这一行，不用满项目搜 catch。\n\n页面侧拿到的永远是同一个形状，闭着眼写都行：\n\n```js\ntry {\n  const data = await api.fetchList()\n  this.list = data\n} catch (error) {\n  const e = normalizeError(error)\n  this.$message.error(e.message)\n}\n```\n\n## 登录失效必须在请求层统一拦\n\n`401` 是我最坚持不让页面碰的一类错误。早期没统一处理时，token 一过期，页面上同时发了五六个请求，于是\"登录已失效\"的弹窗叠了五六层，关都关不完，关完还各自跳一次登录页，URL 被来回重定向得乱七八糟。\n\n正确的做法是在响应拦截器里集中拦截，并且加一个\"防重复跳转\"的开关：\n\n```js\nlet isRedirecting = false\n\nservice.interceptors.response.use(\n  response => { /* ...前面成功处理... */ },\n  error => {\n    const e = normalizeError(error)\n\n    if (e.status === 401) {\n      if (!isRedirecting) {\n        isRedirecting = true\n        Message.error('登录已失效，请重新登录')\n        clearToken()\n        // 带上当前路径，登录后能跳回来\n        const redirect = encodeURIComponent(location.hash.slice(1) || '/')\n        location.href = `/#/login?redirect=${redirect}`\n      }\n      // 直接 reject 一个静默错误，避免页面再弹一次\n      return Promise.reject({ ...e, silent: true })\n    }\n\n    return Promise.reject(e)\n  }\n)\n```\n\n页面侧对 `silent` 标记做个判断，就不会重复弹了：\n\n```js\ncatch (error) {\n  const e = normalizeError(error)\n  if (!e.silent) this.$message.error(e.message)\n}\n```\n\n`isRedirecting` 这个锁是被实际坑出来的——并发请求一起 401 的场景太常见，没有锁就是弹窗地狱。403 我没放进来统一处理，因为\"没权限\"往往需要页面自己决定是跳转、还是只置灰按钮，交给页面更灵活。\n\n## 把约定写成文档，而不是靠口头\n\n最后想说的是流程层面的事。这套结构再好，如果只活在某次联调的口头约定里，新来的后端同学一上手照样我行我素。我们当时干了两件事：\n\n一是把错误码维护成一张表，业务 code 按模块分段（用户模块 40000-40099，商品模块 41000-41099……），前后端共享同一份，前端甚至可以据此做错误码到文案的兜底映射；二是在 Mock 阶段就让 Mock 数据严格遵守这个结构，这样联调前前端的 catch 逻辑已经测过了，真接上去基本不返工。\n\n```js\n// 错误码常量，前后端各维护一份对照\nexport const BIZ_CODE = {\n  NAME_EXISTS: 40001,\n  FORM_INVALID: 40002,\n  STOCK_NOT_ENOUGH: 41001,\n  CAPTCHA_WRONG: 42001\n}\n\n// 某些 code 前端要做特殊动作，而不只是弹文案\nfunction handleBizCode(e) {\n  switch (e.code) {\n    case BIZ_CODE.CAPTCHA_WRONG:\n      refreshCaptcha()   // 验证码错了，自动刷新图形码\n      break\n    case BIZ_CODE.STOCK_NOT_ENOUGH:\n      reloadStock()      // 库存不足，重新拉一次库存\n      break\n  }\n  Message.error(e.message)\n}\n```\n\n## 小结\n\n接口错误约定是前后端协作里最容易被忽略、又最折磨人的地方。回头看，真正起作用的就几条：成功和失败用固定形状，HTTP 状态码和业务 code 分层各管各的，字段错误带上字段名，前端只维护一个 `normalizeError` 收口所有脏活，401 这类全局错误在请求层统一拦截。\n\n前端最怕的从来不是接口失败，而是**每个接口失败得都不一样**。错误结构一旦稳定，catch 块就能从\"逐个猜谜\"变成\"统一处理\"，页面体验和代码维护一起变好——这点投入，绝对值回票价。\n"},"recommendedPosts":[{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":42,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":222,"readTime":null,"coverImage":null},{"title":"用 Biome 替掉 ESLint + Prettier：一次工具链瘦身的得与失","date":"2026-04-18","excerpt":"把项目的 lint 和格式化从 ESLint+Prettier 换成 Biome 的过程：速度提升有多明显、规则迁移缺了什么、以及哪些场景我还是留着 ESLint。","tags":["前端基础","构建与模块化","JavaScript"],"slug":"biome-toolchain-2026","category":"2026","likeCount":6,"postId":"37e39142-8bf2-d042-6270-2228c894603c","viewCount":1078,"readTime":null,"coverImage":null},{"title":"Vite 用上 Rolldown 之后：一次构建提速的真实体感和注意点","date":"2026-02-07","excerpt":"Rolldown 把 Vite 的打包从 esbuild/Rollup 双引擎统一成一套 Rust 实现，记录我在中型项目里切过去的体感、兼容性问题和回退策略。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"vite-rolldown-2026","category":"2026","likeCount":8,"postId":"27afacf5-76c4-4c26-58fd-15ea35bac2d8","viewCount":1567,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue Router 实战整理：页面一多，路由设计比跳转本身更重要","date":"2018-10-03","excerpt":"从实际项目角度整理 Vue Router 的常见用法，补充嵌套路由、命名路由、参数传递、守卫和错误处理等设计细节。","tags":["Vue","组件与状态"],"slug":"vue-router-2018","category":"2018","likeCount":8,"postId":"561b736e-fa6f-c4e3-99db-fe944ce6351b","viewCount":3518,"readTime":null,"coverImage":null},"nextPost":{"title":"HappyPack 提升 Webpack 构建速度的思路","date":"2018-09-17","excerpt":"介绍 HappyPack 的基本原理，结合构建瓶颈分析、配置示例和现代替代方案理解多进程 loader 优化思路。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"frontend-happypack-webpack-2018","category":"2018","likeCount":6,"postId":"dc806306-8dab-e769-72c9-11d27bed6f94","viewCount":1164,"readTime":null,"coverImage":null}},"__N_SSG":true}