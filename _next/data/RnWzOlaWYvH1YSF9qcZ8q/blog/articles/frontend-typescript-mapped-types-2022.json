{"pageProps":{"postData":{"title":"TypeScript 映射类型入门：让类型跟着数据结构一起变化","date":"2022-08-08","excerpt":"用表单、权限和接口 payload 的例子解释 TypeScript 映射类型，理解 keyof、索引访问和常见工具类型背后的设计思路。","tags":["前端基础","TypeScript"],"slug":"frontend-typescript-mapped-types-2022","category":"2022","likeCount":5,"postId":"8e661042-a1e3-ed49-af37-9f8bb5accd62","viewCount":2349,"readTime":"7分钟","coverImage":null,"content":"\nTypeScript 的类型写多了以后，很容易出现重复。\n\n一个数据类型改了，另一个相关类型也要跟着改。如果忘了同步，类型就会和真实业务慢慢脱节。\n\n映射类型就是用来减少这类重复的。\n\n它的核心思想是：不要手动复制一份类型，而是基于已有类型推导出新类型。这样数据结构变化时，相关类型能自动跟着变化。\n\n我最早觉得映射类型只是 TypeScript 的“高级语法”。后来做表单和配置平台多了，才发现它真正解决的是维护成本：后端模型、表单草稿、更新接口、展示字段、权限开关，这些结构经常长得很像，但又不是完全一样。如果每一份都手写，字段一多就很容易漏。\n\n所以这篇不是为了写复杂类型，而是记录一个更实用的思路：同一个业务模型的不同视角，尽量让类型关系可推导。\n\n## 从 keyof 开始\n\n`keyof` 可以拿到一个对象类型的所有 key。\n\n```typescript\ntype User = {\n  name: string;\n  age: number;\n};\n\ntype UserKey = keyof User;\n```\n\n这里的 `UserKey` 等于 `\"name\" | \"age\"`。\n\n有了 key 的联合类型，就可以继续基于这些 key 生成新类型。\n\n`keyof` 常用于约束函数参数：\n\n```typescript\nfunction getValue<T, K extends keyof T>(target: T, key: K): T[K] {\n  return target[key];\n}\n\nconst user = {\n  name: 'Tom',\n  age: 18,\n};\n\nconst name = getValue(user, 'name');\n```\n\n这里 `K extends keyof T` 保证 key 必须来自对象本身，`T[K]` 则表示对应 key 的值类型。理解这两个语法后，映射类型会更自然。\n\n这个写法在配置平台里很常见。比如表格列配置里要声明 `dataIndex`，如果直接写成 `string`，很容易写错字段名还不报错。用 `keyof` 约束后，字段改名时类型系统能第一时间提醒。\n\n```typescript\ntype Column<T> = {\n  title: string;\n  dataIndex: keyof T;\n};\n\nconst userColumns: Column<User>[] = [\n  { title: '姓名', dataIndex: 'name' },\n  { title: '年龄', dataIndex: 'age' },\n];\n```\n\n## 什么是映射类型\n\n映射类型可以遍历一个类型的 key，并生成新的属性。\n\n```typescript\ntype ReadonlyUser = {\n  readonly [Key in keyof User]: User[Key];\n};\n```\n\n这段代码会生成：\n\n```typescript\ntype ReadonlyUser = {\n  readonly name: string;\n  readonly age: number;\n};\n```\n\n如果以后 `User` 新增 `email`，`ReadonlyUser` 也会自动跟着变化。\n\n映射类型的基本模板可以记成：\n\n```typescript\ntype NewType<T> = {\n  [Key in keyof T]: T[Key];\n};\n```\n\n`Key in keyof T` 负责遍历属性名，`T[Key]` 负责取出属性值类型。后面加 `readonly`、`?`、重命名 key，都是在这个模板上变化。\n\n映射类型最有价值的地方不是少写几行代码，而是把“这两个类型有关联”表达出来。以后改 `User` 时，不需要靠人记得同步 `ReadonlyUser`、`UserDraft`、`UserValidators`。\n\n## Partial 的原理\n\n我们常用的 `Partial` 就是映射类型。\n\n```typescript\ntype MyPartial<T> = {\n  [Key in keyof T]?: T[Key];\n};\n```\n\n它的作用是把一个类型的所有属性变成可选。\n\n```typescript\ntype UserDraft = MyPartial<User>;\n```\n\n适合编辑表单、更新接口参数这类场景。\n\n不过 `Partial` 也不能乱用。比如创建用户接口如果明确要求 `name` 必填，就不要直接把 payload 写成 `Partial<User>`。它太宽了，会把不该可选的字段也变成可选。\n\n更稳的做法是先区分业务动作：\n\n```typescript\ntype CreateUserPayload = Pick<User, 'name' | 'age'>;\ntype UpdateUserPayload = Partial<Pick<User, 'name' | 'age'>>;\n```\n\n这样类型能表达真实约束：创建必须给核心字段，更新可以只传变化字段。\n\n和它相对的是 `Required`：\n\n```typescript\ntype MyRequired<T> = {\n  [Key in keyof T]-?: T[Key];\n};\n```\n\n这里的 `-?` 表示移除可选修饰符。类似地，`-readonly` 可以移除只读修饰符：\n\n```typescript\ntype Mutable<T> = {\n  -readonly [Key in keyof T]: T[Key];\n};\n```\n\n这些修饰符让我们可以在类型层面批量调整属性规则。\n\n## Pick、Record 和 Omit\n\n`Pick` 可以从一个类型里挑出部分字段：\n\n```typescript\ntype MyPick<T, K extends keyof T> = {\n  [Key in K]: T[Key];\n};\n\ntype UserBase = MyPick<User, 'name'>;\n```\n\n`Record` 则是根据一组 key 生成同一种值类型：\n\n```typescript\ntype Role = 'admin' | 'editor' | 'guest';\n\ntype RolePermission = Record<Role, boolean>;\n```\n\n它等价于：\n\n```typescript\ntype RolePermission = {\n  admin: boolean;\n  editor: boolean;\n  guest: boolean;\n};\n```\n\n`Omit` 可以理解为先排除一部分 key，再 `Pick` 剩余字段。业务里经常用它从完整模型里去掉不该提交的字段：\n\n```typescript\ntype CreateUserPayload = Omit<User, 'id' | 'createdAt'>;\n```\n\n这些工具类型背后都离不开 `keyof`、映射类型和条件类型。\n\n`Record` 在权限、字典和状态映射里尤其好用。比如订单状态和展示文案必须一一对应，用 `Record` 可以避免漏配：\n\n```typescript\ntype OrderStatus = 'pending' | 'paid' | 'cancelled';\n\nconst statusText: Record<OrderStatus, string> = {\n  pending: '待支付',\n  paid: '已支付',\n  cancelled: '已取消',\n};\n```\n\n如果后面新增 `refunded`，这里会直接报错，提醒你补文案。这个提醒比线上看到一个空状态要早得多。\n\n## 重新映射 key\n\nTypeScript 还支持在映射时改变 key 名称。\n\n```typescript\ntype Permissions<T> = {\n  [Key in keyof T as `canChange${Capitalize<string & Key>}`]: boolean;\n};\n```\n\n如果传入：\n\n```typescript\ntype Config = {\n  username: string;\n  layout: string;\n};\n```\n\n就能得到：\n\n```typescript\ntype ConfigPermissions = {\n  canChangeUsername: boolean;\n  canChangeLayout: boolean;\n};\n```\n\n这种能力很适合把业务模型转换成权限、表单、校验器等衍生类型。\n\n在配置平台里，我更常见的用法是把字段模型转换成权限开关。比如某些字段只有管理员能编辑，就可以让权限对象跟着字段走，而不是手写一份容易漏的配置。\n\n除了新增前缀，也可以过滤 key。例如只保留字符串字段：\n\n```typescript\ntype StringFieldKeys<T> = {\n  [Key in keyof T]: T[Key] extends string ? Key : never;\n}[keyof T];\n\ntype UserStringKeys = StringFieldKeys<User>;\n```\n\n这里最后的 `[keyof T]` 是把映射后的对象类型再取值，得到一个联合类型。这个写法刚看会绕，但在工具类型里很常见。\n\n如果项目里用到了 TypeScript 4.9 之后的 `satisfies`，也可以把它和映射类型配合起来做配置校验。它不会把对象强行收窄成目标类型，但能检查结构是否满足约束：\n\n```typescript\nconst validators = {\n  name: (value: string) => (value ? null : '请输入用户名'),\n} satisfies Validators<User>;\n```\n\n这类写法很适合“配置对象要被检查，但又希望保留字面量推断”的场景。\n\n## 映射类型适合哪些业务\n\n映射类型很适合处理“同一个数据模型的不同视角”：\n\n- 表单草稿：所有字段可选\n- 详情展示：所有字段只读\n- 更新接口：只允许提交部分字段\n- 权限配置：每个字段对应一个权限开关\n- 校验规则：每个字段对应一个校验函数\n\n例如校验规则：\n\n```typescript\ntype Validators<T> = {\n  [Key in keyof T]?: (value: T[Key]) => string | null;\n};\n\nconst userValidators: Validators<User> = {\n  name(value) {\n    return value ? null : '请输入用户名';\n  },\n};\n```\n\n这样 `name` 校验函数拿到的就是 `string`，`age` 校验函数拿到的就是 `number`。类型能跟着字段走，减少手动维护。\n\n类似的还有表单状态：\n\n```typescript\ntype FieldState<T> = {\n  [Key in keyof T]: {\n    value: T[Key];\n    touched: boolean;\n    error?: string;\n  };\n};\n```\n\n如果 `User.age` 是 `number`，那么 `FieldState<User>['age'].value` 也会是 `number`。这种类型关系能让表单封装更可靠，尤其是字段很多的中后台页面。\n\n## 不要为了复杂而复杂\n\n映射类型很强，但也容易写过头。\n\n如果一个类型工具需要嵌套多层条件、递归和模板字符串，团队里大多数人都看不懂，那它可能会成为维护负担。类型系统应该帮助表达业务约束，而不是把简单问题变成类型体操。\n\n我会优先把常用工具封装成清楚的名字：\n\n```typescript\ntype FormDraft<T> = Partial<T>;\ntype ReadonlyModel<T> = Readonly<T>;\n```\n\n名字清楚，比每次把复杂映射类型直接写在业务代码里更好。\n\n如果确实需要写复杂工具类型，我会先问两个问题：\n\n- 这个工具类型是否解决了真实重复维护问题\n- 团队里其他人能不能在几分钟内理解它的输入和输出\n\n如果答案是否定的，宁愿写得朴素一点。类型系统是为了降低维护成本，不是为了证明语法掌握得多。\n\n## 小结\n\n映射类型的价值是让类型之间建立明确关系。\n\n当一个类型变化时，相关类型可以自动同步，减少重复维护。理解 `keyof`、`T[Key]` 和 `[Key in keyof T]` 后，很多 TypeScript 工具类型都会变得容易理解。\n\n真正写业务时，不需要一上来追求复杂类型。先掌握 `Partial`、`Pick`、`Omit`、`Record` 这些常用工具，再理解它们背后的映射类型，类型设计会稳很多。\n"},"recommendedPosts":[{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":42,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":222,"readTime":null,"coverImage":null},{"title":"TanStack Router 的类型安全路由：把跳转和参数错误挪到编译期","date":"2026-02-21","excerpt":"用过一段时间 TanStack Router 后的总结：文件式与代码式路由、search params 的类型校验、loader 数据预取，以及它和 React Router 的取舍。","tags":["React","TypeScript","前端基础"],"slug":"tanstack-router-type-safe-2026","category":"2026","likeCount":25,"postId":"ba186679-ff87-187d-4d34-cff3f06231d3","viewCount":625,"readTime":null,"coverImage":null},{"title":"TypeScript 7 用 Go 重写之后：我更关心反馈链路有没有变短","date":"2025-12-05","excerpt":"从大型前端项目的类型检查、编辑器响应和 CI 等待说起，聊聊我会怎么评估 TypeScript 7 的性能收益和升级风险。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"frontend-typescript-7-go-2025","category":"2025","likeCount":7,"postId":"9ad00781-0f5b-878b-d63a-498ed48ee8ec","viewCount":1510,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端 CI/CD 实践：把发布从手工步骤变成可回放流程","date":"2022-08-16","excerpt":"从 lint、test、build、缓存、预览环境和回滚几个环节，记录我在前端项目里使用 GitHub Actions 搭建发布流程的思路。","tags":["构建与模块化","前端基础"],"slug":"frontend-ci-cd-github-actions-2022","category":"2022","likeCount":6,"postId":"a3658bda-111d-4be9-1c67-184f71ede2b8","viewCount":1304,"readTime":null,"coverImage":null},"nextPost":{"title":"前端拖拽排序怎么选库：别一上来就自己写","date":"2022-07-27","excerpt":"整理前端拖拽排序常见库的选择思路，并记录数据同步、移动端手势、并发保存和可访问性这些生产细节。","tags":["前端基础","交互与动效"],"slug":"frontend-drag-sort-libraries-2022","category":"2022","likeCount":55,"postId":"1cebaf33-2eed-3c86-20e4-44af5730f2db","viewCount":791,"readTime":null,"coverImage":null}},"__N_SSG":true}