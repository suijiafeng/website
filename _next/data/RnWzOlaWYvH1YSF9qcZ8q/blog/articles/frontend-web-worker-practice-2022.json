{"pageProps":{"postData":{"title":"Web Worker 实践：把耗时任务从主线程里挪出去","date":"2022-10-11","excerpt":"结合前端性能优化场景，说明 Web Worker 适合处理哪些任务、如何通信、如何处理错误，以及不适合滥用的边界。","tags":["前端基础","JavaScript","性能与缓存"],"slug":"frontend-web-worker-practice-2022","category":"2022","likeCount":6,"postId":"ed2150e0-8302-7f20-9ca6-16c9a6ae46df","viewCount":2401,"readTime":"6分钟","coverImage":null,"content":"\n前端页面卡顿时，很多人第一反应是减少 DOM、优化 CSS、拆分组件。这些方向都可能有用，但如果真正的问题是 JavaScript 长任务占满主线程，就需要换一个思路。\n\n浏览器主线程负责很多事情：执行 JS、处理用户输入、样式计算、布局、绘制。一个复杂计算如果连续跑几百毫秒，用户就会感觉页面卡住。Web Worker 的价值，就是把一部分耗时任务从主线程挪出去。\n\n随着前端承担的数据处理越来越多，Worker 在后台系统、数据可视化、文件处理、音视频处理里变得更常见。\n\n我第一次明显感受到 Worker 的价值，是做一个导入校验页面。用户上传几万行数据后，前端要做格式检查、重复项检查和预览。如果全部在主线程跑，输入框、滚动和按钮都会卡住。把解析和校验挪到 Worker 后，任务总耗时不一定大幅下降，但页面终于能正常响应了。\n\n## 什么任务适合 Worker\n\n适合放进 Worker 的任务通常有几个特点：\n\n- 计算耗时明显\n- 不需要直接操作 DOM\n- 输入输出可以序列化\n- 对响应性要求高\n\n常见场景包括：\n\n- 大文件解析\n- CSV、Excel 数据预处理\n- 图片压缩\n- 搜索索引构建\n- 数据可视化前的聚合计算\n- 加密、哈希、压缩\n\n不适合 Worker 的任务也很明确：\n\n- 直接修改 DOM\n- 依赖大量浏览器 UI API\n- 非常轻量的小计算\n- 和主线程频繁来回通信的细碎任务\n\nWorker 不是万能性能开关。它解决的是主线程被计算占用的问题，不解决网络慢、图片大、布局抖动这些问题。\n\n所以使用前我会先录一次 Performance，看是不是主线程长任务。如果瓶颈是接口慢或渲染列表太大，Worker 不是第一优先级。\n\n## 基础用法\n\n主线程创建 Worker：\n\n```js\nconst worker = new Worker('/workers/sum.js')\n\nworker.postMessage({\n  numbers: [1, 2, 3, 4, 5],\n})\n\nworker.onmessage = (event) => {\n  console.log('result:', event.data.result)\n}\n\nworker.onerror = (event) => {\n  console.error('worker error:', event.message)\n}\n```\n\nWorker 文件：\n\n```js\nself.onmessage = (event) => {\n  const { numbers } = event.data\n  const result = numbers.reduce((sum, value) => sum + value, 0)\n\n  self.postMessage({ result })\n}\n```\n\n通信基于消息传递。主线程和 Worker 不共享普通对象，数据会通过结构化克隆传过去。\n\n## 大数据通信要注意成本\n\n把任务放进 Worker 不代表没有成本。数据传输本身也会消耗时间。\n\n如果你把一个很大的数组频繁传来传去，性能可能并不会变好。对于 `ArrayBuffer` 这类数据，可以使用 Transferable Objects，把所有权转移过去，避免复制。\n\n```js\nconst buffer = new ArrayBuffer(1024 * 1024)\n\nworker.postMessage(\n  {\n    buffer,\n  },\n  [buffer]\n)\n```\n\n转移后，主线程里的 `buffer` 就不能继续使用了。这是性能和所有权之间的交换。\n\n处理大文件、二进制数据时，这个细节很重要。\n\n对于更大的数据，还要考虑分块。一次把所有内容都读入内存再传给 Worker，可能会造成内存峰值过高。导入、解析、压缩这类任务最好能分阶段处理，并持续回报进度。\n\n## 一个 CSV 解析例子\n\n假设用户上传一个大 CSV，如果在主线程解析，页面可能明显卡顿。\n\n主线程：\n\n```js\nconst worker = new Worker('/workers/parse-csv.js')\n\nfunction parseCsv(file) {\n  worker.postMessage({ file })\n}\n\nworker.onmessage = (event) => {\n  const { type, payload } = event.data\n\n  if (type === 'progress') {\n    updateProgress(payload.percent)\n  }\n\n  if (type === 'done') {\n    renderTable(payload.rows)\n  }\n}\n```\n\nWorker：\n\n```js\nself.onmessage = async (event) => {\n  const { file } = event.data\n  const text = await file.text()\n  const lines = text.split('\\n')\n  const rows = []\n\n  lines.forEach((line, index) => {\n    rows.push(line.split(','))\n\n    if (index % 1000 === 0) {\n      self.postMessage({\n        type: 'progress',\n        payload: {\n          percent: Math.round((index / lines.length) * 100),\n        },\n      })\n    }\n  })\n\n  self.postMessage({\n    type: 'done',\n    payload: { rows },\n  })\n}\n```\n\n这个例子为了说明思路，CSV 解析很简化。真实项目里要处理引号、换行、转义、编码等问题，最好使用成熟解析库。\n\n还有一点很重要：Worker 返回给主线程的数据也不要太大。解析完十万行后，如果一次性把完整二维数组传回来，主线程接收和渲染仍然会卡。更实际的做法是分页返回预览数据，完整数据留在 Worker 或后端流程里继续处理。\n\n## 错误处理要统一\n\nWorker 内部错误不能只靠控制台看。应该把错误传回主线程：\n\n```js\nself.onmessage = async (event) => {\n  try {\n    const result = await runTask(event.data)\n    self.postMessage({\n      type: 'success',\n      payload: result,\n    })\n  } catch (error) {\n    self.postMessage({\n      type: 'error',\n      error: {\n        code: error.code || 'WORKER_ERROR',\n        message: error.message || '任务处理失败',\n      },\n    })\n  }\n}\n```\n\n主线程统一处理：\n\n```js\nworker.onmessage = (event) => {\n  const message = event.data\n\n  if (message.type === 'error') {\n    showToast(message.error.message)\n    reportError(message.error)\n    return\n  }\n\n  handleResult(message.payload)\n}\n```\n\n这和 API 错误处理是同一个原则：错误结构要稳定，调用方才好处理。\n\n## Worker 协议要设计清楚\n\nWorker 通信最好不要只传随意对象。任务一多，消息结构会变乱。\n\n我现在更倾向于约定一个简单协议：\n\n```ts\ntype WorkerMessage =\n  | { type: 'start'; taskId: number; payload: unknown }\n  | { type: 'progress'; taskId: number; percent: number }\n  | { type: 'success'; taskId: number; payload: unknown }\n  | { type: 'error'; taskId: number; error: { code: string; message: string } }\n```\n\n有了 `type` 和 `taskId`，主线程就能稳定处理进度、成功、失败和过期任务。不要等 Worker 逻辑变复杂后再补协议。\n\n## 取消任务\n\nWorker 任务可能很久，用户切换页面或重新上传文件时，需要取消旧任务。\n\n最简单的方式是终止 Worker：\n\n```js\nworker.terminate()\n```\n\n如果希望复用 Worker，可以设计任务 id：\n\n```js\nlet currentTaskId = 0\n\nfunction startTask(payload) {\n  currentTaskId += 1\n  worker.postMessage({\n    taskId: currentTaskId,\n    payload,\n  })\n}\n\nworker.onmessage = (event) => {\n  if (event.data.taskId !== currentTaskId) {\n    return\n  }\n\n  handleMessage(event.data)\n}\n```\n\n这样旧任务即使返回，也不会覆盖新任务结果。\n\n如果任务本身支持分段循环，也可以在 Worker 内部检查取消标记，而不是只能粗暴 `terminate`。`terminate` 简单有效，但会丢掉 Worker 内部状态；长期复用 Worker 时，任务级取消会更细。\n\n## 打包工具里的 Worker\n\n在现代构建工具里，Worker 可以用模块方式引入：\n\n```js\nconst worker = new Worker(\n  new URL('./worker.ts', import.meta.url),\n  { type: 'module' }\n)\n```\n\n这种写法比手写 `/workers/xxx.js` 更适合工程化项目，因为构建工具可以处理路径、hash 和 TypeScript。\n\n如果项目使用 Vite 或 webpack，要确认 Worker 的构建产物和部署路径是否正确。Worker 文件 404 是很常见的问题。\n\n## Worker 数量也要控制\n\n不要每个任务都无限创建 Worker。\n\nWorker 本身也有启动和内存成本。批量任务里，如果一次创建几十个 Worker，页面同样可能变慢。我一般会控制成一个 Worker 或一个小型 Worker 池，根据任务类型限制并发。\n\n```text\n轻任务：主线程或单 Worker\n重任务：单 Worker + 进度\n多文件批处理：小 Worker 池 + 队列\n```\n\n这和请求并发控制很像，目标不是把机器打满，而是让页面保持可用。\n\n## 小结\n\nWeb Worker 适合把耗时计算从主线程挪出去，让页面保持响应。它尤其适合大文件解析、数据预处理、图片压缩和复杂计算。\n\n但 Worker 不是性能万能药。使用前要确认瓶颈确实在主线程计算；使用时要注意通信成本、错误结构、任务取消和构建路径。把这些边界处理好，Worker 才能真正改善用户体验，而不是给项目增加一层难排查的复杂度。\n"},"recommendedPosts":[{"title":"WebAssembly 3.0 我会怎么判断该不该用：别把它当成更快的 JS","date":"2025-09-20","excerpt":"从图像处理、文件解析和跨语言复用说起，记录我如何判断 WebAssembly 是否真的适合前端项目，以及它会带来哪些成本。","tags":["前端基础","JavaScript","性能与缓存"],"slug":"frontend-webassembly-3-2025","category":"2025","likeCount":51,"postId":"62dae08d-dc55-8a4c-89a2-8f2d8c981441","viewCount":459,"readTime":null,"coverImage":null},{"title":"前端并发控制：别让 Promise.all 一次打满所有请求","date":"2025-04-22","excerpt":"介绍前端批量请求时的并发控制思路，说明为什么限制同时执行数量比一次性 Promise.all 更稳。","tags":["前端基础","JavaScript","性能与缓存"],"slug":"frontend-concurrency-control","category":"2025","likeCount":5,"postId":"69a8ae27-190d-d3a3-dcdb-82f4fdebc9ee","viewCount":1113,"readTime":null,"coverImage":null},{"title":"DocumentFragment：批量插入 DOM 时的一个实用技巧","date":"2018-08-28","excerpt":"介绍 createDocumentFragment 的作用，补充批量插入、replaceChildren、模板克隆和事件委托等原生 DOM 操作实践。","tags":["前端基础","JavaScript","性能与缓存"],"slug":"frontend-document-fragment-2018","category":"2018","likeCount":7,"postId":"5f701a9d-cffb-d19a-76ab-a5dd456a8057","viewCount":1108,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端 Monorepo 与 pnpm workspace：多包项目先把边界想清楚","date":"2022-12-06","excerpt":"结合前端工程化实践，讨论 pnpm workspace 管理多包项目的方式、适用场景、依赖边界和版本发布注意点。","tags":["构建与模块化","前端基础"],"slug":"frontend-monorepo-pnpm-workspace-2022","category":"2022","likeCount":5,"postId":"b16db011-44a7-a782-6230-94523e9962e0","viewCount":2533,"readTime":null,"coverImage":null},"nextPost":{"title":"前端主题切换怎么做：从深色模式到多主题","date":"2022-10-05","excerpt":"介绍前端主题切换的常见方案，说明 CSS 变量、全局 class、组件库主题和设计 token 分层之间的关系。","tags":["前端基础","CSS"],"slug":"frontend-theme-switching-2022","category":"2022","likeCount":5,"postId":"90af5be6-8a98-9853-df01-8c166e347755","viewCount":2611,"readTime":null,"coverImage":null}},"__N_SSG":true}