{"pageProps":{"postData":{"title":"Vue 动态组件和 keep-alive：后台 tab 页别每次都重新加载","date":"2018-10-28","excerpt":"记录 Vue 后台系统中动态组件、keep-alive、tab 缓存、刷新页面和状态清理的实践经验。","tags":["Vue","前端基础"],"slug":"vue-dynamic-components-tabs-2018","category":"2018","likeCount":6,"postId":"6360f47d-c9da-cca9-475f-aff8d6cea6c9","viewCount":2908,"readTime":"8分钟","coverImage":null,"content":"\n我接手过一个 CRM 后台，产品同学提的第一个需求不是加功能，而是「这破系统能不能别一切回去就把我刚填的筛选条件清空」。运营每天要在「客户列表」「订单详情」「跟进记录」几个 tab 之间来回横跳，列表页有十几个筛选条件，翻到第 8 页去看某条订单，回来一看——筛选没了、页码归零、滚动条弹回顶部。一天下来重复劳动的怨气全堆在我们前端头上。\n\n这事儿的根子在于：每次切 tab，Vue 都把上一个路由组件销毁了，再切回来又重新 `create` 一遍，所有组件内 state 自然清零。要解决它，绕不开两个东西——动态组件和 `keep-alive`。这篇就把我踩过的坑、最后落地的方案完整记一遍。技术栈是 Vue 2.5 + vue-router 3.0，2018 年后台项目的标配。\n\n## 先搞清楚「动态组件」到底动态在哪\n\n很多人一上来就用 `keep-alive` 包 `router-view`，但其实动态组件（`<component :is>`）和路由缓存是两条腿，搞混了后面会很乱。\n\n动态组件是这样的：\n\n```html\n<component :is=\"currentComponent\" />\n```\n\n`currentComponent` 是个字符串或组件对象，它一变，Vue 就卸掉旧组件、挂上新组件：\n\n```js\nexport default {\n  components: { UserList, UserDetail, OrderForm },\n  data() {\n    return {\n      currentComponent: 'UserList'\n    }\n  },\n  methods: {\n    switchTo(name) {\n      this.currentComponent = name\n    }\n  }\n}\n```\n\n这种写法适合「同一个壳子里换内容」的场景，比如一个配置向导分好几步，每步是一个组件；或者一个 tab 容器，tab 内容由配置数据驱动、跟路由没关系。它最大的特点是**不走路由**，URL 不变，刷新页面状态就没了。\n\n后台 tab 大多数情况要的是另一种——**路由级别的页面缓存**，地址栏要变、要能分享链接、要能前进后退。所以下面的重点会放在 `keep-alive + router-view` 上。但记住动态组件这条路存在，有些纯前端交互（比如一个弹窗里的分步表单）用它反而比硬塞进路由清爽。\n\n## keep-alive：让组件「离开但不死」\n\n把 `router-view` 包进 `keep-alive`，路由组件切走时就不销毁，而是被「冻」在内存里：\n\n```html\n<keep-alive>\n  <router-view />\n</keep-alive>\n```\n\n这时候组件的生命周期会变。原来切走是 `beforeDestroy / destroyed`，现在变成 `deactivated`；切回来不再是 `created / mounted`，而是 `activated`：\n\n```js\nexport default {\n  name: 'UserList',\n  created() {\n    // 只在第一次进入时执行一次\n    console.log('UserList created')\n  },\n  activated() {\n    console.log('页面被重新激活')\n  },\n  deactivated() {\n    console.log('页面离开但未销毁，state 都还在')\n  }\n}\n```\n\n这里有个我当年栽过的坑：列表页的数据请求我习惯性写在 `created` 里。加了 `keep-alive` 之后，列表数据是「保住了」，但产品又提需求——从详情页改完数据返回列表，列表得显示最新的。结果因为 `created` 只跑一次，回来的列表还是旧数据。\n\n我的处理原则是把「请求时机」分两类：\n\n- **要保留状态、不需要刷新的**：请求留在 `created`，靠 `keep-alive` 把 state 冻住，回来啥都不动。\n- **要保留筛选条件、但数据可能变了的**：请求挪到 `activated`，但筛选条件、页码这些查询参数本身存在组件 data 里不动，`activated` 时拿着这些参数重新拉数据。\n\n```js\nexport default {\n  name: 'UserList',\n  data() {\n    return {\n      query: { keyword: '', status: '', page: 1 }, // 这些被 keep-alive 保住\n      list: []\n    }\n  },\n  activated() {\n    // 用保留下来的 query 重新拉一次，数据是新的、条件是旧的\n    this.fetchList()\n  },\n  methods: {\n    async fetchList() {\n      const { data } = await this.$http.get('/users', { params: this.query })\n      this.list = data.items\n    }\n  }\n}\n```\n\n这样运营回到列表，筛选条件原封不动，但能看到刚改的最新数据，两头都满足。\n\n滚动位置也是同理。`keep-alive` 不会自动帮你恢复滚动条，因为 DOM 虽然没销毁，但浏览器的滚动是挂在外层容器上的。我的做法是 `deactivated` 时记下 `scrollTop`，`activated` 时还原：\n\n```js\ndeactivated() {\n  this.savedScrollTop = this.$refs.scrollWrap.scrollTop\n},\nactivated() {\n  // 等 DOM 渲染完再设置，否则可能被覆盖\n  this.$nextTick(() => {\n    this.$refs.scrollWrap.scrollTop = this.savedScrollTop || 0\n  })\n}\n```\n\n## include / exclude：别什么页面都缓存\n\n`keep-alive` 不加约束的话，所有路由组件都会被缓存，内存只增不减，后台开一天能卡到飞起。而且有些页面就不该缓存——比如「新建订单」，每次进来都该是干净的空表单，你缓存它干嘛。\n\n用 `include` 白名单控制：\n\n```html\n<keep-alive :include=\"cachedViews\">\n  <router-view />\n</keep-alive>\n```\n\n```js\ndata() {\n  return {\n    cachedViews: ['UserList', 'OrderList', 'FollowupRecord']\n  }\n}\n```\n\n`include` 匹配的是**组件的 name**，不是路由的 name，也不是文件名。这是另一个超容易踩的坑：我第一次配好了发现死活不缓存，查了半天，原因是组件压根没写 `name`。\n\n```js\nexport default {\n  name: 'UserList', // 没有这行，include 永远匹配不上它\n  // ...\n}\n```\n\n`include` 可以是数组、逗号分隔的字符串、也可以是正则。后台动态 tab 一般用数组，因为要动态增删。另外提一句，Vue 2.6 才支持给 `include` 传组件实例匹配，2018 年大部分项目还在 2.5，老老实实用 name 字符串就好。\n\n## 把缓存和 tab 状态打通\n\n真正的后台 tab 系统，缓存列表不能写死，得跟着用户开关 tab 动态变。我通常用 Vuex 管这份 state：\n\n```js\n// store/modules/tabsView.js\nconst state = {\n  visitedTabs: [],   // 顶部显示的 tab\n  cachedViews: []    // 给 keep-alive 用的 include\n}\n\nconst mutations = {\n  ADD_TAB(state, view) {\n    if (state.visitedTabs.some(v => v.path === view.path)) return\n    state.visitedTabs.push({ name: view.name, title: view.meta.title, path: view.path })\n    // meta.keepAlive 为 true 的才进缓存\n    if (view.meta.keepAlive && state.cachedViews.indexOf(view.name) === -1) {\n      state.cachedViews.push(view.name)\n    }\n  },\n  REMOVE_TAB(state, view) {\n    state.visitedTabs = state.visitedTabs.filter(v => v.path !== view.path)\n    const i = state.cachedViews.indexOf(view.name)\n    if (i > -1) state.cachedViews.splice(i, 1)\n  }\n}\n```\n\n路由里用 `meta` 标记哪些页面需要缓存，这样缓存策略跟着路由配置走，不用散落在各处：\n\n```js\n{\n  path: '/user/list',\n  name: 'UserList',\n  component: () => import('@/views/user/list'),\n  meta: { title: '客户列表', keepAlive: true }\n}\n```\n\n布局组件里监听路由变化往里塞 tab：\n\n```js\nwatch: {\n  $route(route) {\n    this.$store.commit('tabsView/ADD_TAB', route)\n  }\n},\ncomputed: {\n  cachedViews() {\n    return this.$store.state.tabsView.cachedViews\n  }\n}\n```\n\n## 关闭 tab 一定要清缓存，否则是个隐形内存泄漏\n\n这是当初最隐蔽的一个 bug。用户点 tab 上的小叉关页面，我只做了从 `visitedTabs` 移除、跳到相邻 tab，UI 上看页面是「关了」。但 `cachedViews` 里没删，组件实例还冻在内存里。\n\n后果有两个：一是内存一直涨；二更坑——用户重新打开同一个页面，`keep-alive` 发现缓存里有，直接复活旧实例，于是看到的是**上次关闭前的脏状态**，根本不是新页面。用户一脸懵：我不是关了吗，咋还是老样子。\n\n所以关 tab 必须两边一起清：\n\n```js\ncloseTab(view) {\n  this.$store.commit('tabsView/REMOVE_TAB', view)\n  // 如果关的是当前页，跳到最后一个还开着的 tab\n  if (this.$route.path === view.path) {\n    const tabs = this.$store.state.tabsView.visitedTabs\n    const last = tabs[tabs.length - 1]\n    this.$router.push(last ? last.path : '/')\n  }\n}\n```\n\n## 「刷新当前页」怎么做：keep-alive 反而碍事了\n\n后台还有个高频需求：tab 上点「刷新」，把当前页彻底重来一遍。难点在于 `keep-alive` 把组件冻住了，你 `this.$router.push` 到自己的路由，组件根本不会重新创建。\n\n我试过几个办法，最后稳妥的是「先把它踢出缓存，跳个空中转路由，再跳回来」：\n\n```js\n// 中转路由 /redirect/:path\n{\n  path: '/redirect/:path(.*)',\n  component: {\n    beforeRouteEnter(to, from, next) {\n      next(vm => vm.$router.replace('/' + to.params.path))\n    },\n    render: h => h() // 不渲染任何东西\n  }\n}\n```\n\n```js\nrefreshCurrentTab() {\n  const { name, fullPath } = this.$route\n  // 从缓存移除，强制下次重新创建\n  this.$store.commit('tabsView/REMOVE_CACHE', name)\n  this.$nextTick(() => {\n    this.$router.replace('/redirect' + fullPath)\n    // 重新加回缓存\n    this.$store.commit('tabsView/ADD_CACHE', name)\n  })\n}\n```\n\n中转路由那一下让组件经历了一次真正的销毁和重建。比起 `this.$forceUpdate()` 或者给 `router-view` 加 `key` 这类偏方，这个方案干净，而且 element-admin 那套模板也是这么干的，经过验证。\n\n## 表单页和详情页：别盲目跟着缓存\n\n列表、查询这类页面缓存收益最大，但**编辑表单页我建议默认不缓存**，这是血泪教训。\n\n场景是这样：用户打开「编辑客户 A」，填了一半切走，又打开「编辑客户 B」。如果这俩走的是同一个路由组件（`/user/edit/:id`），`keep-alive` 又缓存了它，Vue 会复用同一个组件实例——A 没填完的数据就串到了 B 的表单上。轻则数据错乱，重则把 A 的内容存到了 B 头上，线上事故。\n\n根因是 `keep-alive` 缓存的粒度是「组件」，不是「组件 + 参数」。同一路由不同 `:id` 用的是同一个缓存实例。两条出路：\n\n一是干脆别缓存编辑页，进来就请求，离开就销毁，最省心：\n\n```js\n{\n  path: '/user/edit/:id',\n  name: 'UserEdit',\n  component: () => import('@/views/user/edit'),\n  meta: { title: '编辑客户', keepAlive: false }\n}\n```\n\n二是如果非要缓存，就在 `beforeRouteUpdate` 里按参数重新加载——因为同路由切 `:id` 不会触发 `created`，只会触发 `beforeRouteUpdate`：\n\n```js\nbeforeRouteUpdate(to, from, next) {\n  this.fetchDetail(to.params.id)\n  next()\n}\n```\n\n还有个更暴力但有效的隔离手段：给 `router-view` 套 `key`，让不同参数被当成不同组件，逼着它重建：\n\n```html\n<keep-alive :include=\"cachedViews\">\n  <router-view :key=\"$route.fullPath\" />\n</keep-alive>\n```\n\n不过加了 `fullPath` 作 key，缓存基本就失效了（每个 path 都是新实例），所以这招和缓存是互斥的，按需取舍。我的实际选择是：列表/查询页缓存、用 name；编辑/详情页不缓存、靠 `beforeRouteUpdate` 处理参数变化。\n\n## 几个方案放一起对比\n\n| 需求 | 方案 | 注意点 |\n| --- | --- | --- |\n| 同壳换内容、不走路由 | `<component :is>` | 刷新即丢状态 |\n| tab 切走保留状态 | `keep-alive` 包 `router-view` | 组件必须有 name |\n| 控制谁缓存 | `include` + `meta.keepAlive` | 匹配 name 非路由名 |\n| 关 tab | 同步删 `cachedViews` | 不删会内存泄漏+脏状态复活 |\n| 刷新当前页 | 踢缓存 + redirect 中转路由 | 比 forceUpdate 可靠 |\n| 编辑页防数据串 | 不缓存 / `beforeRouteUpdate` | 缓存粒度是组件非参数 |\n\n## 小结\n\n`keep-alive` 确实能把后台 tab 体验救回来，运营再也没因为筛选条件丢失骂过街。但它不是开了就万事大吉的开关，真正花功夫的是缓存范围的控制、关闭时的清理、刷新的实现，以及哪些页面**不该**碰它。\n\n我现在的判断标准很简单：这个页面用户离开后再回来，希望看到原样还是看到干净的？想看原样的（列表、查询、看板）就缓存；想要干净的（新建、编辑、带参数详情）就别缓存。缓存从来不是越多越好，它是用内存和复杂度换体验，得算账。\n"},"recommendedPosts":[{"title":"刚开始用 Vue3 时，最容易踩的几个坑","date":"2023-06-07","excerpt":"整理 Vue3 入门和迁移时常见的响应式、props、watch、组合式 API、生命周期清理和 Pinia 使用误区。","tags":["Vue","前端基础"],"slug":"vue3-common-mistakes-2023","category":"2023","likeCount":6,"postId":"06bb42c9-56d9-0865-c906-88f606e068a9","viewCount":1085,"readTime":null,"coverImage":null},{"title":"前端请求库不只是发请求：从 Axios 到 Alova 的思路变化","date":"2023-05-04","excerpt":"介绍现代请求库从简单 HTTP 客户端走向请求状态管理、缓存、分页、取消请求和统一错误处理的趋势。","tags":["前端基础","请求与异步","Vue"],"slug":"frontend-request-alova-2023","category":"2023","likeCount":6,"postId":"12618464-0fa9-e0e6-111b-7f00caf1db51","viewCount":2362,"readTime":null,"coverImage":null},{"title":"Vue3 中封装 Element Plus 无限级菜单的基本思路","date":"2022-09-17","excerpt":"介绍 Vue3 和 Element Plus 中封装无限级菜单组件的核心数据结构、递归渲染、权限过滤和动态路由边界。","tags":["Vue","前端基础"],"slug":"vue3-element-plus-menu-2022","category":"2022","likeCount":7,"postId":"3462b2e8-f24c-3869-fb44-dc017a7c3f2f","viewCount":4229,"readTime":null,"coverImage":null}],"prevPost":{"title":"Vue 里 computed 和 watch 到底怎么选：别把两种能力写成一回事","date":"2018-11-16","excerpt":"回到 Vue 日常开发最容易混淆的一组能力，通过派生状态、异步副作用和错误处理示例讲清 computed 和 watch 的职责边界。","tags":["Vue","前端基础"],"slug":"vue-computed-watch-2018","category":"2018","likeCount":6,"postId":"afe7173c-7cea-829f-39ab-d16d3ae725af","viewCount":1249,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue history 模式部署：本地能打开，不代表线上刷新不 404","date":"2018-10-16","excerpt":"整理 Vue Router history 模式部署到 Nginx 时的刷新 404、二级目录、静态资源路径和缓存配置问题。","tags":["Vue","构建与模块化"],"slug":"nginx-vue-history-deploy-2018","category":"2018","likeCount":5,"postId":"3631e00d-daf5-37dd-a18e-012349096bf3","viewCount":5396,"readTime":null,"coverImage":null}},"__N_SSG":true}