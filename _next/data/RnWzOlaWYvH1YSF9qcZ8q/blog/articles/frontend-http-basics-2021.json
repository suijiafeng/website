{"pageProps":{"postData":{"title":"前端为什么也要懂 HTTP：请求能发出去，不代表你真的理解通信","date":"2021-06-21","excerpt":"从请求方法、状态码和报文语义出发，解释前端为什么不能只停留在会调接口这一步。","tags":["前端基础"],"slug":"frontend-http-basics-2021","category":"2021","likeCount":6,"postId":"f4bb7c80-bda3-662c-734d-d657e2cddecb","viewCount":1961,"readTime":"10分钟","coverImage":null,"content":"\n做前端久了会发现，很多问题表面看像页面 bug，实际往下挖都和 HTTP 通信细节有关。\n\n接口为什么返回 304，为什么跨域失败，为什么表单提交和 JSON 请求表现不同，为什么缓存策略会影响页面更新，这些都说明前端并不只是“把请求发出去”就够了。\n\n理解 HTTP，不是为了转行后端，而是为了更稳地处理前后端协作边界。\n\n## 请求方法不只是名字不同\n\n最常见的请求方法当然是 `GET` 和 `POST`。\n\n很多项目里大家会形成一种朴素印象：\n\n- `GET` 用来拿数据\n- `POST` 用来提交数据\n\n这个理解没错，但如果只停在这一步，很多接口设计和调试细节就会看不清。\n\n我自己印象最深的一次坑，是做一个商品搜索页。当时把筛选参数都拼在 `GET` 的 URL 里，本地测试一切正常，上线后却时不时出现“筛选条件变了但列表没变”的反馈。查了半天才发现是 CDN 和浏览器把这个 `GET` 请求缓存了——因为 `GET` 在语义上被认为是安全且幂等的，中间任何一层代理都有理由把它缓存起来。后来我们要么给 URL 带上时间戳打破缓存，要么对真正不该缓存的查询改用 `POST`。这件事让我第一次意识到：方法的选择不是个人偏好，它会被整条链路上的缓存、代理、日志系统按照语义去“理解”。\n\n幂等性这个概念也是后来才真正体会到的。`GET`、`PUT`、`DELETE` 在规范里都是幂等的——同样的请求发一次和发十次，服务端最终状态应该一样。而 `POST` 不是。这意味着如果一个“创建订单”的接口用 `POST`，那网络抖动时的自动重试就很危险，可能创建出两个订单；如果改成 `PUT` 并由前端生成一个唯一 id 作为资源标识，重试就安全得多。所以我现在看到一个“更新”接口，会先问后端到底是整体替换（`PUT`）还是局部修改（`PATCH`），这直接决定我前端要不要把整个对象都传上去。\n\n至于参数放 URL 还是放请求体，也有现实约束。URL 长度在很多服务器和浏览器里有几 KB 的隐性上限，参数一多（比如批量选了几百个 id）`GET` 就会直接 414。这种场景哪怕语义上是“查询”，我也会和后端商量改成 `POST` 传 body。语义和工程现实之间，需要权衡。\n\n## 状态码是接口沟通里最直接的信号\n\n前端最常见的几类状态码，大致可以这样理解：\n\n- 2xx：请求成功\n- 3xx：资源位置或缓存状态发生了某种重定向/协商\n- 4xx：请求本身有问题或权限不满足\n- 5xx：服务端处理失败\n\n很多页面错误处理不清晰，就是因为把所有失败都粗暴地归成“网络错误”。\n\n实际上：\n\n- 401 和 403 往往意味着认证授权问题\n- 404 更像资源不存在\n- 500 通常是服务端异常\n\n如果前端不区分这些类型，用户看到的反馈就会非常模糊，排障成本也会更高。\n\n我后来在请求层里干脆按状态码分流处理，大概是这样：\n\n```js\nasync function request(url, options) {\n  const res = await fetch(url, options)\n\n  if (res.status === 401) {\n    // 登录态失效，统一跳登录，不要弹一个无意义的\"网络错误\"\n    redirectToLogin()\n    throw new HttpError('未登录或登录已过期', 401)\n  }\n  if (res.status === 403) {\n    // 已登录但没权限，提示就该是\"无权限\"而不是\"请重新登录\"\n    throw new HttpError('没有访问权限', 403)\n  }\n  if (res.status === 404) {\n    throw new HttpError('资源不存在', 404)\n  }\n  if (res.status >= 500) {\n    // 5xx 是服务端的锅，提示用户稍后重试，同时上报监控\n    reportError(url, res.status)\n    throw new HttpError('服务繁忙，请稍后再试', res.status)\n  }\n  return res.json()\n}\n```\n\n401 和 403 的区别尤其值得说一句。我见过太多项目把两者混在一起处理，结果一个明明只是“没权限看这个页面”的用户，被反复踢去重新登录，登完回来还是看不了，体验非常糟。401 是“你是谁我不知道”，403 是“我知道你是谁，但你不能进”，这两种该给用户的引导完全不同。\n\n还有一个容易忽略的是 3xx。前端发 `fetch` 时浏览器默认会自动跟随重定向，所以 `res.status` 你通常根本看不到 302，拿到的是最终那一跳的状态。如果某个接口靠重定向来做跳转逻辑，得在 `fetch` 里设置 `redirect: 'manual'` 才能拿到原始的 3xx，这个我也是踩过坑才知道的。\n\n## 请求成功不等于业务成功\n\n这是前端特别容易忽略的一点。\n\n浏览器层面拿到 200，不代表业务逻辑一定成功。很多接口会在响应体里再表达一次业务状态。\n\n例如：\n\n- HTTP 是 200，但业务码提示参数不合法\n- HTTP 是 200，但数据为空且有特殊提示\n- HTTP 是 200，但需要前端跳转登录\n\n这说明请求层和业务层要分开处理。只看浏览器 Network 面板里是不是 200，往往不够。\n\n我们项目里后端返回的结构基本都是这种形态：\n\n```json\n{ \"code\": 0, \"message\": \"ok\", \"data\": { ... } }\n```\n\n`code` 为 0 才算业务成功，非 0 就要按业务错误处理。所以我的请求层在 HTTP 层判完之后，还得再过一层业务码：\n\n```js\nconst json = await res.json()\n\nif (json.code !== 0) {\n  // HTTP 是 200，但业务失败\n  if (json.code === 10001) {\n    // 比如约定好的\"登录失效\"业务码\n    redirectToLogin()\n  }\n  throw new BizError(json.message, json.code)\n}\nreturn json.data\n```\n\n这里有个我吃过亏的细节：有的接口空数据时返回的 `data` 是 `null`，有的是 `[]`，有的甚至是 `{}`。如果前端不在请求层统一兜底，页面层就得到处写 `data?.list ?? []` 这种防御代码，漏一个就白屏。后来我们干脆约定好这类默认值由请求层或后端统一规范，省了大量零碎的判空。\n\n## Header 不是附属信息，它经常决定行为\n\n请求头和响应头里有很多看起来不显眼的字段，但它们实际影响非常大。\n\n例如：\n\n- `Content-Type` 决定数据怎么解析\n- 缓存相关头决定资源是否复用\n- 认证头决定用户是否有权限\n\n很多“前端明明发出去了，后端却说收不到”的问题，最后都落在请求头设置不对上。\n\n`Content-Type` 是我踩坑最多的一个头。最典型的就是表单提交和 JSON 请求的区别：\n\n```js\n// JSON 请求，后端按 @RequestBody 解析\nfetch('/api/user', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({ name: '张三', age: 18 }),\n})\n\n// 表单请求，后端按普通参数解析\nfetch('/api/user', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/x-www-form-urlencoded' },\n  body: new URLSearchParams({ name: '张三', age: '18' }).toString(),\n})\n```\n\n有一次我把数据 `JSON.stringify` 之后发出去，后端却说参数全是空的——原因就是后端那个接口按表单格式接收，而我发的是 `application/json`，body 它根本没去解析。光看前端 Network，请求体明明白白带着数据，但语义对不上，就是收不到。\n\n还有文件上传更要小心：用 `FormData` 时千万别自己手动设 `Content-Type`。浏览器需要自动生成一个带 `boundary` 分隔符的 `multipart/form-data`，你一旦手写覆盖了，boundary 就丢了，后端会直接解析失败。这个反直觉的点也是我调了很久才反应过来的。\n\n```js\nconst fd = new FormData()\nfd.append('file', file)\n// 不要手动加 Content-Type，让浏览器自己带 boundary\nfetch('/api/upload', { method: 'POST', body: fd })\n```\n\n## 前端懂 HTTP，才能写出更像样的请求封装\n\n如果只把请求当成 `fetch(url)`，那封装层通常只能停留在“发一下，拿一下”。\n\n但一旦你理解了：\n\n- 不同状态码的意义\n- 请求头和响应头的作用\n- 浏览器缓存和跨域约束\n- 请求成功与业务成功的区别\n\n那你的封装才更容易做到统一错误处理、重试策略、登录失效兜底和更明确的异常提示。\n\n## 它不是额外知识，而是前端工程的一部分\n\n前端和服务端边界越来越紧密的今天，懂 HTTP 已经不是“懂一些底层原理”那么简单。\n\n它直接影响你：\n\n- 能不能准确描述问题\n- 能不能看懂请求失败原因\n- 能不能和后端顺畅协作\n- 能不能设计出稳定的请求层\n\n页面展示只是结果，通信过程同样是前端工程质量的一部分。理解 HTTP，本质上是在补齐这部分基础。\n\n## 前端真正离不开 HTTP 的地方，是很多产品问题最后都会回到它\n\n比如这些场景都很典型：\n\n- 列表数据为什么明明刷新了却还是旧的\n- 登录态为什么突然失效\n- 文件上传为什么后端说格式不对\n- 某个接口为什么本地能调、线上却失败\n\n这些问题表面看像业务 bug，往下拆却几乎都和请求方法、请求头、缓存、认证或者响应语义有关。前端如果完全不理解这些基础，只能停留在“接口不通”这种很模糊的描述上，协作效率会明显受限。\n\n拿“列表刷新了还是旧数据”举例，往下拆基本就是缓存命中了。这里值得分清两种缓存：强缓存和协商缓存。强缓存是 `Cache-Control: max-age=300` 这种，浏览器在有效期内压根不发请求，直接读本地；协商缓存是带 `ETag` 或 `Last-Modified`，浏览器会发一个请求问服务端“变了没”，没变就返回 304，body 是空的，省的是传输流量而不是请求本身。我那次列表不刷新就是命中了强缓存，Network 里都看不到新请求（或者显示 from disk cache），自然怎么刷都是旧的。\n\n```\n# 协商缓存的一来一回\n请求:  If-None-Match: \"abc123\"\n响应:  304 Not Modified        （body 为空，用本地缓存）\n\n# 或者\n请求:  If-None-Match: \"abc123\"\n响应:  200 OK + ETag: \"def456\"  （内容变了，返回新数据）\n```\n\n对于真实接口数据，我现在倾向于让后端明确返回 `Cache-Control: no-cache` 或 `no-store`，把缓存的控制权握在手里，而不是依赖浏览器和 CDN 的默认行为去猜。\n\n跨域那一类也是同理。跨域不是“前端发不出去”，请求其实发出去了，是浏览器拿到响应后因为没有合规的 `Access-Control-Allow-Origin` 头而把结果拦在了 JS 之外。更隐蔽的是预检请求（preflight）：当你带了自定义头或用了 `PUT`/`DELETE` 这类方法时，浏览器会先自动发一个 `OPTIONS` 去问服务端允不允许，这个 `OPTIONS` 挂了，真正的请求根本不会发。我见过有人对着 Network 里那个莫名其妙的 `OPTIONS` 一脸懵，其实它就是 CORS 预检。\n\n## 会看 Network 面板，本质上是在读通信现场\n\n很多前端调试能力的分水岭，不是会不会写请求，而是会不会看请求。\n\n打开浏览器 Network 面板后，真正应该关心的通常包括：\n\n- 请求有没有发出去\n- 参数和方法是不是预期的\n- 返回状态码是什么\n- 响应头和响应体分别在表达什么\n\n这些信息一旦能连起来看，很多问题就不再只是“前后端互相怀疑”。理解 HTTP 对前端最大的现实价值，正是在于它能让你更准确地描述问题边界，也更容易把请求层做成真正可靠的基础设施。\n\n## 请求层稳定，前端排障成本会下降很多\n\n一个成熟一点的前端项目，通常不会让页面层自己散着处理各种协议细节。因为只要接口数量一多，登录失效、错误提示、重试策略、超时处理这些逻辑就会迅速重复。\n\n这时候懂 HTTP 的价值就会体现出来：你更容易把公共问题识别出来，并沉到统一请求层里，而不是每个页面都自己猜一遍该怎么处理。\n\n## 前后端协作顺不顺，很大程度取决于沟通是否足够具体\n\n比如只说“接口挂了”，信息其实很少。可如果你能说清：\n\n- 请求方法和参数是什么\n- 返回状态码是什么\n- 响应体里有没有业务错误信息\n- 问题发生在本地、测试还是线上\n\n那整个排查过程会快很多。理解 HTTP 对前端来说，最终不是为了懂术语，而是为了把问题说得更精确，把系统做得更稳。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":19,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":238,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从 TTFB、FP、数据包、IP、TCP 三次握手和四次挥手出发，串联 TCP/IP 协议栈与前端首屏性能之间的关系。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":43,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":290,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":42,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":222,"readTime":null,"coverImage":null}],"prevPost":{"title":"浏览器拿到页面后发生了什么：前端为什么要理解渲染过程","date":"2021-07-09","excerpt":"从 HTML 解析、CSS 计算到页面绘制，梳理浏览器渲染的大致流程以及它对前端开发的意义。","tags":["前端基础"],"slug":"frontend-browser-rendering-2021","category":"2021","likeCount":5,"postId":"e59865f0-bf36-fd0a-0e17-433bf6eb089a","viewCount":1283,"readTime":"10分钟","coverImage":null},"nextPost":{"title":"localStorage 使用笔记：前端本地存储方便，但别把它当数据库","date":"2021-06-19","excerpt":"用我在业务里使用 localStorage 的经验，聊聊它适合做什么、不适合做什么，以及序列化、版本升级、跨标签同步这些容易被忽略的边界。","tags":["前端基础"],"slug":"frontend-local-storage-2021","category":"2021","likeCount":5,"postId":"3ad6c7ff-cd80-5e75-52ae-88ba3fd42c5d","viewCount":1585,"readTime":"11分钟","coverImage":null}},"__N_SSG":true}