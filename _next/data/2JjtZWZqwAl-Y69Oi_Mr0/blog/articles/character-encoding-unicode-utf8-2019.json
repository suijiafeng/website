{"pageProps":{"postData":{"title":"字符编码、Unicode 和 UTF-8：乱码问题不是玄学","date":"2019-06-02","excerpt":"从网页乱码和接口编码排查经历出发，聊聊 ASCII、Unicode、UTF-8、URL 编码和中文乱码怎么定位。","tags":["前端基础"],"slug":"character-encoding-unicode-utf8-2019","category":"2019","likeCount":7,"postId":"3fc0a177-59c5-5482-d5f3-7276e8d1d45f","viewCount":1177,"readTime":"10分钟","coverImage":null,"content":"\n我对乱码的\"恨\"，是从一个周五晚上的线上事故开始的。运营在后台导出了一份用户订单 CSV，发给财务，财务用 Excel 一打开，\"张三\"\"李四\"全成了\"寮犱笁\"\"鏉庡洓\"，整张表像被猫踩过键盘。运营截图发群里问是不是系统坏了，我盯着那串方块字，第一反应也是\"见鬼了\"。\n\n后来排查了快一个小时才发现，代码、数据库、接口全都没问题，问题出在 Excel 默认用 GBK 去解一份 UTF-8 编码的文件。那次之后我才真正下决心把编码这件事搞明白——它不是玄学，是一条从\"字符\"到\"字节\"再回到\"字符\"的链路，链路上任何一环用错了\"字典\"，你看到的就是乱码。\n\n这里把我这几年踩过的坑、查过的链路、用过的代码放在一起讲。\n\n## 先分清两件事：字符集和编码\n\n最容易绕晕的地方，是把\"字符集\"和\"编码方式\"当成一回事。它俩其实是两层：\n\n- **字符集**：决定\"有哪些字符，每个字符叫什么号\"。比如 Unicode 规定\"你\"这个字的编号（码点）是 U+4F60。这只是个抽象的数字，跟它在硬盘上占几个字节没半点关系。\n- **编码方式**：决定\"这个号怎么变成字节存起来 / 传出去\"。同一个码点 U+4F60，用 UTF-8 存是 3 个字节，用 UTF-16 存是 2 个字节，用 GBK 存又是另外的字节。\n\n想明白这一层，乱码的本质就清楚了：**用 A 编码存进去的字节，用 B 编码读出来**，于是字节没变，但查的\"字典\"变了，解出来的字符自然就错位。我那份 CSV 就是典型——UTF-8 写入，GBK 读取。\n\n## ASCII：一切的起点\n\nASCII 是最早的方案，用一个字节里的低 7 位，覆盖英文字母、数字、标点和一些控制符，总共 128 个。\n\n```text\n'A' -> 65   (0x41)\n'a' -> 97   (0x61)\n'0' -> 48   (0x30)\n'\\n' -> 10  (换行)\n```\n\n在浏览器控制台随手验证：\n\n```js\n'A'.charCodeAt(0) // 65\nString.fromCharCode(97) // 'a'\n```\n\nASCII 的格局太小，一个字节最多 256 个位置，128 个还空着没用满，根本塞不下中文几万个常用字。于是各国各搞各的：中国大陆有 GB2312、后来扩展成 GBK、GB18030，台湾有 Big5，日本有 Shift_JIS……这就是早年间网页满天飞乱码的根源——同一段字节，北京的机器按 GBK 解、东京的机器按 Shift_JIS 解，结果谁也认不出谁。\n\n## Unicode：给全世界的字符发身份证\n\nUnicode 想终结这种混乱：给地球上所有字符发一张全球唯一的\"身份证号\"，也就是码点（code point），写法是 `U+` 加十六进制。\n\n```text\nU+0041  ->  A\nU+4F60  ->  你\nU+597D  ->  好\nU+1F600 ->  😀（emoji 也有码点）\n```\n\nES6 之后 JS 里可以直接拿到码点：\n\n```js\n'你'.codePointAt(0).toString(16) // '4f60'\nString.fromCodePoint(0x4f60)     // '你'\n```\n\n这里要特别提醒一个我栽过跟头的点：**JS 字符串内部是 UTF-16，而 emoji 这类码点超过 U+FFFF 的字符，会被拆成两个\"代理对\"（surrogate pair）**，占两个 `length`。\n\n```js\n'😀'.length        // 2，不是 1！\n'😀'.charAt(0)     // 半个字符，乱码\n[...'😀'].length   // 1，扩展运算符按码点切分\n```\n\n这事不光 emoji，任何超出 U+FFFF 的字符都一样。拿数学粗体的 `𝟙`（U+1D7D9）在控制台对照一下就清楚了：\n\n```js\n'好'.length        // 1，基本多文种平面里的汉字占 1 个 UTF-16 码元\n'𝟙'.length         // 2，码点 > U+FFFF，被拆成代理对，占 2 个码元\n[...'𝟙'].length    // 1，扩展运算符按码点切分，才是“1 个字符”\n```\n\n顺带把两个常被混的方法也跑一下——对基本平面里的字符，它俩结果一样：\n\n```js\n'好'.charCodeAt(0)   // 22909，返回 UTF-16 码元\n'好'.codePointAt(0)  // 22909，返回完整码点\n```\n\n差别只在代理对上：`'𝟙'.charCodeAt(0)` 只会拿到前半个高位代理 `55349`，而 `'𝟙'.codePointAt(0)` 能识别出完整码点 `120793`（即 `0x1D7D9`）。所以处理可能含 emoji、生僻字的文本，认 `codePointAt` 和 `[...str]`。\n\n当年我做一个昵称长度限制，用 `str.length` 截断，结果有人用 emoji 当昵称，截到一半把代理对劈开了，存进数据库再读出来就是个问号方块。后来才改成用 `Array.from(str)` 或 `[...str]` 按码点处理。\n\n还有个更隐蔽的“长度玄学”：同一个看起来一样的字符，可能有「组合」和「分解」两种码点序列。比如 `é` 既可以是单个码点 U+00E9，也可以是 `e`（U+0065）后跟一个组合用的重音符（U+0301）。这俩肉眼一模一样，但 `length` 不同：\n\n```js\n'café'.normalize('NFC').length  // 4，重音并进一个码点\n'café'.normalize('NFD').length  // 5，重音被拆成独立的组合字符\n```\n\n所以从不同来源（比如 macOS 文件名常用 NFD）拿到的字符串，直接 `===` 比较可能不相等，比较前先 `normalize('NFC')` 统一一下。\n\n但记住：Unicode 只是\"编号标准\"，它没规定怎么存。怎么把这些编号落到字节，是 UTF-8 / UTF-16 这些编码方式的事。\n\n## UTF-8：为什么它赢了\n\nUTF-8 是 Unicode 最主流的编码方式，变长设计：\n\n- ASCII 范围（U+0000 ~ U+007F）：1 个字节，且和 ASCII 完全兼容\n- 常见中文（基本在 U+4E00 ~ U+9FFF）：3 个字节\n- emoji 等增补字符：4 个字节\n\n```text\nA   -> 41                (1 字节)\n你  -> E4 BD A0          (3 字节)\n😀  -> F0 9F 98 80       (4 字节)\n```\n\n在 Node 里能直观看到字节：\n\n```js\nBuffer.from('A').length    // 1\nBuffer.from('你').length   // 3\nBuffer.from('你').toString('hex') // 'e4bda0'\nBuffer.from('😀').length   // 4\n```\n\n浏览器里不开 Node 也能验证字节数——`Blob` 的 `size` 就是按 UTF-8 编码后的字节长度：\n\n```js\nnew Blob(['A']).size   // 1\nnew Blob(['好']).size  // 3，一个汉字在 UTF-8 下占 3 字节\nnew Blob(['😀']).size // 4\n```\n\n注意 `'好'.length` 是 `1`（一个 UTF-16 码元），而 `new Blob(['好']).size` 是 `3`（UTF-8 字节数）——“字符个数”和“字节数”是两码事，这正是前面字符集 vs 编码那层区别的直观体现。\n\nUTF-8 能赢，靠的是两点：兼容 ASCII（老的纯英文系统不用改），以及它的字节有\"自同步\"特性——每个多字节字符的首字节会标明长度，后续字节都以 `10` 开头，所以哪怕从中间截断，解码器也能很快重新对齐，不会一错错到底。\n\n网页里声明编码最常见的就是：\n\n```html\n<meta charset=\"utf-8\">\n```\n\n但我必须强调：**这个 meta 只是\"建议\"，不是\"命令\"。** 如果你的 HTML 文件本身是用 GBK 保存的，或者服务器响应头里 `charset` 写的是别的，浏览器很可能不理会这个 meta。编码这件事，声明的优先级是：HTTP 响应头 > 文件 BOM > HTML meta 标签。三者打架时，响应头说了算。\n\n## 响应头才是老大\n\n我见过太多人在 HTML 里写了 `<meta charset=\"utf-8\">`，页面还是乱码，然后百思不得其解。打开 Network 面板看一眼响应头，往往就真相大白了。\n\n```http\nContent-Type: text/html; charset=utf-8\n```\n\n接口返回 JSON 同理：\n\n```http\nContent-Type: application/json; charset=utf-8\n```\n\n各端的正确姿势：\n\n```js\n// Node / Express\nres.setHeader('Content-Type', 'application/json; charset=utf-8')\n```\n\n```nginx\n# Nginx 静态资源兜底\ncharset utf-8;\n```\n\n一个真实的坑：有个 PHP 老接口本身输出的是 UTF-8 字节，但没设响应头 charset，部分浏览器按操作系统默认（中文 Windows 上是 GBK）去猜，于是乱码。后端补一行 `header('Content-Type: application/json; charset=utf-8')` 就好了。**前端怎么改都没用，因为锅在响应头。**\n\n## URL 编码：另一套规则\n\nURL 里只能安全地出现一部分 ASCII 字符，中文、空格、`&`、`#`、`?` 这些要么非法、要么有特殊含义，必须做百分号编码（percent-encoding）。它的逻辑是：先把字符按 UTF-8 转成字节，再把每个字节写成 `%XX`。\n\n```js\nencodeURIComponent('前端开发')\n// '%E5%89%8D%E7%AB%AF%E5%BC%80%E5%8F%91'\n```\n\n注意上面的字节 `E5 89 8D ...` 正好就是这几个汉字的 UTF-8 编码，串起来加 `%` 而已。\n\n`encodeURI` 和 `encodeURIComponent` 的区别是新手必踩的坑：\n\n```js\nencodeURI('https://a.com/搜索?q=前端&p=1')\n// 不会编码 :/?&= 等结构字符，用于编码整条 URL\n\nencodeURIComponent('前端&p=1')\n// 会把 & 也编码成 %26，用于编码单个参数值\n```\n\n规则很简单：**拼参数值，永远用 `encodeURIComponent`。** 不然参数值里如果带个 `&`，会被当成参数分隔符，把后端的参数解析整个搞乱。\n\n```js\n// 正确\nvar url = '/search?q=' + encodeURIComponent(keyword) + '&page=' + page\n\n// 错误：keyword 里有空格、& 就废了\nvar url = '/search?q=' + keyword\n```\n\n还有个反复坑我的现象：**双重编码**。前端 encode 了一次，某个网关或后端框架又自动 encode 了一次，`%` 自己变成了 `%25`，最后参数里全是 `%2525` 这种。排查办法是在链路每一段把原始值打出来，看是哪一环多编了一次。\n\n## CSV 乱码：BOM 的取舍\n\n回到开头那份 CSV。Excel（尤其是 Windows 版）打开 CSV 时，不看文件内容是不是 UTF-8，默认就按系统区域编码（中文环境是 GBK）去解。文件明明是规规矩矩的 UTF-8，它偏要按 GBK 读，于是中文全乱。\n\n解决办法是在文件开头加一个 UTF-8 BOM（字节 `EF BB BF`），相当于给 Excel 递个明确的纸条：\"我是 UTF-8，请按这个读。\"\n\n```js\nfunction downloadCsv(rows, filename) {\n  var csv = rows.map(function (row) {\n    return row.map(function (cell) {\n      // 处理含逗号、引号、换行的单元格\n      var s = String(cell).replace(/\"/g, '\"\"')\n      return /[\",\\n]/.test(s) ? '\"' + s + '\"' : s\n    }).join(',')\n  }).join('\\r\\n')\n\n  // 关键：开头加 UTF-8 BOM（﻿），否则 Excel 用 GBK 解 UTF-8 必乱\n  var blob = new Blob(['﻿' + csv], { type: 'text/csv;charset=utf-8;' })\n\n  var url = URL.createObjectURL(blob)\n  var a = document.createElement('a')\n  a.href = url\n  a.download = filename\n  a.click()\n  URL.revokeObjectURL(url)\n}\n\ndownloadCsv([['姓名', '城市'], ['张三', '北京']], '订单.csv')\n```\n\n`'﻿'` 就是 BOM 的码点，写在字符串最前面，浏览器生成 Blob 时会把它编码成那三个字节。\n\n但 BOM 不是免费午餐，这是个**取舍**：加了 BOM，Excel 高兴了，可有些程序（某些 Linux 命令行工具、老的 JSON 解析器、shell 脚本）会把开头那三个字节当成正文内容，反而出问题。我的经验法则是——**给人用 Excel 打开的导出文件加 BOM；给机器解析、给程序对接的文件绝不加 BOM**。两类需求最好分两个接口或加参数区分，别图省事都加上。\n\n## 一条完整的排查链路\n\n乱码出现时，别一上来就改前端。先在脑子里画出这条链路，逐段定位：\n\n```text\n源文件编码 → 文件读取编码 → 程序内部处理 → 数据库存储编码 → 接口响应头 → 页面/Excel 解码\n```\n\n我的实际排查顺序：\n\n1. **源文件**：用编辑器右下角看一眼文件本身是不是 UTF-8（VS Code 直接显示）。\n2. **HTML meta**：`<meta charset=\"utf-8\">` 是否声明，且和实际文件编码一致。\n3. **响应头**：Network 面板看 `Content-Type` 里的 `charset`，这一步最常出问题。\n4. **接口传参**：参数值是否用 `encodeURIComponent` 编码，是否被双重编码。\n5. **数据库**：库、表、连接三层字符集是否都是 `utf8mb4`（注意 MySQL 的 `utf8` 是个坑，只支持 3 字节，存不了 emoji，必须用 `utf8mb4`）。\n\n```sql\n-- MySQL 想完整支持中文 + emoji，认准 utf8mb4\nCREATE DATABASE app DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;\n-- 连接也要设，否则连接层会用默认字符集做一次转换\nSET NAMES utf8mb4;\n```\n\nMySQL 那个 `utf8 != 真正的 UTF-8` 的坑我专门吃过：用户昵称里带 emoji，存的时候报错或被截断，查半天才知道得用 `utf8mb4`。\n\n## 一个快速自检的小函数\n\n排查时我常在控制台跑这么个小工具，把一个字符的码点、UTF-8 字节、URL 编码一次性打出来，肉眼对一下哪里不对：\n\n```js\nfunction inspect(ch) {\n  return {\n    char: ch,\n    codePoint: 'U+' + ch.codePointAt(0).toString(16).toUpperCase(),\n    utf8: [].map.call(\n      new TextEncoder().encode(ch),\n      function (b) { return b.toString(16).padStart(2, '0') }\n    ).join(' '),\n    urlEncoded: encodeURIComponent(ch)\n  }\n}\n\ninspect('你')\n// { char: '你', codePoint: 'U+4F60', utf8: 'e4 bd a0', urlEncoded: '%E4%BD%A0' }\n```\n\n`TextEncoder` 在 2019 年的现代浏览器里已经能直接用，输出的就是 UTF-8 字节，比自己手算方便太多。\n\n\n把这几年攒的经验浓缩成一句话：**乱码 = 字节没变，但解码用的\"字典\"换了。**\n\nUnicode 负责给字符发全球唯一编号，UTF-8 是把编号落成字节的主流编码方式。真正的功夫不在背概念，而在于能在脑子里画出\"源文件 → 数据库 → 响应头 → 页面\"这条链路，知道每一环用的是什么编码，并且让它们对齐。\n\n它不是玄学。下次再看到一屏方块字，深呼吸，打开 Network 面板看响应头，按链路一段段查，十有八九能定位到是哪一环用错了字典。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":29,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":356,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":53,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":361,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":52,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":275,"readTime":null,"coverImage":null}],"prevPost":{"title":"浏览器缓存：强缓存和协商缓存别只背概念","date":"2019-06-18","excerpt":"从静态资源部署和接口请求出发，整理 Cache-Control、Expires、ETag、Last-Modified、304 和前端发版缓存策略。","tags":["前端基础","性能与缓存"],"slug":"browser-cache-strong-negotiation-2019","category":"2019","likeCount":5,"postId":"1f22f827-9fbc-7fda-9528-640ac0fea591","viewCount":1324,"readTime":null,"coverImage":null},"nextPost":{"title":"webpack loader 和 plugin：常被问到，也决定构建能不能扩展","date":"2019-05-21","excerpt":"从实际构建配置出发，聊聊 webpack loader、plugin 的职责差异、执行顺序、配置边界和简化实现思路。","tags":["构建与模块化"],"slug":"webpack-loader-plugin-interview-2019","category":"2019","likeCount":8,"postId":"a6d0ab3f-757f-571a-3ae6-1079d59fe636","viewCount":3770,"readTime":null,"coverImage":null}},"__N_SSG":true}