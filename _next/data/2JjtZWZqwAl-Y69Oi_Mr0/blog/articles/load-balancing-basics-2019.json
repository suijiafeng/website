{"pageProps":{"postData":{"title":"负载均衡基础：为什么线上服务不是只放一台机器","date":"2019-11-09","excerpt":"从接口访问、Nginx 和服务扩容出发，整理负载均衡、轮询、权重、健康检查、会话保持和前端排查视角。","tags":["其他"],"slug":"load-balancing-basics-2019","category":"2019","likeCount":5,"postId":"f1e9b8ba-86db-7b2c-0175-f82871a9f5ea","viewCount":4372,"readTime":"10分钟","coverImage":null,"content":"\n我第一次真正意识到“负载均衡”这四个字的分量，是在一次大促前的压测。那时我们的接口测试环境只有一台机器，QPS 一上去就开始超时，运维淡淡地说了句“线上是四台，前面挂了 Nginx”。我当时还以为 Nginx 只是个静态文件服务器，后来排查了几次诡异的线上问题，才慢慢把这套东西摸明白。\n\n线上服务几乎不会只跑在一台机器上。一台机器有它的天花板：CPU、内存、连接数、带宽，任何一项被打满，整个服务就开始抖。更要命的是单点故障——这台机器一重启、一 OOM、网卡一抽风，全站就挂了。所以正经的做法是放好几台一模一样的服务实例，前面摆一个入口，把进来的请求分发到后面去。这个分发请求的家伙，就是负载均衡。\n\n作为前端，我们平时确实不直接去配负载均衡。但我后来发现，很多让人摸不着头脑的线上现象，根子都在这一层：接口十次里偶尔失败一两次、某个页面在一部分用户那里数据是旧的、某个同事电脑上死活登录态会掉、灰度发布时一半人看到新功能一半人看到老的。这些都不是浏览器的锅，而是请求被分到了不同的后端机器上。理解这一层，排查问题时心里会踏实很多。\n\n## 请求到底经过了几道手\n\n先把链路捋清楚。用户在浏览器里发一个请求：\n\n```text\nGET https://api.example.com/users\n```\n\nDNS 解析后，请求并不会直接打到某台业务服务器上，而是先到负载均衡器。负载均衡器维护着一个“后端池”，里面是几台真正干活的服务：\n\n```text\n        ┌─────────────┐\n请求 ──▶ │ Nginx (LB)  │ ──┬──▶ server-1  10.0.0.1:3000\n        └─────────────┘   ├──▶ server-2  10.0.0.2:3000\n                          └──▶ server-3  10.0.0.3:3000\n```\n\n它按照某种策略挑一台，把请求转过去，拿到响应再原路返回给用户。对用户来说，他自始至终只看到 `api.example.com` 这一个地址，根本感知不到后面有几台机器，也不知道这次是谁服务的。这种“对外一个口、对内一群机器”的结构，就是水平扩展的基础——扛不住了，加机器就行，理论上能一直加下去。\n\n这里有个概念上的分层值得记一下。负载均衡大致分两类：四层（L4）和七层（L7）。四层工作在 TCP/UDP 层面，只看 IP 和端口，转发快、消耗低，但它不懂 HTTP，没法按 URL、按 Header 做路由，典型代表是 LVS、F5、云上的 NLB。七层工作在 HTTP 层面，能看到完整的请求，可以按路径分流、改请求头、做 SSL 卸载，Nginx 就是干这个的。我们前端打交道最多的就是七层，因为它直接和 URL、Header、Cookie 打交道，问题也大多出在这一层。\n\n## 几种分发策略，以及我踩过的坑\n\n负载均衡器怎么决定“这次请求给谁”，靠的就是调度策略。Nginx 自带这么几种。\n\n**轮询（round-robin）** 是默认策略，挨个发：\n\n```text\nreq1 → server-1\nreq2 → server-2\nreq3 → server-3\nreq4 → server-1   （转一圈回来）\n```\n\n实现简单、绝对公平，前提是几台机器配置差不多。我吃过的第一个亏就是机器不对等：有两台是新采购的高配，一台是临时拉来顶班的老机器，轮询照样三家平摊，结果老机器先被打趴下，整体表现被那台最弱的拖着走。\n\n**加权轮询（weighted round-robin）** 就是为这种情况准备的，给每台机器一个权重：\n\n```nginx\nupstream api_servers {\n  server 10.0.0.1:3000 weight=5;\n  server 10.0.0.2:3000 weight=5;\n  server 10.0.0.3:3000 weight=1;\n}\n```\n\n性能强的多分，老弱的少分。上面这个配置里，每 11 个请求大概有 5 个给 server-1、5 个给 server-2、1 个给 server-3。灰度发布也常用这招：新版本先给 1 的权重，观察没问题再慢慢加上去。\n\n**最少连接（least_conn）** 不按次数轮，而是看谁手上当前未完成的连接最少就给谁：\n\n```nginx\nupstream api_servers {\n  least_conn;\n  server 10.0.0.1:3000;\n  server 10.0.0.2:3000;\n}\n```\n\n这个策略特别适合请求耗时差异大的场景。我们有个导出报表的接口，有的请求几十毫秒就回来了，有的要跑十几秒。用轮询的话，某台机器可能连续接了好几个慢请求，连接全堵在那儿，新来的快请求还硬往里塞。换成 least_conn 之后，慢请求多的机器会自然少分一点，明显平顺了。\n\n**IP hash** 按客户端 IP 算个哈希，让同一个 IP 尽量固定打到同一台机器：\n\n```nginx\nupstream api_servers {\n  ip_hash;\n  server 10.0.0.1:3000;\n  server 10.0.0.2:3000;\n}\n```\n\n它解决的是会话粘连问题（后面会专门说），但它有个我后来才意识到的坑：很多用户走的是同一个公司出口 IP 或者运营商 NAT，对负载均衡来说他们就是“一个 IP”，结果一大票人全被压到同一台机器上，流量根本不均。所以 ip_hash 我现在基本不用，宁可去把会话做成无状态。\n\n策略没有银弹。机器对等、请求都很短，轮询就够了；机器不对等用加权；请求耗时方差大用最少连接；万不得已要粘连再考虑 ip_hash。\n\n## 一份能直接用的 Nginx 配置\n\n原文那段配置太简略，真上线会出问题。我把一份相对完整、贴近我们当年实际用的版本贴出来，关键点都写在注释里：\n\n```nginx\nupstream api_servers {\n  least_conn;\n\n  server 10.0.0.1:3000 weight=5 max_fails=3 fail_timeout=15s;\n  server 10.0.0.2:3000 weight=5 max_fails=3 fail_timeout=15s;\n  server 10.0.0.3:3000 weight=1 backup;   # 平时不接流量，前面都挂了才顶上\n\n  keepalive 32;   # 与后端保持长连接，省掉反复握手的开销\n}\n\nserver {\n  listen 80;\n  server_name api.example.com;\n\n  location /api/ {\n    proxy_pass http://api_servers;\n\n    # 把真实客户端信息透传给后端，否则后端拿到的全是 LB 的 IP\n    proxy_set_header Host              $host;\n    proxy_set_header X-Real-IP         $remote_addr;\n    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;\n    proxy_set_header X-Forwarded-Proto $scheme;\n\n    # 用上面 upstream 里的长连接，必须把版本设成 1.1 并清掉 Connection 头\n    proxy_http_version 1.1;\n    proxy_set_header Connection \"\";\n\n    # 超时控制，避免一个慢后端拖死整条链路\n    proxy_connect_timeout 3s;\n    proxy_read_timeout    30s;\n    proxy_send_timeout    30s;\n\n    # 后端返回 5xx 或连不上时，自动换下一台重试（注意幂等性）\n    proxy_next_upstream error timeout http_502 http_503 http_504;\n  }\n}\n```\n\n几个我当年被坑过的点。`proxy_set_header X-Forwarded-For` 不写，后端日志里看到的来源 IP 全是 Nginx 自己，风控和限流直接失灵。`keepalive` 配了却忘了把 `proxy_http_version` 改成 1.1，长连接根本没生效，高并发下握手开销吃掉一大块性能。还有 `proxy_next_upstream` 很香——后端偶发 502 时 Nginx 会默默换台机器重试，用户压根没感觉——但对非幂等接口（比如下单、支付）一定要小心，重试可能造成重复提交，这种接口我们会单独把它从重试列表里摘出去。\n\n前端部署也离不开这套。我们的静态资源和接口代理常常在同一个 Nginx 里，页面走 `try_files` 兜底到 `index.html`（单页应用路由），接口走 `proxy_pass`：\n\n```nginx\nlocation / {\n  root /var/www/dist;\n  try_files $uri $uri/ /index.html;\n}\n\nlocation /api/ {\n  proxy_pass http://api_servers;\n}\n```\n\n## 健康检查：那些“刷新几次时好时坏”的元凶\n\n负载均衡最怕的事情，是它不知道后端已经挂了，还在傻乎乎地往一台死机器上转请求。用户的体感就是：刷新十次，七八次正常，两三次报错或者转圈，毫无规律。这种间歇性故障最折磨人，因为它不稳定复现，前端查半天发现自己代码没问题。\n\n开源版 Nginx 的健康检查是“被动”的，靠的就是上面配的 `max_fails` 和 `fail_timeout`：当某台机器在 `fail_timeout` 时间内失败次数达到 `max_fails`，Nginx 就把它标记为不可用，暂时不往那儿发请求，过一会儿再试探性地放点流量看看活没活过来。这种方式的缺点是“事后诸葛亮”——总得先有几个真实请求倒霉撞上死机器、失败了，才能触发摘除。\n\n更稳妥的是“主动”健康检查：负载均衡定期主动去探一个专门的接口，确认活着才给它发流量。这个能力在 Nginx Plus（商业版）里直接支持，开源版当年我们是用 Tengine 或者 `nginx_upstream_check_module` 来补的：\n\n```nginx\nupstream api_servers {\n  server 10.0.0.1:3000;\n  server 10.0.0.2:3000;\n\n  # nginx_upstream_check_module 提供的主动检查\n  check interval=3000 rise=2 fall=3 timeout=2000 type=http;\n  check_http_send \"GET /health HTTP/1.0\\r\\n\\r\\n\";\n  check_http_expect_alive http_2xx http_3xx;\n}\n```\n\n后端要配合提供一个 `/health` 接口。这里有个容易被忽略的细节：健康检查不能只回个 `200 OK` 就完事，那只能证明进程还在喘气。我们后来把它做成“深度检查”，连带探一下关键依赖：\n\n```js\n// Express 版健康检查接口\napp.get('/health', async (req, res) => {\n  try {\n    await db.ping();              // 数据库连得上吗\n    await redis.ping();           // 缓存连得上吗\n    res.status(200).json({ status: 'ok' });\n  } catch (err) {\n    // 依赖挂了，主动报不健康，让 LB 把自己摘掉\n    res.status(503).json({ status: 'unhealthy', error: err.message });\n  }\n});\n```\n\n为什么要这样？我们出过一次事故：某台机器的进程活着，端口也通，但它和数据库之间的连接池全卡死了，所有业务请求都超时，可健康检查只看进程，一直觉得它“健康”，流量源源不断地往火坑里送。改成深度检查后，依赖一断它自己就报 503 把自己踢出去，故障范围立刻收窄。\n\n## 会话保持：登录态为什么会“随机”丢\n\n这是我职业生涯里查得最久的一个“前端 bug”，最后发现完全不是前端的问题。现象是：测试同学说登录之后点几下就被踢回登录页，但不是每次都发生，换台电脑又好了。\n\n根因是后端把登录态（session）存在了进程内存里。请求第一次落到 server-1，session 建在 server-1 的内存里；下一个请求被轮询分到了 server-2，server-2 内存里根本没有这个 session，于是它认为你没登录，返回 401。在多机环境下，只要会话状态是“本地的”，就一定会出这种问题。\n\n解决思路有三条，优劣很清楚：\n\n- **会话粘连（ip_hash / sticky）**：让同一个用户固定打到同一台机器。最省事，但前面说过 ip_hash 在 NAT 环境下会失衡；而且这台机器一旦重启，上面所有人的 session 全没了，照样掉登录。属于治标。\n- **session 集中存储（Redis）**：把 session 从进程内存挪到外部的 Redis，所有机器共享。无论请求落到哪台，都能读到同一份会话。这是当年最主流的做法，我们最后就是这么改的。\n- **无状态 Token（JWT）**：干脆不在服务端存会话，登录态全塞进客户端持有的 token 里，每台机器自己验签就行，谁都不用存。扩展性最好，代价是 token 没法轻易主动失效，得配合短有效期或额外的吊销列表。\n\n把 session 挪到 Redis 之后那个“随机掉登录”的问题当场消失。从那以后我形成一个判断习惯：前端碰到“时好时坏、和机器有关”的问题，先怀疑后端是不是有本地状态没共享，而不是埋头改自己的代码。\n\n## 前端的排查视角\n\n前端虽然不配负载均衡，但我们往往是第一个发现问题的人，能不能快速把锅甩到对的地方，靠的就是会看几样东西。\n\n最有用的是让后端在响应头里带上机器标识。我们约定每台机器在响应里加一个头：\n\n```http\nX-Server-Id: api-02\n```\n\nNginx 这边一行就能加上：\n\n```nginx\nadd_header X-Server-Id $upstream_addr always;\n```\n\n有了它，偶发失败时我直接在浏览器 Network 面板里看失败请求的响应头，如果发现失败的全是 `api-02`，那就基本锁定是那台机器有问题，截图丢给运维，省去一来一回的扯皮。\n\n其次是前端的错误上报。我们在统一的请求封装里，把每个失败请求的状态码、`X-Server-Id`、后端返回的 `trace-id` 一起上报：\n\n```js\naxios.interceptors.response.use(\n  res => res,\n  err => {\n    const res = err.response;\n    if (res) {\n      report('api_error', {\n        url: res.config.url,\n        status: res.status,\n        serverId: res.headers['x-server-id'],\n        traceId: res.headers['x-trace-id'],\n      });\n    }\n    return Promise.reject(err);\n  }\n);\n```\n\n带上 trace-id，后端就能拿着它直接去日志系统里捞出这一整条请求链路，定位飞快。还有一类问题是“某些用户看到旧数据”——这通常是某台机器上的缓存没刷新，或者发版时几台机器版本不一致（滚动发布的中间态），同样可以靠 `X-Server-Id` 配合版本号头确认是不是版本不齐导致的。\n\n\n负载均衡的本质，是把“对外的一个服务”和“对内的一群机器”解耦：对外稳定一个入口，对内随便加减机器，从而同时换来高可用和水平扩展能力。\n\n对前端来说，真正的价值不在于会不会写 upstream，而在于理解了这套结构之后，碰到“偶发失败、数据不一致、登录态随机丢失、灰度只对一部分人生效”这些线上现象时，能立刻反应过来——这很可能是请求被分到了不同机器，问题在负载均衡这一层，而不是我的浏览器代码。轮询、加权、最少连接、ip_hash、健康检查、会话保持，这几个词背后都对应着我实实在在踩过的坑。面试会问，线上更会遇到，早点搞懂不亏。\n"},"recommendedPosts":[{"title":"Docker 入门：容器化你的应用","date":"2024-01-24","excerpt":"了解 Docker 的基础知识，记录前端和 Node 项目在镜像构建、缓存、多阶段构建、健康检查和运行安全上的实践。","tags":["其他"],"slug":"introduction-to-docker","category":"2024","likeCount":9,"postId":"a9891cd0-2d11-474e-b5be-cb55809c5177","viewCount":6722,"readTime":null,"coverImage":null},{"title":"消息队列基础：为什么有些操作不适合在接口里立刻做完","date":"2019-11-16","excerpt":"从下单、发短信和日志处理出发，整理消息队列的异步削峰、解耦、可靠性、重复消费和前端体验影响。","tags":["其他"],"slug":"message-queue-basics-2019","category":"2019","likeCount":8,"postId":"93739beb-a478-addd-c245-a78e2b8daccd","viewCount":1020,"readTime":null,"coverImage":null},{"title":"HTTPS、TLS 和证书：为什么登录页不能再裸奔","date":"2019-09-18","excerpt":"从登录、支付和接口安全出发，整理 HTTPS、TLS、证书、加密、身份验证和混合内容这些基础问题。","tags":["其他"],"slug":"https-tls-certificate-basics-2019","category":"2019","likeCount":7,"postId":"75234b55-17e3-e9ad-b1fc-a58643f81c8a","viewCount":1587,"readTime":null,"coverImage":null}],"prevPost":{"title":"消息队列基础：为什么有些操作不适合在接口里立刻做完","date":"2019-11-16","excerpt":"从下单、发短信和日志处理出发，整理消息队列的异步削峰、解耦、可靠性、重复消费和前端体验影响。","tags":["其他"],"slug":"message-queue-basics-2019","category":"2019","likeCount":8,"postId":"93739beb-a478-addd-c245-a78e2b8daccd","viewCount":1020,"readTime":null,"coverImage":null},"nextPost":{"title":"数组方法整理：map、filter、reduce 不只是面试题","date":"2019-11-07","excerpt":"结合列表渲染和数据转换场景，聊聊 map、filter、reduce、some、every、find、forEach 的区别和误用。","tags":["JavaScript","前端基础"],"slug":"javascript-array-methods-interview-2019","category":"2019","likeCount":5,"postId":"bcaedf8b-cf73-c00b-7424-73b7d5333dad","viewCount":2951,"readTime":null,"coverImage":null}},"__N_SSG":true}