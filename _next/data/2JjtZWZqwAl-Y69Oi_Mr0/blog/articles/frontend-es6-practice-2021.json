{"pageProps":{"postData":{"title":"ES6 常用特性回顾：真正改变前端开发体验的是哪些能力","date":"2021-03-27","excerpt":"从我日常写业务代码的体验出发，回顾 ES6 里真正改变开发方式的能力：变量语义、解构、模块、Promise 和更清楚的代码边界。","tags":["前端基础"],"slug":"frontend-es6-practice-2021","category":"2021","likeCount":7,"postId":"2ea9acea-c69b-855a-9c70-ffef137a1ae1","viewCount":1942,"readTime":"12分钟","coverImage":null,"content":"\n前端开发近几年的编码体验变化，很大一部分都来自 JavaScript 语言本身的演进。\n\nES6 之后，很多过去写起来很别扭的代码，开始变得更自然。它的价值不只是语法更好看，而是让代码表达能力更强了。\n\n我刚开始学 ES6 时，也会把它当成一堆“新语法”来背。后来写业务写多了，才发现真正有价值的不是语法新，而是它们让代码里的意图更容易被看出来。\n\n## let 和 const 让变量语义更清楚\n\n以前大家主要使用 `var`，它在作用域和提升上的行为比较容易引发困惑。\n\n`let` 和 `const` 的出现，让变量声明的意图更明确：\n\n- `let` 表示后续可能修改\n- `const` 表示引用不再重新赋值\n\n这能显著减少一些低级错误。\n\n我现在的习惯是能用 `const` 就先用 `const`。不是因为它更高级，而是因为它能告诉后来读代码的人：这个绑定不会被重新指向。一段函数里如果满屏都是 `const`，少数几个 `let` 反而会跳出来——那几个就是真正在流转、需要重点盯的状态。这种\"靠声明方式做视觉降噪\"的效果，是当年用 `var` 时完全体会不到的。\n\n`let`、`const` 还顺手解决了一个困扰我很久的经典面试题，也是真实业务里的坑——循环里注册回调：\n\n```javascript\n// var 版本：三个按钮点下去都打印 3\nfor (var i = 0; i < 3; i++) {\n  buttons[i].addEventListener('click', () => console.log(i));\n}\n\n// let 版本：每次迭代是一个新的块级绑定，分别打印 0 1 2\nfor (let i = 0; i < 3; i++) {\n  buttons[i].addEventListener('click', () => console.log(i));\n}\n```\n\n以前为了让 `var` 拿到正确的值，得套一层 IIFE 把 `i` 当参数传进去，写出来很难看。`let` 的块级作用域直接把这个问题消化掉了。我第一次知道这个差别，是在排查一个表格行操作按钮全部指向最后一行数据的 bug，找了半天才意识到是闭包捕获的是同一个 `var i`。\n\n还有暂时性死区（TDZ）这个点也值得记一下：`let`/`const` 声明之前访问变量会直接抛 `ReferenceError`，而不是像 `var` 那样得到 `undefined`。这看着像是\"更严格了不方便\"，实际是好事——它逼着你把声明放在使用之前，省掉了一类\"看起来有值其实是 undefined\"的隐蔽问题。\n\n```javascript\nconst user = {\n  name: 'Tom',\n};\n\nuser.name = 'Jerry';\n```\n\n这里容易误会：`const` 限制的是变量绑定，不是对象内容。这个细节如果没弄清楚，很容易误以为 `const` 声明的对象就完全不能变。\n\n如果确实希望对象内容也更稳定，可以在特定场景使用 `Object.freeze`，但它也只是浅层冻结：\n\n```javascript\nconst config = Object.freeze({\n  theme: 'dark',\n  layout: {\n    sidebar: true,\n  },\n});\n\nconfig.theme = 'light'; // 严格模式下会报错\nconfig.layout.sidebar = false; // 嵌套对象仍然可能被修改\n```\n\n所以在业务里不要把 `const`、不可变更新和深度不可变混为一谈。变量不会重新赋值，不等于数据结构不会变化。\n\n在业务代码里，`let` 更多表示流程里确实会变化的值：\n\n```javascript\nlet currentPage = 1;\n\nfunction nextPage() {\n  currentPage += 1;\n}\n```\n\n变量声明本身就是一种文档。`var` 最大的问题，是它表达不出这些边界。\n\n## 模板字符串让拼接更自然\n\n过去拼接字符串时，代码经常会变得很碎：\n\n```javascript\nvar text = 'Hello, ' + name + '!';\n```\n\n模板字符串让这种表达更直观：\n\n```javascript\nconst text = `Hello, ${name}!`;\n```\n\n这看起来只是语法糖，但在构造复杂文本时体验差别非常明显。\n\n比如错误提示和接口参数拼接，模板字符串会让代码少很多噪音：\n\n```javascript\nconst message = `用户 ${user.name} 的权限已过期，请重新登录`;\nconst url = `/api/users/${user.id}/orders?page=${page}`;\n```\n\n当然，我不会把复杂 HTML 拼接都交给模板字符串。涉及用户输入时，字符串拼 HTML 仍然要注意 XSS 风险。语法更舒服，不代表边界消失了。\n\n比如下面这种写法就要谨慎：\n\n```javascript\nconst html = `<div>${userInput}</div>`;\ncontainer.innerHTML = html;\n```\n\n如果 `userInput` 来自用户输入，就应该优先使用文本节点、框架的默认转义能力，或者经过明确的安全处理。模板字符串解决的是可读性，不负责安全。\n\n模板字符串还有两个我后来才用顺手的细节。一个是天然支持多行，不用再写一堆 `\\n` 拼接：\n\n```javascript\nconst sql = `\n  SELECT id, name\n  FROM users\n  WHERE status = ${status}\n`;\n```\n\n写日志、拼提示文案、生成一段说明性文本时，这种保留换行和缩进的写法清爽很多。不过要注意模板里的换行和缩进会原样保留在字符串里，如果对空白敏感（比如拿去做精确比对），得自己 `trim` 一下。\n\n另一个是标签模板（tagged template），刚开始我觉得这是炫技用不上，直到做国际化和高亮才发现它的价值——它能把静态部分和插值部分拆开交给函数处理：\n\n```javascript\nfunction safe(strings, ...values) {\n  return strings.reduce((acc, str, i) => {\n    const value = values[i - 1];\n    return acc + escapeHtml(String(value)) + str;\n  });\n}\n\nconst html = safe`<div>${userInput}</div>`; // 插值自动转义\n```\n\n像 `styled-components` 的 `css\\`...\\`` 本质上就是这个机制。日常业务里我用得不算多，但知道它存在，遇到\"想对插值统一做处理\"的需求时就不会绕弯路。\n\n## 解构赋值提升了可读性\n\n对象和数组解构是 ES6 里非常实用的能力。\n\n例如：\n\n```javascript\nconst user = { name: 'Tom', age: 18 };\nconst { name, age } = user;\n```\n\n它的价值在于，把“我要用哪些字段”直接写在赋值处，让代码更聚焦。\n\n我最常用的是函数参数解构：\n\n```javascript\nfunction renderUserCard({ name, role, avatar }) {\n  return {\n    title: name,\n    description: role,\n    image: avatar,\n  };\n}\n```\n\n它让函数依赖哪些字段一眼就能看出来。\n\n但解构也不能滥用。嵌套太深时，我宁愿分几步写：\n\n```javascript\nconst { profile } = user;\nconst { address } = profile;\n```\n\n一行里解构三四层，看起来高级，排查空值时会很痛苦。\n\n还有一个常见边界是默认值。默认值只会在字段是 `undefined` 时生效，遇到 `null` 不会生效：\n\n```javascript\nconst user = {\n  name: null,\n};\n\nconst { name = '匿名用户' } = user;\n\nconsole.log(name); // null\n```\n\n如果接口可能返回 `null`，就要额外处理，而不能只依赖解构默认值。我吃过一次亏：后端字段在\"无数据\"时返回的是 `null` 而不是不返回，结果解构默认值形同虚设，页面直接渲染出一个 `null`。后来这种地方我都改成先用 `??`（可选链和空值合并那会儿还没普及，是用 `||` 或者显式判断）兜底，再解构。\n\n解构还有几个我离不开的用法。一个是重命名，接口字段名和组件里想用的名字对不上时特别顺手：\n\n```javascript\nconst { user_name: userName, is_vip: isVip } = res.data;\n```\n\n一个是交换变量，不用再借助临时变量：\n\n```javascript\n[a, b] = [b, a];\n```\n\n还有从数组里按位置取值，配合默认值处理\"可能没有第二项\"的情况：\n\n```javascript\nconst [first, second = ''] = input.split('-');\n```\n\n剩余元素也很实用，常用来把\"已知的几个\"和\"其余的\"分开。比如组件透传 props，把自己关心的几个拿出来，剩下的原样往下传：\n\n```javascript\nfunction Button({ size, ...rest }) {\n  return <button className={`btn-${size}`} {...rest} />;\n}\n```\n\n这个 `...rest` 的写法在做组件封装时几乎每天都要用。\n\n## 箭头函数简化了函数表达\n\n箭头函数最大的好处不只是短，而是让很多短小逻辑写起来更自然。\n\n例如：\n\n```javascript\nconst list = items.map(item => item.name);\n```\n\n这种写法在数组操作、回调和函数组合里非常常见，也大幅提升了函数式写法的可读性。\n\n箭头函数还改变了 `this` 的行为。它不会创建自己的 `this`，这在回调里很有用：\n\n```javascript\nexport default {\n  data() {\n    return {\n      count: 0,\n    };\n  },\n  mounted() {\n    window.setTimeout(() => {\n      this.count += 1;\n    }, 1000);\n  },\n};\n```\n\n但也正因为如此，我不会在所有地方无脑用箭头函数。对象方法、类方法、需要动态 `this` 的场景，普通函数仍然更清楚。\n\n例如 DOM 事件里如果你想通过 `this` 拿到当前元素，普通函数更直观：\n\n```javascript\nbutton.addEventListener('click', function () {\n  this.classList.add('active');\n});\n```\n\n当然，也可以用 `event.currentTarget`，但关键是要知道箭头函数不会绑定自己的 `this`，不是简单地“箭头函数更现代”。\n\n还有几个具体的坑我专门记一下。一是箭头函数没有自己的 `arguments`，老代码里习惯用 `arguments` 拿不定参数的，换成箭头函数会拿到外层的 `arguments` 或者直接报错，这种场景该用剩余参数 `(...args) =>`。二是箭头函数不能当构造函数，`new` 一下直接抛错，所以它不适合写需要 `new` 的类工厂。三是简写返回对象时一定要加括号，不然会被当成函数体：\n\n```javascript\n// 错误：{} 被当成函数体，返回 undefined\nconst make = id => { id };\n// 正确：用括号包住对象字面量\nconst make = id => ({ id });\n```\n\n这个括号我自己漏过好几次，`map` 出来一片 `undefined`，盯着代码看半天才反应过来。\n\n在 Vue 选项式组件里还有个反直觉的点：`methods` 和 `data` 里的函数不能写成箭头函数，因为框架要靠 `this` 把它们绑到组件实例上，箭头函数会让 `this` 指向 `undefined` 或 `window`。这个规则我是踩坑之后才记牢的——把一个 method 顺手改成箭头函数，结果 `this.xxx` 全是 undefined。\n\n## Promise 改变了异步代码结构\n\n虽然异步问题并不是 ES6 才出现，但 Promise 让异步流程开始有了更统一的表达方式。\n\n相比层层嵌套的回调，它至少让流程关系更清楚。\n\n这也为后来的 `async/await` 铺平了道路。\n\n我觉得 Promise 真正重要的地方，是它让异步结果可以被统一描述：成功、失败、继续往下传。\n\n在业务里，我更喜欢把请求错误收敛成统一结构：\n\n```javascript\nfunction requestJson(url) {\n  return fetch(url)\n    .then((response) => {\n      if (!response.ok) {\n        return { ok: false, message: `请求失败：${response.status}` };\n      }\n\n      return response.json().then((data) => ({\n        ok: true,\n        data,\n      }));\n    })\n    .catch(() => ({\n      ok: false,\n      message: '网络异常，请稍后重试',\n    }));\n}\n```\n\n这样组件拿到的永远是 `{ ok, data, message }` 这种稳定形状，而不是每个页面自己猜接口会怎么失败。\n\n这里还有一个容易漏掉的点：Promise 链里如果不 `return`，后续就拿不到预期结果。\n\n```javascript\nfetch('/api/user')\n  .then((response) => {\n    response.json();\n  })\n  .then((data) => {\n    console.log(data); // undefined\n  });\n```\n\n正确写法应该把解析结果返回：\n\n```javascript\nfetch('/api/user')\n  .then((response) => response.json())\n  .then((data) => {\n    console.log(data);\n  });\n```\n\n这类问题很小，但在异步代码里很隐蔽。后来的 `async/await` 能减少一部分链式心智负担，但 Promise 的基本规则仍然要清楚。\n\n实际项目里我还经常用到几个组合能力。`Promise.all` 用来并发拉多个互不依赖的接口，比串行快很多：\n\n```javascript\nconst [profile, orders, coupons] = await Promise.all([\n  requestJson('/api/profile'),\n  requestJson('/api/orders'),\n  requestJson('/api/coupons'),\n]);\n```\n\n但 `Promise.all` 有个要命的特性：只要有一个 reject，整体就 reject，其余结果直接拿不到。做首页那种\"多个模块各自加载、谁挂了谁显示兜底\"的场景，用它就不合适——一个边角接口超时会把整页拖垮。这时候要么把每个请求自己 `catch` 成\"成功的失败结果\"（就是前面那种统一 `{ ok }` 结构），要么用 `Promise.allSettled`（不过 2021 那会儿还得看目标浏览器支不支持，必要时上 polyfill）。\n\n另一个反复踩的坑是 `forEach` 配 `async`。`forEach` 根本不会等你的 `await`：\n\n```javascript\n// 不会按预期串行，循环瞬间走完，await 形同虚设\nlist.forEach(async (item) => {\n  await save(item);\n});\n\n// 要串行用 for...of\nfor (const item of list) {\n  await save(item);\n}\n```\n\n需要并发就 `Promise.all(list.map(save))`，需要串行就老老实实 `for...of`。我见过太多人以为 `forEach` 里加了 `await` 就会一个个等，结果数据写乱了还查不出原因。\n\n## 模块化能力意义很大\n\nES6 模块并不只是语法更新，它实际上推动了前端工程结构进一步标准化。\n\n通过 `import` 和 `export`，代码之间的依赖关系变得更清楚，也更适合现代构建工具处理。\n\n我以前见过一些工具函数文件越写越大，最后变成 `utils.js` 大杂烩。模块化本身不能阻止这种事，但它至少给了我们拆边界的能力。\n\n```javascript\n// services/userService.js\nexport function fetchUserProfile() {}\n\n// utils/formatDate.js\nexport function formatDate() {}\n\n// constants/role.js\nexport const roleMap = {};\n```\n\n一个模块应该有自己的主题。不是所有东西都值得 export，也不是所有公共函数都应该塞进同一个文件。\n\n我通常会让模块暴露稳定能力，而不是暴露内部过程。比如页面只需要 `getUserProfile`，就不应该顺手把拼 URL、转换字段、处理缓存的小函数全部导出。导出的东西越多，模块对外承诺越多，后面调整内部实现就越难。\n\n命名导出和默认导出怎么选，我也纠结过一阵。后来形成的习惯是：一个文件就一个核心东西（一个组件、一个类）用默认导出，工具集、常量集这种\"一堆并列的东西\"用命名导出。命名导出在重构时更友好——改名字 IDE 能帮你全局跟着改，而默认导出在不同文件里可以随便起名，搜索和重命名都更麻烦。\n\n还有一个容易忽略但很重要的特性：ESM 是静态结构，`import` 在编译期就能确定依赖关系。这正是 tree-shaking 能生效的前提。所以我尽量避免 `import * as utils` 这种整包导入，而是按需 `import { formatDate } from '...'`，这样打包时没用到的导出能被摇掉。CommonJS 的 `require` 是运行时动态的，构建工具没法这么干，体积上吃亏。\n\n```javascript\n// 利于 tree-shaking\nimport { formatDate } from '@/utils/date';\n\n// 整包拉进来，没用到的也可能被打包\nimport * as dateUtils from '@/utils/date';\n```\n\n另外 ESM 默认开启严格模式，导入的绑定是只读的活引用——你改不了别人 export 的变量，但能拿到它最新的值。这跟早年那种\"导出一个对象、大家随便往上挂属性\"的脏写法是两个世界，模块边界因此清楚了很多。\n\n## 默认参数和展开语法让配置更自然\n\n我在写配置合并时，经常会用默认参数和展开语法：\n\n```javascript\nfunction createRequestOptions(options = {}) {\n  return {\n    method: 'GET',\n    headers: {\n      'Content-Type': 'application/json',\n    },\n    ...options,\n  };\n}\n```\n\n对象展开让“默认值 + 覆盖项”的意图很清楚。数组展开也常用于不可变更新：\n\n```javascript\nconst nextList = [\n  ...list,\n  newItem,\n];\n```\n\n这些能力看起来小，但它们让业务代码少了很多临时变量和手动拷贝。\n\n不过对象展开是浅拷贝。嵌套对象不会被深层复制：\n\n```javascript\nconst nextOptions = {\n  ...options,\n  headers: {\n    ...options.headers,\n    Authorization: token,\n  },\n};\n```\n\n如果直接写 `{ ...options, headers: { Authorization: token } }`，原来的 headers 字段就会被整个覆盖。配置合并时这类细节很常见，尤其是请求封装、表格配置、图表配置里。ECharts 那种动辄嵌套四五层的 option，我就被浅拷贝坑过——以为合并了用户配置，结果把整个子对象覆盖掉了，图表少了一半东西。深层合并要么手动一层层展开，要么上 `lodash.merge`，别指望一个 `...` 搞定。\n\n展开语法还顺手替代了几个老写法。把类数组转真数组、合并数组、求最大值传参，过去得用 `Array.prototype.slice.call` 或 `apply`，现在都很直白：\n\n```javascript\nconst arr = [...document.querySelectorAll('.item')]; // NodeList -> Array\nconst merged = [...a, ...b];\nconst max = Math.max(...nums);\n```\n\n默认参数这边也有个细节值得说：默认值是惰性求值的，每次调用没传才会计算，所以可以放函数调用甚至引用前面的参数：\n\n```javascript\nfunction createId(prefix = 'id', seq = Date.now()) {\n  return `${prefix}_${seq}`;\n}\n```\n\n不过别在默认值里放有副作用的东西（比如发请求），调试时很容易看不出它什么时候执行了。\n\n## Map 和 Set 解决了对象当容器的尴尬\n\n以前没有 `Map`、`Set` 的时候，我们经常拿普通对象当字典、拿数组配合 `indexOf` 当集合，凑合是能凑合，但有些场景很别扭。\n\n`Set` 最常见的就是去重，一行搞定：\n\n```javascript\nconst unique = [...new Set(list)];\n```\n\n`Map` 比普通对象强在两点。一是键可以是任意类型，对象、DOM 节点都能当键，不会像普通对象那样被强转成字符串。我做过一个\"给每个 DOM 元素挂一份关联数据\"的需求，用 `Map` 把元素本身当键，比维护一个自增 id 干净太多。二是 `Map` 保证插入顺序、能直接拿 `size`、能直接 `for...of` 遍历键值对，不用 `Object.keys` 绕一圈，也不用担心原型链上的 `toString` 之类的键来捣乱：\n\n```javascript\nconst cache = new Map();\ncache.set('user:1', profile);\n\nif (cache.has('user:1')) {\n  return cache.get('user:1');\n}\n\nfor (const [key, value] of cache) {\n  console.log(key, value);\n}\n```\n\n当然不是所有地方都该上 `Map`。需要 JSON 序列化、或者就是个固定结构的配置，普通对象更顺手——`Map` 不能直接 `JSON.stringify` 出键值。我的判断标准是：键不固定、会频繁增删、或者键不是字符串，就用 `Map`；否则普通对象就够了。\n\n## 新特性的价值不在“高级”，而在“更稳定”\n\n很多人学习 ES6 时会关注“有哪些新语法”，但真正更重要的问题其实是：\n\n这些能力是不是让代码更容易读、写、改和协作。\n\n从这个角度看，不少 ES6 特性之所以重要，不是因为它们炫，而是因为它们让前端开发更成熟了。\n\n我现在判断一个 ES6 写法值不值得用，不看它是不是新，而看它有没有让意图更清楚。如果只是为了显得“现代”，把简单代码写得绕，那就没必要。\n\n\nES6 对前端开发的影响，远不只是多了几种新写法。它真正改变的是代码组织方式和表达效率。\n\n当你回头看这些常用能力，会发现它们已经逐渐成为现代前端开发的基础配置。好的语言特性不是让代码显得更高级，而是让日常开发更顺手、更稳定。\n\n对我来说，ES6 最大的价值不是多了几个语法点，而是让 JavaScript 更适合写大型前端项目。变量边界、模块边界、异步边界、数据转换边界，都比以前更容易表达出来了。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":29,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":356,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":53,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":361,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":52,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":275,"readTime":null,"coverImage":null}],"prevPost":{"title":"CSS 优先级为什么总打架：选择器、继承和覆盖规则怎么理解","date":"2021-04-06","excerpt":"样式覆盖失效通常不是一个选择器的问题，而是优先级、作用域和团队写法长期叠在一起。","tags":["前端基础"],"slug":"frontend-css-selector-specificity-2021","category":"2021","likeCount":8,"postId":"110b85e0-fc8f-6b14-d033-e3ceb7ced6b1","viewCount":2246,"readTime":"10分钟","coverImage":null},"nextPost":{"title":"表单校验不是正则堆砌：前端表单体验真正容易出问题的地方","date":"2021-03-17","excerpt":"从输入体验、错误提示和提交时机出发，讨论前端表单校验为什么不只是写几个正则表达式。","tags":["前端基础"],"slug":"frontend-form-validation-2021","category":"2021","likeCount":5,"postId":"8e1ff5cc-bb05-dd24-dbfa-b9c2699e51c1","viewCount":2383,"readTime":"10分钟","coverImage":null}},"__N_SSG":true}