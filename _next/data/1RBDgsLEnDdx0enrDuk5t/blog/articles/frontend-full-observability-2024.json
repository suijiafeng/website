{"pageProps":{"postData":{"title":"前端全链路可观测：用 traceId 把错误、日志、请求串成一条线","date":"2024-12-06","excerpt":"错误平台有堆栈、网关有访问日志、后端有 trace，三套数据却互相对不上号——traceparent 怎么从前端请求头种下去、错误上报怎么带上 release 和用户操作面包屑、日志分级采样怎么定、Session Replay 的隐私与体积代价怎么算，把三座数据孤岛串成一条可以从头查到尾的链路。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"frontend-full-observability-2024","category":"2024","likeCount":5,"postId":"ba21cad5-0d13-62c3-1774-5025611fa79b","viewCount":2238,"readTime":"15分钟","coverImage":null,"content":"\n上周排查一个线上问题，我在三个系统之间来回切了一个下午，最后发现它们说的可能根本不是同一件事。\n\n事情本身不复杂：错误平台报了一批 `Request failed with status code 502`，发生在订单导出页。我去网关日志里捞 502，确实有一批，时间段大致吻合；再让后端查 trace，后端说那个时间段导出服务有一波慢查询，部分请求超时被网关掐断了。听起来严丝合缝，三边一凑，结论呼之欲出——慢查询导致超时，网关返回 502，前端报错。\n\n问题是，我没法证明这三份数据描述的是同一批请求。\n\n错误平台里的报错带的是用户 ID 和页面 URL；网关日志里是 IP、路径和状态码；后端 trace 里是 traceId 和 span。三套系统三套主键，彼此之间没有任何一个字段能精确对上。我只能靠\"时间段大致吻合\"来推断，而\"大致吻合\"在排查里是个很危险的词——那个时间段导出页的请求有几千个，502 有几十个，慢查询有十几个，它们是不是同一批，全靠信仰。\n\n这个问题最后定位出来了，确实是慢查询。但过程里暴露的问题比问题本身更值得记：**我们有错误采集、有日志、有 trace，但它们是三座孤岛，一个横跨三层的问题查起来就是三边取证再人肉对时间戳**。这篇记一下这两个月我们把三座岛串起来的过程。\n\n## 三套数据是怎么变成孤岛的\n\n先捋一下现状。\n\n错误采集是 2020 年就搭起来的那套：`window.onerror` 加 `unhandledrejection`，后来接了 Sentry，堆栈还原走 SourceMap 上传。这些在之前的文章里都完整写过，细节不重复。性能侧今年八月接了 Web Vitals 的采集，也单独写过一篇。这两套是前端自己维护的。\n\n网关日志是运维维护的，Nginx access log 进了 ELK，能按路径、状态码、时间段查询。后端服务这两年陆续接了 OpenTelemetry，trace 数据进了自建的 Jaeger，服务之间的调用链是全的——订单服务调库存、库存调缓存，每一跳的耗时都能看到。\n\n单看每一套都没毛病，问题出在边界上。前端发出去的请求，在离开浏览器的那一刻就\"失联\"了——它到了网关变成一条 access log，到了后端变成一个 trace，但这两个形态和前端错误平台里那条报错之间，没有任何血缘关系。\n\n更要命的是链路起点的位置。后端的 traceId 是从网关或者服务入口才开始生成的，也就是说整条链路被砍掉了最前面一截：用户到底点了什么、前端带了什么参数、这个请求在浏览器队列里排了多久、DNS 和 TLS 花了多少时间，trace 里一概没有。后端看到的\"请求进来了\"，距离用户点下按钮可能已经过去了两秒，这两秒是链路上的暗物质。\n\n反过来也一样。前端错误平台里那条 502，我知道它是哪个用户、哪个页面、什么时间，但我不知道它对应后端哪条 trace。想查这个请求在服务端到底经历了什么，做不到。\n\n十一月初的周会上把这个事立了项，前端、后端、运维各出人。目标说起来一句话：让任何一条前端报错都能一键定位到它对应的后端调用链。\n\n## traceparent：把 traceId 从浏览器种下去\n\n串联的思路不需要发明，W3C 有现成的 Trace Context 规范，核心就是一个叫 `traceparent` 的请求头：\n\n```\ntraceparent: 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01\n```\n\n四段用连字符分隔：版本号（目前恒为 `00`）、32 位十六进制的 trace-id、16 位的 parent-id（也就是发出这个请求的当前 span 的 id）、trace-flags（最低位表示是否采样）。规范里还有个配套的 `tracestate` 头，用来携带各厂商的私有状态，我们暂时用不上。\n\n后端的 OpenTelemetry SDK 天然认这个头——只要请求带着 `traceparent` 进来，后端就不会自己新起一个 traceId，而是接着这条链往下挂 span。所以改造的第一步，是让 traceId 的出生地从网关挪到浏览器。\n\nOpenTelemetry 的 JS 端 SDK 今年已经相当成熟了，`@opentelemetry/sdk-trace-web` 加上 fetch 和 XHR 的自动注入 instrumentation：\n\n```typescript\nimport { WebTracerProvider, BatchSpanProcessor } from '@opentelemetry/sdk-trace-web';\nimport { OTLPTraceExporter } from '@opentelemetry/exporter-trace-otlp-http';\nimport { registerInstrumentations } from '@opentelemetry/instrumentation';\nimport { FetchInstrumentation } from '@opentelemetry/instrumentation-fetch';\nimport { XMLHttpRequestInstrumentation } from '@opentelemetry/instrumentation-xml-http-request';\nimport { ZoneContextManager } from '@opentelemetry/context-zone';\n\nconst provider = new WebTracerProvider();\n\nprovider.addSpanProcessor(\n  new BatchSpanProcessor(\n    new OTLPTraceExporter({ url: 'https://otel.example.com/v1/traces' }),\n    { maxQueueSize: 100, scheduledDelayMillis: 5000 }\n  )\n);\n\nprovider.register({\n  contextManager: new ZoneContextManager(),\n});\n\nregisterInstrumentations({\n  instrumentations: [\n    new FetchInstrumentation({\n      // 只对自己的网关域名注入，别把 traceparent 甩给第三方\n      propagateTraceHeaderCorsUrls: [/^https:\\/\\/api\\.example\\.com/],\n    }),\n    new XMLHttpRequestInstrumentation({\n      propagateTraceHeaderCorsUrls: [/^https:\\/\\/api\\.example\\.com/],\n    }),\n  ],\n});\n```\n\n几个配置值得逐个说。\n\n`BatchSpanProcessor` 是必须的，别用文档示例里的 SimpleSpanProcessor——后者每产生一个 span 就发一次上报请求，等于给每个业务请求配了一个观测请求，流量直接翻倍。批量处理器攒够一批或者到时间再发，页面卸载时配合 `sendBeacon` 兜底，这套打法和八月做性能上报时一模一样。\n\n`ZoneContextManager` 解决的是异步上下文问题。浏览器里没有 Node 那种 AsyncLocalStorage，一个 `await` 之后\"当前 span 是谁\"就丢了，zone.js 的打补丁方案是目前 Web 端的现实解。它有侵入性，我们评估后接受了——只在需要精确父子关系的场景有影响，而我们的主要诉求是请求级串联，容忍度高。\n\n`propagateTraceHeaderCorsUrls` 一定要收紧。给第三方统计、支付回调之类的请求带上 `traceparent`，轻则被对方的 WAF 拦掉，重则把内部的 trace 结构暴露出去。这个正则我们在 code review 里盯得很死。\n\n## 两个上线前撞到的坑\n\n第一个是 CORS。`traceparent` 不在简单请求头的白名单里，跨域请求会因为它触发预检，而网关的 `Access-Control-Allow-Headers` 里没有它——测试环境部署完，所有跨域接口整齐划一地 CORS 报错，页面直接白给。回滚之后才反应过来，先让运维在网关加了头再重新上：\n\n```nginx\nadd_header Access-Control-Allow-Headers\n  'Content-Type, Authorization, traceparent, tracestate' always;\n```\n\n吃了 CORS 这一亏之后，正式上线改成了按路由灰度：instrumentation 的注入用一个远程配置的开关控制，先只对订单域的接口开，观察了三天网关和后端的日志无异常，再逐步放到全站。观测系统自己也是会出故障的系统，它的发布要按生产变更的规矩来，不能因为\"只是加个监控\"就裸奔全量。\n\n第二个坑在网关日志。要让 access log 和 trace 对上号，Nginx 得把 `traceparent` 里的 trace-id 段打进日志。原始头可以用 `$http_traceparent` 拿到，用 map 切出中间那段：\n\n```nginx\nmap $http_traceparent $trace_id {\n  \"~*^[0-9a-f]{2}-([0-9a-f]{32})-\" $1;\n  default \"-\";\n}\n\nlog_format main_trace '$remote_addr [$time_local] \"$request\" '\n                      '$status $body_bytes_sent $request_time '\n                      'trace=$trace_id';\n```\n\n这步做完，网关日志和后端 trace 有了共同主键。\n\n最后一块拼图是错误平台。前端每次请求发出时，SDK 里已经有当前的 traceId 了，把它塞进错误上报的上下文即可。我们在 axios 的响应拦截器里做的：\n\n```typescript\nimport { context, trace } from '@opentelemetry/api';\n\nrequest.interceptors.response.use(undefined, (error) => {\n  const span = trace.getSpan(context.active());\n  Sentry.captureException(error, {\n    tags: {\n      trace_id: span?.spanContext().traceId ?? 'none',\n    },\n    contexts: {\n      request: {\n        url: error.config?.url,\n        status: error.response?.status,\n        method: error.config?.method,\n      },\n    },\n  });\n  return Promise.reject(error);\n});\n```\n\n到这里，链路闭环了：**错误平台里任何一条接口相关的报错，都带着一个 traceId，拿着它可以直接去 Jaeger 看这个请求在服务端的完整经历，也可以去 ELK 里精确捞出网关那条日志**。上周那种三边取证对时间戳的排查方式，变成了单主键查询。\n\n十二月初有个订单状态不一致的问题练了手：从用户报障到定位出是某个下游服务的缓存脏了，前后二十分钟。放在两个月前，光是确认\"前端看到的报错和后端查到的异常是不是同一个请求\"就得半天。\n\n性能数据也顺路搭上了这班车。fetch instrumentation 生成的 span 里带着 Resource Timing 的分解——DNS、TCP、TTFB、下载各占多少——于是\"这个接口慢\"第一次可以在同一条 trace 里回答\"慢在浏览器排队、慢在网络、还是慢在后端\"。八月做 Web Vitals 采集时，接口耗时和后端处理耗时还是两张对不上的报表，现在它们是同一条链路上相邻的两段 span。这算是改造前没预料到的附带收益。\n\n## 错误上报的上下文：光有堆栈不够\n\ntraceId 解决的是\"跨系统对号\"，但一条报错要能查，前端侧自己的上下文也得够厚。借着这次改造，把错误上报的信息结构整个重新梳理了一遍，有三样东西的价值远超预期。\n\n第一是 release。每次构建把版本号注入到上报里——我们用 git 的 short hash 拼上构建时间：\n\n```typescript\nSentry.init({\n  dsn: SENTRY_DSN,\n  release: `admin-web@${__GIT_HASH__}.${__BUILD_TIME__}`,\n  environment: import.meta.env.MODE,\n});\n```\n\n`__GIT_HASH__` 是 Vite 的 `define` 在构建时注入的。Sentry 里按 release 分组之后，\"这个错误是不是新版本引入的\"从猜测变成了事实查询：如果一个错误只在最新 release 出现，八成是这次上线带上去的；如果横跨好几个 release，那是存量问题被流量放大了。这两种情况的处理优先级和回滚决策完全不同，而区分它们只需要一个构建时注入的字符串。SourceMap 也是按 release 关联上传的，还原堆栈才不会串版本——SourceMap 那套流程 2021 年写过，不赘述。\n\n第二是面包屑（breadcrumbs）。Sentry 的 SDK 默认会记录报错前的用户操作序列：点了哪个按钮、路由从哪跳到哪、发了哪些请求、console 里打了什么。这个东西在复现\"只有特定操作路径才触发\"的错误时几乎是决定性的。\n\n默认面包屑有个不足：它记录的是 DOM 级别的事件，`click on button.el-button` 这种描述在中后台系统里信息量很低——满屏都是 el-button。我们额外往面包屑里塞了业务事件：\n\n```typescript\nSentry.addBreadcrumb({\n  category: 'business',\n  message: 'switch-shop',\n  data: { shopId: shop.id, shopType: shop.type },\n  level: 'info',\n});\n```\n\n切换了哪个店铺、改了什么筛选条件、打开了哪个批量操作弹窗，这些业务维度的状态才是中后台报错的真正语境。加了业务面包屑之后，有一类\"只有代发店铺才触发\"的报错，从面包屑里直接就能看出规律，不用再让用户回忆操作步骤。\n\n第三是路由信息。SPA 的 URL 未必能反映真实的页面状态——弹窗、抽屉、Tab 这些\"页面里的页面\"在 URL 上没有体现。我们在路由切换和关键弹窗的开关时都记面包屑，报错时把当前的\"逻辑页面栈\"一起带上。一个报错发生在\"订单列表页\"还是\"订单列表页上叠着的退款审核抽屉里\"，排查方向完全不同。\n\n上下文做厚的同时，噪音也要主动压。浏览器插件注入脚本的报错、`ResizeObserver loop limit exceeded` 这类无害的浏览器内部告警、爬虫环境里的各种诡异异常，混在错误列表里会稀释所有人的注意力。Sentry 的 `ignoreErrors` 和 `denyUrls` 各配了一份清单，另外在 `beforeSend` 里过滤掉堆栈完全来自插件路径（`chrome-extension://`）的事件。清单每季度过一遍，进清单的每一条都要写清楚理由——过滤规则本身也是需要被审视的代码，否则总有一天会把真问题滤掉。\n\n## 日志分级与采样：不是越全越好\n\n链路通了之后，很快撞上第二个问题：量。\n\n前端日志这个东西，采集起来没有成本感——一行代码的事——但存储和查询的成本是运维在担。我们最初把请求级别的观测数据全量上报，一周之后运维拿着存储增速曲线过来聊了聊，数字不太体面：中后台系统日活不算高，但人均操作密度大，一个运营一天点出几千个请求很正常。\n\n后来定下来的规则是分级加采样。\n\n错误级别（error）全量上报，这个不能省——错误本来就是稀疏事件，对稀疏事件采样等于主动丢线索。警告级别（warn，接口慢于阈值、重试后成功、降级触发这类\"没出事但不正常\"的事件）按 30% 采样。信息级别（info，正常请求的耗时分布、页面停留时长这类统计性数据）按 10% 采样——它们的用途是算分布看趋势，不是查个案，10% 的样本量对分布估计完全够用。\n\n采样有个容易忽略的细节：**采样决策要在 trace 级别做，不能在单条数据级别做**。如果每条日志独立掷骰子，一条链路上的数据会残缺不全——请求发出的 span 采到了、响应的 span 没采到，查起来比没有还难受。\n\n正确做法是拿 traceId 做哈希决定整条链路采不采，同一条 trace 要么全留要么全丢。OpenTelemetry 的 `TraceIdRatioBasedSampler` 就是这个逻辑：\n\n```typescript\nimport { TraceIdRatioBasedSampler, ParentBasedSampler } from '@opentelemetry/sdk-trace-base';\n\nconst provider = new WebTracerProvider({\n  sampler: new ParentBasedSampler({\n    root: new TraceIdRatioBasedSampler(0.1),\n  }),\n});\n```\n\n前端作为链路起点做出采样决策后，决策写进 `traceparent` 第四段的 trace-flags 里，后端跟着这个标志位走，整条链路的采样口径就一致了。这也是那个头要设计第四段的意义——采样是全链路的共识，不是每一层各自的自由。\n\n这里有个和后端对齐时的分歧值得记录。后端原本在服务入口设了自己的采样率，前端接进来之后，两层采样叠乘，实际留存率变成了预期的十分之一，Jaeger 里一度出现\"错误平台有 traceId、Jaeger 里查无此链\"的尴尬——链路是通的，数据被采样丢了。最后的约定是：**采样决策由链路最前端做出，中间和后端只跟随 trace-flags，不再各自掷骰子**；同时错误关联的 trace 强制保留，前端在捕获到异常时把当前 trace 标记为必采（OpenTelemetry 里对应把该请求的 span 记录为 error 并确保 flags 置位），保证\"能查错\"这个底线不被采样规则误伤。\n\n还有一条给量兜底的通道问题。观测数据的上报和业务请求走的是同一个浏览器，页面关闭时批量队列里没发完的数据会直接丢。`BatchSpanProcessor` 配合 `visibilitychange` 里主动 `forceFlush`，再加 `navigator.sendBeacon` 作为卸载时的最后一枪，能把丢失率压到可以接受——这套手法八月做性能上报时验证过，这次原样搬过来，两套采集共用同一个上报通道和同一份重试逻辑，运维侧也只用维护一个接收入口。\n\n## Session Replay：录像很香，代价先算清\n\n排查上下文这条路走到头，就是 Session Replay——把用户报错前的页面操作直接录下来回放。Sentry 的 Session Replay 今年已经很成熟，我们评估后小范围开了。但这个东西的取舍值得单独写一节，因为它是全套方案里代价最重的一块。\n\n它的原理不是录屏，是基于 rrweb 记录 DOM 变更序列，回放时在沙盒里重建 DOM。这个原理决定了它的两项代价。\n\n第一是隐私。DOM 里有什么它就录什么，而中后台系统的 DOM 里满屏都是订单金额、客户手机号、收货地址。Sentry 默认把所有文本打码（`maskAllText: true`）、所有图片和视频块掉（`blockAllMedia: true`），这两个默认值绝对不要动：\n\n```typescript\nSentry.init({\n  integrations: [\n    Sentry.replayIntegration({\n      maskAllText: true,\n      blockAllMedia: true,\n    }),\n  ],\n  replaysSessionSampleRate: 0,\n  replaysOnErrorSampleRate: 1.0,\n});\n```\n\n我们在全量打码的基础上，只对少数确认无敏感信息的区域（导航菜单、状态标签）用 `data-sentry-unmask` 显式放开。方案上线前过了一遍安全团队的评审，评审里还真拦下一处：有个自定义组件把客户昵称画在 canvas 上，文本打码对 canvas 不生效，最后给那个组件加了 block。**录像回放的排查收益再大，也大不过一次用户数据泄露的事故，这类工具的默认姿势必须是\"全遮，按需放开\"，而不是反过来**。\n\n第二是体积。全量录制的上报流量和服务端存储都不小，所以上面配置里 `replaysSessionSampleRate` 设了 0、`replaysOnErrorSampleRate` 设了 1.0——平时只在内存里滚动缓存最近约一分钟的 DOM 事件，不上报；一旦出错，把缓存的这一段连同后续录制一起发出去。绝大多数正常会话零流量，出问题的会话有完整现场，这是我们能接受的平衡点。\n\n体积的账还有一笔在页面这头：Replay 的 SDK 本身不小，打进主包会白白拖累所有用户的首屏。Sentry 支持把 replayIntegration 用动态 `import()` 懒加载后再 `addIntegration` 挂上，我们把它放在首屏渲染完成之后加载——录制的起点晚几秒，换主包不背这几十 KB，值。\n\n开了一个月的体感：对\"用户说不清自己干了什么\"的报障，回放几乎是降维打击；但它是排查的最后一招，不是第一招——大多数问题走 traceId 加面包屑就能定位，用不着看录像。\n\n## 告警：看错误率，别看错误数\n\n链路和数据都齐了，最后一环是告警。这里有个我们踩过的经典坑：按错误数告警。\n\n最初的规则是\"五分钟内同一错误超过 50 条就告警\"。结果是双向失灵。白天流量高峰，一个影响面 0.1% 的小问题也能轻松冲过 50 条，告警群天天响，两周之后就没人点开看了；凌晨流量低谷，一个把某类用户全部堵死的问题，可能一小时也凑不够 50 条，安静得像什么都没发生。\n\n**错误数是流量的函数，不是健康度的函数**。改成错误率之后世界清净了很多：把前端上报的 JS 错误数、接口失败数，除以同期的 PV 或请求总数，按比率设阈值。分母的数据正好来自八月那套性能采集的 PV 统计，两套系统在这里自然接上了。\n\n比率告警还要叠一个最小样本量的下限：窗口内请求数低于 100 不触发。不加这条的话，凌晨三个请求错一个，33% 的错误率照样把人从被窝里薅起来，而那大概率只是某个用户的网络抽风。\n\n另外给\"新 release 上线后的前 30 分钟\"单独设了一档更敏感的阈值。大部分前端事故是发布带上去的，这半小时的错误率哪怕只是轻微抬头也值得有人看一眼——release 字段在上报里本来就有，按它切一个观察窗口是顺手的事。发现得早，回滚就是五分钟；发现得晚，就是第二天的客诉工单。\n\n告警的接收面也做了切分：错误率越阈值进值班群并且电话呼值班人，新 release 观察窗的告警只进发布群找当次发布的负责人，单个错误的新增（首次出现的错误指纹）只进日报。所有告警都能一键跳到带 traceId 的错误详情。告警的目的不是\"让所有人知道出事了\"，是\"让对的人在最短路径上开始查\"，这条是十一月对着一堆没人认领的告警消息才想明白的。\n\n## 收尾\n\n十二月初又遇到一次和上周结构相同的问题：错误平台报接口超时，横跨前端、网关、后端三层。这次点开报错详情，traceId 就在 tags 里，贴到 Jaeger 里一查，卡在下游一个营销服务的 span 上，网关日志用同一个 id 一搜就是那条 504。全程没有一句\"你那边时间段是几点到几点\"的对话。\n\n采集本身我们四年前就在做了，这次做的事情说穿了只有一件：给所有数据发同一张身份证。\n\ntraceparent 这个头一共就一行字符串，但它把浏览器里的一次点击和数据库里的一次慢查询，变成了同一个故事的两页。\n"},"recommendedPosts":[{"title":"Vite 用上 Rolldown 之后：一次构建提速的真实体感和注意点","date":"2026-02-07","excerpt":"Rolldown 把 Vite 的打包从 esbuild/Rollup 双引擎统一成一套 Rust 实现，记录我在中型项目里切过去的体感、兼容性问题和回退策略。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"vite-rolldown-2026","category":"2026","likeCount":8,"postId":"27afacf5-76c4-4c26-58fd-15ea35bac2d8","viewCount":1708,"readTime":null,"coverImage":null},{"title":"前端构建速度优化：别只怪打包工具慢","date":"2024-08-23","excerpt":"构建慢不一定是打包工具的锅：依赖体积、类型检查边界、缓存命中率、代码生成方式，任何一处出问题都会拖慢构建，换工具往往只是把慢延后了。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"frontend-build-performance-2024","category":"2024","likeCount":50,"postId":"a36ea56d-4d31-dfb1-a4be-293b079f286b","viewCount":834,"readTime":null,"coverImage":null},{"title":"前端监控从 Web Vitals 开始：别只在用户投诉后才看性能","date":"2024-08-09","excerpt":"用户已经觉得页面慢了，开发手上却没有任何证据——Web Vitals 怎么采集才可靠、错误监控怎么带上下文、接口耗时和用户行为怎么串成一条能复现问题的时间线。","tags":["前端基础","性能与缓存","构建与模块化"],"slug":"frontend-observability-web-vitals-2024","category":"2024","likeCount":29,"postId":"a4472209-40c8-7d31-0d01-f7a0a89cea1f","viewCount":866,"readTime":null,"coverImage":null}],"prevPost":{"title":"我对 Edge 渲染降温之后：先问数据在哪，再问页面跑在哪","date":"2025-01-26","excerpt":"页面慢不一定该上 Edge——渲染位置离用户近了，数据如果还在中心区域，那一跳只是换了个地方出现，缓存策略和运行时限制才是真正要先算清的账。","tags":["前端基础","组件与状态","性能与缓存"],"slug":"frontend-edge-rendering-2025","category":"2025","likeCount":29,"postId":"7e704928-1927-54a7-f81f-f27839a55994","viewCount":953,"readTime":null,"coverImage":null},"nextPost":{"title":"前端安全从 XSS 和 CSP 开始：别把用户输入当成可信内容","date":"2024-11-21","excerpt":"用户输入一旦被当成 HTML 插进页面，风险就已经埋下了：XSS 的常见入口、富文本清洗、Token 存储的取舍，以及 CSP 从 Report-Only 到正式收紧的落地路径。","tags":["前端基础","构建与模块化"],"slug":"frontend-security-csp-xss-2024","category":"2024","likeCount":9,"postId":"be8ce21b-8e0f-1718-40ef-9a588a8dd9fa","viewCount":1311,"readTime":null,"coverImage":null}},"__N_SSG":true}