{"pageProps":{"postData":{"title":"掌握JavaScript异步编程","date":"2024-09-07","excerpt":"探索 JavaScript 中的异步编程技术，从回调函数到 Promise，再到 async/await、并发控制、请求取消和统一错误处理。","tags":["前端基础"],"slug":"javascript-async-await","category":"2024","likeCount":8,"postId":"2fc17dce-93a5-8a87-c22b-7adeceb84e60","viewCount":3692,"readTime":"8分钟","coverImage":null,"content":"\nJavaScript 的异步编程能力是它最重要的基础能力之一。只要写过前端项目，就绕不开接口请求、定时器、文件上传、图片加载、用户输入防抖、服务端渲染数据获取这些场景。\n\n很多人学习异步时，会停在 `async/await` 的语法层面：知道 `await fetch()`，知道 `try/catch`，但一到实际项目就开始困惑：多个请求应该串行还是并行？一个请求失败是否要影响其他请求？用户切换页面后旧请求要不要取消？接口错误结构不统一时前端怎么处理？\n\n这篇文章从基础语法讲起，但重点放在项目里真正会遇到的边界。\n\n## 为什么需要异步编程\n\nJavaScript 运行在单线程模型上，浏览器主线程既要执行脚本，也要处理渲染、用户输入和事件回调。如果一个耗时任务一直占着主线程，页面就会卡住。\n\n异步编程的核心价值是：把需要等待的事情交出去，等结果回来时再继续处理。\n\n常见异步任务包括：\n\n- 网络请求\n- 定时器\n- 文件读写\n- IndexedDB 操作\n- 图片、字体、脚本资源加载\n- Web Worker 计算结果返回\n- Node.js 中的数据库和文件系统调用\n\n需要注意的是，异步不等于并行。`setTimeout`、`fetch`、事件监听这些 API 会把回调安排到未来执行，但 JavaScript 代码本身依然遵循事件循环规则。理解这一点，才能解释为什么一个长时间运行的同步循环依然会让页面假死。\n\n```js\nwhile (true) {\n  // 主线程被占满，异步回调也没有机会执行\n}\n```\n\n## 回调函数：最原始也最直接\n\n回调函数是处理异步操作最传统的方式：\n\n```js\nfunction fetchData(callback) {\n  setTimeout(() => {\n    callback('Data fetched')\n  }, 2000)\n}\n\nfetchData((result) => {\n  console.log(result)\n})\n\nconsole.log('Fetching data...')\n```\n\n输出顺序是：\n\n```text\nFetching data...\nData fetched\n```\n\n回调的优点是简单，没有额外抽象。事件监听本质上也是回调：\n\n```js\nbutton.addEventListener('click', () => {\n  console.log('clicked')\n})\n```\n\n但当多个异步任务存在依赖关系时，回调会迅速变得难读：\n\n```js\nfetchUser((user) => {\n  fetchPosts(user.id, (posts) => {\n    fetchComments(posts[0].id, (comments) => {\n      render(comments)\n    })\n  })\n})\n```\n\n这就是常说的“回调地狱”。它的问题不只是缩进深，更麻烦的是错误处理分散、流程控制困难、后续维护成本高。\n\nNode.js 早期常见的 error-first callback 用法稍微规范一些：\n\n```js\nreadFile('config.json', (error, content) => {\n  if (error) {\n    console.error(error)\n    return\n  }\n\n  console.log(content)\n})\n```\n\n但大量业务代码叠加后，还是很容易失控。\n\n## Promise：让异步结果变成对象\n\nPromise 提供了一种更结构化的方式来描述异步操作。一个 Promise 代表一个未来才会完成的结果，它最终会进入 fulfilled 或 rejected 状态。\n\n```js\nfunction fetchData() {\n  return new Promise((resolve) => {\n    setTimeout(() => {\n      resolve('Data fetched')\n    }, 2000)\n  })\n}\n\nfetchData()\n  .then((result) => console.log(result))\n  .catch((error) => console.error(error))\n\nconsole.log('Fetching data...')\n```\n\nPromise 的价值在于它可以链式组织流程：\n\n```js\nfetchUserData()\n  .then((user) => fetchUserPosts(user.id))\n  .then((posts) => fetchPostComments(posts[0].id))\n  .then((comments) => render(comments))\n  .catch((error) => showError(error))\n```\n\n这里有一个重要规则：在 `then` 里返回 Promise，后面的 `then` 会等待它完成。\n\n```js\nPromise.resolve(1)\n  .then((value) => value + 1)\n  .then((value) => Promise.resolve(value + 1))\n  .then((value) => console.log(value)) // 3\n```\n\n如果中间任何一步抛错，都会进入最近的 `catch`：\n\n```js\nfetchUserData()\n  .then((user) => {\n    if (!user.active) {\n      throw new Error('User is inactive')\n    }\n    return fetchUserPosts(user.id)\n  })\n  .catch((error) => {\n    console.error(error.message)\n  })\n```\n\nPromise 让异步流程从“嵌套回调”变成“线性链条”，这是很大的进步。\n\n## async/await：让异步代码像同步代码\n\n`async/await` 是基于 Promise 的语法糖。它不会改变异步本质，但会让代码更接近同步流程。\n\n```js\nasync function fetchDataAndProcess() {\n  try {\n    const user = await fetchUserData()\n    const posts = await fetchUserPosts(user.id)\n    const comments = await fetchPostComments(posts[0].id)\n\n    render(comments)\n  } catch (error) {\n    showError(error)\n  }\n}\n\nfetchDataAndProcess()\n```\n\n这段代码比 Promise 链更容易插入条件判断：\n\n```js\nasync function loadDashboard(userId) {\n  const user = await fetchUser(userId)\n\n  if (!user.permissions.includes('dashboard:read')) {\n    throw new Error('No permission')\n  }\n\n  return fetchDashboard(user.id)\n}\n```\n\n但 `await` 也有一个常见坑：不必要的串行。\n\n```js\nconst user = await fetchUser()\nconst posts = await fetchPosts()\nconst comments = await fetchComments()\n```\n\n如果这三个请求互不依赖，这样写会让总耗时变成三者相加。更好的方式是并行：\n\n```js\nconst [user, posts, comments] = await Promise.all([\n  fetchUser(),\n  fetchPosts(),\n  fetchComments(),\n])\n```\n\n我的习惯是先判断依赖关系：后一个请求依赖前一个结果，就串行；互不依赖，就并行。\n\n## Promise.all、allSettled 和 race\n\n`Promise.all()` 适合“要么全部成功，要么整体失败”的场景：\n\n```js\nasync function loadHomePage() {\n  const [profile, notifications, shortcuts] = await Promise.all([\n    fetchProfile(),\n    fetchNotifications(),\n    fetchShortcuts(),\n  ])\n\n  return { profile, notifications, shortcuts }\n}\n```\n\n如果任意一个失败，`Promise.all` 会直接 reject。这对强依赖数据很合理，比如订单详情页里订单、商品、支付信息缺一不可。\n\n但有些场景不应该因为一个模块失败导致整页不可用。比如首页推荐失败了，用户信息和通知仍然可以展示。这时可以用 `Promise.allSettled()`：\n\n```js\nasync function loadHomeWidgets() {\n  const results = await Promise.allSettled([\n    fetchProfile(),\n    fetchRecommendations(),\n    fetchNotifications(),\n  ])\n\n  return results.map((result) => {\n    if (result.status === 'fulfilled') {\n      return { ok: true, data: result.value }\n    }\n\n    return { ok: false, error: normalizeError(result.reason) }\n  })\n}\n```\n\n`Promise.race()` 适合“谁先完成用谁”的场景，不过业务里更常见的是超时控制：\n\n```js\nfunction timeout(ms) {\n  return new Promise((_, reject) => {\n    setTimeout(() => reject(new Error('Request timeout')), ms)\n  })\n}\n\nasync function fetchWithTimeout(url, ms = 8000) {\n  return Promise.race([\n    fetch(url),\n    timeout(ms),\n  ])\n}\n```\n\n不过这个版本只是让等待超时，真实请求不一定被取消。要真正取消请求，需要 `AbortController`。\n\n## 取消请求：避免旧结果覆盖新结果\n\n在搜索框、列表筛选、路由切换中，请求取消非常重要。\n\n想象用户先搜索 `react`，很快又搜索 `next`。如果 `react` 的请求更慢，最后才返回，它可能覆盖 `next` 的结果。这就是典型的竞态问题。\n\n浏览器 `fetch` 支持 `AbortController`：\n\n```js\nlet currentController = null\n\nasync function search(keyword) {\n  if (currentController) {\n    currentController.abort()\n  }\n\n  currentController = new AbortController()\n\n  try {\n    const response = await fetch(`/api/search?q=${encodeURIComponent(keyword)}`, {\n      signal: currentController.signal,\n    })\n\n    if (!response.ok) {\n      throw new Error('Search failed')\n    }\n\n    return response.json()\n  } catch (error) {\n    if (error.name === 'AbortError') {\n      return null\n    }\n\n    throw error\n  }\n}\n```\n\n在 React 组件中，通常会在 `useEffect` 清理函数里取消：\n\n```jsx\nuseEffect(() => {\n  const controller = new AbortController()\n\n  async function load() {\n    try {\n      const response = await fetch(`/api/posts?tag=${tag}`, {\n        signal: controller.signal,\n      })\n      const data = await response.json()\n      setPosts(data)\n    } catch (error) {\n      if (error.name !== 'AbortError') {\n        setError(error)\n      }\n    }\n  }\n\n  load()\n\n  return () => controller.abort()\n}, [tag])\n```\n\n这类代码看起来啰嗦，但它解决的是实实在在的用户体验问题：页面不应该被过期请求改写。\n\n## 统一错误处理\n\n异步编程里最容易被低估的是错误处理。\n\n`fetch` 有一个反直觉点：只要服务器返回了响应，即使状态码是 404 或 500，Promise 也会 fulfilled。只有网络错误、跨域失败、请求被取消这类情况，才会 reject。\n\n所以不能只写：\n\n```js\nconst response = await fetch('/api/user')\nconst data = await response.json()\n```\n\n应该检查 `response.ok`：\n\n```js\nasync function request(url, options) {\n  const response = await fetch(url, options)\n  const contentType = response.headers.get('content-type') || ''\n  const isJson = contentType.includes('application/json')\n  const body = isJson ? await response.json() : await response.text()\n\n  if (!response.ok) {\n    throw normalizeApiError(response.status, body)\n  }\n\n  return body\n}\n\nfunction normalizeApiError(status, body) {\n  if (body && typeof body === 'object' && body.error) {\n    return {\n      status,\n      message: body.error.message || 'Request failed',\n      code: body.error.code,\n    }\n  }\n\n  return {\n    status,\n    message: typeof body === 'string' && body ? body : 'Request failed',\n  }\n}\n```\n\n对应的 API 错误也应该尽量统一：\n\n```json\n{\n  \"error\": {\n    \"message\": \"Invalid request\",\n    \"code\": \"INVALID_REQUEST\"\n  }\n}\n```\n\n这样前端无论调用哪个接口，都能用同一套 toast、日志上报和错误边界处理。\n\n## async/await 的几个边界\n\n第一，`Array.prototype.forEach` 不会等待 `await`：\n\n```js\nitems.forEach(async (item) => {\n  await saveItem(item)\n})\n```\n\n这段代码不会等待所有保存完成。需要串行时用 `for...of`：\n\n```js\nfor (const item of items) {\n  await saveItem(item)\n}\n```\n\n需要并行时用 `Promise.all`：\n\n```js\nawait Promise.all(items.map((item) => saveItem(item)))\n```\n\n第二，不要吞掉错误：\n\n```js\ntry {\n  await submitForm()\n} catch (error) {\n  console.error(error)\n}\n```\n\n如果这里只打印日志，调用方会以为提交成功了。更好的方式是处理完再抛出，或者返回明确状态：\n\n```js\ntry {\n  await submitForm()\n  return { ok: true }\n} catch (error) {\n  reportError(error)\n  return { ok: false, error: normalizeError(error) }\n}\n```\n\n第三，避免在顶层随意触发副作用。模块一被 import 就发请求，后续测试、服务端渲染、预加载都可能受到影响。更稳的是把异步行为放进明确函数里。\n\n## 一个更接近项目的例子\n\n下面是一个带取消、超时、错误归一化的请求函数：\n\n```js\nfunction createTimeoutSignal(ms) {\n  const controller = new AbortController()\n  const timer = setTimeout(() => controller.abort(), ms)\n\n  return {\n    signal: controller.signal,\n    clear: () => clearTimeout(timer),\n  }\n}\n\nasync function getJson(url, options = {}) {\n  const timeout = createTimeoutSignal(options.timeout || 8000)\n\n  try {\n    const response = await fetch(url, {\n      ...options,\n      signal: timeout.signal,\n      headers: {\n        Accept: 'application/json',\n        ...options.headers,\n      },\n    })\n\n    const body = await response.json().catch(() => null)\n\n    if (!response.ok) {\n      throw normalizeApiError(response.status, body)\n    }\n\n    return body\n  } catch (error) {\n    if (error.name === 'AbortError') {\n      throw { status: 0, message: 'Request timeout or aborted' }\n    }\n\n    throw error\n  } finally {\n    timeout.clear()\n  }\n}\n```\n\n真实项目里我会继续补上日志上报、鉴权失败跳转、重试策略等能力。但即使只是这个版本，也比到处手写 `fetch` 稳定很多。\n\n## 小结\n\n异步编程不是只会写 `await` 就够了。回调、Promise、async/await 解决的是表达方式；并行控制、取消请求、超时处理、错误归一化解决的是工程可靠性。\n\n我的判断标准很简单：先看任务之间有没有依赖，再决定串行或并行；再看结果是否可能过期，决定是否需要取消；最后统一错误结构，让调用方不用猜。把这几件事做好，JavaScript 异步代码会从“能跑”变成“可维护”。\n"},"randomPosts":[{"title":"React useEffectEvent 实战：我是怎么把一个越改越乱的 effect 拆开的","date":"2026-03-24","excerpt":"从一次聊天室连接逻辑改造出发，聊聊 useEffectEvent 真正适合解决什么问题，以及它为什么比硬塞依赖或 useRef 更自然。","tags":["React","组件与状态"],"slug":"react-useeffectevent","category":"2026","likeCount":7,"postId":"3c292465-d9eb-4436-8f15-34b233a184f7","viewCount":1027,"readTime":null,"coverImage":null},{"title":"DocumentFragment：批量插入 DOM 时的一个实用技巧","date":"2018-08-28","excerpt":"介绍 createDocumentFragment 的作用，补充批量插入、replaceChildren、模板克隆和事件委托等原生 DOM 操作实践。","tags":["前端基础","JavaScript","性能与缓存"],"slug":"frontend-document-fragment-2018","category":"2018","likeCount":7,"postId":"5f701a9d-cffb-d19a-76ab-a5dd456a8057","viewCount":1105,"readTime":null,"coverImage":null},{"title":"TypeScript 5.3 值得关注的几个变化","date":"2023-09-15","excerpt":"整理 TypeScript 5.3 计划中的导入属性、类型收窄和运行时互操作等变化，帮助你理解版本演进方向。","tags":["前端基础","TypeScript"],"slug":"frontend-typescript-53-preview-2023","category":"2023","likeCount":6,"postId":"c2b43b41-de0a-31b4-b831-d43d1ab3e5ca","viewCount":1566,"readTime":null,"coverImage":null}],"prevPost":{"title":"Next.js路由传参全攻略","date":"2024-09-07","excerpt":"深入探讨 Next.js 中各种路由传参方法，包括动态路由、查询字符串、Link 组件、App Router 参数读取，以及不同场景下的边界处理。","tags":["React","前端基础"],"slug":"javascript-advanced","category":"2024","likeCount":7,"postId":"90a82100-3dee-7a79-0ace-e30e41291732","viewCount":2115,"readTime":null,"coverImage":null},"nextPost":{"title":"前端监控从 Web Vitals 开始：别只在用户投诉后才看性能","date":"2024-08-09","excerpt":"围绕 Web Vitals、错误采集、接口耗时和用户行为上下文，梳理前端监控体系的落地方式和边界。","tags":["前端基础","性能与缓存","构建与模块化"],"slug":"frontend-observability-web-vitals-2024","category":"2024","likeCount":28,"postId":"a4472209-40c8-7d31-0d01-f7a0a89cea1f","viewCount":838,"readTime":null,"coverImage":null}},"__N_SSG":true}