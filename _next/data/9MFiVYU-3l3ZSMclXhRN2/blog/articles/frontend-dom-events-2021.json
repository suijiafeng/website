{"pageProps":{"postData":{"title":"DOM 事件机制入门：捕获、冒泡和事件委托到底该怎么理解","date":"2021-11-21","excerpt":"事件传播、事件委托和默认行为这些概念，单看例子不难，难的是在弹窗、表单和列表里别互相误伤。","tags":["前端基础"],"slug":"frontend-dom-events-2021","category":"2021","likeCount":7,"postId":"0c1209e1-551f-35a4-a489-a3c18ab70bb4","viewCount":1847,"readTime":"12分钟","coverImage":null,"content":"\n前端开发里，事件是最基础也最常见的能力之一。\n\n点击按钮、输入内容、提交表单、滚动页面，这些交互的背后其实都离不开事件机制。\n\n实际开发中大家会直接使用事件，但一旦遇到下面这些情况，就会发现自己其实并没有真正理解它：\n\n- 为什么点击子元素时父元素的事件也触发了\n- 为什么有些事件监听加在父级上也能生效\n- 为什么 `stopPropagation` 有时能解决问题，有时又不够\n\n这些问题都和 DOM 事件机制有关。\n\n我自己刚工作那两年，对事件的理解就停留在\"绑 click 就能用\"的程度。直到有一次做一个表格组件，每行有编辑、删除、查看三个按钮，整行又能点开详情，结果点删除的时候详情弹窗也一起弹出来了。当时我对着代码看了半天，怎么也想不通\"我明明只给删除按钮绑了事件\"，最后才意识到问题出在冒泡上。从那以后我才认真去补这块的知识。后来发现，事件这套机制看着基础，真正绕不开的坑其实不少，下面这些都是我实际踩过或者帮同事排查过的。\n\n## 什么是事件传播\n\n当你点击一个页面元素时，浏览器并不是只把这次点击交给当前元素处理。\n\n它会沿着 DOM 树进行一轮传播，这个传播过程通常分成三个阶段：\n\n1. 捕获阶段\n2. 目标阶段\n3. 冒泡阶段\n\n简单理解就是，事件会先从外往里走，再从里往外走。\n\n假设页面结构是这样：\n\n```html\n<div id=\"app\">\n  <button id=\"save\">保存</button>\n</div>\n```\n\n当点击按钮时，事件路径大致会经过 `window`、`document`、`html`、`body`、`#app`、`#save`，然后再沿着路径往回走。捕获阶段是从外到内，冒泡阶段是从内到外。\n\n大多数时候我们写的监听器都在冒泡阶段执行，因为 `addEventListener` 默认的 `capture` 是 `false`。\n\n这里有个细节我一开始没注意：目标元素本身上面的监听器，无论你写 `capture: true` 还是默认的冒泡，都会在\"目标阶段\"被触发，区别只是同一个元素上多个监听器之间的相对顺序。也就是说捕获和冒泡的差异主要体现在祖先元素上，目标节点是个特殊点。这点在调试事件顺序时容易让人困惑，我后来干脆写了个小 demo 把整条路径打印出来，看一遍就清楚了：\n\n```javascript\n['app', 'save'].forEach((id) => {\n  const el = document.getElementById(id);\n  el.addEventListener('click', () => console.log(`${id} 冒泡`), false);\n  el.addEventListener('click', () => console.log(`${id} 捕获`), true);\n});\n```\n\n点一下保存按钮，控制台输出顺序是：`app 捕获` → `save 冒泡` → `save 捕获` → `app 冒泡`。可以看到目标节点 `save` 上两个监听器是按**注册顺序**走的（上面先注册了 `冒泡` 那个、再注册 `捕获`，所以 `save 冒泡` 先打印），跟捕获/冒泡标志无关；而祖先 `app` 则严格分两头——捕获在最前、冒泡在最后。这个输出我贴在工位便签上看了很久才真正记住。\n\n## 冒泡为什么这么重要\n\n在日常开发中，最常遇到的通常是冒泡。\n\n例如：\n\n```javascript\nparent.addEventListener('click', () => {\n  console.log('parent clicked');\n});\n\nchild.addEventListener('click', () => {\n  console.log('child clicked');\n});\n```\n\n当点击子元素时，通常会先触发子元素，再触发父元素。这就是冒泡。\n\n理解这一点很重要，因为很多交互问题其实不是“事件绑错了”，而是没有意识到事件还会继续向上传播。\n\n一个很典型的场景是卡片点击：\n\n```html\n<article class=\"card\">\n  <h3>文章标题</h3>\n  <button type=\"button\" class=\"delete\">删除</button>\n</article>\n```\n\n```javascript\ncard.addEventListener('click', () => {\n  openDetail();\n});\n\ndeleteButton.addEventListener('click', (event) => {\n  event.stopPropagation();\n  deleteArticle();\n});\n```\n\n点击删除按钮时，如果不阻止传播，卡片的 `openDetail` 也会执行。这不是浏览器异常，而是事件冒泡的正常结果。\n\n我前面提到的那个表格组件，本质就是这个问题。当时我的第一反应是给删除按钮也加 `stopPropagation`，确实管用，但后来按钮越来越多（编辑、复制、更多菜单……），每个都要记得写一句，漏一个就出 bug。这种\"靠每个子元素自觉\"的方案在团队里几乎一定会坏掉，因为新来的同事根本不知道这条隐形规则。后面我会讲到我更倾向的做法，是把判断收到父级去。\n\n还有个容易忽略的点：冒泡不只发生在 click 上。`focus` 和 `blur` 这两个事件本身是不冒泡的，但浏览器提供了对应的 `focusin` / `focusout`，它们是冒泡的。我有次想用事件委托统一处理表单里所有输入框的失焦校验，绑 `blur` 怎么都不触发，查了文档才知道得用 `focusout`。这种\"看着该冒泡却不冒泡\"的事件还有 `mouseenter`、`mouseleave`，对应能冒泡的是 `mouseover`、`mouseout`，做悬浮交互时要分清楚。\n\n## 捕获阶段什么时候会用到\n\n捕获阶段在业务开发里不像冒泡那样常见，但它确实存在。\n\n如果你通过第三个参数或配置项启用捕获监听，事件会先在外层元素触发，再到目标元素。\n\n```javascript\nparent.addEventListener(\n  'click',\n  () => {\n    console.log('capture');\n  },\n  true\n);\n```\n\n大多数场景下你不一定主动用它，但知道它的存在，能帮助你更好理解事件执行顺序。\n\n捕获阶段适合少数需要更早拦截事件的场景，比如全局点击统计、弹窗外点击判断、某些基础组件的统一拦截。不过它不应该被滥用，因为捕获监听比普通冒泡监听更不容易被业务代码察觉。\n\n我真正用到捕获，是在做一个全局埋点的时候。需求是无论页面上哪个带 `data-track` 的元素被点了，都要上报一次，而且要保证业务自己的 `stopPropagation` 不会把埋点吞掉。如果埋点绑在冒泡阶段，业务代码里随便一个 `stopPropagation` 就会让上报丢失。把埋点放在 `document` 的捕获阶段就稳了，因为捕获是从最外层先走，业务的冒泡阻断发生在它之后：\n\n```javascript\ndocument.addEventListener(\n  'click',\n  (event) => {\n    const node = event.target.closest('[data-track]');\n    if (node) {\n      report(node.dataset.track);\n    }\n  },\n  { capture: true }\n);\n```\n\n这个方案上线后基本没再丢过点击事件。代价是捕获监听确实\"隐身\"，后来有同事排查\"为什么每次点击都多一个网络请求\"，找了好久才定位到这段代码，所以我现在写这类全局捕获都会加明显的注释。\n\n现在更推荐用配置对象写法：\n\n```javascript\nparent.addEventListener(\n  'click',\n  () => {\n    console.log('capture');\n  },\n  {\n    capture: true,\n  }\n);\n```\n\n配置对象可读性更好，后续也方便加 `once`、`passive` 等选项。\n\n## 什么是事件委托\n\n事件委托是前端里非常实用的一种技巧。\n\n它的思路是：不把事件直接绑定在每个子元素上，而是绑定在它们的父元素上，再通过事件对象判断具体是哪个子元素触发的。\n\n例如列表：\n\n```javascript\nlist.addEventListener('click', (event) => {\n  if (event.target.matches('li')) {\n    console.log('clicked item');\n  }\n});\n```\n\n这样做的好处很明显：\n\n- 减少重复绑定\n- 动态新增元素也能响应\n- 结构更集中\n\n不过真实业务里，点击目标不一定正好是 `li`。用户可能点到列表项里的文字、图标、按钮，这时 `event.target.matches('li')` 就不一定成立。\n\n更稳的方式通常是使用 `closest`：\n\n```javascript\nlist.addEventListener('click', (event) => {\n  const item = event.target.closest('[data-id]');\n\n  if (!item || !list.contains(item)) {\n    return;\n  }\n\n  openDetail(item.dataset.id);\n});\n```\n\n这里有两个边界：\n\n- 用 `closest` 向上找真正的列表项\n- 用 `contains` 确认找到的元素仍然属于当前列表\n\n第二个判断看起来多余，但在嵌套列表、弹层、复杂组件里很有用，可以避免误处理外部元素。\n\n事件委托也不是所有场景都适合。如果每个子元素的交互差异很大，或者需要独立清理资源，直接绑定在子组件上反而更清楚。\n\n我吃过一次委托相关的性能亏，反而让我对它印象更深。那是一个上千行的长列表，最初每行都单独绑了 click、mouseenter、mouseleave，结果滚动起来很卡，DevTools 里能看到内存里挂着几千个监听器。改成在容器上委托之后，监听器从几千个降到三个，滚动流畅了不少，初次渲染也快了。这是事件委托除了\"动态元素也能响应\"之外，另一个很实在的好处：监听器数量从 O(n) 降到 O(1)。\n\n委托里还有个坑是关于 `event.target` 的取值。如果列表项里有 SVG 图标，点到 SVG 内部的 `path` 时，`event.target` 会是那个 `path` 元素，而 SVG 元素的 `className` 是 `SVGAnimatedString` 而不是普通字符串，用 `classList` 判断有时会出意外。所以我现在做委托一律走 `closest`，让它自己往上找到带 `data-id` 的业务节点，不去纠结到底点中了哪个子标签，省心很多。\n\n## stopPropagation 是做什么的\n\n如果你不希望事件继续向上传播，可以调用：\n\n```javascript\nevent.stopPropagation();\n```\n\n这通常用于阻止父级事件误触发。\n\n例如一个卡片整体可点击，卡片内部又有删除按钮，这时删除按钮点击后如果不阻止传播，卡片点击逻辑也可能被触发。\n\n但 `stopPropagation` 不是万能药。它只阻止事件继续向父级传播，不会阻止当前元素上其他监听器执行。如果你需要阻止同一元素上后续监听器执行，才会用到 `stopImmediatePropagation`。\n\n日常业务里我会谨慎使用这类方法。很多时候更好的方式是让父级判断点击来源，而不是让子级强行打断所有传播。\n\n```javascript\ncard.addEventListener('click', (event) => {\n  if (event.target.closest('button')) {\n    return;\n  }\n\n  openDetail();\n});\n```\n\n这种写法把规则放在父级：如果点到按钮，就不打开详情。相比在每个内部按钮都写 `stopPropagation`，更容易维护。\n\n回到我那个表格组件，最后我就是用这个思路重构的。父级只认一条规则——点到任意带 `data-action` 的元素就交给对应处理函数，否则才算\"点了整行\"，删除复制编辑这些按钮再也不用各自去阻断传播。代码集中在一处，新增按钮只要加个 `data-action` 属性就行，再没出过误触详情的问题。\n\n另外提醒一个我见过别人踩的坑：`stopPropagation` 在 React 里和原生不完全是一回事。React 的合成事件是统一委托在根节点上的，你在某个子组件里对合成事件调 `e.stopPropagation()`，它只能拦住 React 体系内的其他合成监听器，拦不住你另外用 `addEventListener` 绑在 `document` 上的原生监听器。要拦那种得用 `e.nativeEvent.stopImmediatePropagation()`。这种混用原生和框架事件的场景虽然不多，但一旦碰上排查起来特别费劲。\n\n## 事件对象里最常用的几个属性\n\n日常开发中比较常见的有：\n\n- `target`\n- `currentTarget`\n- `preventDefault`\n\n其中最容易混的是：\n\n- `event.target` 表示实际触发事件的元素\n- `event.currentTarget` 表示当前正在处理事件的元素\n\n理解这两个区别，对写事件委托特别重要。\n\n举个例子：\n\n```javascript\nlist.addEventListener('click', (event) => {\n  console.log(event.target);\n  console.log(event.currentTarget);\n});\n```\n\n如果点击的是 `li` 里面的 `span`，`target` 可能是那个 `span`，而 `currentTarget` 始终是绑定监听器的 `list`。写委托时要找真实业务节点，通常看 `target`；要访问监听器所在容器，通常看 `currentTarget`。\n\n可以用 `tagName` 直观对照一下，结构如下：\n\n```html\n<ul id=\"list\"><li><span>文本</span></li></ul>\n```\n\n```js\ndocument.getElementById('list').addEventListener('click', (event) => {\n  console.log(event.target.tagName)        // 点在文本上是 'SPAN'，随点击位置变化\n  console.log(event.currentTarget.tagName) // 始终是 'UL'，恒等于绑定监听器的元素\n})\n```\n\n`target.tagName` 跟着你点的位置变（点到 `span` 就是 `'SPAN'`，注意是大写），`currentTarget.tagName` 在这个 handler 里永远是 `'UL'`。另外 `currentTarget` 只在事件分发期间有效，事件跑完会被置为 `null`，想在 `setTimeout` 里用得先存到局部变量。\n\n还有一个常用属性是 `event.defaultPrevented`。如果前面的逻辑已经调用过 `preventDefault`，后面的逻辑可以根据它决定是否继续处理。\n\n## 默认行为和传播不是一回事\n\n很多人刚开始会把这两个概念混在一起。\n\n例如点击链接时：\n\n- 阻止跳转，要用 `preventDefault`\n- 阻止事件继续向父级传播，要用 `stopPropagation`\n\n它们解决的是两类不同问题。\n\n表单提交也是类似的例子：\n\n```javascript\nform.addEventListener('submit', (event) => {\n  event.preventDefault();\n  submitForm();\n});\n```\n\n这里阻止的是浏览器默认提交行为，不是阻止事件冒泡。如果这个表单外层还有监听器，事件仍然可能继续传播。\n\n反过来，只调用 `stopPropagation` 也不会阻止链接跳转或表单提交。把这两个 API 的职责分清，能减少很多“明明写了阻止，为什么还跳转”的问题。\n\n## once、passive 和清理监听\n\n`addEventListener` 的第三个参数除了 `capture`，还可以设置其他选项。\n\n`once` 表示监听器只执行一次：\n\n```javascript\nbutton.addEventListener(\n  'click',\n  () => {\n    submitOnce();\n  },\n  {\n    once: true,\n  }\n);\n```\n\n这适合一次性确认、引导提示、动画结束等场景。它是真的只触发一次——可以用计数器验证，贴进控制台多点几下：\n\n```js\nlet count = 0\nconst btn = document.createElement('button')\nbtn.addEventListener('click', () => { count++; console.log(count) }, { once: true })\n\nbtn.click()\nbtn.click()\nbtn.click()\nconsole.log('total:', count) // total: 1，第一次触发后监听器已自动解绑\n```\n\n不管点多少次，`count` 都停在 `1`，第一次回调跑完浏览器就把这个监听器移除了，省得自己 `removeEventListener`。\n\n`passive` 常用于滚动相关事件：\n\n```javascript\nwindow.addEventListener('scroll', handleScroll, {\n  passive: true,\n});\n```\n\n它告诉浏览器这个监听器不会调用 `preventDefault`，浏览器可以更放心地优化滚动性能。注意，如果设置了 `passive: true`，再调用 `preventDefault` 不仅不生效，控制台还会打一条警告：`Unable to preventDefault inside passive event listener invocation.`——因为你已经向浏览器承诺过不拦了。\n\n还有个规范层面的细节值得记：从 Chrome 51 / Firefox 49 起，`wheel`、`mousewheel`、以及绑在 `window`/`document`/`document.body` 上的 `touchstart`、`touchmove`，它们的 `passive` 默认值是 `true`（出于滚动性能考虑）。所以如果你真要在这些事件里 `preventDefault`（比如自定义手势、阻止页面滚动），必须显式写 `{ passive: false }`，否则默认的 passive 会让你的 `preventDefault` 静默失效。\n\n还有一个经常被忽略的点是清理监听。组件销毁、弹窗关闭、页面切换时，如果之前绑定过全局事件，应该移除：\n\n```javascript\nfunction handleResize() {\n  updateLayout();\n}\n\nwindow.addEventListener('resize', handleResize);\n\nfunction destroy() {\n  window.removeEventListener('resize', handleResize);\n}\n```\n\n匿名函数不方便移除，所以需要清理的监听器最好使用具名函数或保存函数引用。\n\n我以前最容易忘清理的就是这种绑在 `window` / `document` 上的全局监听。组件本身的 DOM 被销毁时，绑在它内部元素上的监听器会跟着一起回收，不用太操心；但绑在全局对象上的不会，组件反复挂载卸载几次，全局上就堆了一串旧回调，旧回调里又闭包引用着已经销毁的组件状态，既漏内存又会触发奇怪的逻辑。我有次做单页应用切路由，回到上一个页面发现滚动逻辑被执行了两遍，就是上一次的 `scroll` 监听没清掉。\n\n后来我习惯用 `AbortController` 来管理这种成批的监听，销毁时一次性 `abort` 掉，比一个个 `removeEventListener` 清爽很多：\n\n```javascript\nconst controller = new AbortController();\nconst { signal } = controller;\n\nwindow.addEventListener('resize', updateLayout, { signal });\nwindow.addEventListener('scroll', handleScroll, { signal });\ndocument.addEventListener('keydown', onKeydown, { signal });\n\nfunction destroy() {\n  controller.abort(); // 上面三个一起移除\n}\n```\n\n用 `signal` 还有个隐藏好处：连匿名函数也能被统一清掉，不必为了能 remove 而特意提取具名函数。这个写法是我现在新项目里的默认选择。\n\n## 输入、组合输入和边界\n\n输入框事件看起来简单，但中文输入法会带来一个常见边界：组合输入。\n\n如果你在 `input` 事件里实时搜索，用户输入拼音的过程中可能会不断触发请求。可以结合 `compositionstart` 和 `compositionend` 判断用户是否还在组合输入：\n\n```javascript\nlet composing = false;\n\ninput.addEventListener('compositionstart', () => {\n  composing = true;\n});\n\ninput.addEventListener('compositionend', () => {\n  composing = false;\n  search(input.value);\n});\n\ninput.addEventListener('input', () => {\n  if (composing) {\n    return;\n  }\n\n  search(input.value);\n});\n```\n\n这类细节不一定每个页面都需要，但搜索框、自动补全、在线编辑器会经常遇到。\n\n补一个我后来才搞清楚的细节：`compositionend` 和 `input` 的触发顺序在不同浏览器、不同输入法下并不完全一致。早期我只在 `compositionend` 里 `search`，结果在某些环境下，组合结束那一下又补了一次 `input`，导致重复请求。稳妥的做法是把搜索逻辑统一放到 `input` 里，由 `composing` 标志位决定要不要执行，而不是在 `compositionend` 里直接发请求。再叠加一层防抖，搜索框的请求次数就基本可控了：\n\n```javascript\nlet composing = false;\nlet timer = null;\n\nfunction debouncedSearch(value) {\n  clearTimeout(timer);\n  timer = setTimeout(() => search(value), 300);\n}\n\ninput.addEventListener('compositionstart', () => {\n  composing = true;\n});\n\ninput.addEventListener('compositionend', () => {\n  composing = false;\n  debouncedSearch(input.value); // 组合结束后补发一次\n});\n\ninput.addEventListener('input', () => {\n  if (composing) return;\n  debouncedSearch(input.value);\n});\n```\n\n\nDOM 事件机制最重要的，不是死记捕获和冒泡的定义，而是理解事件在页面中的传播路径。\n\n只要把事件传播、事件委托、默认行为和当前目标这几个概念理顺，很多日常交互问题都会更容易分析。事件机制看起来基础，但它几乎贯穿了整个前端交互开发。\n\n我现在写事件逻辑时会习惯性问几个问题：这个监听器应该绑在哪一层？是否需要委托？是否会误触发父级逻辑？是否需要阻止默认行为？组件销毁时是否要清理？这些问题想清楚，事件代码通常就不会太乱。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":29,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":356,"readTime":null,"coverImage":null},{"title":"TCP/IP 与前端性能：从数据包到首次渲染的底层逻辑","date":"2026-05-04","excerpt":"从一次首屏慢但 JS 并不大的排查经历出发，把 TTFB、FP、数据包、IP、TCP 握手和前端性能串起来看。","tags":["前端基础","性能与缓存"],"slug":"tcp-ip-frontend-performance-2026","category":"2026","likeCount":53,"postId":"2d4f712b-1d52-a901-e0e2-cef9d9af04aa","viewCount":361,"readTime":null,"coverImage":null},{"title":"Zod 4 实践：把校验、类型和接口契约收在一处","date":"2026-05-03","excerpt":"Zod 4 的性能改进和 API 变化落到真实项目里：表单校验、接口出入参解析、和 TypeScript 类型互推，以及 schema 复用时容易踩的坑。","tags":["前端基础","TypeScript","构建与模块化"],"slug":"zod-4-schema-validation-2026","category":"2026","likeCount":52,"postId":"4dba5dc0-897d-90b6-5c0d-6f36518662e8","viewCount":275,"readTime":null,"coverImage":null}],"prevPost":{"title":"前端代码评审在看什么：不是挑格式，而是提前拦住后续成本","date":"2021-11-22","excerpt":"从协作和维护角度讨论前端代码评审的重点，说明 review 为什么不只是找语法问题。","tags":["前端基础"],"slug":"frontend-code-review-2021","category":"2021","likeCount":7,"postId":"ee5aa082-328e-2198-376e-f5dd296eba81","viewCount":3907,"readTime":"10分钟","coverImage":null},"nextPost":{"title":"为什么前端要关心可访问性：它不是额外需求，而是页面基本质量","date":"2021-11-03","excerpt":"从键盘操作、语义结构和辅助技术支持出发，讨论前端可访问性为什么值得被当成基础能力。","tags":["前端基础"],"slug":"frontend-accessibility-basics-2021","category":"2021","likeCount":7,"postId":"dd5dcb9b-2bc2-c786-c7ec-6a4a56dff023","viewCount":1356,"readTime":"10分钟","coverImage":null}},"__N_SSG":true}