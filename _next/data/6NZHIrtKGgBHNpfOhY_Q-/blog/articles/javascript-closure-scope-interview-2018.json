{"pageProps":{"postData":{"title":"JavaScript 闭包和作用域：面试常问，项目里也经常踩坑","date":"2018-01-17","excerpt":"从日常项目和面试题出发，整理 JavaScript 作用域、闭包、循环变量、模块封装和内存占用这些老问题。","tags":["JavaScript","前端基础"],"slug":"javascript-closure-scope-interview-2018","category":"2018","likeCount":8,"postId":"eb97bec5-8d23-7302-d18f-180a248f3e54","viewCount":1385,"readTime":"8分钟","coverImage":null,"content":"\n闭包是前端面试里绕不开的问题。\n\n刚开始听到这个词时，很容易觉得它像一个专门用来考人的概念。可写项目久了会发现，闭包并不只存在于面试题里。事件回调、定时器、模块封装、私有变量、异步请求，很多地方都在用它。\n\n只是我们平时不一定会把它叫作闭包。\n\n我自己就是这么过来的。面试被问闭包，背得滚瓜烂熟，什么“函数加上其引用的词法环境”，可真到项目里写 `setTimeout` 里引用外层变量、给每个列表项绑事件、用立即执行函数把工具方法包起来——这些天天在写的东西，当时根本没意识到就是闭包。后来踩了几次坑、又面了几次试，才把“课本定义”和“手上的代码”对上号。所以这篇我想尽量从实际写代码的角度来讲，而不是背概念。\n\n## 先从作用域说起\n\nJavaScript 里函数会创建自己的作用域：\n\n```js\nfunction outer() {\n  var name = 'Tom'\n\n  function inner() {\n    console.log(name)\n  }\n\n  inner()\n}\n\nouter()\n```\n\n`inner` 可以访问 `outer` 里的 `name`。这是词法作用域决定的：函数能访问它定义时所在作用域里的变量。\n\n注意是“定义时”，不是“调用时”。\n\n```js\nvar name = 'global'\n\nfunction printName() {\n  console.log(name)\n}\n\nfunction run() {\n  var name = 'local'\n  printName()\n}\n\nrun()\n```\n\n输出是 `global`。因为 `printName` 定义在全局作用域，它访问的是全局的 `name`。\n\n这类题在面试里很常见，目的不是为难人，而是确认你知道 JavaScript 作用域不是动态绑定的。\n\n这里有个特别容易混淆的地方，我专门拎出来说：作用域是“定义时”决定的（词法/静态作用域），但 `this` 是“调用时”决定的，这俩是两套完全不同的规则。新人最容易在这栽跟头——把变量查找的规律套到 `this` 上，或者反过来。比如：\n\n```js\nvar obj = {\n  name: '对象',\n  greet: function () {\n    console.log(this.name)\n  }\n}\n\nvar fn = obj.greet\nfn() // this 是 window/undefined，不是 obj\n```\n\n`fn` 里能访问哪些变量，看的是 `greet` 定义在哪；而 `this` 指向谁，看的是 `fn()` 这一刻怎么调的。我面试别人也爱问这组对比，能把“作用域看定义、this 看调用”讲清楚的，基础一般都比较扎实。\n\n## 什么是闭包\n\n如果一个函数离开了它定义时的作用域，仍然能访问那个作用域里的变量，就形成了闭包。\n\n```js\nfunction createCounter() {\n  var count = 0\n\n  return function() {\n    count += 1\n    return count\n  }\n}\n\nvar counter = createCounter()\n\nconsole.log(counter()) // 1\nconsole.log(counter()) // 2\nconsole.log(counter()) // 3\n```\n\n`createCounter` 执行完了，但 `count` 没有被释放，因为返回的函数还在引用它。\n\n这就是闭包最常见的用法：保留一份外部访问不到、但内部函数可以继续使用的状态。\n\n我特别喜欢用闭包做防抖和节流，这是项目里最实在的用法之一。比如搜索框输入联想，不能每敲一个字就发请求：\n\n```js\nfunction debounce(fn, delay) {\n  var timer = null\n  return function () {\n    var context = this\n    var args = arguments\n    clearTimeout(timer)\n    timer = setTimeout(function () {\n      fn.apply(context, args)\n    }, delay)\n  }\n}\n\ninput.oninput = debounce(function () {\n  search(this.value)\n}, 300)\n```\n\n这里的 `timer` 就是靠闭包保存下来的。每次返回的那个函数都还引用着同一个 `timer`，所以它能记住上一次的定时器并清掉。如果没有闭包，`timer` 要么写成全局变量（多个防抖互相打架），要么每次调用都重置（根本起不到防抖效果）。我早期不懂这个，真就把 timer 提到外面当全局变量，结果页面上两个搜索框一起用的时候互相干扰，查了好久才明白问题出在共享了同一个 timer。闭包在这里的价值就是给每个防抖函数一份独立、私密的状态。\n\n## 私有变量\n\n在没有 class 私有字段的写法里，闭包经常用来模拟私有变量：\n\n```js\nfunction createUser(name) {\n  var score = 0\n\n  return {\n    getName: function() {\n      return name\n    },\n    addScore: function(value) {\n      score += value\n    },\n    getScore: function() {\n      return score\n    }\n  }\n}\n\nvar user = createUser('Alice')\nuser.addScore(10)\n\nconsole.log(user.getScore()) // 10\nconsole.log(user.score) // undefined\n```\n\n外部不能直接改 `score`，只能通过方法访问。\n\n这种写法在一些组件库、工具模块里很常见。它比全局变量安全，也让状态边界更清楚。\n\n需要说清楚的是，闭包提供的“私有”是逻辑上的封装，不是安全意义上的加密。`score` 外部确实拿不到引用，但它存在于内存里，懂行的人通过调试器照样能看到。所以别指望用闭包藏什么敏感数据，它的价值在于约束“正常使用的边界”，避免别人无意中改坏你的内部状态，而不是防黑客。这点我以前没想清楚，还真有同事问过“闭包里的变量是不是就安全了”，得分清楚封装和加密是两回事。\n\n另外提一个 2018 年的现实：ES2015 的 class 虽然有了，但真正的私有字段（`#field` 语法）还远没普及，浏览器支持也不齐。所以这种闭包模拟私有变量的写法，在当时仍然是工程里最靠谱的选择，而不是“过时写法”。\n\n## 循环里的经典坑\n\n这道题太常见了：\n\n```js\nfor (var i = 0; i < 3; i++) {\n  setTimeout(function() {\n    console.log(i)\n  }, 0)\n}\n```\n\n输出是：\n\n```text\n3\n3\n3\n```\n\n因为 `var` 是函数作用域，三个回调引用的是同一个 `i`。等定时器执行时，循环已经结束，`i` 变成了 3。\n\n老写法通常用立即执行函数解决：\n\n```js\nfor (var i = 0; i < 3; i++) {\n  ;(function(index) {\n    setTimeout(function() {\n      console.log(index)\n    }, 0)\n  })(i)\n}\n```\n\n如果项目里已经能用 `let`，就简单很多：\n\n```js\nfor (let i = 0; i < 3; i++) {\n  setTimeout(function() {\n    console.log(i)\n  }, 0)\n}\n```\n\n`let` 会为每次循环创建新的块级作用域。\n\n`let` 这里的行为其实挺有意思，它不只是“块级作用域”这么一句话能概括的。在 `for (let i ...)` 里，规范规定每一轮迭代都会拷贝出一个新的 `i` 绑定，下一轮的 `i` 是在上一轮的值基础上重新创建的。所以三个 `setTimeout` 回调闭包捕获到的，是三个不同的 `i`，分别是 0、1、2。这也是为什么换成 `let` 就“神奇地”对了——本质上它帮你做了立即执行函数手动做的那件事：每轮一个独立作用域。\n\n面试时如果能讲到“每次迭代新建绑定”这个层面，而不只是说一句“let 是块级的”，区分度会很明显。我面过不少人，知道答案是 0 1 2，但说不清为什么 `let` 能做到，往往就是只背了结论没理解机制。\n\n补一个延伸题，也常被追问——如果是用 `forEach` 呢？\n\n```js\n[10, 20, 30].forEach(function (n, i) {\n  setTimeout(function () {\n    console.log(i, n)\n  }, 0)\n})\n```\n\n这个输出是 `0 10`、`1 20`、`2 30`，完全正常。因为 `forEach` 的回调本身就是个函数，每次调用都有自己独立的 `i` 和 `n`，天然就不存在共享变量的问题。理解了这点，就明白经典坑的根源不是“循环”，而是“多个闭包共享了同一个用 `var` 声明的变量”。\n\n## 事件回调里的闭包\n\n闭包在事件处理中非常常见：\n\n```js\nfunction bindRemove(button, id) {\n  button.onclick = function() {\n    removeItem(id)\n  }\n}\n```\n\n点击按钮时，回调还能拿到当初传入的 `id`。这就是闭包。\n\n如果列表很长，并且每一项都绑定独立回调，要注意内存和解绑。页面销毁时，事件引用还在，可能导致 DOM 或数据无法释放。\n\n这也是为什么事件委托很重要。不是所有按钮都需要单独绑定。\n\n事件委托的思路是把监听挂到父容器上，利用事件冒泡统一处理，需要的数据从 DOM 上读，而不是为每一项闭包捕获一份：\n\n```js\nlist.addEventListener('click', function (e) {\n  var item = e.target.closest('[data-id]')\n  if (!item) return\n  removeItem(item.dataset.id)\n})\n```\n\n这样不管列表有十项还是一万项，都只有一个监听函数，没有成百上千个各自捕获 `id` 的闭包，内存友好得多，列表动态增删也不用反复绑定解绑。我维护过一个长列表表格，原来给每行每个操作按钮都单独 `onclick` 绑了闭包，滚动加载几千行之后页面内存蹭蹭涨，改成事件委托后立竿见影。\n\n如果确实有场景必须逐个绑定，那一定要记得在元素移除或组件销毁时 `removeEventListener` 解绑。闭包会牢牢拽住它引用的东西，只要监听还挂在那、回调闭包没释放，它捕获的变量、甚至关联的 DOM 节点都没法被垃圾回收，这就是很多“DOM 都删了内存还不降”的内存泄漏的来源。\n\n## 模块封装\n\n以前没有普遍使用 ES Module 时，闭包经常配合立即执行函数做模块：\n\n```js\nvar request = (function() {\n  var baseURL = '/api'\n\n  function get(url) {\n    return ajax(baseURL + url)\n  }\n\n  return {\n    get: get\n  }\n})()\n```\n\n`baseURL` 不暴露到全局，但 `get` 可以使用它。\n\n这种写法现在看起来有点老，但在很多老项目里很常见。理解它，接手老代码会轻松很多。\n\n## 闭包不是越多越好\n\n闭包会让变量生命周期变长。用得好是封装，用不好就是内存占用和难排查状态。\n\n比如：\n\n```js\nfunction bindHugeData() {\n  var data = new Array(100000).fill('*')\n\n  return function() {\n    console.log(data.length)\n  }\n}\n```\n\n只要返回函数还在，`data` 就不会释放。这里 `data` 是十万个元素的数组，但返回的函数其实只用到了 `data.length`，完全没必要把整个数组挂着。这种时候可以提前把需要的值取出来，让大数组能被回收：\n\n```js\nfunction bindHugeData() {\n  var data = new Array(100000).fill('*')\n  var length = data.length // 只留下真正需要的\n  return function () {\n    console.log(length)\n  }\n  // data 在这之后没人引用，可以被回收\n}\n```\n\n判断有没有这类问题，可以用 Chrome DevTools 的 Memory 面板拍 heap snapshot，搜一下那些本该释放的大对象还在不在。我排查过一个内存缓慢上涨的页面，最后定位到就是一个闭包顺手把整个大配置对象捕获进去了，而它只需要里面的一个字段。把那个字段单独取出来之后，内存曲线立马平了。\n\n项目里不要为了“高级”到处写闭包。它应该用在确实需要保存状态、隐藏变量、延迟执行的地方。我的经验是，闭包是顺其自然形成的工具，写防抖、写计数器、做模块封装时它该出现就出现；而不是一个值得专门去“用”的炫技对象。哪天你写代码时压根没意识到自己在用闭包，但用得恰到好处，那才算真懂了。\n\n## 小结\n\n闭包的核心并不复杂：函数记住了自己定义时的作用域。\n\n面试问闭包，通常会连着问作用域、循环变量、定时器、私有变量和内存。项目里用闭包，也要记住它的代价：变量不会因为外层函数执行结束就一定释放。\n\n理解闭包以后，再看事件回调、模块封装和异步代码，会清楚很多。\n"},"recommendedPosts":[{"title":"用 Biome 替掉 ESLint + Prettier：一次工具链瘦身的得与失","date":"2026-04-18","excerpt":"把项目的 lint 和格式化从 ESLint+Prettier 换成 Biome 的过程：速度提升有多明显、规则迁移缺了什么、以及哪些场景我还是留着 ESLint。","tags":["前端基础","构建与模块化","JavaScript"],"slug":"biome-toolchain-2026","category":"2026","likeCount":6,"postId":"37e39142-8bf2-d042-6270-2228c894603c","viewCount":1078,"readTime":null,"coverImage":null},{"title":"TC39 Signals 提案与框架里的信号：响应式终于要进语言层了吗","date":"2026-03-07","excerpt":"从 Vue ref、Solid signal 到 TC39 的 Signals 提案，聊聊信号这套响应式模型解决了什么、polyfill 怎么用、以及它和现有框架的关系。","tags":["JavaScript","前端基础","组件与状态"],"slug":"js-signals-proposal-2026","category":"2026","likeCount":59,"postId":"10625fc7-1fb0-e156-eaf9-5cace589f74e","viewCount":689,"readTime":null,"coverImage":null},{"title":"少装一个库之后：我重新补了一遍 JavaScript 原生能力","date":"2025-12-30","excerpt":"记录我在维护后台项目时重新审视 JavaScript 原生 API 的过程：哪些地方可以少装库，哪些地方仍然要尊重兼容性和团队成本。","tags":["前端基础","JavaScript"],"slug":"frontend-js-native-features-2025","category":"2025","likeCount":17,"postId":"8d843f2f-478d-b940-dc91-1d1109e067f2","viewCount":521,"readTime":null,"coverImage":null}],"prevPost":{"title":"webpack 4 迁移笔记：从配置堆里把构建速度找回来","date":"2018-01-28","excerpt":"记录 webpack 4 迁移中的常见变化，包括 mode、optimization、splitChunks、loader 版本和构建排查思路。","tags":["构建与模块化"],"slug":"webpack4-migration-notes-2018","category":"2018","likeCount":5,"postId":"d09a3309-cab2-a27e-9016-85f7795d51d4","viewCount":4681,"readTime":null,"coverImage":null},"nextPost":{"title":"Vue CLI 3 项目体验：脚手架变轻以后，配置反而要更清楚","date":"2018-01-12","excerpt":"从实际项目角度记录 Vue CLI 3 的使用体验，包括插件化配置、vue.config.js、环境变量、打包目录和团队协作中的取舍。","tags":["Vue","构建与模块化"],"slug":"vue-cli3-project-experience-2018","category":"2018","likeCount":6,"postId":"1ed0cb12-5492-17ff-96ff-c7868aec68e4","viewCount":1801,"readTime":null,"coverImage":null}},"__N_SSG":true}