{"pageProps":{"postData":{"title":"从 Ajax 到 Fetch：前端请求方式的演进到底带来了什么","date":"2021-10-06","excerpt":"回顾前端请求方式从 Ajax 到 Fetch 的变化，并讨论它们在开发体验和代码组织上的差异。","tags":["前端基础"],"slug":"frontend-ajax-fetch-2021","category":"2021","likeCount":8,"postId":"dc8f68fd-bdf8-7126-0128-5b73c677b185","viewCount":2745,"readTime":"4分钟","coverImage":null,"content":"\n前端开发离不开请求。\n\n页面展示数据、提交表单、刷新列表、加载详情，这些动作几乎都要和服务端通信。\n\n如果把时间往前拉，大家最熟悉的方式可能是 Ajax；再往后，Fetch 逐渐成为更常见的写法。它们都能完成请求，但带来的开发体验并不完全一样。\n\n## Ajax 解决了什么问题\n\nAjax 最重要的价值，是让页面不必整页刷新也能和服务端交换数据。\n\n这在早期前端开发里是一次很大的改变，因为它让页面开始具备更强的交互性和动态性。\n\n大家最熟悉的实现通常是 `XMLHttpRequest`。\n\n```javascript\nconst xhr = new XMLHttpRequest();\nxhr.open('GET', '/api/list');\nxhr.onreadystatechange = function () {\n  if (xhr.readyState === 4 && xhr.status === 200) {\n    console.log(xhr.responseText);\n  }\n};\nxhr.send();\n```\n\n这个写法并不是不能用，但确实比较底层。\n\n它的底层感主要体现在几个地方：\n\n- 需要关心 `readyState`\n- 成功和失败判断分散\n- 响应内容需要自己解析\n- 超时、取消、错误提示都要额外处理\n\n如果只是一个简单请求，这些代码还能接受。一旦页面里有十几个接口，每个地方都写一遍状态判断，维护成本就会很快上来。\n\n早期很多项目会基于 `XMLHttpRequest` 再封装一层，目的就是把这些重复逻辑收起来：\n\n```javascript\nfunction ajaxGet(url, onSuccess, onError) {\n  const xhr = new XMLHttpRequest();\n  xhr.open('GET', url);\n\n  xhr.onreadystatechange = function () {\n    if (xhr.readyState !== 4) {\n      return;\n    }\n\n    if (xhr.status >= 200 && xhr.status < 300) {\n      onSuccess(xhr.responseText);\n      return;\n    }\n\n    onError(`请求失败：${xhr.status}`);\n  };\n\n  xhr.onerror = function () {\n    onError('网络异常，请稍后重试');\n  };\n\n  xhr.send();\n}\n```\n\n这已经比散写好很多，但回调形式在复杂流程里仍然容易变乱。\n\n## Fetch 为什么更容易被接受\n\nFetch 更贴近现代 JavaScript 的编码方式。\n\n它的 API 更简洁，也更容易和 Promise、`async/await` 组合。\n\n```javascript\nasync function loadData() {\n  const response = await fetch('/api/list');\n  const data = await response.json();\n  console.log(data);\n}\n```\n\n相比 `XMLHttpRequest`，这种写法的优势主要体现在：\n\n- 更容易阅读\n- 更容易封装\n- 更适合现代异步流程\n\n尤其是和 `async/await` 组合后，请求流程可以写得更接近同步代码：\n\n```javascript\nasync function renderList() {\n  try {\n    const response = await fetch('/api/list');\n    const list = await response.json();\n\n    render(list);\n  } catch (error) {\n    showToast('列表加载失败');\n  }\n}\n```\n\n这种写法最大的收益不是少写几行，而是流程更直。加载、解析、渲染、错误提示都能按执行顺序展开。\n\n## 两者的差异不只是“新旧”\n\n不少人会简单理解成“Fetch 就是更现代的 Ajax”，但更准确地说，它们代表了两种不同层级的开发体验。\n\n`XMLHttpRequest` 更像底层接口，你需要自己处理很多状态变化。\n\nFetch 则更像一个围绕请求流程设计得更现代的抽象。\n\n## Fetch 使用时也有需要注意的地方\n\n虽然 Fetch 更简洁，但它也有一些容易踩坑的点。\n\n例如很多人第一次用时会以为：\n\n- 请求失败就会进入 `catch`\n\n实际上不是所有 HTTP 错误状态都会自动抛异常。比如返回 404、500，Fetch 依然可能算“成功收到响应”，只是 `response.ok` 为 `false`。\n\n所以通常还要自己判断：\n\n```javascript\nconst response = await fetch('/api/list');\n\nif (!response.ok) {\n  throw new Error('request failed');\n}\n```\n\n另外，`response.json()` 本身也可能失败。比如服务端返回了空内容、HTML 错误页，或者 JSON 格式不合法，都会导致解析异常。所以一个健壮的请求封装不能只检查 `response.ok`。\n\n更实际的写法是把 HTTP 错误、解析错误、网络错误统一成一种业务层能理解的结构：\n\n```javascript\nasync function parseJson(response) {\n  try {\n    return await response.json();\n  } catch (error) {\n    return null;\n  }\n}\n\nasync function request(url, options = {}) {\n  try {\n    const response = await fetch(url, options);\n    const data = await parseJson(response);\n\n    if (!response.ok) {\n      return {\n        ok: false,\n        status: response.status,\n        message: data?.message || `请求失败：${response.status}`,\n      };\n    }\n\n    return {\n      ok: true,\n      data,\n    };\n  } catch (error) {\n    return {\n      ok: false,\n      status: 0,\n      message: '网络异常，请稍后重试',\n    };\n  }\n}\n```\n\n这样页面层不需要知道底层到底是 500、JSON 解析失败，还是网络断开。它只要根据 `ok` 判断下一步。\n\n## 请求封装的重要性\n\n无论使用 Ajax 还是 Fetch，真正影响项目维护体验的，往往不是底层 API 本身，而是你有没有做好请求层封装。\n\n例如：\n\n- base URL\n- 通用 headers\n- 错误处理\n- token 注入\n- 超时控制\n\n如果这些都散落在页面里，请求方式再现代，项目后期也会很难维护。\n\n我比较推荐的原则是：页面只表达业务意图，请求层负责协议细节。\n\n页面里最好是这样：\n\n```javascript\nconst result = await request('/api/articles');\n\nif (!result.ok) {\n  showToast(result.message);\n  return;\n}\n\nrenderArticles(result.data);\n```\n\n而不是每个页面都写一遍 `fetch`、`response.ok`、`response.json()`、`catch`。一旦错误提示文案、登录失效处理、埋点规则发生变化，散写代码会非常难改。\n\n统一错误处理还可以把常见状态码集中起来：\n\n```javascript\nfunction normalizeHttpError(status, message) {\n  if (status === 401) {\n    return '登录已过期，请重新登录';\n  }\n\n  if (status === 403) {\n    return '没有权限执行该操作';\n  }\n\n  if (status >= 500) {\n    return '服务暂时不可用，请稍后重试';\n  }\n\n  return message || '请求失败，请稍后重试';\n}\n```\n\n这不是为了把代码写复杂，而是避免每个页面各自处理错误，最后出现同一个 401 有三种提示、同一个 500 有五种兜底逻辑。\n\n## 超时和取消请求\n\nFetch 默认没有传统意义上的 timeout 参数。如果接口长时间不返回，请求可能一直挂着。实际项目里通常会用 `AbortController` 做超时控制：\n\n```javascript\nasync function requestWithTimeout(url, options = {}, timeout = 8000) {\n  const controller = new AbortController();\n  const timer = window.setTimeout(() => {\n    controller.abort();\n  }, timeout);\n\n  try {\n    return await request(url, {\n      ...options,\n      signal: controller.signal,\n    });\n  } finally {\n    window.clearTimeout(timer);\n  }\n}\n```\n\n取消请求在页面切换、搜索联想、弹窗关闭时也很有用。比如用户连续输入搜索关键字，旧请求比新请求更晚返回，就可能把新结果覆盖掉。取消旧请求，或者在响应回来时检查请求序号，都是常见处理方式。\n\n```javascript\nlet currentSearchController = null;\n\nasync function search(keyword) {\n  currentSearchController?.abort();\n\n  currentSearchController = new AbortController();\n\n  const result = await request('/api/search?q=' + encodeURIComponent(keyword), {\n    signal: currentSearchController.signal,\n  });\n\n  if (result.ok) {\n    renderSearchResult(result.data);\n  }\n}\n```\n\n需要注意的是，被取消的请求也会进入异常分支。封装层最好能区分“用户主动取消”和“真正失败”，否则用户每次快速输入都可能看到错误提示。\n\n## GET 和 POST 的边界\n\n请求方式不只是后端的事情，前端也应该明确它们的语义。\n\n一般来说：\n\n- 读取数据用 GET\n- 创建数据用 POST\n- 整体更新用 PUT\n- 局部更新用 PATCH\n- 删除数据用 DELETE\n\n实际项目未必每个接口都严格遵守，但前端代码里至少应该保持清晰的命名和调用边界。比如 `loadArticles`、`createArticle`、`deleteArticle` 比 `handleRequest1` 这种名字更能表达意图。\n\n提交 JSON 时，也要记得设置请求头和序列化：\n\n```javascript\nasync function createArticle(payload) {\n  return request('/api/articles', {\n    method: 'POST',\n    headers: {\n      'Content-Type': 'application/json',\n    },\n    body: JSON.stringify(payload),\n  });\n}\n```\n\n如果涉及表单上传，就不能照搬 JSON 请求头，而应该使用 `FormData`，让浏览器自己处理边界信息。\n\n## 从工具切换到抽象思维\n\n很多技术演进表面看起来只是 API 更新，实际上还有一个更关键的问题是推动开发者思维变化。\n\n从 Ajax 到 Fetch，更有价值的变化不只是代码少了几行，而是前端异步请求开始更自然地融入整体工程结构。\n\n请求层做好以后，页面代码会轻很多。它不用关心底层用的是 `XMLHttpRequest`、Fetch，还是第三方库；只要返回的数据结构稳定，业务层就能稳定工作。\n\n这也是我后来对请求代码的一个判断标准：不是看它是否用了最新 API，而是看它能不能让页面少处理协议细节、少重复错误逻辑、少暴露不稳定边界。\n\n## 写在最后\n\n从 Ajax 到 Fetch，是前端请求方式一次很自然的演进。前者解决了页面局部更新的问题，后者则让请求代码更容易和现代 JavaScript 编程方式结合。\n\n工具当然会变化，但真正值得沉淀的是请求层的组织方式。只要这层思路清楚，用什么接口都不会太乱。\n"},"randomPosts":[{"title":"Vue 组件通信：props、emit、provide 和事件总线怎么选","date":"2019-12-05","excerpt":"从父子组件、兄弟组件和跨层级组件出发，整理 Vue 组件通信方式、适用场景和常见误用。","tags":["Vue","前端基础"],"slug":"vue-component-communication-interview-2019","category":"2019","likeCount":8,"postId":"824edead-23dc-4dd6-7e33-4b38db2829b6","viewCount":5124,"readTime":null,"coverImage":null},{"title":"前端代码评审怎么做才有效：不是挑语法，而是提前发现维护风险","date":"2023-02-26","excerpt":"讨论前端代码评审中更有价值的关注点，包括边界、可读性、状态流转和潜在维护成本。","tags":["前端基础","构建与模块化"],"slug":"frontend-code-review-2023","category":"2023","likeCount":5,"postId":"a1cc0fb7-cfc8-ee93-3560-f6d71e9e17c9","viewCount":1217,"readTime":null,"coverImage":null},{"title":"负载均衡基础：为什么线上服务不是只放一台机器","date":"2019-11-09","excerpt":"从接口访问、Nginx 和服务扩容出发，整理负载均衡、轮询、权重、健康检查、会话保持和前端排查视角。","tags":["其他"],"slug":"load-balancing-basics-2019","category":"2019","likeCount":5,"postId":"f1e9b8ba-86db-7b2c-0175-f82871a9f5ea","viewCount":4331,"readTime":null,"coverImage":null}],"prevPost":{"title":"DOM 事件机制入门：捕获、冒泡和事件委托到底该怎么理解","date":"2021-11-21","excerpt":"介绍 DOM 事件机制中的几个核心概念，帮助你理解事件传播、事件委托以及日常开发中的常见问题。","tags":["前端基础"],"slug":"frontend-dom-events-2021","category":"2021","likeCount":7,"postId":"0c1209e1-551f-35a4-a489-a3c18ab70bb4","viewCount":1823,"readTime":null,"coverImage":null},"nextPost":{"title":"语义化 HTML 为什么重要：它不只是面试题，而是页面质量的基础","date":"2021-08-12","excerpt":"讨论语义化 HTML 在结构表达、可维护性和可访问性方面的价值，说明它为什么不只是理论概念。","tags":["前端基础"],"slug":"frontend-semantic-html-2021","category":"2021","likeCount":5,"postId":"cb1b19ce-035b-e5b0-0c8a-edaed582c4d2","viewCount":1792,"readTime":null,"coverImage":null}},"__N_SSG":true}