{"pageProps":{"postData":{"title":"React 19 表单与 Actions：别再把提交状态全塞进 useState","date":"2024-06-12","excerpt":"结合 React 19 的 Actions、useActionState、useFormStatus 和 useOptimistic，讨论表单提交流程、乐观更新和错误处理的更清晰写法。","tags":["React","前端基础"],"slug":"react-19-actions-form-2024","category":"2024","likeCount":26,"postId":"32b76503-f486-38fb-e293-699c0cdea8a5","viewCount":826,"readTime":"6分钟","coverImage":null,"content":"\nReact 19 最值得关注的变化之一，是它开始更认真地处理“异步交互”这件事。过去我们写表单时，经常会手动维护一堆状态：`isSubmitting`、`error`、`success`、`formValues`、`serverMessage`。需求一复杂，组件就会从一个表单变成一个状态仓库。\n\n2024 年我重新看 React 表单时，最明显的感受是：React 不再只把表单当作一组受控输入，而是把它当作一次完整的用户动作。输入、提交、等待、失败、成功、回滚，这些都应该有更清楚的表达方式。\n\nReact 19 里的 Actions、`useActionState`、`useFormStatus`、`useOptimistic` 就是在解决这个问题。\n\n## 传统写法的问题\n\n先看一个很常见的登录表单：\n\n```jsx\nfunction LoginForm() {\n  const [email, setEmail] = useState('')\n  const [password, setPassword] = useState('')\n  const [loading, setLoading] = useState(false)\n  const [error, setError] = useState('')\n\n  async function handleSubmit(event) {\n    event.preventDefault()\n    setLoading(true)\n    setError('')\n\n    try {\n      await login({ email, password })\n    } catch (error) {\n      setError(error.message || '登录失败')\n    } finally {\n      setLoading(false)\n    }\n  }\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <input value={email} onChange={(event) => setEmail(event.target.value)} />\n      <input\n        type=\"password\"\n        value={password}\n        onChange={(event) => setPassword(event.target.value)}\n      />\n      {error ? <p>{error}</p> : null}\n      <button disabled={loading}>{loading ? '提交中' : '登录'}</button>\n    </form>\n  )\n}\n```\n\n这段代码没有错，但它有几个长期维护问题。\n\n第一，提交状态和业务状态混在组件里。表单越复杂，`useState` 越多，后面很难判断哪个状态属于输入，哪个状态属于请求，哪个状态属于服务端返回。\n\n第二，错误处理很分散。每个表单都自己 `try...catch`，项目里很容易出现十几种错误提示风格。\n\n第三，用户连续点击、网络慢、页面切走、接口超时这些边界都需要手动处理。简单表单还能忍，复杂表单会非常啰嗦。\n\n## Actions 的思路\n\nReact 19 的 Actions 可以把“提交动作”作为一个异步函数来处理。它强调的是动作本身，而不是按钮点击之后手动维护一堆状态。\n\n一个简化例子：\n\n```jsx\nasync function submitProfile(formData) {\n  const name = String(formData.get('name') || '').trim()\n\n  if (!name) {\n    return { ok: false, message: '请输入昵称' }\n  }\n\n  await updateProfile({ name })\n\n  return { ok: true, message: '保存成功' }\n}\n```\n\n表单提交时，`formData` 会自然带上输入字段。这个模型有一个很大的好处：代码更接近浏览器原生表单，而不是一上来就把所有输入都变成受控状态。\n\n并不是说受控组件不好，而是它不应该成为唯一选择。很多表单只是提交时读取一次值，并不需要每次输入都触发 React 状态更新。\n\n## useActionState：把提交结果收回来\n\n`useActionState` 适合处理“提交后需要展示结果”的表单。\n\n```jsx\nimport { useActionState } from 'react'\n\nconst initialState = {\n  ok: false,\n  message: '',\n}\n\nasync function saveProfile(prevState, formData) {\n  const name = String(formData.get('name') || '').trim()\n\n  if (name.length < 2) {\n    return { ok: false, message: '昵称至少需要 2 个字符' }\n  }\n\n  try {\n    await updateProfile({ name })\n    return { ok: true, message: '保存成功' }\n  } catch (error) {\n    return { ok: false, message: normalizeError(error).message }\n  }\n}\n\nexport default function ProfileForm() {\n  const [state, formAction] = useActionState(saveProfile, initialState)\n\n  return (\n    <form action={formAction}>\n      <input name=\"name\" placeholder=\"昵称\" />\n      {state.message ? <p>{state.message}</p> : null}\n      <button>保存</button>\n    </form>\n  )\n}\n```\n\n这里有几个细节值得注意。\n\n第一，提交函数返回的是 UI 需要展示的状态，而不是直接在函数里操作组件状态。\n\n第二，校验逻辑可以放在 action 里，提交结果以统一结构返回。成功和失败都走同一个出口。\n\n第三，表单字段通过 `name` 和 `FormData` 传递，减少了不必要的受控状态。\n\n我的经验是：如果输入过程不需要实时联动，就不要急着用 `useState` 管每个字段。把表单值留在 DOM 里，提交时再读取，代码会轻很多。\n\n## useFormStatus：提交按钮不必知道整个表单\n\n传统写法里，按钮是否 loading 通常由父组件传入：\n\n```jsx\n<SubmitButton loading={loading} />\n```\n\n表单复杂后，按钮组件会被迫知道父组件的提交状态。`useFormStatus` 可以让按钮从所在表单上下文中读取状态。\n\n```jsx\nimport { useFormStatus } from 'react-dom'\n\nfunction SubmitButton() {\n  const { pending } = useFormStatus()\n\n  return (\n    <button disabled={pending}>\n      {pending ? '保存中...' : '保存'}\n    </button>\n  )\n}\n\nexport default function ProfileForm() {\n  const [state, formAction] = useActionState(saveProfile, initialState)\n\n  return (\n    <form action={formAction}>\n      <input name=\"name\" placeholder=\"昵称\" />\n      {state.message ? <p>{state.message}</p> : null}\n      <SubmitButton />\n    </form>\n  )\n}\n```\n\n这个变化看起来小，但对组件拆分很有帮助。提交按钮不再需要从父组件接收 `loading`，也不需要父组件为了按钮维护额外状态。\n\n## useOptimistic：让反馈先发生\n\n有些交互不应该等服务端返回后才更新界面。点赞、收藏、切换开关、追加评论，都适合做乐观更新。\n\n```jsx\nimport { useOptimistic } from 'react'\n\nfunction CommentList({ comments, postId }) {\n  const [optimisticComments, addOptimisticComment] = useOptimistic(\n    comments,\n    (currentComments, newComment) => [\n      ...currentComments,\n      { ...newComment, pending: true },\n    ]\n  )\n\n  async function addComment(formData) {\n    const content = String(formData.get('content') || '').trim()\n\n    if (!content) return\n\n    addOptimisticComment({\n      id: `temp-${Date.now()}`,\n      content,\n    })\n\n    await createComment(postId, content)\n  }\n\n  return (\n    <>\n      <ul>\n        {optimisticComments.map((comment) => (\n          <li key={comment.id}>\n            {comment.content}\n            {comment.pending ? '（发送中）' : ''}\n          </li>\n        ))}\n      </ul>\n\n      <form action={addComment}>\n        <input name=\"content\" />\n        <button>评论</button>\n      </form>\n    </>\n  )\n}\n```\n\n乐观更新的关键不是“先改 UI”，而是要想清楚失败后怎么办。\n\n对于点赞，失败后回滚通常可以接受。对于付款、删除、权限变更，就不能随便乐观更新。用户看到“已删除”，结果服务端失败，反而更糟。\n\n我会按风险分层：\n\n- 低风险：点赞、收藏、已读状态，可以乐观更新\n- 中风险：评论、编辑标题，可以乐观追加但要显示 pending\n- 高风险：支付、删除、权限、库存，不建议只靠乐观更新\n\n## 错误处理要统一\n\n无论用不用 React 19，API 错误都不应该每个表单各写一套。\n\n我更推荐让请求层统一把错误转换成稳定结构：\n\n```js\nasync function request(url, options) {\n  const response = await fetch(url, options)\n  const body = await response.json().catch(() => null)\n\n  if (!response.ok) {\n    throw normalizeApiError(response.status, body)\n  }\n\n  if (body && body.error) {\n    throw normalizeApiError(response.status, body)\n  }\n\n  return body\n}\n\nfunction normalizeApiError(status, body) {\n  return {\n    status,\n    code: body?.error?.code || 'REQUEST_FAILED',\n    message: body?.error?.message || '请求失败，请稍后重试',\n  }\n}\n```\n\n表单 action 里只关心展示结果：\n\n```js\nasync function saveProfile(prevState, formData) {\n  try {\n    await request('/api/profile', {\n      method: 'POST',\n      body: formData,\n    })\n\n    return { ok: true, message: '保存成功' }\n  } catch (error) {\n    return { ok: false, message: error.message }\n  }\n}\n```\n\n这样错误来源可以很多，但表单看到的结构始终一致。\n\n## 不是所有表单都要追新\n\nReact 19 的新能力很适合改善表单提交流程，但不代表所有旧代码都要立刻迁移。\n\n如果你的项目已经用 React Hook Form、Formik 或内部表单框架，并且校验、错误、提交状态都已经稳定，迁移收益不一定大。尤其是复杂后台表单，字段联动、数组字段、异步校验和草稿恢复往往需要更完整的表单状态模型。\n\n我更建议从小表单开始试：\n\n- 登录注册\n- 个人资料保存\n- 评论提交\n- 搜索筛选\n- 简单设置项\n\n这些场景最容易感受到 Actions 的好处，也最不容易引发大规模重构。\n\n## 小结\n\nReact 19 的表单能力不是为了替代所有表单库，而是让常见异步提交流程有了更自然的表达方式。\n\n`useActionState` 适合收拢提交结果，`useFormStatus` 适合解耦按钮状态，`useOptimistic` 适合低风险即时反馈。真正要写好表单，关键仍然是边界意识：哪些字段需要受控，哪些结果需要回滚，哪些错误需要统一，哪些操作不能乐观更新。\n\n表单不是几个输入框的组合，而是一次用户动作的完整生命周期。React 19 的价值，就是让这个生命周期更容易被写清楚。\n"},"randomPosts":[{"title":"前端团队协作中的 Git 习惯：很多低级问题其实和工具本身无关","date":"2022-05-29","excerpt":"总结前端团队在 Git 协作中更容易忽略的习惯问题，包括提交粒度、分支管理和冲突处理。","tags":["前端基础","构建与模块化"],"slug":"frontend-git-collaboration-2022","category":"2022","likeCount":6,"postId":"1ea68fe9-2685-1e0c-b30f-a81e272ec9ee","viewCount":2918,"readTime":null,"coverImage":null},{"title":"前端新手引导功能怎么做：库只是其中一步","date":"2023-12-25","excerpt":"介绍前端新手引导的常见形态、选库思路和落地时容易忽略的体验问题。","tags":["前端基础","交互与动效"],"slug":"frontend-onboarding-libraries-2023","category":"2023","likeCount":7,"postId":"6160c88a-d305-7145-fe11-7024249f70c4","viewCount":3008,"readTime":null,"coverImage":null},{"title":"Docker Compose 开发环境实践：把依赖服务从本机里搬出去","date":"2024-11-15","excerpt":"用 Docker Compose 管理数据库、缓存和消息队列等开发依赖，减少本机环境差异，并说明配置、数据卷和健康检查的边界。","tags":["构建与模块化"],"slug":"docker-compose-dev-env-2024","category":"2024","likeCount":5,"postId":"5a459d76-cf2a-ae22-5449-393e7c7f716f","viewCount":1080,"readTime":null,"coverImage":null}],"prevPost":{"title":"TypeScript 5.5 实用变化：类型推断越来越懂业务代码","date":"2024-06-28","excerpt":"围绕 TypeScript 5.5 的推断改进和项目实践，讨论类型收窄、数组过滤、配置边界和团队落地方式。","tags":["TypeScript","前端基础"],"slug":"typescript-55-practical-2024","category":"2024","likeCount":5,"postId":"fa5d2ef8-0fba-56af-0db0-a46175a0cb51","viewCount":2355,"readTime":null,"coverImage":null},"nextPost":{"title":"Node.js 22 与 Web 平台 API：后端 JavaScript 正在变得更统一","date":"2024-05-30","excerpt":"从 fetch、Web Streams、AbortController 和测试工具出发，聊聊 Node.js 22 时代后端 JavaScript 开发体验的变化。","tags":["JavaScript","构建与模块化"],"slug":"nodejs-22-web-platform-2024","category":"2024","likeCount":73,"postId":"3f247185-9c0d-0b98-ae51-99c391369df7","viewCount":867,"readTime":null,"coverImage":null}},"__N_SSG":true}