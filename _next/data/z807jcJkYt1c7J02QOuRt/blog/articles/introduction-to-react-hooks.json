{"pageProps":{"postData":{"title":"React Hooks 刚上手时，真正容易乱的是副作用和边界","date":"2023-08-15","excerpt":"从我从 class 组件切到 Hooks 时踩过的坑出发，聊聊 useState、useEffect、自定义 Hook 和依赖数组该怎么理解。","tags":["前端基础","React"],"slug":"introduction-to-react-hooks","category":"2023","likeCount":29,"postId":"67be19d1-bc39-63a0-609b-e737d6146b7c","viewCount":803,"readTime":"8分钟","coverImage":null,"content":"\nReact Hooks 是 React 16.8 引入的一组能力，它让函数组件也能拥有状态、副作用、上下文和复杂状态管理。Hooks 出现以后，React 组件不再必须依赖 class 才能处理交互逻辑，函数组件逐渐成为主流写法。\n\n不过，Hooks 不只是把 class 换成函数。它背后真正重要的变化是：组件逻辑可以按功能组织，而不是被拆散在生命周期方法里。写得好时，Hooks 能让组件更清晰；写得不好时，也很容易出现重复请求、闭包旧值、依赖数组混乱等问题。\n\n下面从几个常见 Hooks 入手，重点看它们适合解决什么问题，以及使用时要注意哪些边界。\n\n我刚开始从 class 组件切到 Hooks 时，最不习惯的是副作用和依赖数组。以前生命周期方法虽然啰嗦，但执行时机比较直观；Hooks 写起来更轻，但如果没有理解闭包和依赖，很容易出现“请求重复发”“按钮读到旧状态”“effect 一直循环”的问题。所以 Hooks 入门不能只背 API，要理解它们和渲染之间的关系。\n\n## useState：组件自己的状态\n\n`useState` 是最基础的 Hook，它让函数组件拥有本地状态。\n\n```javascript\nimport { useState } from \"react\";\n\nfunction Counter() {\n  const [count, setCount] = useState(0);\n\n  return (\n    <div>\n      <p>You clicked {count} times</p>\n      <button onClick={() => setCount(count + 1)}>Click me</button>\n    </div>\n  );\n}\n```\n\n`useState` 返回两个值：当前状态和更新状态的方法。调用 `setCount` 后，React 会重新渲染组件，并使用新的状态值。\n\n如果新状态依赖旧状态，更推荐使用函数式写法：\n\n```javascript\nsetCount((prevCount) => prevCount + 1);\n```\n\n这样可以避免在连续更新、异步回调或事件批处理里读到旧值。\n\n为什么直接 `setCount(count + 1)` 写两遍不一定加 2？因为同一次渲染里 `count` 是个固定的常量快照，两次都读到同一个旧值。用纯 JS 模拟一下批处理：\n\n```javascript\nlet count = 0;\nconst setCount = (next) =>\n  (count = typeof next === \"function\" ? next(count) : next);\n\n// 模拟“同一次渲染里 count 这个变量被冻结成 0”\nconst snapshot = 0;\nsetCount(snapshot + 1);\nsetCount(snapshot + 1);\nconsole.log(count); // 1 —— 两次都基于旧值 0，第二次覆盖第一次\n\ncount = 0;\nsetCount((c) => c + 1);\nsetCount((c) => c + 1);\nconsole.log(count); // 2 —— 函数式更新拿到的是上一次的结果\n```\n\n另外一个容易踩的点：`useState` 的更新是**替换**而不是合并。这跟 class 组件的 `this.setState` 不一样——后者会把传入对象浅合并进 `state`，而 `useState` 的 setter 直接用新值整个替换。所以对象状态要自己展开旧值：\n\n```javascript\n// class：会自动合并，age 保留\nthis.setState({ name: \"Tom\" });\n\n// hooks：直接替换，少写 ...user 就把 age 丢了\nsetUser({ name: \"Tom\" });        // ❌ age 没了\nsetUser((u) => ({ ...u, name: \"Tom\" })); // ✅ 手动合并\n```\n\n## useState 不适合所有数据\n\n不是所有变量都应该放进 `useState`。\n\n适合放进状态的数据通常有两个特点：会影响页面渲染，并且变化后需要触发重新渲染。比如计数、输入框内容、弹窗开关、当前选中的 tab。\n\n如果只是临时计算值，可以直接在渲染过程中得到：\n\n```javascript\nfunction UserCard({ user }) {\n  const displayName = user.nickname || user.name || \"匿名用户\";\n\n  return <span>{displayName}</span>;\n}\n```\n\n不要为了“看起来统一”把所有派生值都放进状态。派生状态越多，越容易出现同步问题。\n\n我见过不少 bug 都来自派生状态。比如 `selectedUser` 已经能从 `users` 和 `selectedId` 算出来，却又单独存一份。列表刷新后用户对象变了，`selectedUser` 还是旧引用，弹窗里显示的就是过期数据。能计算就计算，不要为了省一行 `find` 多维护一份状态。\n\n## useEffect：处理副作用\n\n`useEffect` 用来处理渲染之外的事情，比如请求数据、订阅事件、操作浏览器 API、同步标题。\n\n```javascript\nimport { useEffect, useState } from \"react\";\n\nfunction PostTitle({ postId }) {\n  const [title, setTitle] = useState(\"\");\n\n  useEffect(() => {\n    let ignore = false;\n\n    async function loadPost() {\n      const response = await fetch(`/api/posts/${postId}`);\n      const data = await response.json();\n\n      if (!ignore) {\n        setTitle(data.title);\n      }\n    }\n\n    loadPost();\n\n    return () => {\n      ignore = true;\n    };\n  }, [postId]);\n\n  return <h1>{title}</h1>;\n}\n```\n\n这里的依赖数组 `[postId]` 表示：当 `postId` 变化时重新执行 effect。清理函数里的 `ignore` 用来避免旧请求回来后覆盖新状态。\n\n实际项目里，请求还有错误处理、加载状态、取消请求、缓存等问题。简单页面可以直接写在 `useEffect` 里，复杂项目更适合使用请求状态管理工具，把这些重复逻辑集中起来。\n\n如果只是因为某个 props 变化就同步本地状态，也要警惕是不是可以直接用 props。很多 effect 其实是在弥补状态设计问题。React 新文档一直强调 “You Might Not Need an Effect”，这个提醒很实用：能在渲染时计算的，不要放 effect；能在事件里做的，不要绕到 effect。\n\n## useEffect 的常见误区\n\n很多 Hooks 问题都和 `useEffect` 有关。\n\n第一个误区是把它当成“组件加载时执行一次”。空依赖数组确实只会在挂载后执行，但 `useEffect` 处理的是副作用同步，不是生命周期方法的简单替代。\n\n第二个误区是依赖数组随便写。依赖漏了，可能读到旧值；依赖多了，可能反复执行。遇到 ESLint 提醒时，不要第一反应禁用规则，而是先检查 effect 里到底使用了哪些外部变量。\n\n第三个误区是在 effect 里做本来可以在事件里做的事。比如点击按钮后提交表单，通常应该在点击事件里直接调用，而不是先改一个状态，再靠 effect 观察这个状态去提交。\n\n## useContext：跨层传递数据\n\n当数据需要被多层组件使用时，可以用 `useContext` 避免层层传 props。\n\n```javascript\nimport { createContext, useContext } from \"react\";\n\nconst ThemeContext = createContext(\"light\");\n\nfunction Toolbar() {\n  const theme = useContext(ThemeContext);\n  return <button className={theme}>Save</button>;\n}\n\nfunction App() {\n  return (\n    <ThemeContext.Provider value=\"dark\">\n      <Toolbar />\n    </ThemeContext.Provider>\n  );\n}\n```\n\n`useContext` 适合放全局主题、语言、当前用户、权限信息等跨层数据。但它不是所有状态管理问题的答案。\n\n如果一个 context 的值频繁变化，而且被很多组件消费，可能导致较大范围重新渲染。真实项目里要注意拆分 context，避免把“所有全局状态”都塞进一个对象里。\n\nContext 更适合低频变化的全局信息，比如主题、语言、当前用户。把输入框内容、列表筛选、实时计数这类高频状态放进一个大 Context，后面性能问题会很明显。必要时拆 context，或者引入更细粒度的状态管理方案。\n\n## useReducer：复杂状态更清楚\n\n当一个组件状态变化规则比较复杂时，`useReducer` 会比多个 `useState` 更容易维护。\n\n例如请求状态可以这样写：\n\n```javascript\nimport { useReducer } from \"react\";\n\nconst initialState = {\n  loading: false,\n  data: null,\n  error: null,\n};\n\nfunction reducer(state, action) {\n  switch (action.type) {\n    case \"start\":\n      return { loading: true, data: null, error: null };\n    case \"success\":\n      return { loading: false, data: action.payload, error: null };\n    case \"error\":\n      return { loading: false, data: null, error: action.payload };\n    default:\n      return state;\n  }\n}\n\nfunction UserPanel() {\n  const [state, dispatch] = useReducer(reducer, initialState);\n\n  // 根据 state.loading、state.data、state.error 渲染\n}\n```\n\n`useReducer` 的好处是把“状态怎么变化”集中到一个函数里。对于表单流程、弹窗步骤、请求状态、复杂筛选条件，这种写法会比多个状态散落在组件里更稳定。\n\n## 自定义 Hook：复用的是逻辑，不只是代码\n\n当多个组件里出现相似逻辑时，可以提取自定义 Hook。\n\n```javascript\nimport { useEffect, useState } from \"react\";\n\nfunction useWindowWidth() {\n  const [width, setWidth] = useState(window.innerWidth);\n\n  useEffect(() => {\n    function handleResize() {\n      setWidth(window.innerWidth);\n    }\n\n    window.addEventListener(\"resize\", handleResize);\n    return () => window.removeEventListener(\"resize\", handleResize);\n  }, []);\n\n  return width;\n}\n```\n\n自定义 Hook 的命名必须以 `use` 开头，这样 React 和 ESLint 才能识别 Hooks 调用规则。\n\n提取 Hook 时要注意边界：它应该复用一段稳定的逻辑，而不是为了减少几行代码强行抽象。如果一个 Hook 需要十几个参数才能兼容不同页面，可能说明抽象得太早。\n\n我现在写自定义 Hook 会先问一句：它复用的是业务规则，还是只是把代码挪走。如果只是为了让组件短一点，把一堆页面专属状态塞进 `usePageLogic`，其实可读性不一定变好。好的 Hook 应该有明确输入输出，最好能脱离页面名字理解。\n\n## Hooks 的基本规则\n\nHooks 有两条重要规则：\n\n- 只能在 React 函数组件或自定义 Hook 顶层调用\n- 不能在条件、循环、嵌套函数里调用\n\n不要这样写：\n\n```javascript\nif (visible) {\n  const [count, setCount] = useState(0);\n}\n```\n\nReact 依赖 Hooks 的调用顺序来关联状态。如果条件执行导致顺序变化，状态就会错乱。\n\n还要注意闭包旧值。定时器、事件监听、Promise 回调里读到的状态，可能是创建回调那一刻的值。遇到这类问题，可以用函数式更新、把依赖补全，或者用 `useRef` 保存最新值。不要靠“禁用 exhaustive-deps”硬压过去，很多线上问题就是这么来的。\n\n这个“读到旧值”的原理用纯 JS 就能复现，跟 React 没关系——每次渲染相当于用当时的 state 重新创建一批函数，函数闭包住的是那一刻的值：\n\n```javascript\n// 模拟“每次渲染都用当时的 count 造一个回调”\nfunction render(count) {\n  return () => console.log(\"看到的 count =\", count);\n}\n\nconst callbackFromRender0 = render(0);\nconst callbackFromRender1 = render(1);\n\ncallbackFromRender0(); // 看到的 count = 0\ncallbackFromRender1(); // 看到的 count = 1\n```\n\n`setInterval(callback, 1000)` 的坑就在这：如果它只在挂载时（`count` 还是 0 的那次渲染）注册了一次，那它永远拿着 `callbackFromRender0`，之后界面上 count 怎么变，定时器里读到的都是 0。函数式更新 `setCount(c => c + 1)` 之所以能绕开，是因为它不读闭包里的 `count`，而是让 React 把最新值传进来。\n\n\n我后来对 Hooks 的理解，基本是被几个 bug 逼出来的：effect 重复请求、定时器读到旧状态、自定义 Hook 名字很漂亮但里面全是页面细节。\n\n入门时先掌握 `useState`、`useEffect`、`useContext` 和 `useReducer` 就够了。真正要小心的不是 API 数量，而是几个判断：这份状态该不该存在，effect 是不是必要，依赖数组有没有说实话，自定义 Hook 复用的是规则还是只是搬代码。\n\nHooks 的目标不是让组件“看起来更函数式”，而是让状态、副作用和业务逻辑的边界更清楚。边界没想清楚，函数组件一样会写成另一种大组件。\n"},"recommendedPosts":[{"title":"shadcn/ui 这种「把代码抄进项目」的组件方案，用半年后的复盘","date":"2026-05-17","excerpt":"shadcn/ui 不发 npm 包、直接把组件源码装进你的仓库。聊聊这种 copy-in 模式的好处、维护成本、和传统组件库（Antd/MUI）的取舍。","tags":["React","前端基础","组件与状态"],"slug":"shadcn-ui-component-system-2026","category":"2026","likeCount":19,"postId":"42e26346-5c9f-6eab-0196-167529529112","viewCount":238,"readTime":null,"coverImage":null},{"title":"TanStack Router 的类型安全路由：把跳转和参数错误挪到编译期","date":"2026-02-21","excerpt":"用过一段时间 TanStack Router 后的复盘：文件式与代码式路由、search params 的类型校验、loader 数据预取，以及它和 React Router 的取舍。","tags":["React","TypeScript","前端基础"],"slug":"tanstack-router-type-safe-2026","category":"2026","likeCount":25,"postId":"ba186679-ff87-187d-4d34-cff3f06231d3","viewCount":625,"readTime":null,"coverImage":null},{"title":"React Server Components 复盘：我真正卡住的不是语法，是边界","date":"2025-02-18","excerpt":"记录我理解 React Server Components 时从关注少发 JS，转向关注数据获取、客户端交互、缓存和错误边界的过程。","tags":["React","组件与状态","前端基础"],"slug":"react-server-components-boundary-2025","category":"2025","likeCount":7,"postId":"c73bf7d3-87d6-78cc-2ac7-b955531a80dd","viewCount":2925,"readTime":null,"coverImage":null}],"prevPost":{"title":"CSS Grid 真正好用的地方：把二维布局写清楚","date":"2023-08-20","excerpt":"从一次后台仪表盘布局重写出发，聊聊 CSS Grid 在页面骨架、卡片列表、区域命名和响应式布局里的真实用法。","tags":["前端基础","CSS"],"slug":"mastering-css-grid","category":"2023","likeCount":7,"postId":"d85d9915-5fb3-ed85-72bc-53f6fc206b79","viewCount":3290,"readTime":null,"coverImage":null},"nextPost":{"title":"前端打包优化经验：包体积不是只靠压缩就能降下来的","date":"2023-07-02","excerpt":"从后台项目包体积膨胀的排查经历出发，聊依赖、拆包、资源和缓存策略怎么配才算真的有效。","tags":["前端基础","构建与模块化","性能与缓存"],"slug":"frontend-bundling-2023","category":"2023","likeCount":5,"postId":"ef208b58-4020-7c41-1b0f-a4516cc7cbaa","viewCount":1927,"readTime":null,"coverImage":null}},"__N_SSG":true}